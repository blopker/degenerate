//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/package.dart';import '../models/package_version.dart';import '../models/packages_delete_package_for_authenticated_user_package_type.dart';import '../models/packages_delete_package_for_org_package_type.dart';import '../models/packages_delete_package_for_user_package_type.dart';import '../models/packages_delete_package_version_for_authenticated_user_package_type.dart';import '../models/packages_delete_package_version_for_org_package_type.dart';import '../models/packages_delete_package_version_for_user_package_type.dart';import '../models/packages_get_all_package_versions_for_package_owned_by_authenticated_user_package_type.dart';import '../models/packages_get_all_package_versions_for_package_owned_by_authenticated_user_state.dart';import '../models/packages_get_all_package_versions_for_package_owned_by_org_package_type.dart';import '../models/packages_get_all_package_versions_for_package_owned_by_org_state.dart';import '../models/packages_get_all_package_versions_for_package_owned_by_user_package_type.dart';import '../models/packages_get_package_for_authenticated_user_package_type.dart';import '../models/packages_get_package_for_organization_package_type.dart';import '../models/packages_get_package_for_user_package_type.dart';import '../models/packages_get_package_version_for_authenticated_user_package_type.dart';import '../models/packages_get_package_version_for_organization_package_type.dart';import '../models/packages_get_package_version_for_user_package_type.dart';import '../models/packages_list_packages_for_authenticated_user_package_type.dart';import '../models/packages_list_packages_for_authenticated_user_visibility.dart';import '../models/packages_list_packages_for_organization_package_type.dart';import '../models/packages_list_packages_for_organization_visibility.dart';import '../models/packages_list_packages_for_user_package_type.dart';import '../models/packages_list_packages_for_user_visibility.dart';import '../models/packages_restore_package_for_authenticated_user_package_type.dart';import '../models/packages_restore_package_for_org_package_type.dart';import '../models/packages_restore_package_for_user_package_type.dart';import '../models/packages_restore_package_version_for_authenticated_user_package_type.dart';import '../models/packages_restore_package_version_for_org_package_type.dart';import '../models/packages_restore_package_version_for_user_package_type.dart';/// PackagesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class PackagesApi {const PackagesApi(this._config);

final ApiConfig _config;

/// Get list of conflicting packages during Docker migration for organization
///
/// Lists all packages that are in a specific organization, are readable by the requesting user, and that encountered a conflict during a Docker migration.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
///
/// `GET /orgs/{org}/docker/conflicts`
Future<ApiResult<List<Package>>> packagesListDockerMigrationConflictingPackagesForOrganization({required String org}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/docker/conflicts',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// List packages for an organization
///
/// Lists packages in an organization readable by the user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /orgs/{org}/packages`
Future<ApiResult<List<Package>>> packagesListPackagesForOrganization({required String org, required PackagesListPackagesForOrganizationPackageType packageType, PackagesListPackagesForOrganizationVisibility? visibility, int? page, int? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/packages',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'package_type': packageType.toJson(),
    if (visibility != null) 'visibility': visibility.toJson(),
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package for an organization
///
/// Gets a specific package in an organization.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /orgs/{org}/packages/{package_type}/{package_name}`
Future<ApiResult<Package>> packagesGetPackageForOrganization({required PackagesGetPackageForOrganizationPackageType packageType, required String packageName, required String org, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Package.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a package for an organization
///
/// Deletes an entire package in an organization. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
/// 
/// The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /orgs/{org}/packages/{package_type}/{package_name}`
Future<ApiResult<void>> packagesDeletePackageForOrg({required PackagesDeletePackageForOrgPackageType packageType, required String packageName, required String org, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore a package for an organization
///
/// Restores an entire package in an organization.
/// 
/// You can restore a deleted package under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /orgs/{org}/packages/{package_type}/{package_name}/restore`
Future<ApiResult<void>> packagesRestorePackageForOrg({required PackagesRestorePackageForOrgPackageType packageType, required String packageName, required String org, String? token, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/restore',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'token': ?token,
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// List package versions for a package owned by an organization
///
/// Lists package versions for a package owned by an organization.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /orgs/{org}/packages/{package_type}/{package_name}/versions`
Future<ApiResult<List<PackageVersion>>> packagesGetAllPackageVersionsForPackageOwnedByOrg({required PackagesGetAllPackageVersionsForPackageOwnedByOrgPackageType packageType, required String packageName, required String org, int? page, int? perPage, PackagesGetAllPackageVersionsForPackageOwnedByOrgState? state, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (state != null) 'state': state.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => PackageVersion.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package version for an organization
///
/// Gets a specific package version in an organization.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<PackageVersion>> packagesGetPackageVersionForOrganization({required PackagesGetPackageVersionForOrganizationPackageType packageType, required String packageName, required String org, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return PackageVersion.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete package version for an organization
///
/// Deletes a specific package version in an organization. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
/// 
/// The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<void>> packagesDeletePackageVersionForOrg({required PackagesDeletePackageVersionForOrgPackageType packageType, required String packageName, required String org, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore package version for an organization
///
/// Restores a specific package version in an organization.
/// 
/// You can restore a deleted package under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// The authenticated user must have admin permissions in the organization to use this endpoint. If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must also have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /orgs/{org}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore`
Future<ApiResult<void>> packagesRestorePackageVersionForOrg({required PackagesRestorePackageVersionForOrgPackageType packageType, required String packageName, required String org, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/orgs/${Uri.encodeComponent(org)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}/restore',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Get list of conflicting packages during Docker migration for authenticated-user
///
/// Lists all packages that are owned by the authenticated user within the user's namespace, and that encountered a conflict during a Docker migration.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
///
/// `GET /user/docker/conflicts`
Future<ApiResult<List<Package>>> packagesListDockerMigrationConflictingPackagesForAuthenticatedUser() async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/docker/conflicts',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// List packages for the authenticated user's namespace
///
/// Lists packages owned by the authenticated user within the user's namespace.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /user/packages`
Future<ApiResult<List<Package>>> packagesListPackagesForAuthenticatedUser({required PackagesListPackagesForAuthenticatedUserPackageType packageType, PackagesListPackagesForAuthenticatedUserVisibility? visibility, int? page, int? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/packages',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'package_type': packageType.toJson(),
    if (visibility != null) 'visibility': visibility.toJson(),
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package for the authenticated user
///
/// Gets a specific package for a package owned by the authenticated user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /user/packages/{package_type}/{package_name}`
Future<ApiResult<Package>> packagesGetPackageForAuthenticatedUser({required PackagesGetPackageForAuthenticatedUserPackageType packageType, required String packageName, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Package.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a package for the authenticated user
///
/// Deletes a package owned by the authenticated user. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /user/packages/{package_type}/{package_name}`
Future<ApiResult<void>> packagesDeletePackageForAuthenticatedUser({required PackagesDeletePackageForAuthenticatedUserPackageType packageType, required String packageName, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore a package for the authenticated user
///
/// Restores a package owned by the authenticated user.
/// 
/// You can restore a deleted package under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /user/packages/{package_type}/{package_name}/restore`
Future<ApiResult<void>> packagesRestorePackageForAuthenticatedUser({required PackagesRestorePackageForAuthenticatedUserPackageType packageType, required String packageName, String? token, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/restore',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'token': ?token,
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// List package versions for a package owned by the authenticated user
///
/// Lists package versions for a package owned by the authenticated user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /user/packages/{package_type}/{package_name}/versions`
Future<ApiResult<List<PackageVersion>>> packagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUser({required PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserPackageType packageType, required String packageName, int? page, int? perPage, PackagesGetAllPackageVersionsForPackageOwnedByAuthenticatedUserState? state, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (state != null) 'state': state.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => PackageVersion.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package version for the authenticated user
///
/// Gets a specific package version for a package owned by the authenticated user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /user/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<PackageVersion>> packagesGetPackageVersionForAuthenticatedUser({required PackagesGetPackageVersionForAuthenticatedUserPackageType packageType, required String packageName, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return PackageVersion.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a package version for the authenticated user
///
/// Deletes a specific package version for a package owned by the authenticated user.  If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
/// 
/// The authenticated user must have admin permissions in the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /user/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<void>> packagesDeletePackageVersionForAuthenticatedUser({required PackagesDeletePackageVersionForAuthenticatedUserPackageType packageType, required String packageName, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore a package version for the authenticated user
///
/// Restores a package version owned by the authenticated user.
/// 
/// You can restore a deleted package version under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /user/packages/{package_type}/{package_name}/versions/{package_version_id}/restore`
Future<ApiResult<void>> packagesRestorePackageVersionForAuthenticatedUser({required PackagesRestorePackageVersionForAuthenticatedUserPackageType packageType, required String packageName, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/user/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}/restore',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Get list of conflicting packages during Docker migration for user
///
/// Lists all packages that are in a specific user's namespace, that the requesting user has access to, and that encountered a conflict during Docker migration.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint.
///
/// `GET /users/{username}/docker/conflicts`
Future<ApiResult<List<Package>>> packagesListDockerMigrationConflictingPackagesForUser({required String username}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/users/${Uri.encodeComponent(username)}/docker/conflicts',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// List packages for a user
///
/// Lists all packages in a user's namespace for which the requesting user has access.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /users/{username}/packages`
Future<ApiResult<List<Package>>> packagesListPackagesForUser({required String username, required PackagesListPackagesForUserPackageType packageType, PackagesListPackagesForUserVisibility? visibility, int? page, int? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/users/${Uri.encodeComponent(username)}/packages',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'package_type': packageType.toJson(),
    if (visibility != null) 'visibility': visibility.toJson(),
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => Package.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package for a user
///
/// Gets a specific package metadata for a public package owned by a user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /users/{username}/packages/{package_type}/{package_name}`
Future<ApiResult<Package>> packagesGetPackageForUser({required PackagesGetPackageForUserPackageType packageType, required String packageName, required String username, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Package.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a package for a user
///
/// Deletes an entire package for a user. You cannot delete a public package if any version of the package has more than 5,000 downloads. In this scenario, contact GitHub support for further assistance.
/// 
/// If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /users/{username}/packages/{package_type}/{package_name}`
Future<ApiResult<void>> packagesDeletePackageForUser({required PackagesDeletePackageForUserPackageType packageType, required String packageName, required String username, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore a package for a user
///
/// Restores an entire package for a user.
/// 
/// You can restore a deleted package under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /users/{username}/packages/{package_type}/{package_name}/restore`
Future<ApiResult<void>> packagesRestorePackageForUser({required PackagesRestorePackageForUserPackageType packageType, required String packageName, required String username, String? token, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/restore',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'token': ?token,
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// List package versions for a package owned by a user
///
/// Lists package versions for a public package owned by a specified user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /users/{username}/packages/{package_type}/{package_name}/versions`
Future<ApiResult<List<PackageVersion>>> packagesGetAllPackageVersionsForPackageOwnedByUser({required PackagesGetAllPackageVersionsForPackageOwnedByUserPackageType packageType, required String packageName, required String username, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => PackageVersion.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a package version for a user
///
/// Gets a specific package version for a public package owned by a specified user.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` scope to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `GET /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<PackageVersion>> packagesGetPackageVersionForUser({required PackagesGetPackageVersionForUserPackageType packageType, required String packageName, required int packageVersionId, required String username, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return PackageVersion.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete package version for a user
///
/// Deletes a specific package version for a user. If the package is public and the package version has more than 5,000 downloads, you cannot delete the package version. In this scenario, contact GitHub support for further assistance.
/// 
/// If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `delete:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `DELETE /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}`
Future<ApiResult<void>> packagesDeletePackageVersionForUser({required PackagesDeletePackageVersionForUserPackageType packageType, required String packageName, required String username, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Restore package version for a user
///
/// Restores a specific package version for a user.
/// 
/// You can restore a deleted package under the following conditions:
///   - The package was deleted within the last 30 days.
///   - The same package namespace and version is still available and not reused for a new package. If the same package namespace is not available, you will not be able to restore your package. In this scenario, to restore the deleted package, you must delete the new package that uses the deleted package's namespace first.
/// 
/// If the `package_type` belongs to a GitHub Packages registry that supports granular permissions, the authenticated user must have admin permissions to the package. For the list of these registries, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#granular-permissions-for-userorganization-scoped-packages)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:packages` and `write:packages` scopes to use this endpoint. For more information, see "[About permissions for GitHub Packages](https://docs.github.com/packages/learn-github-packages/about-permissions-for-github-packages#permissions-for-repository-scoped-packages)."
///
/// `POST /users/{username}/packages/{package_type}/{package_name}/versions/{package_version_id}/restore`
Future<ApiResult<void>> packagesRestorePackageVersionForUser({required PackagesRestorePackageVersionForUserPackageType packageType, required String packageName, required String username, required int packageVersionId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/users/${Uri.encodeComponent(username)}/packages/${Uri.encodeComponent(packageType.toString())}/${Uri.encodeComponent(packageName)}/versions/${Uri.encodeComponent(packageVersionId.toString())}/restore',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
