//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/user.dart';import '../models/user_delete_response.dart';import '../models/user_list_response.dart';import '../models/user_role_update_request.dart';/// UsersApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class UsersApi {const UsersApi(this._config);

final ApiConfig _config;

/// Lists all of the users in the organization.
///
/// `GET /organization/users`
Future<ApiResult<UserListResponse>> listUsers({int? limit, String? after, List<String>? emails, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/organization/users',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (limit != null) 'limit': limit.toString(),
    'after': ?after,
    if (emails != null) 'emails': emails.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return UserListResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Retrieves a user by their identifier.
///
/// `GET /organization/users/{user_id}`
Future<ApiResult<User>> retrieveUser({required String userId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/organization/users/${Uri.encodeComponent(userId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return User.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Modifies a user's role in the organization.
///
/// `POST /organization/users/{user_id}`
Future<ApiResult<User>> modifyUser({required String userId, required UserRoleUpdateRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/organization/users/${Uri.encodeComponent(userId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return User.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Deletes a user from the organization.
///
/// `DELETE /organization/users/{user_id}`
Future<ApiResult<UserDeleteResponse>> deleteUser({required String userId}) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/organization/users/${Uri.encodeComponent(userId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return UserDeleteResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
