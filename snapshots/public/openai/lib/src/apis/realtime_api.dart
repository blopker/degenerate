//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/realtime_call_create_request.dart';import '../models/realtime_call_refer_request.dart';import '../models/realtime_call_reject_request.dart';import '../models/realtime_create_client_secret_request.dart';import '../models/realtime_create_client_secret_response.dart';import '../models/realtime_session_create_request.dart';import '../models/realtime_session_create_request_ga.dart';import '../models/realtime_session_create_response.dart';import '../models/realtime_transcription_session_create_request.dart';import '../models/realtime_transcription_session_create_response.dart';/// RealtimeApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RealtimeApi {const RealtimeApi(this._config);

final ApiConfig _config;

/// Create a new Realtime API call over WebRTC and receive the SDP answer needed
/// to complete the peer connection.
///
/// `POST /realtime/calls`
Future<ApiResult<void>> createRealtimeCall({required RealtimeCallCreateRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/calls',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Accept an incoming SIP call and configure the realtime session that will
/// handle it.
///
/// `POST /realtime/calls/{call_id}/accept`
Future<ApiResult<void>> acceptRealtimeCall({required String callId, required RealtimeSessionCreateRequestGa body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/calls/${Uri.encodeComponent(callId)}/accept',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// End an active Realtime API call, whether it was initiated over SIP or
/// WebRTC.
///
/// `POST /realtime/calls/{call_id}/hangup`
Future<ApiResult<void>> hangupRealtimeCall({required String callId}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/calls/${Uri.encodeComponent(callId)}/hangup',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Transfer an active SIP call to a new destination using the SIP REFER verb.
///
/// `POST /realtime/calls/{call_id}/refer`
Future<ApiResult<void>> referRealtimeCall({required String callId, required RealtimeCallReferRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/calls/${Uri.encodeComponent(callId)}/refer',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Decline an incoming SIP call by returning a SIP status code to the caller.
///
/// `POST /realtime/calls/{call_id}/reject`
Future<ApiResult<void>> rejectRealtimeCall({required String callId, RealtimeCallRejectRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/calls/${Uri.encodeComponent(callId)}/reject',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Create a Realtime client secret with an associated session configuration.
/// 
/// Client secrets are short-lived tokens that can be passed to a client app,
/// such as a web frontend or mobile client, which grants access to the Realtime API without
/// leaking your main API key. You can configure a custom TTL for each client secret.
/// 
/// You can also attach session configuration options to the client secret, which will be
/// applied to any sessions created using that client secret, but these can also be overridden
/// by the client connection.
/// 
/// [Learn more about authentication with client secrets over WebRTC](/docs/guides/realtime-webrtc).
/// 
/// Returns the created client secret and the effective session object. The client secret is a string that looks like `ek_1234`.
/// 
///
/// `POST /realtime/client_secrets`
Future<ApiResult<RealtimeCreateClientSecretResponse>> createRealtimeClientSecret({required RealtimeCreateClientSecretRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/client_secrets',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimeCreateClientSecretResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create an ephemeral API token for use in client-side applications with the
/// Realtime API. Can be configured with the same session parameters as the
/// `session.update` client event.
/// 
/// It responds with a session object, plus a `client_secret` key which contains
/// a usable ephemeral API token that can be used to authenticate browser clients
/// for the Realtime API.
/// 
/// Returns the created Realtime session object, plus an ephemeral key.
/// 
///
/// `POST /realtime/sessions`
Future<ApiResult<RealtimeSessionCreateResponse>> createRealtimeSession({required RealtimeSessionCreateRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/sessions',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimeSessionCreateResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create an ephemeral API token for use in client-side applications with the
/// Realtime API specifically for realtime transcriptions.
/// Can be configured with the same session parameters as the `transcription_session.update` client event.
/// 
/// It responds with a session object, plus a `client_secret` key which contains
/// a usable ephemeral API token that can be used to authenticate browser clients
/// for the Realtime API.
/// 
/// Returns the created Realtime transcription session object, plus an ephemeral key.
/// 
///
/// `POST /realtime/transcription_sessions`
Future<ApiResult<RealtimeTranscriptionSessionCreateResponse>> createRealtimeTranscriptionSession({required RealtimeTranscriptionSessionCreateRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/realtime/transcription_sessions',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimeTranscriptionSessionCreateResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
