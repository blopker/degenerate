//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/create_image_request.dart';import '../models/create_image_variation_request.dart';import '../models/edit_image_body_json_param.dart';import '../models/images_response.dart';/// ImagesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ImagesApi {const ImagesApi(this._config);

final ApiConfig _config;

/// Creates an edited or extended image given one or more source images and a prompt. This endpoint supports GPT Image models (`gpt-image-1.5`, `gpt-image-1`, `gpt-image-1-mini`, and `chatgpt-image-latest`) and `dall-e-2`.
///
/// You can call this endpoint with either:
/// 
/// - `multipart/form-data`: use binary uploads via `image` (and optional `mask`).
/// - `application/json`: use `images` (and optional `mask`) as references with either `image_url` or `file_id`.
/// 
/// Note that JSON requests use `images` (array) instead of the multipart `image` field.
/// 
///
/// `POST /images/edits`
Future<ApiResult<ImagesResponse, Never>> createImageEdit({required EditImageBodyJsonParam body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/images/edits',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ImagesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Creates an image given a prompt. [Learn more](/docs/guides/images).
/// 
///
/// `POST /images/generations`
Future<ApiResult<ImagesResponse, Never>> createImage({required CreateImageRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/images/generations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ImagesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Creates a variation of a given image. This endpoint only supports `dall-e-2`.
///
/// `POST /images/variations`
Future<ApiResult<ImagesResponse, Never>> createImageVariation({required CreateImageVariationRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/images/variations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'multipart/form-data'
  },
  body: throw UnsupportedError('Cannot encode non-JSON multipart/form-data request body from CreateImageVariationRequest');,
);

return _execute(
  request,
  onSuccess: (response) {
    return ImagesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
