//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from shopify-openapi.yaml
//  OpenAPI spec version: 3.0.0

import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';/// CallsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class CallsApi {const CallsApi(this._config);

final ApiConfig _config;

/// Retrieves a list of deprecated API calls made by the authenticated private app in the past 30 days.
///
/// https://shopify.dev/docs/admin-api/rest/reference/deprecated_api_calls#index-2021-01
///
/// `GET /admin/api/2021-01/deprecated_api_calls.json`
Future<ApiResult<void>> deprecated202101GetDeprecatedApiCalls() async  { final request = ApiRequest(
  method: 'GET',
  path: '/admin/api/2021-01/deprecated_api_calls.json',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Retrieves a list of deprecated API calls made by the authenticated private app in the past 30 days.
///
/// https://shopify.dev/docs/admin-api/rest/reference/deprecated_api_calls#index-unstable
///
/// `GET /admin/api/unstable/deprecated_api_calls.json`
Future<ApiResult<void>> deprecatedUnstableGetDeprecatedApiCalls() async  { final request = ApiRequest(
  method: 'GET',
  path: '/admin/api/unstable/deprecated_api_calls.json',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
