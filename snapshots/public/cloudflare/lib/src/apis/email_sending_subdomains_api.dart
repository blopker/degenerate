//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/email_create_sending_subdomain_properties.dart';import '../models/email_identifier.dart';import '../models/email_sending_subdomain_identifier.dart';import '../models/response_common30.dart';/// EmailSendingSubdomainsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class EmailSendingSubdomainsApi {const EmailSendingSubdomainsApi(this._config);

final ApiConfig _config;

/// List sending subdomains
///
/// Lists all sending-enabled subdomains for the zone.
///
/// `GET /zones/{zone_id}/email/sending/subdomains`
Future<ApiResult<ResponseCommon30, Never>> emailSendingSubdomainsListSendingSubdomains({required EmailIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/email/sending/subdomains',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon30.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a sending subdomain
///
/// Creates a new sending subdomain or re-enables sending on an existing subdomain that had it disabled.
///
/// `POST /zones/{zone_id}/email/sending/subdomains`
Future<ApiResult<ResponseCommon30, Never>> emailSendingSubdomainsCreateSendingSubdomain({required EmailIdentifier zoneId, required EmailCreateSendingSubdomainProperties body, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};
headers['Content-Type'] = 'application/json';

final request = ApiRequest(
  method: 'POST',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/email/sending/subdomains',
  headers: headers,
  body: jsonEncode(body.toJson()),
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon30.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get a sending subdomain
///
/// Gets information for a specific sending subdomain.
///
/// `GET /zones/{zone_id}/email/sending/subdomains/{subdomain_id}`
Future<ApiResult<ResponseCommon30, Never>> emailSendingSubdomainsGetSendingSubdomain({required EmailSendingSubdomainIdentifier subdomainId, required EmailIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/email/sending/subdomains/${Uri.encodeComponent(subdomainId.toString())}',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon30.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a sending subdomain
///
/// Disables sending on a subdomain and removes its DNS records. If routing is still active on the subdomain, only sending is disabled.
///
/// `DELETE /zones/{zone_id}/email/sending/subdomains/{subdomain_id}`
Future<ApiResult<ResponseCommon30, Never>> emailSendingSubdomainsDeleteSendingSubdomain({required EmailSendingSubdomainIdentifier subdomainId, required EmailIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'DELETE',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/email/sending/subdomains/${Uri.encodeComponent(subdomainId.toString())}',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon30.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get sending subdomain DNS records
///
/// Returns the expected DNS records for a sending subdomain.
///
/// `GET /zones/{zone_id}/email/sending/subdomains/{subdomain_id}/dns`
Future<ApiResult<ResponseCommon30, Never>> emailSendingSubdomainsGetSendingSubdomainDns({required EmailSendingSubdomainIdentifier subdomainId, required EmailIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/email/sending/subdomains/${Uri.encodeComponent(subdomainId.toString())}/dns',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon30.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final cancelToken = request.options?.cancelToken;
  if (cancelToken?.isCancelled ?? false) throw const CancelledException();

  final effectiveTimeout = request.options?.timeout ?? _config.timeout;
  final extraHeaders = request.options?.extraHeaders;
  final effectiveRequest = extraHeaders != null
      ? request.copyWith(headers: {...request.headers, ...extraHeaders})
      : request;

  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      if (cancelToken?.isCancelled ?? false) throw const CancelledException();
      final future = _config.client.send(req);
      return effectiveTimeout != null
          ? await future.timeout(effectiveTimeout)
          : await future;
    },
  );

  final response = await chain(effectiveRequest);

  try {
    if (response.isSuccessful) {
      return ApiSuccess(
        onSuccess(response),
        statusCode: response.statusCode,
        headers: response.headers,
      );
    }
    return ApiError(
      statusCode: response.statusCode,
      error: onError != null ? onError(response) : null,
      rawError: response.body,
      headers: response.headers,
    );
  } catch (e, st) {
    return ApiParseException(e, st, response: response);
  }
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
