//  dart format off
//  GENERATED CODE - DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:async';import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/page_rules_create_a_page_rule_request.dart';import '../models/page_rules_edit_a_page_rule_request.dart';import '../models/page_rules_list_page_rules_direction.dart';import '../models/page_rules_list_page_rules_match.dart';import '../models/page_rules_list_page_rules_order.dart';import '../models/page_rules_list_page_rules_status.dart';import '../models/page_rules_update_a_page_rule_request.dart';import '../models/response_common87.dart';import '../models/zones_schemas_identifier.dart';/// PageRulesApi operations.
///
/// All operations return [ApiResult] - use pattern matching to handle
/// success, error, and exception cases.
final class PageRulesApi {const PageRulesApi(this._config);

final ApiConfig _config;

/// List Page Rules
///
/// Fetches Page Rules in a zone.
///
/// `GET /zones/{zone_id}/pagerules`
Future<ApiResult<ResponseCommon87, Never>> pageRulesListPageRules({required ZonesSchemasIdentifier zoneId, PageRulesListPageRulesOrder? order, PageRulesListPageRulesDirection? direction, PageRulesListPageRulesMatch? match, PageRulesListPageRulesStatus? status, RequestOptions? options, }) async  { final queryParameters = <String, String>{..._config.defaultQueryParameters};
final queryParametersList = <ApiQueryParameter>[];
if (order != null) queryParameters['order'] = order.toJson();
if (direction != null) queryParameters['direction'] = direction.toJson();
if (match != null) queryParameters['match'] = match.toJson();
if (status != null) queryParameters['status'] = status.toJson();

final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules',
  headers: headers,
  queryParameters: queryParameters,
  queryParametersList: queryParametersList,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a Page Rule
///
/// Creates a new Page Rule.
///
/// `POST /zones/{zone_id}/pagerules`
Future<ApiResult<ResponseCommon87, Never>> pageRulesCreateAPageRule({required ZonesSchemasIdentifier zoneId, required PageRulesCreateAPageRuleRequest body, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};
headers['Content-Type'] = 'application/json';

final request = ApiRequest(
  method: 'POST',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules',
  headers: headers,
  body: jsonEncode(body.toJson()),
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get a Page Rule
///
/// Fetches the details of a Page Rule.
///
/// `GET /zones/{zone_id}/pagerules/{pagerule_id}`
Future<ApiResult<ResponseCommon87, Never>> pageRulesGetAPageRule({required ZonesSchemasIdentifier pageruleId, required ZonesSchemasIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules/${Uri.encodeComponent(pageruleId.toString())}',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a Page Rule
///
/// Replaces the configuration of an existing Page Rule. The configuration of the updated Page Rule will exactly match the data passed in the API request.
///
/// `PUT /zones/{zone_id}/pagerules/{pagerule_id}`
Future<ApiResult<ResponseCommon87, Never>> pageRulesUpdateAPageRule({required ZonesSchemasIdentifier pageruleId, required ZonesSchemasIdentifier zoneId, required PageRulesUpdateAPageRuleRequest body, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};
headers['Content-Type'] = 'application/json';

final request = ApiRequest(
  method: 'PUT',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules/${Uri.encodeComponent(pageruleId.toString())}',
  headers: headers,
  body: jsonEncode(body.toJson()),
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Edit a Page Rule
///
/// Updates one or more fields of an existing Page Rule.
///
/// `PATCH /zones/{zone_id}/pagerules/{pagerule_id}`
Future<ApiResult<ResponseCommon87, Never>> pageRulesEditAPageRule({required ZonesSchemasIdentifier pageruleId, required ZonesSchemasIdentifier zoneId, required PageRulesEditAPageRuleRequest body, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};
headers['Content-Type'] = 'application/json';

final request = ApiRequest(
  method: 'PATCH',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules/${Uri.encodeComponent(pageruleId.toString())}',
  headers: headers,
  body: jsonEncode(body.toJson()),
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a Page Rule
///
/// Deletes an existing Page Rule.
///
/// `DELETE /zones/{zone_id}/pagerules/{pagerule_id}`
Future<ApiResult<ResponseCommon87, Never>> pageRulesDeleteAPageRule({required ZonesSchemasIdentifier pageruleId, required ZonesSchemasIdentifier zoneId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'DELETE',
  path: '/zones/${Uri.encodeComponent(zoneId.toString())}/pagerules/${Uri.encodeComponent(pageruleId.toString())}',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon87.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final userCancelToken = request.options?.cancelToken;
  if (userCancelToken?.isCancelled ?? false) throw const CancelledException();

  final effectiveTimeout = request.options?.timeout ?? _config.timeout;
  final extraHeaders = request.options?.extraHeaders;

  // Merge timeout and user cancel into a single adapter-level cancel token.
  final adapterToken = (effectiveTimeout != null || userCancelToken != null)
      ? CancelToken()
      : null;
  Timer? timeoutTimer;
  bool timedOut = false;

  if (adapterToken != null) {
    if (userCancelToken != null) {
      final token = adapterToken;
      userCancelToken.whenCancelled.then((_) {
        if (!token.isCancelled) token.cancel();
      });
    }
    if (effectiveTimeout != null) {
      final token = adapterToken;
      timeoutTimer = Timer(effectiveTimeout, () {
        timedOut = true;
        if (!token.isCancelled) token.cancel();
      });
    }
  }

  final effectiveRequest = request.copyWith(
    headers: extraHeaders != null
        ? {...request.headers, ...extraHeaders}
        : null,
    options: RequestOptions(cancelToken: adapterToken),
  );

  try {
    final chain = buildInterceptorChain(
      interceptors: _config.interceptors,
      terminal: (req) => _config.client.send(req),
    );

    final response = await chain(effectiveRequest);
    timeoutTimer?.cancel();

    try {
      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        error: onError != null ? onError(response) : null,
        rawError: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      return ApiParseException(e, st, response: response);
    }
  } on CancelledException {
    timeoutTimer?.cancel();
    if (timedOut) {
      throw TimeoutException('Request timed out', effectiveTimeout);
    }
    rethrow;
  }
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
