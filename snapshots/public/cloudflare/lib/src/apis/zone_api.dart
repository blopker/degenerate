//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/response_common83.dart';import '../models/response_common85.dart';import '../models/response_single_id.dart';import '../models/zone_purge_request.dart';import '../models/zones0_patch_request.dart';import '../models/zones_get_direction.dart';import '../models/zones_get_match.dart';import '../models/zones_get_order.dart';import '../models/zones_get_status.dart';import '../models/zones_post_request.dart';/// ZoneApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZoneApi {const ZoneApi(this._config);

final ApiConfig _config;

/// List Zones
///
/// Lists, searches, sorts, and filters your zones. Listing zones across more than 500 accounts
/// is currently not allowed.
/// 
///
/// `GET /zones`
Future<ApiResult<ResponseCommon85, Never>> zonesGet({String? name, ZonesGetStatus? status, String? accountId, String? accountName, double? page, double? perPage, ZonesGetOrder? order, ZonesGetDirection? direction, ZonesGetMatch? match, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'name': ?name,
    if (status != null) 'status': status.toJson(),
    'account.id': ?accountId,
    'account.name': ?accountName,
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (order != null) 'order': order.toJson(),
    if (direction != null) 'direction': direction.toJson(),
    if (match != null) 'match': match.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon85.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create Zone
///
/// `POST /zones`
Future<ApiResult<ResponseCommon85, Never>> zonesPost({required ZonesPostRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/zones',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon85.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Zone Details
///
/// `GET /zones/{zone_id}`
Future<ApiResult<ResponseCommon85, Never>> zones0Get({required String zoneId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon85.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Edit Zone
///
/// Edits a zone. Only one zone property can be changed at a time.
///
/// `PATCH /zones/{zone_id}`
Future<ApiResult<ResponseCommon85, Never>> zones0Patch({required String zoneId, required Zones0PatchRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/zones/${Uri.encodeComponent(zoneId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon85.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete Zone
///
/// Deletes an existing zone.
///
/// `DELETE /zones/{zone_id}`
Future<ApiResult<ResponseCommon85, Never>> zones0Delete({required String zoneId}) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/zones/${Uri.encodeComponent(zoneId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon85.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Rerun the Activation Check
///
/// Triggeres a new activation check for a PENDING Zone. This can be
/// triggered every 5 min for paygo/ent customers, every hour for FREE
/// Zones.
///
/// `PUT /zones/{zone_id}/activation_check`
Future<ApiResult<ResponseCommon83, Never>> putZonesZoneIdActivationCheck({required String zoneId}) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/zones/${Uri.encodeComponent(zoneId)}/activation_check',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon83.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Purge Cached Content
///
/// ### Purge All Cached Content
/// Removes ALL files from Cloudflare's cache. All tiers can purge everything.
/// ````
/// {"purge_everything": true}
/// ````
/// 
/// ### Purge Cached Content by URL
/// Granularly removes one or more files from Cloudflare's cache by specifying URLs. All tiers can purge by URL.
/// 
/// To purge files with custom cache keys, include the headers used to compute the cache key as in the example. If you have a device type or geo in your cache key, you will need to include the CF-Device-Type or CF-IPCountry headers. If you have lang in your cache key, you will need to include the Accept-Language header.
/// 
/// **NB:** When including the Origin header, be sure to include the **scheme** and **hostname**. The port number can be omitted if it is the default port (80 for http, 443 for https), but must be included otherwise.
/// 
/// Single file purge example with files:
/// ````
/// {"files": ["http://www.example.com/css/styles.css", "http://www.example.com/js/index.js"]}
/// ````
/// Single file purge example with url and header pairs:
/// ````
/// {"files": [{url: "http://www.example.com/cat_picture.jpg", headers: { "CF-IPCountry": "US", "CF-Device-Type": "desktop", "Accept-Language": "zh-CN" }}, {url: "http://www.example.com/dog_picture.jpg", headers: { "CF-IPCountry": "EU", "CF-Device-Type": "mobile", "Accept-Language": "en-US" }}]}
/// ````
/// 
/// ### Purge Cached Content by Tag, Host or Prefix
/// Granularly removes one or more files from Cloudflare's cache either by specifying the host, the associated Cache-Tag, or a Prefix.
/// 
/// Flex purge with tags:
/// ````
/// {"tags": ["a-cache-tag", "another-cache-tag"]}
/// ````
/// Flex purge with hosts:
/// ````
/// {"hosts": ["www.example.com", "images.example.com"]}
/// ````
/// Flex purge with prefixes:
/// ````
/// {"prefixes": ["www.example.com/foo", "images.example.com/bar/baz"]}
/// ````
/// 
/// ### Availability and limits
/// please refer to [purge cache availability and limits documentation page](https://developers.cloudflare.com/cache/how-to/purge-cache/#availability-and-limits).
/// 
///
/// `POST /zones/{zone_id}/purge_cache`
Future<ApiResult<ResponseSingleId, Never>> zonePurge({required String zoneId, required ZonePurgeRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/zones/${Uri.encodeComponent(zoneId)}/purge_cache',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseSingleId.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
