//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/dns_analytics_time_delta.dart';import '../models/response_common22.dart';/// DnsAnalyticsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DnsAnalyticsApi {const DnsAnalyticsApi(this._config);

final ApiConfig _config;

/// Table
///
/// Retrieves a list of summarised aggregate metrics over a given time period.
/// 
/// See [Analytics API properties](https://developers.cloudflare.com/dns/reference/analytics-api-properties/) for detailed information about the available query parameters.
///
/// `GET /zones/{zone_id}/dns_analytics/report`
Future<ApiResult<ResponseCommon22, Never>> dnsAnalyticsTable({required String zoneId, String? metrics, String? dimensions, DateTime? since, DateTime? until, int? limit, String? sort, String? filters, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId)}/dns_analytics/report',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'metrics': ?metrics,
    'dimensions': ?dimensions,
    if (since != null) 'since': since.toString(),
    if (until != null) 'until': until.toString(),
    if (limit != null) 'limit': limit.toString(),
    'sort': ?sort,
    'filters': ?filters,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon22.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// By Time
///
/// Retrieves a list of aggregate metrics grouped by time interval.
/// 
/// See [Analytics API properties](https://developers.cloudflare.com/dns/reference/analytics-api-properties/) for detailed information about the available query parameters.
///
/// `GET /zones/{zone_id}/dns_analytics/report/bytime`
Future<ApiResult<ResponseCommon22, Never>> dnsAnalyticsByTime({required String zoneId, String? metrics, String? dimensions, DateTime? since, DateTime? until, int? limit, String? sort, String? filters, DnsAnalyticsTimeDelta? timeDelta, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId)}/dns_analytics/report/bytime',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'metrics': ?metrics,
    'dimensions': ?dimensions,
    if (since != null) 'since': since.toString(),
    if (until != null) 'until': until.toString(),
    if (limit != null) 'limit': limit.toString(),
    'sort': ?sort,
    'filters': ?filters,
    if (timeDelta != null) 'time_delta': timeDelta.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon22.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
