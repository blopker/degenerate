//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic1_b_request.dart';import '../models/workers_ai_post_run_cf_ai4bharat_omni_indictrans2_en_indic1b_request.dart';import '../models/workers_ai_post_run_cf_meta_m2m10012b_request.dart';/// WorkersAiTranslationApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiTranslationApi {const WorkersAiTranslationApi(this._config);

final ApiConfig _config;

/// Execute @cf/ai4bharat/indictrans2-en-indic-1B model.
///
/// Runs inference on the @cf/ai4bharat/indictrans2-en-indic-1B model.
///
/// `POST /accounts/{account_id}/ai/run/@cf/ai4bharat/indictrans2-en-indic-1B`
Future<ApiResult<Map<String, String>>> workersAiPostRunCfAi4bharatIndictrans2EnIndic1B({required String accountId, String? queueRequest, String? tags, WorkersAiPostRunCfAi4bharatIndictrans2EnIndic1BRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/ai4bharat/indictrans2-en-indic-1B',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    'queueRequest': ?queueRequest,
    'tags': ?tags,
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Execute @cf/ai4bharat/omni-indictrans2-en-indic-1b model.
///
/// Runs inference on the @cf/ai4bharat/omni-indictrans2-en-indic-1b model.
///
/// `POST /accounts/{account_id}/ai/run/@cf/ai4bharat/omni-indictrans2-en-indic-1b`
Future<ApiResult<Map<String, String>>> workersAiPostRunCfAi4bharatOmniIndictrans2EnIndic1b({required String accountId, String? queueRequest, String? tags, WorkersAiPostRunCfAi4bharatOmniIndictrans2EnIndic1bRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/ai4bharat/omni-indictrans2-en-indic-1b',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    'queueRequest': ?queueRequest,
    'tags': ?tags,
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Execute @cf/meta/m2m100-1.2b model.
///
/// Runs inference on the @cf/meta/m2m100-1.2b model.
///
/// `POST /accounts/{account_id}/ai/run/@cf/meta/m2m100-1.2b`
Future<ApiResult<Map<String, String>>> workersAiPostRunCfMetaM2m10012b({required String accountId, String? queueRequest, String? tags, WorkersAiPostRunCfMetaM2m10012bRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/meta/m2m100-1.2b',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    'queueRequest': ?queueRequest,
    'tags': ?tags,
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
