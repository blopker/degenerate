//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/magic_app_config_add_single_request.dart';import '../models/magic_app_config_update_request.dart';import '../models/magic_apps_response_object.dart';/// MagicSiteAppConfigsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class MagicSiteAppConfigsApi {const MagicSiteAppConfigsApi(this._config);

final ApiConfig _config;

/// List App Configs
///
/// Lists App Configs associated with a site.
///
/// `GET /accounts/{account_id}/magic/sites/{site_id}/app_configs`
Future<ApiResult<MagicAppsResponseObject, Never>> magicSiteAppConfigsListAppConfigs({required String accountId, required String siteId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/magic/sites/${Uri.encodeComponent(siteId)}/app_configs',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return MagicAppsResponseObject.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a new App Config
///
/// Creates a new App Config for a site
///
/// `POST /accounts/{account_id}/magic/sites/{site_id}/app_configs`
Future<ApiResult<MagicAppsResponseObject, Never>> magicSiteAppConfigsAddAppConfig({required String accountId, required String siteId, required MagicAppConfigAddSingleRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/magic/sites/${Uri.encodeComponent(siteId)}/app_configs',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return MagicAppsResponseObject.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update an App Config
///
/// Updates an App Config for a site
///
/// `PUT /accounts/{account_id}/magic/sites/{site_id}/app_configs/{app_config_id}`
Future<ApiResult<MagicAppsResponseObject, Never>> magicSiteAppConfigsUpdateAppConfig({required String accountId, required String siteId, required String appConfigId, required MagicAppConfigUpdateRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/accounts/${Uri.encodeComponent(accountId)}/magic/sites/${Uri.encodeComponent(siteId)}/app_configs/${Uri.encodeComponent(appConfigId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return MagicAppsResponseObject.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update an App Config
///
/// Updates an App Config for a site
///
/// `PATCH /accounts/{account_id}/magic/sites/{site_id}/app_configs/{app_config_id}`
Future<ApiResult<MagicAppsResponseObject, Never>> magicSiteAppConfigsPatchAppConfig({required String accountId, required String siteId, required String appConfigId, required MagicAppConfigUpdateRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/accounts/${Uri.encodeComponent(accountId)}/magic/sites/${Uri.encodeComponent(siteId)}/app_configs/${Uri.encodeComponent(appConfigId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return MagicAppsResponseObject.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete App Config
///
/// Deletes specific App Config associated with a site.
///
/// `DELETE /accounts/{account_id}/magic/sites/{site_id}/app_configs/{app_config_id}`
Future<ApiResult<MagicAppsResponseObject, Never>> magicSiteAppConfigsDeleteAppConfig({required String accountId, required String siteId, required String appConfigId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/magic/sites/${Uri.encodeComponent(siteId)}/app_configs/${Uri.encodeComponent(appConfigId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return MagicAppsResponseObject.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, error: onError != null ? onError(recovered) : null, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
