//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/list_warp_change_events_sort_order.dart';import '../models/list_warp_change_events_toggle.dart';import '../models/list_warp_change_events_type.dart';import '../models/response_common19.dart';/// WarpChangeEventsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WarpChangeEventsApi {const WarpChangeEventsApi(this._config);

final ApiConfig _config;

/// List WARP change events.
///
/// List WARP configuration and enablement toggle change events by device.
///
/// `GET /accounts/{account_id}/dex/warp-change-events`
Future<ApiResult<ResponseCommon19, Never>> listWarpChangeEvents({required String accountId, required double page, required double perPage, required String from, required String to, ListWarpChangeEventsType? type, ListWarpChangeEventsToggle? toggle, String? configName, String? accountName, ListWarpChangeEventsSortOrder? sortOrder, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/dex/warp-change-events',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'page': page.toString(),
    'per_page': perPage.toString(),
    'from': from,
    'to': to,
    if (type != null) 'type': type.toJson(),
    if (toggle != null) 'toggle': toggle.toJson(),
    'config_name': ?configName,
    'account_name': ?accountName,
    if (sortOrder != null) 'sort_order': sortOrder.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon19.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, error: onError != null ? onError(recovered) : null, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
