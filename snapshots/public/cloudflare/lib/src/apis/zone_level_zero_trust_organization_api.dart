//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/access_empty_response.dart';import '../models/response_common3.dart';import '../models/zone_level_zero_trust_organization_create_your_zero_trust_organization_request.dart';import '../models/zone_level_zero_trust_organization_revoke_all_access_tokens_for_a_user_request.dart';import '../models/zone_level_zero_trust_organization_update_your_zero_trust_organization_request.dart';/// ZoneLevelZeroTrustOrganizationApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZoneLevelZeroTrustOrganizationApi {const ZoneLevelZeroTrustOrganizationApi(this._config);

final ApiConfig _config;

/// Get your Zero Trust organization
///
/// Returns the configuration for your Zero Trust organization.
///
/// `GET /zones/{zone_id}/access/organizations`
Future<ApiResult<ResponseCommon3>> zoneLevelZeroTrustOrganizationGetYourZeroTrustOrganization({required String zoneId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId)}/access/organizations',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon3.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create your Zero Trust organization
///
/// Sets up a Zero Trust organization for your account.
///
/// `POST /zones/{zone_id}/access/organizations`
Future<ApiResult<ResponseCommon3>> zoneLevelZeroTrustOrganizationCreateYourZeroTrustOrganization({required String zoneId, required ZoneLevelZeroTrustOrganizationCreateYourZeroTrustOrganizationRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/zones/${Uri.encodeComponent(zoneId)}/access/organizations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon3.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update your Zero Trust organization
///
/// Updates the configuration for your Zero Trust organization.
///
/// `PUT /zones/{zone_id}/access/organizations`
Future<ApiResult<ResponseCommon3>> zoneLevelZeroTrustOrganizationUpdateYourZeroTrustOrganization({required String zoneId, required ZoneLevelZeroTrustOrganizationUpdateYourZeroTrustOrganizationRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/zones/${Uri.encodeComponent(zoneId)}/access/organizations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon3.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Revoke all Access tokens for a user
///
/// Revokes a user's access across all applications.
///
/// `POST /zones/{zone_id}/access/organizations/revoke_user`
Future<ApiResult<AccessEmptyResponse>> zoneLevelZeroTrustOrganizationRevokeAllAccessTokensForAUser({required String zoneId, required ZoneLevelZeroTrustOrganizationRevokeAllAccessTokensForAUserRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/zones/${Uri.encodeComponent(zoneId)}/access/organizations/revoke_user',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return AccessEmptyResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
