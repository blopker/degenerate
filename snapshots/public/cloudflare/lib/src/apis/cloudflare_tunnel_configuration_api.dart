//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/cloudflare_tunnel_configuration_put_configuration_request.dart';import '../models/response_common70.dart';/// CloudflareTunnelConfigurationApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class CloudflareTunnelConfigurationApi {const CloudflareTunnelConfigurationApi(this._config);

final ApiConfig _config;

/// Get configuration
///
/// Gets the configuration for a remotely-managed tunnel
///
/// `GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations`
Future<ApiResult<ResponseCommon70>> cloudflareTunnelConfigurationGetConfiguration({required String accountId, required String tunnelId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cfd_tunnel/${Uri.encodeComponent(tunnelId)}/configurations',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon70.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Put configuration
///
/// Adds or updates the configuration for a remotely-managed tunnel.
///
/// `PUT /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations`
Future<ApiResult<ResponseCommon70>> cloudflareTunnelConfigurationPutConfiguration({required String accountId, required String tunnelId, required CloudflareTunnelConfigurationPutConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cfd_tunnel/${Uri.encodeComponent(tunnelId)}/configurations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon70.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
