//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/response_common42.dart';/// LoadBalancerHealthcheckEventsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class LoadBalancerHealthcheckEventsApi {const LoadBalancerHealthcheckEventsApi(this._config);

final ApiConfig _config;

/// List Healthcheck Events
///
/// List origin health changes.
///
/// `GET /user/load_balancing_analytics/events`
Future<ApiResult<ResponseCommon42, Never>> loadBalancerHealthcheckEventsListHealthcheckEvents({DateTime? until, String? poolName, bool? originHealthy, String? poolId, DateTime? since, String? originName, bool? poolHealthy, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/load_balancing_analytics/events',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (until != null) 'until': until.toString(),
    'pool_name': ?poolName,
    if (originHealthy != null) 'origin_healthy': originHealthy.toString(),
    'pool_id': ?poolId,
    if (since != null) 'since': since.toString(),
    'origin_name': ?originName,
    if (poolHealthy != null) 'pool_healthy': poolHealthy.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon42.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
