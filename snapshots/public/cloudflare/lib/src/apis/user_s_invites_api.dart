//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/response_common35.dart';import '../models/user_s_invites_respond_to_invitation_request.dart';/// UserSInvitesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class UserSInvitesApi {const UserSInvitesApi(this._config);

final ApiConfig _config;

/// List Invitations
///
/// Lists all invitations associated with my user.
///
/// `GET /user/invites`
Future<ApiResult<ResponseCommon35>> userSInvitesListInvitations() async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/invites',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon35.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Invitation Details
///
/// Gets the details of an invitation.
///
/// `GET /user/invites/{invite_id}`
Future<ApiResult<ResponseCommon35>> userSInvitesInvitationDetails({required String inviteId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/invites/${Uri.encodeComponent(inviteId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon35.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Respond to Invitation
///
/// Responds to an invitation.
///
/// `PATCH /user/invites/{invite_id}`
Future<ApiResult<ResponseCommon35>> userSInvitesRespondToInvitation({required String inviteId, required UserSInvitesRespondToInvitationRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/user/invites/${Uri.encodeComponent(inviteId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon35.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
