//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/tenants_list_accounts_response.dart';import '../models/tenants_list_entitlements_response.dart';import '../models/tenants_list_memberships_response.dart';import '../models/tenants_retrieve_tenant_response.dart';import '../models/tenants_valid_account_types_response.dart';/// TenantsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TenantsApi {const TenantsApi(this._config);

final ApiConfig _config;

/// Get tenant
///
/// Retrieves a Tenant by Tenant ID.
///
/// `GET /tenants/{tenant_id}`
Future<ApiResult<TenantsRetrieveTenantResponse>> tenantsRetrieveTenant({required String tenantId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/tenants/${Uri.encodeComponent(tenantId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return TenantsRetrieveTenantResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get tenant account types
///
/// List of account types available for the Tenant to provision accounts.
///
/// `GET /tenants/{tenant_id}/account_types`
Future<ApiResult<TenantsValidAccountTypesResponse>> tenantsValidAccountTypes({required String tenantId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/tenants/${Uri.encodeComponent(tenantId)}/account_types',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return TenantsValidAccountTypesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List tenant accounts
///
/// List of accounts for the Tenant.
///
/// `GET /tenants/{tenant_id}/accounts`
Future<ApiResult<TenantsListAccountsResponse>> tenantsListAccounts({required String tenantId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/tenants/${Uri.encodeComponent(tenantId)}/accounts',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return TenantsListAccountsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List tenant entitlements
///
/// List of innate entitlements available for the Tenant.
///
/// `GET /tenants/{tenant_id}/entitlements`
Future<ApiResult<TenantsListEntitlementsResponse>> tenantsListEntitlements({required String tenantId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/tenants/${Uri.encodeComponent(tenantId)}/entitlements',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return TenantsListEntitlementsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List tenant memberships
///
/// List of active members (Cloudflare users) for the Tenant.
///
/// `GET /tenants/{tenant_id}/memberships`
Future<ApiResult<TenantsListMembershipsResponse>> tenantsListMemberships({required String tenantId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/tenants/${Uri.encodeComponent(tenantId)}/memberships',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return TenantsListMembershipsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
