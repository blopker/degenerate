//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/builds_create_build_request.dart';import '../models/builds_create_trigger_request.dart';import '../models/builds_update_trigger_request.dart';import '../models/response.dart';/// TriggersApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TriggersApi {const TriggersApi(this._config);

final ApiConfig _config;

/// Create trigger
///
/// Create a new CI/CD trigger
///
/// `POST /accounts/{account_id}/builds/triggers`
Future<ApiResult<Response>> createTrigger({required String accountId, required BuildsCreateTriggerRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/builds/triggers',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return Response.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update trigger
///
/// Update an existing CI/CD trigger
///
/// `PATCH /accounts/{account_id}/builds/triggers/{trigger_uuid}`
Future<ApiResult<Response>> updateTrigger({required String accountId, required String triggerUuid, required BuildsUpdateTriggerRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/accounts/${Uri.encodeComponent(accountId)}/builds/triggers/${Uri.encodeComponent(triggerUuid)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return Response.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete trigger
///
/// Remove a CI/CD trigger
///
/// `DELETE /accounts/{account_id}/builds/triggers/{trigger_uuid}`
Future<ApiResult<Response>> deleteTrigger({required String accountId, required String triggerUuid, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/builds/triggers/${Uri.encodeComponent(triggerUuid)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Response.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create manual build
///
/// Trigger a manual build for a specific trigger
///
/// `POST /accounts/{account_id}/builds/triggers/{trigger_uuid}/builds`
Future<ApiResult<Response>> createManualBuild({required String accountId, required String triggerUuid, required BuildsCreateBuildRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/builds/triggers/${Uri.encodeComponent(triggerUuid)}/builds',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return Response.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Purge build cache
///
/// Clear the build cache for a specific trigger
///
/// `POST /accounts/{account_id}/builds/triggers/{trigger_uuid}/purge_build_cache`
Future<ApiResult<Response>> purgeBuildCache({required String accountId, required String triggerUuid, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/builds/triggers/${Uri.encodeComponent(triggerUuid)}/purge_build_cache',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Response.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
