//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/response_common6.dart';import '../models/shield_configuration.dart';/// ShieldSettingsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ShieldSettingsApi {const ShieldSettingsApi(this._config);

final ApiConfig _config;

/// Retrieve information about specific configuration properties
///
/// Gets the current API Shield configuration settings for a zone, including validation behavior and enforcement mode.
///
/// `GET /zones/{zone_id}/api_gateway/configuration`
Future<ApiResult<ResponseCommon6, Never>> apiShieldSettingsRetrieveInformationAboutSpecificConfigurationProperties({required String zoneId, bool? normalize, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/zones/${Uri.encodeComponent(zoneId)}/api_gateway/configuration',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (normalize != null) 'normalize': normalize.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon6.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update configuration properties
///
/// Updates API Shield configuration settings for a zone. Can modify validation strictness, enforcement mode, and other global settings.
///
/// `PUT /zones/{zone_id}/api_gateway/configuration`
Future<ApiResult<ResponseCommon6, Never>> apiShieldSettingsSetConfigurationProperties({required String zoneId, bool? normalize, required ShieldConfiguration body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/zones/${Uri.encodeComponent(zoneId)}/api_gateway/configuration',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    if (normalize != null) 'normalize': normalize.toString(),
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon6.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
