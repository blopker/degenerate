//  dart format off
//  GENERATED CODE - DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:async';import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_response400.dart';import '../models/workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_response4002.dart';import '../models/workers_ai_post_websocket_run_cf_test_hello_world_cog_response400.dart';/// WorkersAiDumbPipeApi operations.
///
/// All operations return [ApiResult] - use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiDumbPipeApi {const WorkersAiDumbPipeApi(this._config);

final ApiConfig _config;

/// Open Websocket connection with @cf/pipecat-ai/smart-turn-v2 model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/pipecat-ai/smart-turn-v2 model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/pipecat-ai/smart-turn-v2`
Future<ApiResult<void, WorkersAiPostWebsocketRunCfPipecatAiSmartTurnResponse400>> workersAiPostWebsocketRunCfPipecatAiSmartTurnV2({required String accountId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/pipecat-ai/smart-turn-v2',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (_) {},
  onError: (response) {
    return WorkersAiPostWebsocketRunCfPipecatAiSmartTurnResponse400.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Open Websocket connection with @cf/pipecat-ai/smart-turn-v3 model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/pipecat-ai/smart-turn-v3 model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/pipecat-ai/smart-turn-v3`
Future<ApiResult<void, WorkersAiPostWebsocketRunCfPipecatAiSmartTurnResponse4002>> workersAiPostWebsocketRunCfPipecatAiSmartTurnV3({required String accountId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/pipecat-ai/smart-turn-v3',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (_) {},
  onError: (response) {
    return WorkersAiPostWebsocketRunCfPipecatAiSmartTurnResponse4002.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Open Websocket connection with @cf/test/hello-world-cog model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/test/hello-world-cog model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/test/hello-world-cog`
Future<ApiResult<void, WorkersAiPostWebsocketRunCfTestHelloWorldCogResponse400>> workersAiPostWebsocketRunCfTestHelloWorldCog({required String accountId, RequestOptions? options, }) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/test/hello-world-cog',
  headers: headers,
  options: options,
);

return _execute(
  request,
  onSuccess: (_) {},
  onError: (response) {
    return WorkersAiPostWebsocketRunCfTestHelloWorldCogResponse400.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final userCancelToken = request.options?.cancelToken;
  if (userCancelToken?.isCancelled ?? false) throw const CancelledException();

  final effectiveTimeout = request.options?.timeout ?? _config.timeout;
  final extraHeaders = request.options?.extraHeaders;

  // Merge timeout and user cancel into a single adapter-level cancel token.
  final adapterToken = (effectiveTimeout != null || userCancelToken != null)
      ? CancelToken()
      : null;
  Timer? timeoutTimer;
  bool timedOut = false;

  if (adapterToken != null) {
    if (userCancelToken != null) {
      final token = adapterToken;
      userCancelToken.whenCancelled.then((_) {
        if (!token.isCancelled) token.cancel();
      });
    }
    if (effectiveTimeout != null) {
      final token = adapterToken;
      timeoutTimer = Timer(effectiveTimeout, () {
        timedOut = true;
        if (!token.isCancelled) token.cancel();
      });
    }
  }

  final effectiveRequest = request.copyWith(
    headers: extraHeaders != null
        ? {...request.headers, ...extraHeaders}
        : null,
    options: RequestOptions(cancelToken: adapterToken),
  );

  try {
    final chain = buildInterceptorChain(
      interceptors: _config.interceptors,
      terminal: (req) => _config.client.send(req),
    );

    final response = await chain(effectiveRequest);
    timeoutTimer?.cancel();

    try {
      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        error: onError != null ? onError(response) : null,
        rawError: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      return ApiParseException(e, st, response: response);
    }
  } on CancelledException {
    timeoutTimer?.cancel();
    if (timedOut) {
      throw TimeoutException('Request timed out', effectiveTimeout);
    }
    rethrow;
  }
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
