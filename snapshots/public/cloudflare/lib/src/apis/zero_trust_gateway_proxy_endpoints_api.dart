//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/response_common82.dart';import '../models/zero_trust_gateway_proxy_endpoints_create_proxy_endpoint_request.dart';import '../models/zero_trust_gateway_proxy_endpoints_update_proxy_endpoint_request.dart';/// ZeroTrustGatewayProxyEndpointsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZeroTrustGatewayProxyEndpointsApi {const ZeroTrustGatewayProxyEndpointsApi(this._config);

final ApiConfig _config;

/// List proxy endpoints
///
/// List all Zero Trust Gateway proxy endpoints for an account.
///
/// `GET /accounts/{account_id}/gateway/proxy_endpoints`
Future<ApiResult<ResponseCommon82>> zeroTrustGatewayProxyEndpointsListProxyEndpoints({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/proxy_endpoints',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon82.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a proxy endpoint
///
/// Create a new Zero Trust Gateway proxy endpoint.
///
/// `POST /accounts/{account_id}/gateway/proxy_endpoints`
Future<ApiResult<ResponseCommon82>> zeroTrustGatewayProxyEndpointsCreateProxyEndpoint({required String accountId, required ZeroTrustGatewayProxyEndpointsCreateProxyEndpointRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/proxy_endpoints',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon82.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get a proxy endpoint
///
/// Get a single Zero Trust Gateway proxy endpoint.
///
/// `GET /accounts/{account_id}/gateway/proxy_endpoints/{proxy_endpoint_id}`
Future<ApiResult<ResponseCommon82>> zeroTrustGatewayProxyEndpointsProxyEndpointDetails({required String proxyEndpointId, required String accountId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/proxy_endpoints/${Uri.encodeComponent(proxyEndpointId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon82.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a proxy endpoint
///
/// Update a configured Zero Trust Gateway proxy endpoint.
///
/// `PATCH /accounts/{account_id}/gateway/proxy_endpoints/{proxy_endpoint_id}`
Future<ApiResult<ResponseCommon82>> zeroTrustGatewayProxyEndpointsUpdateProxyEndpoint({required String proxyEndpointId, required String accountId, required ZeroTrustGatewayProxyEndpointsUpdateProxyEndpointRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/proxy_endpoints/${Uri.encodeComponent(proxyEndpointId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon82.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a proxy endpoint
///
/// Delete a configured Zero Trust Gateway proxy endpoint.
///
/// `DELETE /accounts/{account_id}/gateway/proxy_endpoints/{proxy_endpoint_id}`
Future<ApiResult<ResponseCommon82>> zeroTrustGatewayProxyEndpointsDeleteProxyEndpoint({required String proxyEndpointId, required String accountId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/proxy_endpoints/${Uri.encodeComponent(proxyEndpointId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon82.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
