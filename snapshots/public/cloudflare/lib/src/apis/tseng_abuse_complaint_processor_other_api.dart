//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/abuse_reports_mitigated_entity_type.dart';import '../models/abuse_reports_mitigation_appeal_request.dart';import '../models/abuse_reports_mitigation_appeal_result.dart';import '../models/abuse_reports_mitigation_status.dart';import '../models/abuse_reports_mitigation_type.dart';import '../models/abuse_reports_report_status.dart';import '../models/abuse_reports_report_type.dart';import '../models/abuse_reports_submit_report_request.dart';import '../models/abuse_reports_submit_report_response.dart';import '../models/get_abuse_report_response.dart';import '../models/list_abuse_reports_response.dart';import '../models/list_emails_response.dart';import '../models/list_mitigations_response.dart';import '../models/list_mitigations_sort.dart';/// TsengAbuseComplaintProcessorOtherApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TsengAbuseComplaintProcessorOtherApi {const TsengAbuseComplaintProcessorOtherApi(this._config);

final ApiConfig _config;

/// List abuse reports
///
/// List the abuse reports for a given account
///
/// `GET /accounts/{account_id}/abuse-reports`
Future<ApiResult<ListAbuseReportsResponse>> listAbuseReports({required String accountId, int? page, int? perPage, String? sort, String? domain, String? createdBefore, String? createdAfter, AbuseReportsReportStatus? status, AbuseReportsReportType? type, AbuseReportsMitigationStatus? mitigationStatus, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    'sort': ?sort,
    'domain': ?domain,
    'created_before': ?createdBefore,
    'created_after': ?createdAfter,
    if (status != null) 'status': status.toJson(),
    if (type != null) 'type': type.toJson(),
    if (mitigationStatus != null) 'mitigation_status': mitigationStatus.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ListAbuseReportsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List abuse report emails
///
/// List emails sent to the customer for an abuse report. Returns all successful customer emails sent for the specified abuse report. Does not include emails sent to hosts or submitters.
///
/// `GET /accounts/{account_id}/abuse-reports/{report_id}/emails`
Future<ApiResult<ListEmailsResponse>> listEmails({required String accountId, required String reportId, int? page, int? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports/${Uri.encodeComponent(reportId)}/emails',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ListEmailsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List abuse report mitigations
///
/// List mitigations done to remediate the abuse report.
///
/// `GET /accounts/{account_id}/abuse-reports/{report_id}/mitigations`
Future<ApiResult<ListMitigationsResponse>> listMitigations({required String accountId, required String reportId, int? page, int? perPage, ListMitigationsSort? sort, AbuseReportsMitigationType? type, String? effectiveBefore, String? effectiveAfter, AbuseReportsMitigationStatus? status, AbuseReportsMitigatedEntityType? entityType, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports/${Uri.encodeComponent(reportId)}/mitigations',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (sort != null) 'sort': sort.toJson(),
    if (type != null) 'type': type.toJson(),
    'effective_before': ?effectiveBefore,
    'effective_after': ?effectiveAfter,
    if (status != null) 'status': status.toJson(),
    if (entityType != null) 'entity_type': entityType.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ListMitigationsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Request review on mitigations
///
/// Request a review for mitigations on an account.
///
/// `POST /accounts/{account_id}/abuse-reports/{report_id}/mitigations/appeal`
Future<ApiResult<AbuseReportsMitigationAppealResult>> requestReview({required String accountId, required String reportId, required AbuseReportsMitigationAppealRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports/${Uri.encodeComponent(reportId)}/mitigations/appeal',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return AbuseReportsMitigationAppealResult.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Abuse Report Details
///
/// Retrieve the details of an abuse report.
///
/// `GET /accounts/{account_id}/abuse-reports/{report_param}`
Future<ApiResult<GetAbuseReportResponse>> getAbuseReport({required String accountId, required String reportParam, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports/${Uri.encodeComponent(reportParam)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return GetAbuseReportResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Submit an abuse report
///
/// Submit the Abuse Report of a particular type
///
/// `POST /accounts/{account_id}/abuse-reports/{report_param}`
Future<ApiResult<AbuseReportsSubmitReportResponse>> submitAbuseReport({required String accountId, required String reportParam, required AbuseReportsSubmitReportRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/abuse-reports/${Uri.encodeComponent(reportParam)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return AbuseReportsSubmitReportResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
