//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/response_common58.dart';import '../models/secondary_dns_peer.dart';import '../models/secondary_dns_peer_create_peer_request.dart';/// SecondaryDnsPeerApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SecondaryDnsPeerApi {const SecondaryDnsPeerApi(this._config);

final ApiConfig _config;

/// List Peers
///
/// List Peers.
///
/// `GET /accounts/{account_id}/secondary_dns/peers`
Future<ApiResult<ResponseCommon58, Never>> secondaryDnsPeerListPeers({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/secondary_dns/peers',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon58.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create Peer
///
/// Create Peer.
///
/// `POST /accounts/{account_id}/secondary_dns/peers`
Future<ApiResult<ResponseCommon58, Never>> secondaryDnsPeerCreatePeer({required String accountId, required SecondaryDnsPeerCreatePeerRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/secondary_dns/peers',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon58.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Peer Details
///
/// Get Peer.
///
/// `GET /accounts/{account_id}/secondary_dns/peers/{peer_id}`
Future<ApiResult<ResponseCommon58, Never>> secondaryDnsPeerPeerDetails({required String peerId, required String accountId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/secondary_dns/peers/${Uri.encodeComponent(peerId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon58.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update Peer
///
/// Modify Peer.
///
/// `PUT /accounts/{account_id}/secondary_dns/peers/{peer_id}`
Future<ApiResult<ResponseCommon58, Never>> secondaryDnsPeerUpdatePeer({required String peerId, required String accountId, required SecondaryDnsPeer body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/accounts/${Uri.encodeComponent(accountId)}/secondary_dns/peers/${Uri.encodeComponent(peerId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon58.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete Peer
///
/// Delete Peer.
///
/// `DELETE /accounts/{account_id}/secondary_dns/peers/{peer_id}`
Future<ApiResult<ResponseCommon58, Never>> secondaryDnsPeerDeletePeer({required String peerId, required String accountId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/secondary_dns/peers/${Uri.encodeComponent(peerId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return ResponseCommon58.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, error: onError != null ? onError(recovered) : null, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
