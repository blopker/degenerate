//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/nsc_cni.dart';import '../models/nsc_cni_create.dart';import '../models/nsc_cni_list.dart';/// CnIsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class CnIsApi {const CnIsApi(this._config);

final ApiConfig _config;

/// List existing CNI objects
///
/// `GET /accounts/{account_id}/cni/cnis`
Future<ApiResult<NscCniList>> listCnis({required String accountId, String? slot, String? tunnelId, int? cursor, int? limit, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cni/cnis',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'slot': ?slot,
    'tunnel_id': ?tunnelId,
    if (cursor != null) 'cursor': cursor.toString(),
    if (limit != null) 'limit': limit.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return NscCniList.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a new CNI object
///
/// `POST /accounts/{account_id}/cni/cnis`
Future<ApiResult<NscCni>> createCni({required String accountId, required NscCniCreate body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cni/cnis',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return NscCni.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get information about a CNI object
///
/// `GET /accounts/{account_id}/cni/cnis/{cni}`
Future<ApiResult<NscCni>> getCni({required String cni, required String accountId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cni/cnis/${Uri.encodeComponent(cni)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return NscCni.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Modify stored information about a CNI object
///
/// `PUT /accounts/{account_id}/cni/cnis/{cni}`
Future<ApiResult<NscCni>> updateCni({required String cni, required String accountId, required NscCni body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cni/cnis/${Uri.encodeComponent(cni)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return NscCni.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a specified CNI object
///
/// `DELETE /accounts/{account_id}/cni/cnis/{cni}`
Future<ApiResult<void>> deleteCni({required String cni, required String accountId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/cni/cnis/${Uri.encodeComponent(cni)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
