//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from 03-operations-parameters-request-body.json
//  OpenAPI spec version: 3.1.1

import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/create_token_request.dart';import '../models/get_item_filter.dart';import '../models/upload_file_request.dart';/// ItemsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ItemsApi {const ItemsApi(this._config);

final ApiConfig _config;

/// Read item
///
/// Returns one item
///
/// `GET /items/{itemId}`
Future<ApiResult<void, Never>> getItem({required String itemId, List<String>? fields, List<String>? tagsPipe, List<String>? tagsSpace, GetItemFilter? filter, String? xTraceId, String? session, }) async  { final queryParameters = <String, String>{..._config.defaultQueryParameters};
final queryParametersList = <ApiQueryParameter>[];
if (fields != null) {
queryParametersList.add(ApiQueryParameter(name: 'fields', value: fields.join(','), allowReserved: true));
}
if (tagsPipe != null) {
queryParameters['tagsPipe'] = tagsPipe.join('|');
}
if (tagsSpace != null) {
queryParameters['tagsSpace'] = tagsSpace.join(' ');
}
if (filter != null) {
queryParameters['filter[status]'] = filter.status;
queryParameters['filter[limit]'] = filter.limit.toString();
}

final cookies = <String, String>{..._config.defaultCookies};
if (session != null) cookies['session'] = session;

final headers = <String, String>{..._config.defaultHeaders};
if (xTraceId != null) headers['X-Trace-Id'] = xTraceId;

final request = ApiRequest(
  method: 'GET',
  path: '/items/${Uri.encodeComponent(itemId)}',
  headers: headers,
  queryParameters: queryParameters,
  queryParametersList: queryParametersList,
  cookies: cookies,
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Upload a file with metadata
///
/// `POST /upload`
Future<ApiResult<void, Never>> uploadFile({required UploadFileRequest body}) async  { final headers = <String, String>{..._config.defaultHeaders};

final request = ApiRequest(
  method: 'POST',
  path: '/upload',
  headers: headers,
  body: [
    ApiMultipartField.file('file', body.file),
    ApiMultipartField.text('description', body.description),
    if (body.tags case final _tags?)
      ApiMultipartField.text('tags', _tags),
  ],
  contentType: 'multipart/form-data',
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Create an auth token
///
/// `POST /token`
Future<ApiResult<void, Never>> createToken({required CreateTokenRequest body}) async  { final headers = <String, String>{..._config.defaultHeaders};
headers['Content-Type'] = 'application/x-www-form-urlencoded';

final request = ApiRequest(
  method: 'POST',
  path: '/token',
  headers: headers,
  body: [
    'grant_type=${Uri.encodeQueryComponent(body.grantType)}',
    if (body.scope case final _scope?)
      'scope=${Uri.encodeQueryComponent(_scope)}',
    if (body.timeout case final _timeout?)
      'timeout=${Uri.encodeQueryComponent(_timeout.toString())}',
  ].join('&'),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
