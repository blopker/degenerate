name: degenerate_http
description: package:http adapter for degenerate-generated API clients.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  degenerate_runtime:
    path: ../degenerate_runtime
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
