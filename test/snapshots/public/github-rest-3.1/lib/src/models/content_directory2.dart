//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'content_directory2_links.dart';
import 'content_directory2_type.dart';

final class ContentDirectory2 {
  const ContentDirectory2({
    required this.type,
    required this.size,
    required this.name,
    required this.path,
    this.content,
    required this.sha,
    required this.url,
    required this.gitUrl,
    required this.htmlUrl,
    required this.downloadUrl,
    required this.links,
  });

  factory ContentDirectory2.fromJson(Map<String, dynamic> json) {
    return ContentDirectory2(
      type: ContentDirectory2Type.fromJson(json['type'] as String),
      size: (json['size'] as num).toInt(),
      name: json['name'] as String,
      path: json['path'] as String,
      content: json['content'] as String?,
      sha: json['sha'] as String,
      url: Uri.parse(json['url'] as String),
      gitUrl: Uri.parse(json['git_url'] as String),
      htmlUrl: Uri.parse(json['html_url'] as String),
      downloadUrl: Uri.parse(json['download_url'] as String),
      links: ContentDirectory2Links.fromJson(
        json['_links'] as Map<String, dynamic>,
      ),
    );
  }

  final ContentDirectory2Type type;

  final int size;

  final String name;

  final String path;

  final String? content;

  final String sha;

  final Uri url;

  final Uri? gitUrl;

  final Uri? htmlUrl;

  final Uri? downloadUrl;

  final ContentDirectory2Links links;

  Map<String, dynamic> toJson() {
    return {
      'type': type.toJson(),
      'size': size,
      'name': name,
      'path': path,
      'content': ?content,
      'sha': sha,
      'url': url.toString(),
      if (gitUrl != null) 'git_url': gitUrl?.toString(),
      if (htmlUrl != null) 'html_url': htmlUrl?.toString(),
      if (downloadUrl != null) 'download_url': downloadUrl?.toString(),
      '_links': links.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('type') &&
        json.containsKey('size') &&
        json['size'] is num &&
        json.containsKey('name') &&
        json['name'] is String &&
        json.containsKey('path') &&
        json['path'] is String &&
        json.containsKey('sha') &&
        json['sha'] is String &&
        json.containsKey('url') &&
        json['url'] is String &&
        json.containsKey('git_url') &&
        json['git_url'] is String &&
        json.containsKey('html_url') &&
        json['html_url'] is String &&
        json.containsKey('download_url') &&
        json['download_url'] is String &&
        json.containsKey('_links');
  }

  ContentDirectory2 copyWith({
    ContentDirectory2Type? type,
    int? size,
    String? name,
    String? path,
    String Function()? content,
    String? sha,
    Uri? url,
    Uri? Function()? gitUrl,
    Uri? Function()? htmlUrl,
    Uri? Function()? downloadUrl,
    ContentDirectory2Links? links,
  }) {
    return ContentDirectory2(
      type: type ?? this.type,
      size: size ?? this.size,
      name: name ?? this.name,
      path: path ?? this.path,
      content: content != null ? content() : this.content,
      sha: sha ?? this.sha,
      url: url ?? this.url,
      gitUrl: gitUrl != null ? gitUrl() : this.gitUrl,
      htmlUrl: htmlUrl != null ? htmlUrl() : this.htmlUrl,
      downloadUrl: downloadUrl != null ? downloadUrl() : this.downloadUrl,
      links: links ?? this.links,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is ContentDirectory2 &&
            type == other.type &&
            size == other.size &&
            name == other.name &&
            path == other.path &&
            content == other.content &&
            sha == other.sha &&
            url == other.url &&
            gitUrl == other.gitUrl &&
            htmlUrl == other.htmlUrl &&
            downloadUrl == other.downloadUrl &&
            links == other.links;
  }

  @override
  int get hashCode {
    return Object.hash(
      type,
      size,
      name,
      path,
      content,
      sha,
      url,
      gitUrl,
      htmlUrl,
      downloadUrl,
      links,
    );
  }

  @override
  String toString() {
    return 'ContentDirectory2(type: $type, size: $size, name: $name, path: $path, content: $content, sha: $sha, url: $url, gitUrl: $gitUrl, htmlUrl: $htmlUrl, downloadUrl: $downloadUrl, links: $links)';
  }
}
