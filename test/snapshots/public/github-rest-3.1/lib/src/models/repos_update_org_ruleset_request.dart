//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'package:collection/collection.dart';
import 'org_rules.dart';
import 'org_ruleset_conditions.dart';
import 'repos_update_org_ruleset_request_target.dart';
import 'repository_rule_enforcement.dart';
import 'repository_ruleset_bypass_actor.dart';

final class ReposUpdateOrgRulesetRequest {
  const ReposUpdateOrgRulesetRequest({
    this.name,
    this.target,
    this.enforcement,
    this.bypassActors,
    this.conditions,
    this.rules,
  });

  factory ReposUpdateOrgRulesetRequest.fromJson(Map<String, dynamic> json) {
    return ReposUpdateOrgRulesetRequest(
      name: json['name'] as String?,
      target: json['target'] != null
          ? ReposUpdateOrgRulesetRequestTarget.fromJson(
              json['target'] as String,
            )
          : null,
      enforcement: json['enforcement'] != null
          ? RepositoryRuleEnforcement.fromJson(json['enforcement'] as String)
          : null,
      bypassActors: (json['bypass_actors'] as List<dynamic>?)
          ?.map(
            (e) => RepositoryRulesetBypassActor.fromJson(
              e as Map<String, dynamic>,
            ),
          )
          .toList(),
      conditions: json['conditions'] != null
          ? OrgRulesetConditions.fromJson(
              json['conditions'] as Map<String, dynamic>,
            )
          : null,
      rules: (json['rules'] as List<dynamic>?)
          ?.map((e) => OrgRules.fromJson(e as Map<String, dynamic>))
          .toList(),
    );
  }

  /// The name of the ruleset.
  final String? name;

  /// The target of the ruleset
  final ReposUpdateOrgRulesetRequestTarget? target;

  final RepositoryRuleEnforcement? enforcement;

  /// The actors that can bypass the rules in this ruleset
  final List<RepositoryRulesetBypassActor>? bypassActors;

  final OrgRulesetConditions? conditions;

  /// An array of rules within the ruleset.
  final List<OrgRules>? rules;

  Map<String, dynamic> toJson() {
    return {
      'name': ?name,
      if (target != null) 'target': target?.toJson(),
      if (enforcement != null) 'enforcement': enforcement?.toJson(),
      if (bypassActors != null)
        'bypass_actors': bypassActors?.map((e) => e.toJson()).toList(),
      if (conditions != null) 'conditions': conditions?.toJson(),
      if (rules != null) 'rules': rules?.map((e) => e.toJson()).toList(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  ReposUpdateOrgRulesetRequest copyWith({
    String Function()? name,
    ReposUpdateOrgRulesetRequestTarget Function()? target,
    RepositoryRuleEnforcement Function()? enforcement,
    List<RepositoryRulesetBypassActor> Function()? bypassActors,
    OrgRulesetConditions Function()? conditions,
    List<OrgRules> Function()? rules,
  }) {
    return ReposUpdateOrgRulesetRequest(
      name: name != null ? name() : this.name,
      target: target != null ? target() : this.target,
      enforcement: enforcement != null ? enforcement() : this.enforcement,
      bypassActors: bypassActors != null ? bypassActors() : this.bypassActors,
      conditions: conditions != null ? conditions() : this.conditions,
      rules: rules != null ? rules() : this.rules,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is ReposUpdateOrgRulesetRequest &&
            name == other.name &&
            target == other.target &&
            enforcement == other.enforcement &&
            const ListEquality<dynamic>().equals(
              bypassActors,
              other.bypassActors,
            ) &&
            conditions == other.conditions &&
            const ListEquality<dynamic>().equals(rules, other.rules);
  }

  @override
  int get hashCode {
    return Object.hash(
      name,
      target,
      enforcement,
      Object.hashAll(bypassActors ?? const []),
      conditions,
      Object.hashAll(rules ?? const []),
    );
  }

  @override
  String toString() {
    return 'ReposUpdateOrgRulesetRequest(name: $name, target: $target, enforcement: $enforcement, bypassActors: $bypassActors, conditions: $conditions, rules: $rules)';
  }
}
