//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'package:collection/collection.dart';
import 'campaign_state.dart';

final class CampaignsUpdateCampaignRequest {
  const CampaignsUpdateCampaignRequest({
    this.name,
    this.description,
    this.managers,
    this.teamManagers,
    this.endsAt,
    this.contactLink,
    this.state,
  });

  factory CampaignsUpdateCampaignRequest.fromJson(Map<String, dynamic> json) {
    return CampaignsUpdateCampaignRequest(
      name: json['name'] as String?,
      description: json['description'] as String?,
      managers: (json['managers'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      teamManagers: (json['team_managers'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      endsAt: json['ends_at'] != null
          ? DateTime.parse(json['ends_at'] as String)
          : null,
      contactLink: json['contact_link'] != null
          ? Uri.parse(json['contact_link'] as String)
          : null,
      state: json['state'] != null
          ? CampaignState.fromJson(json['state'] as String)
          : null,
    );
  }

  /// The name of the campaign
  final String? name;

  /// A description for the campaign
  final String? description;

  /// The logins of the users to set as the campaign managers. At this time, only a single manager can be supplied.
  final List<String>? managers;

  /// The slugs of the teams to set as the campaign managers.
  final List<String>? teamManagers;

  /// The end date and time of the campaign, in ISO 8601 format':' YYYY-MM-DDTHH:MM:SSZ.
  final DateTime? endsAt;

  /// The contact link of the campaign. Must be a URI.
  final Uri? contactLink;

  final CampaignState? state;

  Map<String, dynamic> toJson() {
    return {
      'name': ?name,
      'description': ?description,
      'managers': ?managers,
      'team_managers': ?teamManagers,
      if (endsAt != null) 'ends_at': endsAt?.toIso8601String(),
      if (contactLink != null) 'contact_link': contactLink?.toString(),
      if (state != null) 'state': state?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  CampaignsUpdateCampaignRequest copyWith({
    String Function()? name,
    String Function()? description,
    List<String> Function()? managers,
    List<String> Function()? teamManagers,
    DateTime Function()? endsAt,
    Uri? Function()? contactLink,
    CampaignState Function()? state,
  }) {
    return CampaignsUpdateCampaignRequest(
      name: name != null ? name() : this.name,
      description: description != null ? description() : this.description,
      managers: managers != null ? managers() : this.managers,
      teamManagers: teamManagers != null ? teamManagers() : this.teamManagers,
      endsAt: endsAt != null ? endsAt() : this.endsAt,
      contactLink: contactLink != null ? contactLink() : this.contactLink,
      state: state != null ? state() : this.state,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is CampaignsUpdateCampaignRequest &&
            name == other.name &&
            description == other.description &&
            const ListEquality<dynamic>().equals(managers, other.managers) &&
            const ListEquality<dynamic>().equals(
              teamManagers,
              other.teamManagers,
            ) &&
            endsAt == other.endsAt &&
            contactLink == other.contactLink &&
            state == other.state;
  }

  @override
  int get hashCode {
    return Object.hash(
      name,
      description,
      Object.hashAll(managers ?? const []),
      Object.hashAll(teamManagers ?? const []),
      endsAt,
      contactLink,
      state,
    );
  }

  @override
  String toString() {
    return 'CampaignsUpdateCampaignRequest(name: $name, description: $description, managers: $managers, teamManagers: $teamManagers, endsAt: $endsAt, contactLink: $contactLink, state: $state)';
  }
}
