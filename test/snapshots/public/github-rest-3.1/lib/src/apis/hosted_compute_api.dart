//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/hosted_compute_create_network_configuration_for_org_request.dart';
import '../models/hosted_compute_list_network_configurations_for_org_response.dart';
import '../models/hosted_compute_update_network_configuration_for_org_request.dart';
import '../models/network_configuration.dart';
import '../models/network_settings.dart';

/// HostedComputeApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class HostedComputeApi {
  const HostedComputeApi(this._config);

  final ApiConfig _config;

  /// List hosted compute network configurations for an organization
  ///
  /// Lists all hosted compute network configurations configured in an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  ///
  /// `GET /orgs/{org}/settings/network-configurations`
  Future<ApiResult<HostedComputeListNetworkConfigurationsForOrgResponse>>
  hostedComputeListNetworkConfigurationsForOrg({
    required String org,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/orgs/${Uri.encodeComponent(org)}/settings/network-configurations',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return HostedComputeListNetworkConfigurationsForOrgResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a hosted compute network configuration for an organization
  ///
  /// Creates a hosted compute network configuration for an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  ///
  /// `POST /orgs/{org}/settings/network-configurations`
  Future<ApiResult<NetworkConfiguration>>
  hostedComputeCreateNetworkConfigurationForOrg({
    required String org,
    required HostedComputeCreateNetworkConfigurationForOrgRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/orgs/${Uri.encodeComponent(org)}/settings/network-configurations',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NetworkConfiguration.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a hosted compute network configuration for an organization
  ///
  /// Gets a hosted compute network configuration configured in an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  ///
  /// `GET /orgs/{org}/settings/network-configurations/{network_configuration_id}`
  Future<ApiResult<NetworkConfiguration>>
  hostedComputeGetNetworkConfigurationForOrg({
    required String org,
    required String networkConfigurationId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/orgs/${Uri.encodeComponent(org)}/settings/network-configurations/${Uri.encodeComponent(networkConfigurationId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NetworkConfiguration.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a hosted compute network configuration for an organization
  ///
  /// Updates a hosted compute network configuration for an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  ///
  /// `PATCH /orgs/{org}/settings/network-configurations/{network_configuration_id}`
  Future<ApiResult<NetworkConfiguration>>
  hostedComputeUpdateNetworkConfigurationForOrg({
    required String org,
    required String networkConfigurationId,
    required HostedComputeUpdateNetworkConfigurationForOrgRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/orgs/${Uri.encodeComponent(org)}/settings/network-configurations/${Uri.encodeComponent(networkConfigurationId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NetworkConfiguration.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a hosted compute network configuration from an organization
  ///
  /// Deletes a hosted compute network configuration from an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `write:network_configurations` scope to use this endpoint.
  ///
  /// `DELETE /orgs/{org}/settings/network-configurations/{network_configuration_id}`
  Future<ApiResult<void>> hostedComputeDeleteNetworkConfigurationFromOrg({
    required String org,
    required String networkConfigurationId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/orgs/${Uri.encodeComponent(org)}/settings/network-configurations/${Uri.encodeComponent(networkConfigurationId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Get a hosted compute network settings resource for an organization
  ///
  /// Gets a hosted compute network settings resource configured for an organization.
  ///
  /// OAuth app tokens and personal access tokens (classic) need the `read:network_configurations` scope to use this endpoint.
  ///
  /// `GET /orgs/{org}/settings/network-settings/{network_settings_id}`
  Future<ApiResult<NetworkSettings>> hostedComputeGetNetworkSettingsForOrg({
    required String org,
    required String networkSettingsId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/orgs/${Uri.encodeComponent(org)}/settings/network-settings/${Uri.encodeComponent(networkSettingsId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NetworkSettings.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
