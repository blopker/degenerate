//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/organization_secret_scanning_alert.dart';import '../models/secret_scanning_alert.dart';import '../models/secret_scanning_create_push_protection_bypass_request.dart';import '../models/secret_scanning_list_alerts_for_org_direction.dart';import '../models/secret_scanning_list_alerts_for_org_sort.dart';import '../models/secret_scanning_list_alerts_for_org_state.dart';import '../models/secret_scanning_list_alerts_for_repo_direction.dart';import '../models/secret_scanning_list_alerts_for_repo_sort.dart';import '../models/secret_scanning_list_alerts_for_repo_state.dart';import '../models/secret_scanning_location.dart';import '../models/secret_scanning_pattern_configuration.dart';import '../models/secret_scanning_push_protection_bypass.dart';import '../models/secret_scanning_scan_history.dart';import '../models/secret_scanning_update_alert_request.dart';import '../models/secret_scanning_update_org_pattern_configs_request.dart';import '../models/secret_scanning_update_org_pattern_configs_response.dart';/// SecretScanningApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SecretScanningApi {const SecretScanningApi(this._config);

final ApiConfig _config;

/// List secret scanning alerts for an organization
///
/// Lists secret scanning alerts for eligible repositories in an organization, from newest to oldest.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `GET /orgs/{org}/secret-scanning/alerts`
Future<ApiResult<List<OrganizationSecretScanningAlert>>> secretScanningListAlertsForOrg({required String org, SecretScanningListAlertsForOrgState? state, String? secretType, String? resolution, String? assignee, SecretScanningListAlertsForOrgSort? sort, SecretScanningListAlertsForOrgDirection? direction, int? page, int? perPage, String? before, String? after, String? validity, bool? isPubliclyLeaked, bool? isMultiRepo, bool? hideSecret, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/secret-scanning/alerts',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (state != null) 'state': state.toJson(),
    'secret_type': ?secretType,
    'resolution': ?resolution,
    'assignee': ?assignee,
    if (sort != null) 'sort': sort.toJson(),
    if (direction != null) 'direction': direction.toJson(),
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
    'validity': ?validity,
    if (isPubliclyLeaked != null) 'is_publicly_leaked': isPubliclyLeaked.toString(),
    if (isMultiRepo != null) 'is_multi_repo': isMultiRepo.toString(),
    if (hideSecret != null) 'hide_secret': hideSecret.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => OrganizationSecretScanningAlert.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// List organization pattern configurations
///
/// Lists the secret scanning pattern configurations for an organization.
/// 
/// Personal access tokens (classic) need the `read:org` scope to use this endpoint.
///
/// `GET /orgs/{org}/secret-scanning/pattern-configurations`
Future<ApiResult<SecretScanningPatternConfiguration>> secretScanningListOrgPatternConfigs({required String org}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/secret-scanning/pattern-configurations',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningPatternConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update organization pattern configurations
///
/// Updates the secret scanning pattern configurations for an organization.
/// 
/// Personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `PATCH /orgs/{org}/secret-scanning/pattern-configurations`
Future<ApiResult<SecretScanningUpdateOrgPatternConfigsResponse>> secretScanningUpdateOrgPatternConfigs({required String org, required SecretScanningUpdateOrgPatternConfigsRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/orgs/${Uri.encodeComponent(org)}/secret-scanning/pattern-configurations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningUpdateOrgPatternConfigsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List secret scanning alerts for a repository
///
/// Lists secret scanning alerts for an eligible repository, from newest to oldest.
/// 
/// The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `GET /repos/{owner}/{repo}/secret-scanning/alerts`
Future<ApiResult<List<SecretScanningAlert>>> secretScanningListAlertsForRepo({required String owner, required String repo, SecretScanningListAlertsForRepoState? state, String? secretType, String? resolution, String? assignee, SecretScanningListAlertsForRepoSort? sort, SecretScanningListAlertsForRepoDirection? direction, int? page, int? perPage, String? before, String? after, String? validity, bool? isPubliclyLeaked, bool? isMultiRepo, bool? hideSecret, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/alerts',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (state != null) 'state': state.toJson(),
    'secret_type': ?secretType,
    'resolution': ?resolution,
    'assignee': ?assignee,
    if (sort != null) 'sort': sort.toJson(),
    if (direction != null) 'direction': direction.toJson(),
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
    'validity': ?validity,
    if (isPubliclyLeaked != null) 'is_publicly_leaked': isPubliclyLeaked.toString(),
    if (isMultiRepo != null) 'is_multi_repo': isMultiRepo.toString(),
    if (hideSecret != null) 'hide_secret': hideSecret.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => SecretScanningAlert.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a secret scanning alert
///
/// Gets a single secret scanning alert detected in an eligible repository.
/// 
/// The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}`
Future<ApiResult<SecretScanningAlert>> secretScanningGetAlert({required String owner, required String repo, required int alertNumber, bool? hideSecret, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/alerts/${Uri.encodeComponent(alertNumber.toString())}',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (hideSecret != null) 'hide_secret': hideSecret.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningAlert.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a secret scanning alert
///
/// Updates the status of a secret scanning alert in an eligible repository.
/// 
/// You can also use this endpoint to assign or unassign an alert to a user who has write access to the repository.
/// 
/// The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `PATCH /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}`
Future<ApiResult<SecretScanningAlert>> secretScanningUpdateAlert({required String owner, required String repo, required int alertNumber, required SecretScanningUpdateAlertRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/alerts/${Uri.encodeComponent(alertNumber.toString())}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningAlert.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List locations for a secret scanning alert
///
/// Lists all locations for a given secret scanning alert for an eligible repository.
/// 
/// The authenticated user must be an administrator for the repository or for the organization that owns the repository to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `GET /repos/{owner}/{repo}/secret-scanning/alerts/{alert_number}/locations`
Future<ApiResult<List<SecretScanningLocation>>> secretScanningListLocationsForAlert({required String owner, required String repo, required int alertNumber, int? page, int? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/alerts/${Uri.encodeComponent(alertNumber.toString())}/locations',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (page != null) 'page': page.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => SecretScanningLocation.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Create a push protection bypass
///
/// Creates a bypass for a previously push protected secret.
/// 
/// The authenticated user must be the original author of the committed secret.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
///
/// `POST /repos/{owner}/{repo}/secret-scanning/push-protection-bypasses`
Future<ApiResult<SecretScanningPushProtectionBypass>> secretScanningCreatePushProtectionBypass({required String owner, required String repo, required SecretScanningCreatePushProtectionBypassRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/push-protection-bypasses',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningPushProtectionBypass.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get secret scanning scan history for a repository
///
/// Lists the latest default incremental and backfill scans by type for a repository. Scans from Copilot Secret Scanning are not included.
/// 
/// > [!NOTE]
/// > This endpoint requires [GitHub Advanced Security](https://docs.github.com/get-started/learning-about-github/about-github-advanced-security)."
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `security_events` scope to use this endpoint. If this endpoint is only used with public repositories, the token can use the `public_repo` scope instead.
///
/// `GET /repos/{owner}/{repo}/secret-scanning/scan-history`
Future<ApiResult<SecretScanningScanHistory>> secretScanningGetScanHistory({required String owner, required String repo, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/secret-scanning/scan-history',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return SecretScanningScanHistory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
