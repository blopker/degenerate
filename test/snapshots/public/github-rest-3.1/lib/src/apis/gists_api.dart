//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/base_gist.dart';
import '../models/gist_comment.dart';
import '../models/gist_commit.dart';
import '../models/gist_simple.dart';
import '../models/gists_create_comment_request.dart';
import '../models/gists_create_request.dart';
import '../models/gists_update_comment_request.dart';
import '../models/gists_update_request.dart';

/// GistsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class GistsApi {
  const GistsApi(this._config);

  final ApiConfig _config;

  /// List gists for the authenticated user
  ///
  /// Lists the authenticated user's gists or if called anonymously, this endpoint returns all public gists:
  ///
  /// `GET /gists`
  Future<ApiResult<List<BaseGist>>> gistsList({
    DateTime? since,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (since != null) 'since': since.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => BaseGist.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Create a gist
  ///
  /// Allows you to add a new gist with one or more files.
  ///
  /// > [!NOTE]
  /// > Don't name your files "gistfile" with a numerical suffix. This is the format of the automatic naming scheme that Gist uses internally.
  ///
  /// `POST /gists`
  Future<ApiResult<GistSimple>> gistsCreate({
    required GistsCreateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/gists',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistSimple.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List public gists
  ///
  /// List public gists sorted by most recently updated to least recently updated.
  ///
  /// Note: With [pagination](https://docs.github.com/rest/guides/using-pagination-in-the-rest-api), you can fetch up to 3000 gists. For example, you can fetch 100 pages with 30 gists per page or 30 pages with 100 gists per page.
  ///
  /// `GET /gists/public`
  Future<ApiResult<List<BaseGist>>> gistsListPublic({
    DateTime? since,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/public',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (since != null) 'since': since.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => BaseGist.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// List starred gists
  ///
  /// List the authenticated user's starred gists:
  ///
  /// `GET /gists/starred`
  Future<ApiResult<List<BaseGist>>> gistsListStarred({
    DateTime? since,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/starred',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (since != null) 'since': since.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => BaseGist.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Get a gist
  ///
  /// Gets a specified gist.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `GET /gists/{gist_id}`
  Future<ApiResult<GistSimple>> gistsGet({required String gistId}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistSimple.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a gist
  ///
  /// Allows you to update a gist's description and to update, delete, or rename gist files. Files
  /// from the previous version of the gist that aren't explicitly changed during an edit
  /// are unchanged.
  ///
  /// At least one of `description` or `files` is required.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `PATCH /gists/{gist_id}`
  Future<ApiResult<GistSimple>> gistsUpdate({
    required String gistId,
    required GistsUpdateRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path: '/gists/${Uri.encodeComponent(gistId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistSimple.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a gist
  ///
  ///
  ///
  /// `DELETE /gists/{gist_id}`
  Future<ApiResult<void>> gistsDelete({required String gistId}) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/gists/${Uri.encodeComponent(gistId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// List gist comments
  ///
  /// Lists the comments on a gist.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `GET /gists/{gist_id}/comments`
  Future<ApiResult<List<GistComment>>> gistsListComments({
    required String gistId,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}/comments',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => GistComment.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Create a gist comment
  ///
  /// Creates a comment on a gist.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `POST /gists/{gist_id}/comments`
  Future<ApiResult<GistComment>> gistsCreateComment({
    required String gistId,
    required GistsCreateCommentRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/gists/${Uri.encodeComponent(gistId)}/comments',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistComment.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a gist comment
  ///
  /// Gets a comment on a gist.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `GET /gists/{gist_id}/comments/{comment_id}`
  Future<ApiResult<GistComment>> gistsGetComment({
    required String gistId,
    required int commentId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/gists/${Uri.encodeComponent(gistId)}/comments/${Uri.encodeComponent(commentId.toString())}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistComment.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a gist comment
  ///
  /// Updates a comment on a gist.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `PATCH /gists/{gist_id}/comments/{comment_id}`
  Future<ApiResult<GistComment>> gistsUpdateComment({
    required String gistId,
    required int commentId,
    required GistsUpdateCommentRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/gists/${Uri.encodeComponent(gistId)}/comments/${Uri.encodeComponent(commentId.toString())}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistComment.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a gist comment
  ///
  ///
  ///
  /// `DELETE /gists/{gist_id}/comments/{comment_id}`
  Future<ApiResult<void>> gistsDeleteComment({
    required String gistId,
    required int commentId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/gists/${Uri.encodeComponent(gistId)}/comments/${Uri.encodeComponent(commentId.toString())}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// List gist commits
  ///
  ///
  ///
  /// `GET /gists/{gist_id}/commits`
  Future<ApiResult<List<GistCommit>>> gistsListCommits({
    required String gistId,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}/commits',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => GistCommit.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// List gist forks
  ///
  ///
  ///
  /// `GET /gists/{gist_id}/forks`
  Future<ApiResult<List<GistSimple>>> gistsListForks({
    required String gistId,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}/forks',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => GistSimple.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Fork a gist
  ///
  ///
  ///
  /// `POST /gists/{gist_id}/forks`
  Future<ApiResult<BaseGist>> gistsFork({required String gistId}) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/gists/${Uri.encodeComponent(gistId)}/forks',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return BaseGist.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Check if a gist is starred
  ///
  ///
  ///
  /// `GET /gists/{gist_id}/star`
  Future<ApiResult<void>> gistsCheckIsStarred({required String gistId}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}/star',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Star a gist
  ///
  /// Note that you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP method](https://docs.github.com/rest/guides/getting-started-with-the-rest-api#http-method)."
  ///
  /// `PUT /gists/{gist_id}/star`
  Future<ApiResult<void>> gistsStar({required String gistId}) async {
    final request = ApiRequest(
      method: 'PUT',
      path: '/gists/${Uri.encodeComponent(gistId)}/star',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Unstar a gist
  ///
  ///
  ///
  /// `DELETE /gists/{gist_id}/star`
  Future<ApiResult<void>> gistsUnstar({required String gistId}) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/gists/${Uri.encodeComponent(gistId)}/star',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Get a gist revision
  ///
  /// Gets a specified gist revision.
  ///
  /// This endpoint supports the following custom media types. For more information, see "[Media types](https://docs.github.com/rest/using-the-rest-api/getting-started-with-the-rest-api#media-types)."
  ///
  /// - **`application/vnd.github.raw+json`**: Returns the raw markdown. This is the default if you do not pass any specific media type.
  /// - **`application/vnd.github.base64+json`**: Returns the base64-encoded contents. This can be useful if your gist contains any invalid UTF-8 sequences.
  ///
  /// `GET /gists/{gist_id}/{sha}`
  Future<ApiResult<GistSimple>> gistsGetRevision({
    required String gistId,
    required String sha,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/gists/${Uri.encodeComponent(gistId)}/${Uri.encodeComponent(sha)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GistSimple.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List gists for a user
  ///
  /// Lists public gists for the specified user:
  ///
  /// `GET /users/{username}/gists`
  Future<ApiResult<List<BaseGist>>> gistsListForUser({
    required String username,
    DateTime? since,
    int? perPage,
    int? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/users/${Uri.encodeComponent(username)}/gists',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (since != null) 'since': since.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => BaseGist.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
