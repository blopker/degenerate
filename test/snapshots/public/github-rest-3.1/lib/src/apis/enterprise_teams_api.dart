//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/enterprise_team.dart';import '../models/enterprise_teams_create_request.dart';import '../models/enterprise_teams_update_request.dart';/// EnterpriseTeamsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class EnterpriseTeamsApi {const EnterpriseTeamsApi(this._config);

final ApiConfig _config;

/// List enterprise teams
///
/// List all teams in the enterprise for the authenticated user
///
/// `GET /enterprises/{enterprise}/teams`
Future<ApiResult<List<EnterpriseTeam>>> enterpriseTeamsList({required String enterprise, int? perPage, int? page, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (perPage != null) 'per_page': perPage.toString(),
    if (page != null) 'page': page.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => EnterpriseTeam.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Create an enterprise team
///
/// To create an enterprise team, the authenticated user must be an owner of the enterprise.
///
/// `POST /enterprises/{enterprise}/teams`
Future<ApiResult<EnterpriseTeam>> enterpriseTeamsCreate({required String enterprise, required EnterpriseTeamsCreateRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return EnterpriseTeam.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get an enterprise team
///
/// Gets a team using the team's slug. To create the slug, GitHub replaces special characters in the name string, changes all words to lowercase, and replaces spaces with a `-` separator and adds the "ent:" prefix. For example, "My TEam Näme" would become `ent:my-team-name`.
///
/// `GET /enterprises/{enterprise}/teams/{team_slug}`
Future<ApiResult<EnterpriseTeam>> enterpriseTeamsGet({required String enterprise, required String teamSlug, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(teamSlug)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return EnterpriseTeam.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update an enterprise team
///
/// To edit a team, the authenticated user must be an enterprise owner.
///
/// `PATCH /enterprises/{enterprise}/teams/{team_slug}`
Future<ApiResult<EnterpriseTeam>> enterpriseTeamsUpdate({required String enterprise, required String teamSlug, required EnterpriseTeamsUpdateRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(teamSlug)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return EnterpriseTeam.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete an enterprise team
///
/// To delete an enterprise team, the authenticated user must be an enterprise owner.
/// 
/// If you are an enterprise owner, deleting an enterprise team will delete all of its IdP mappings as well.
///
/// `DELETE /enterprises/{enterprise}/teams/{team_slug}`
Future<ApiResult<void>> enterpriseTeamsDelete({required String enterprise, required String teamSlug, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(teamSlug)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
