//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/full_repository.dart';import '../models/global_advisory.dart';import '../models/private_vulnerability_report_create.dart';import '../models/repository_advisory.dart';import '../models/repository_advisory_create.dart';import '../models/repository_advisory_update.dart';import '../models/security_advisories_list_global_advisories_affects.dart';import '../models/security_advisories_list_global_advisories_cwes.dart';import '../models/security_advisories_list_global_advisories_direction.dart';import '../models/security_advisories_list_global_advisories_severity.dart';import '../models/security_advisories_list_global_advisories_sort.dart';import '../models/security_advisories_list_global_advisories_type.dart';import '../models/security_advisories_list_org_repository_advisories_direction.dart';import '../models/security_advisories_list_org_repository_advisories_sort.dart';import '../models/security_advisories_list_org_repository_advisories_state.dart';import '../models/security_advisories_list_repository_advisories_direction.dart';import '../models/security_advisories_list_repository_advisories_sort.dart';import '../models/security_advisories_list_repository_advisories_state.dart';import '../models/security_advisory_ecosystems.dart';/// SecurityAdvisoriesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SecurityAdvisoriesApi {const SecurityAdvisoriesApi(this._config);

final ApiConfig _config;

/// List global security advisories
///
/// Lists all global security advisories that match the specified parameters. If no other parameters are defined, the request will return only GitHub-reviewed advisories that are not malware.
/// 
/// By default, all responses will exclude advisories for malware, because malware are not standard vulnerabilities. To list advisories for malware, you must include the `type` parameter in your request, with the value `malware`. For more information about the different types of security advisories, see "[About the GitHub Advisory database](https://docs.github.com/code-security/security-advisories/global-security-advisories/about-the-github-advisory-database#about-types-of-security-advisories)."
///
/// `GET /advisories`
Future<ApiResult<List<GlobalAdvisory>>> securityAdvisoriesListGlobalAdvisories({String? ghsaId, SecurityAdvisoriesListGlobalAdvisoriesType? type, String? cveId, SecurityAdvisoryEcosystems? ecosystem, SecurityAdvisoriesListGlobalAdvisoriesSeverity? severity, SecurityAdvisoriesListGlobalAdvisoriesCwes? cwes, bool? isWithdrawn, SecurityAdvisoriesListGlobalAdvisoriesAffects? affects, String? published, String? updated, String? modified, String? epssPercentage, String? epssPercentile, String? before, String? after, SecurityAdvisoriesListGlobalAdvisoriesDirection? direction, int? perPage, SecurityAdvisoriesListGlobalAdvisoriesSort? sort, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/advisories',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'ghsa_id': ?ghsaId,
    if (type != null) 'type': type.toJson(),
    'cve_id': ?cveId,
    if (ecosystem != null) 'ecosystem': ecosystem.toJson(),
    if (severity != null) 'severity': severity.toJson(),
    if (cwes != null) 'cwes': cwes.toString(),
    if (isWithdrawn != null) 'is_withdrawn': isWithdrawn.toString(),
    if (affects != null) 'affects': affects.toString(),
    'published': ?published,
    'updated': ?updated,
    'modified': ?modified,
    'epss_percentage': ?epssPercentage,
    'epss_percentile': ?epssPercentile,
    'before': ?before,
    'after': ?after,
    if (direction != null) 'direction': direction.toJson(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (sort != null) 'sort': sort.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => GlobalAdvisory.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get a global security advisory
///
/// Gets a global security advisory using its GitHub Security Advisory (GHSA) identifier.
///
/// `GET /advisories/{ghsa_id}`
Future<ApiResult<GlobalAdvisory>> securityAdvisoriesGetGlobalAdvisory({required String ghsaId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/advisories/${Uri.encodeComponent(ghsaId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return GlobalAdvisory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List repository security advisories for an organization
///
/// Lists repository security advisories for an organization.
/// 
/// The authenticated user must be an owner or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
///
/// `GET /orgs/{org}/security-advisories`
Future<ApiResult<List<RepositoryAdvisory>>> securityAdvisoriesListOrgRepositoryAdvisories({required String org, SecurityAdvisoriesListOrgRepositoryAdvisoriesDirection? direction, SecurityAdvisoriesListOrgRepositoryAdvisoriesSort? sort, String? before, String? after, int? perPage, SecurityAdvisoriesListOrgRepositoryAdvisoriesState? state, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/security-advisories',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (direction != null) 'direction': direction.toJson(),
    if (sort != null) 'sort': sort.toJson(),
    'before': ?before,
    'after': ?after,
    if (perPage != null) 'per_page': perPage.toString(),
    if (state != null) 'state': state.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => RepositoryAdvisory.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// List repository security advisories
///
/// Lists security advisories in a repository.
/// 
/// The authenticated user can access unpublished security advisories from a repository if they are a security manager or administrator of that repository, or if they are a collaborator on any security advisory.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:read` scope to to get a published security advisory in a private repository, or any unpublished security advisory that the authenticated user has access to.
///
/// `GET /repos/{owner}/{repo}/security-advisories`
Future<ApiResult<List<RepositoryAdvisory>>> securityAdvisoriesListRepositoryAdvisories({required String owner, required String repo, SecurityAdvisoriesListRepositoryAdvisoriesDirection? direction, SecurityAdvisoriesListRepositoryAdvisoriesSort? sort, String? before, String? after, int? perPage, SecurityAdvisoriesListRepositoryAdvisoriesState? state, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (direction != null) 'direction': direction.toJson(),
    if (sort != null) 'sort': sort.toJson(),
    'before': ?before,
    'after': ?after,
    if (perPage != null) 'per_page': perPage.toString(),
    if (state != null) 'state': state.toJson(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => RepositoryAdvisory.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Create a repository security advisory
///
/// Creates a new repository security advisory.
/// 
/// In order to create a draft repository security advisory, the authenticated user must be a security manager or administrator of that repository.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
///
/// `POST /repos/{owner}/{repo}/security-advisories`
Future<ApiResult<RepositoryAdvisory>> securityAdvisoriesCreateRepositoryAdvisory({required String owner, required String repo, required RepositoryAdvisoryCreate body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RepositoryAdvisory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Privately report a security vulnerability
///
/// Report a security vulnerability to the maintainers of the repository.
/// See "[Privately reporting a security vulnerability](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)" for more information about private vulnerability reporting.
///
/// `POST /repos/{owner}/{repo}/security-advisories/reports`
Future<ApiResult<RepositoryAdvisory>> securityAdvisoriesCreatePrivateVulnerabilityReport({required String owner, required String repo, required PrivateVulnerabilityReportCreate body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories/reports',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RepositoryAdvisory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get a repository security advisory
///
/// Get a repository security advisory using its GitHub Security Advisory (GHSA) identifier.
/// 
/// Anyone can access any published security advisory on a public repository.
/// 
/// The authenticated user can access an unpublished security advisory from a repository if they are a security manager or administrator of that repository, or if they are a
/// collaborator on the security advisory.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:read` scope to to get a published security advisory in a private repository, or any unpublished security advisory that the authenticated user has access to.
///
/// `GET /repos/{owner}/{repo}/security-advisories/{ghsa_id}`
Future<ApiResult<RepositoryAdvisory>> securityAdvisoriesGetRepositoryAdvisory({required String owner, required String repo, required String ghsaId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories/${Uri.encodeComponent(ghsaId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RepositoryAdvisory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a repository security advisory
///
/// Update a repository security advisory using its GitHub Security Advisory (GHSA) identifier.
/// 
/// In order to update any security advisory, the authenticated user must be a security manager or administrator of that repository,
/// or a collaborator on the repository security advisory.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
///
/// `PATCH /repos/{owner}/{repo}/security-advisories/{ghsa_id}`
Future<ApiResult<RepositoryAdvisory>> securityAdvisoriesUpdateRepositoryAdvisory({required String owner, required String repo, required String ghsaId, required RepositoryAdvisoryUpdate body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories/${Uri.encodeComponent(ghsaId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RepositoryAdvisory.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Request a CVE for a repository security advisory
///
/// If you want a CVE identification number for the security vulnerability in your project, and don't already have one, you can request a CVE identification number from GitHub. For more information see "[Requesting a CVE identification number](https://docs.github.com/code-security/security-advisories/repository-security-advisories/publishing-a-repository-security-advisory#requesting-a-cve-identification-number-optional)."
/// 
/// You may request a CVE for public repositories, but cannot do so for private repositories.
/// 
/// In order to request a CVE for a repository security advisory, the authenticated user must be a security manager or administrator of that repository.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` or `repository_advisories:write` scope to use this endpoint.
///
/// `POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/cve`
Future<ApiResult<Map<String, String>>> securityAdvisoriesCreateRepositoryAdvisoryCveRequest({required String owner, required String repo, required String ghsaId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories/${Uri.encodeComponent(ghsaId)}/cve',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Create a temporary private fork
///
/// Create a temporary private fork to collaborate on fixing a security vulnerability in your repository.
/// 
/// > [!NOTE]
/// > Forking a repository happens asynchronously. You may have to wait up to 5 minutes before you can access the fork.
///
/// `POST /repos/{owner}/{repo}/security-advisories/{ghsa_id}/forks`
Future<ApiResult<FullRepository>> securityAdvisoriesCreateFork({required String owner, required String repo, required String ghsaId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/security-advisories/${Uri.encodeComponent(ghsaId)}/forks',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return FullRepository.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
