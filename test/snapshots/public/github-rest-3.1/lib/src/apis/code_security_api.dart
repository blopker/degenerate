//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/code_security_attach_configuration_request.dart';import '../models/code_security_attach_enterprise_configuration_request.dart';import '../models/code_security_configuration.dart';import '../models/code_security_configuration_for_repository.dart';import '../models/code_security_configuration_repositories.dart';import '../models/code_security_create_configuration_for_enterprise_request.dart';import '../models/code_security_create_configuration_request.dart';import '../models/code_security_default_configurations2.dart';import '../models/code_security_detach_configuration_request.dart';import '../models/code_security_get_configurations_for_org_target_type.dart';import '../models/code_security_set_configuration_as_default_for_enterprise_request.dart';import '../models/code_security_set_configuration_as_default_for_enterprise_response.dart';import '../models/code_security_set_configuration_as_default_request.dart';import '../models/code_security_set_configuration_as_default_response.dart';import '../models/code_security_update_configuration_request.dart';import '../models/code_security_update_enterprise_configuration_request.dart';/// CodeSecurityApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class CodeSecurityApi {const CodeSecurityApi(this._config);

final ApiConfig _config;

/// Get code security configurations for an enterprise
///
/// Lists all code security configurations available in an enterprise.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
///
/// `GET /enterprises/{enterprise}/code-security/configurations`
Future<ApiResult<List<CodeSecurityConfiguration>>> codeSecurityGetConfigurationsForEnterprise({required String enterprise, int? perPage, String? before, String? after, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityConfiguration.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Create a code security configuration for an enterprise
///
/// Creates a code security configuration in an enterprise.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
///
/// `POST /enterprises/{enterprise}/code-security/configurations`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityCreateConfigurationForEnterprise({required String enterprise, required CodeSecurityCreateConfigurationForEnterpriseRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get default code security configurations for an enterprise
///
/// Lists the default code security configurations for an enterprise.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
///
/// `GET /enterprises/{enterprise}/code-security/configurations/defaults`
Future<ApiResult<List<CodeSecurityDefaultConfigurations2>>> codeSecurityGetDefaultConfigurationsForEnterprise({required String enterprise}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/defaults',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityDefaultConfigurations2.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Retrieve a code security configuration of an enterprise
///
/// Gets a code security configuration available in an enterprise.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
///
/// `GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityGetSingleConfigurationForEnterprise({required String enterprise, required int configurationId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a custom code security configuration for an enterprise
///
/// Updates a code security configuration in an enterprise.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
///
/// `PATCH /enterprises/{enterprise}/code-security/configurations/{configuration_id}`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityUpdateEnterpriseConfiguration({required String enterprise, required int configurationId, required CodeSecurityUpdateEnterpriseConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a code security configuration for an enterprise
///
/// Deletes a code security configuration from an enterprise.
/// Repositories attached to the configuration will retain their settings but will no longer be associated with
/// the configuration.
/// 
/// The authenticated user must be an administrator for the enterprise to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
///
/// `DELETE /enterprises/{enterprise}/code-security/configurations/{configuration_id}`
Future<ApiResult<void>> codeSecurityDeleteConfigurationForEnterprise({required String enterprise, required int configurationId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Attach an enterprise configuration to repositories
///
/// Attaches an enterprise code security configuration to repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration.
/// 
/// If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled.
/// 
/// The authenticated user must be an administrator for the enterprise to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
///
/// `POST /enterprises/{enterprise}/code-security/configurations/{configuration_id}/attach`
Future<ApiResult<Map<String, String>>> codeSecurityAttachEnterpriseConfiguration({required String enterprise, required int configurationId, required CodeSecurityAttachEnterpriseConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/attach',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Set a code security configuration as a default for an enterprise
///
/// Sets a code security configuration as a default to be applied to new repositories in your enterprise.
/// 
/// This configuration will be applied by default to the matching repository type when created, but only for organizations within the enterprise that do not already have a default code security configuration set.
/// 
/// The authenticated user must be an administrator for the enterprise to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `admin:enterprise` scope to use this endpoint.
///
/// `PUT /enterprises/{enterprise}/code-security/configurations/{configuration_id}/defaults`
Future<ApiResult<CodeSecuritySetConfigurationAsDefaultForEnterpriseResponse>> codeSecuritySetConfigurationAsDefaultForEnterprise({required String enterprise, required int configurationId, required CodeSecuritySetConfigurationAsDefaultForEnterpriseRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/defaults',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecuritySetConfigurationAsDefaultForEnterpriseResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get repositories associated with an enterprise code security configuration
///
/// Lists the repositories associated with an enterprise code security configuration in an organization.
/// 
/// The authenticated user must be an administrator of the enterprise in order to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:enterprise` scope to use this endpoint.
///
/// `GET /enterprises/{enterprise}/code-security/configurations/{configuration_id}/repositories`
Future<ApiResult<List<CodeSecurityConfigurationRepositories>>> codeSecurityGetRepositoriesForEnterpriseConfiguration({required String enterprise, required int configurationId, int? perPage, String? before, String? after, String? status, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/repositories',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
    'status': ?status,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityConfigurationRepositories.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get code security configurations for an organization
///
/// Lists all code security configurations available in an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
///
/// `GET /orgs/{org}/code-security/configurations`
Future<ApiResult<List<CodeSecurityConfiguration>>> codeSecurityGetConfigurationsForOrg({required String org, CodeSecurityGetConfigurationsForOrgTargetType? targetType, int? perPage, String? before, String? after, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (targetType != null) 'target_type': targetType.toJson(),
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityConfiguration.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Create a code security configuration
///
/// Creates a code security configuration in an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `POST /orgs/{org}/code-security/configurations`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityCreateConfiguration({required String org, required CodeSecurityCreateConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get default code security configurations
///
/// Lists the default code security configurations for an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
///
/// `GET /orgs/{org}/code-security/configurations/defaults`
Future<ApiResult<List<CodeSecurityDefaultConfigurations2>>> codeSecurityGetDefaultConfigurations({required String org}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/defaults',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityDefaultConfigurations2.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Detach configurations from repositories
///
/// Detach code security configuration(s) from a set of repositories.
/// Repositories will retain their settings but will no longer be associated with the configuration.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `DELETE /orgs/{org}/code-security/configurations/detach`
Future<ApiResult<void>> codeSecurityDetachConfiguration({required String org, required CodeSecurityDetachConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/detach',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Get a code security configuration
///
/// Gets a code security configuration available in an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `GET /orgs/{org}/code-security/configurations/{configuration_id}`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityGetConfiguration({required String org, required int configurationId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a code security configuration
///
/// Updates a code security configuration in an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `PATCH /orgs/{org}/code-security/configurations/{configuration_id}`
Future<ApiResult<CodeSecurityConfiguration>> codeSecurityUpdateConfiguration({required String org, required int configurationId, required CodeSecurityUpdateConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfiguration.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a code security configuration
///
/// Deletes the desired code security configuration from an organization.
/// Repositories attached to the configuration will retain their settings but will no longer be associated with
/// the configuration.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `DELETE /orgs/{org}/code-security/configurations/{configuration_id}`
Future<ApiResult<void>> codeSecurityDeleteConfiguration({required String org, required int configurationId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Attach a configuration to repositories
///
/// Attach a code security configuration to a set of repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration.
/// 
/// If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `POST /orgs/{org}/code-security/configurations/{configuration_id}/attach`
Future<ApiResult<Map<String, String>>> codeSecurityAttachConfiguration({required String org, required int configurationId, required CodeSecurityAttachConfigurationRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/attach',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Set a code security configuration as a default for an organization
///
/// Sets a code security configuration as a default to be applied to new repositories in your organization.
/// 
/// This configuration will be applied to the matching repository type (all, none, public, private and internal) by default when they are created.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
///
/// `PUT /orgs/{org}/code-security/configurations/{configuration_id}/defaults`
Future<ApiResult<CodeSecuritySetConfigurationAsDefaultResponse>> codeSecuritySetConfigurationAsDefault({required String org, required int configurationId, required CodeSecuritySetConfigurationAsDefaultRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/defaults',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecuritySetConfigurationAsDefaultResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Get repositories associated with a code security configuration
///
/// Lists the repositories associated with a code security configuration in an organization.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `read:org` scope to use this endpoint.
///
/// `GET /orgs/{org}/code-security/configurations/{configuration_id}/repositories`
Future<ApiResult<List<CodeSecurityConfigurationRepositories>>> codeSecurityGetRepositoriesForConfiguration({required String org, required int configurationId, int? perPage, String? before, String? after, String? status, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/orgs/${Uri.encodeComponent(org)}/code-security/configurations/${Uri.encodeComponent(configurationId.toString())}/repositories',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (perPage != null) 'per_page': perPage.toString(),
    'before': ?before,
    'after': ?after,
    'status': ?status,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => CodeSecurityConfigurationRepositories.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get the code security configuration associated with a repository
///
/// Get the code security configuration that manages a repository's code security settings.
/// 
/// The authenticated user must be an administrator or security manager for the organization to use this endpoint.
/// 
/// OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
///
/// `GET /repos/{owner}/{repo}/code-security-configuration`
Future<ApiResult<CodeSecurityConfigurationForRepository>> codeSecurityGetConfigurationForRepository({required String owner, required String repo, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/repos/${Uri.encodeComponent(owner)}/${Uri.encodeComponent(repo)}/code-security-configuration',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return CodeSecurityConfigurationForRepository.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
