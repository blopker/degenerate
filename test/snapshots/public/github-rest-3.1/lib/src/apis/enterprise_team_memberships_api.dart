//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from github-rest-3.1.yaml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/enterprise_team_memberships_bulk_add_request.dart';import '../models/enterprise_team_memberships_bulk_remove_request.dart';import '../models/simple_user.dart';/// EnterpriseTeamMembershipsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class EnterpriseTeamMembershipsApi {const EnterpriseTeamMembershipsApi(this._config);

final ApiConfig _config;

/// List members in an enterprise team
///
/// Lists all team members in an enterprise team.
///
/// `GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships`
Future<ApiResult<List<SimpleUser>>> enterpriseTeamMembershipsList({required String enterprise, required String enterpriseTeam, int? perPage, int? page, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (perPage != null) 'per_page': perPage.toString(),
    if (page != null) 'page': page.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => SimpleUser.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Bulk add team members
///
/// Add multiple team members to an enterprise team.
///
/// `POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/add`
Future<ApiResult<List<SimpleUser>>> enterpriseTeamMembershipsBulkAdd({required String enterprise, required String enterpriseTeam, required EnterpriseTeamMembershipsBulkAddRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships/add',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => SimpleUser.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Bulk remove team members
///
/// Remove multiple team members from an enterprise team.
///
/// `POST /enterprises/{enterprise}/teams/{enterprise-team}/memberships/remove`
Future<ApiResult<List<SimpleUser>>> enterpriseTeamMembershipsBulkRemove({required String enterprise, required String enterpriseTeam, required EnterpriseTeamMembershipsBulkRemoveRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships/remove',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    final json = jsonDecode(response.body) as List<dynamic>;
    return json.map((e) => SimpleUser.fromJson(e as Map<String, dynamic>)).toList();
  },
);
 } 
/// Get enterprise team membership
///
/// Returns whether the user is a member of the enterprise team.
///
/// `GET /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}`
Future<ApiResult<SimpleUser>> enterpriseTeamMembershipsGet({required String enterprise, required String enterpriseTeam, required String username, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return SimpleUser.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Add team member
///
/// Add a team member to an enterprise team.
///
/// `PUT /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}`
Future<ApiResult<SimpleUser>> enterpriseTeamMembershipsAdd({required String enterprise, required String enterpriseTeam, required String username, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return SimpleUser.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Remove team membership
///
/// Remove membership of a specific user from a particular team in an enterprise.
///
/// `DELETE /enterprises/{enterprise}/teams/{enterprise-team}/memberships/{username}`
Future<ApiResult<void>> enterpriseTeamMembershipsRemove({required String enterprise, required String enterpriseTeam, required String username, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/enterprises/${Uri.encodeComponent(enterprise)}/teams/${Uri.encodeComponent(enterpriseTeam)}/memberships/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
