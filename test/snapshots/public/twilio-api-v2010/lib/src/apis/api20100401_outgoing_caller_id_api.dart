//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from twilio-api-v2010.yaml
//  OpenAPI spec version: 3.0.1

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/account_outgoing_caller_id.dart';
import '../models/list_outgoing_caller_id_response.dart';
import '../models/update_outgoing_caller_id_request.dart';

/// Api20100401OutgoingCallerIdApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class Api20100401OutgoingCallerIdApi {
  const Api20100401OutgoingCallerIdApi(this._config);

  final ApiConfig _config;

  /// Fetch an outgoing-caller-id belonging to the account used to make the request
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/OutgoingCallerIds/{Sid}.json`
  Future<ApiResult<AccountOutgoingCallerId>> fetchOutgoingCallerId({
    required String accountSid,
    required String sid,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/OutgoingCallerIds/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountOutgoingCallerId.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Updates the caller-id
  ///
  /// `POST /2010-04-01/Accounts/{AccountSid}/OutgoingCallerIds/{Sid}.json`
  Future<ApiResult<AccountOutgoingCallerId>> updateOutgoingCallerId({
    required String accountSid,
    required String sid,
    UpdateOutgoingCallerIdRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/OutgoingCallerIds/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountOutgoingCallerId.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete the caller-id specified from the account
  ///
  /// `DELETE /2010-04-01/Accounts/{AccountSid}/OutgoingCallerIds/{Sid}.json`
  Future<ApiResult<void>> deleteOutgoingCallerId({
    required String accountSid,
    required String sid,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/OutgoingCallerIds/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Retrieve a list of outgoing-caller-ids belonging to the account used to make the request
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/OutgoingCallerIds.json`
  Future<ApiResult<ListOutgoingCallerIdResponse>> listOutgoingCallerId({
    required String accountSid,
    String? phoneNumber,
    String? friendlyName,
    int? pageSize,
    int? page,
    String? pageToken,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/OutgoingCallerIds.json',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'PhoneNumber': ?phoneNumber,
        'FriendlyName': ?friendlyName,
        if (pageSize != null) 'PageSize': pageSize.toString(),
        if (page != null) 'Page': page.toString(),
        'PageToken': ?pageToken,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListOutgoingCallerIdResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
