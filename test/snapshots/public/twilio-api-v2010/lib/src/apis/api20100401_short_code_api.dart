//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from twilio-api-v2010.yaml
//  OpenAPI spec version: 3.0.1

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/account_short_code.dart';
import '../models/list_short_code_response.dart';
import '../models/update_short_code_request.dart';

/// Api20100401ShortCodeApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class Api20100401ShortCodeApi {
  const Api20100401ShortCodeApi(this._config);

  final ApiConfig _config;

  /// Fetch an instance of a short code
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/SMS/ShortCodes/{Sid}.json`
  Future<ApiResult<AccountShortCode>> fetchShortCode({
    required String accountSid,
    required String sid,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/SMS/ShortCodes/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountShortCode.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a short code with the following parameters
  ///
  /// `POST /2010-04-01/Accounts/{AccountSid}/SMS/ShortCodes/{Sid}.json`
  Future<ApiResult<AccountShortCode>> updateShortCode({
    required String accountSid,
    required String sid,
    UpdateShortCodeRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/SMS/ShortCodes/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountShortCode.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Retrieve a list of short-codes belonging to the account used to make the request
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/SMS/ShortCodes.json`
  Future<ApiResult<ListShortCodeResponse>> listShortCode({
    required String accountSid,
    String? friendlyName,
    String? shortCode,
    int? pageSize,
    int? page,
    String? pageToken,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/SMS/ShortCodes.json',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'FriendlyName': ?friendlyName,
        'ShortCode': ?shortCode,
        if (pageSize != null) 'PageSize': pageSize.toString(),
        if (page != null) 'Page': page.toString(),
        'PageToken': ?pageToken,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListShortCodeResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
