//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from twilio-api-v2010.yaml
//  OpenAPI spec version: 3.0.1

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/account_conference.dart';
import '../models/conference_enum_status.dart';
import '../models/list_conference_response.dart';
import '../models/update_conference_request.dart';

/// Api20100401ConferenceApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class Api20100401ConferenceApi {
  const Api20100401ConferenceApi(this._config);

  final ApiConfig _config;

  /// Fetch an instance of a conference
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/Conferences/{Sid}.json`
  Future<ApiResult<AccountConference>> fetchConference({
    required String accountSid,
    required String sid,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/Conferences/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountConference.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  ///
  ///
  /// `POST /2010-04-01/Accounts/{AccountSid}/Conferences/{Sid}.json`
  Future<ApiResult<AccountConference>> updateConference({
    required String accountSid,
    required String sid,
    UpdateConferenceRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/Conferences/${Uri.encodeComponent(sid)}.json',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AccountConference.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Retrieve a list of conferences belonging to the account used to make the request
  ///
  /// `GET /2010-04-01/Accounts/{AccountSid}/Conferences.json`
  Future<ApiResult<ListConferenceResponse>> listConference({
    required String accountSid,
    String? dateCreated,
    String? dateCreatedBefore,
    String? dateCreatedAfter,
    String? dateUpdated,
    String? dateUpdatedBefore,
    String? dateUpdatedAfter,
    String? friendlyName,
    ConferenceEnumStatus? status,
    int? pageSize,
    int? page,
    String? pageToken,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/2010-04-01/Accounts/${Uri.encodeComponent(accountSid)}/Conferences.json',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'DateCreated': ?dateCreated,
        'DateCreated<': ?dateCreatedBefore,
        'DateCreated>': ?dateCreatedAfter,
        'DateUpdated': ?dateUpdated,
        'DateUpdated<': ?dateUpdatedBefore,
        'DateUpdated>': ?dateUpdatedAfter,
        'FriendlyName': ?friendlyName,
        if (status != null) 'Status': status.toJson(),
        if (pageSize != null) 'PageSize': pageSize.toString(),
        if (page != null) 'Page': page.toString(),
        'PageToken': ?pageToken,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListConferenceResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
