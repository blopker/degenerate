//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'package:collection/collection.dart';
import 'model_ids_responses.dart';
import 'prompt.dart';
import 'reasoning.dart';
import 'response_properties_truncation.dart';
import 'response_text_param.dart';
import 'tool.dart';
import 'tool_choice_param.dart';

final class ResponseProperties {
  const ResponseProperties({
    this.previousResponseId,
    this.model,
    this.reasoning,
    this.background,
    this.maxOutputTokens,
    this.maxToolCalls,
    this.text,
    this.tools,
    this.toolChoice,
    this.prompt,
    this.truncation,
  });

  factory ResponseProperties.fromJson(Map<String, dynamic> json) {
    return ResponseProperties(
      previousResponseId: json['previous_response_id'] as String?,
      model: json['model'] != null
          ? ModelIdsResponses.fromJson(json['model'] as Map<String, dynamic>)
          : null,
      reasoning: json['reasoning'] != null
          ? Reasoning.fromJson(json['reasoning'] as Map<String, dynamic>)
          : null,
      background: json['background'] as bool?,
      maxOutputTokens: json['max_output_tokens'] != null
          ? (json['max_output_tokens'] as num).toInt()
          : null,
      maxToolCalls: json['max_tool_calls'] != null
          ? (json['max_tool_calls'] as num).toInt()
          : null,
      text: json['text'] != null
          ? ResponseTextParam.fromJson(json['text'] as Map<String, dynamic>)
          : null,
      tools: (json['tools'] as List<dynamic>?)
          ?.map((e) => Tool.fromJson(e as Map<String, dynamic>))
          .toList(),
      toolChoice: json['tool_choice'] != null
          ? ToolChoiceParam.fromJson(
              json['tool_choice'] as Map<String, dynamic>,
            )
          : null,
      prompt: json['prompt'] != null
          ? Prompt.fromJson(json['prompt'] as Map<String, dynamic>)
          : null,
      truncation: json['truncation'] != null
          ? ResponsePropertiesTruncation.fromJson(json['truncation'] as String)
          : null,
    );
  }

  final String? previousResponseId;

  /// Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI
  /// offers a wide range of models with different capabilities, performance
  /// characteristics, and price points. Refer to the [model guide](/docs/models)
  /// to browse and compare available models.
  ///
  final ModelIdsResponses? model;

  final Reasoning? reasoning;

  final bool? background;

  final int? maxOutputTokens;

  final int? maxToolCalls;

  final ResponseTextParam? text;

  final List<Tool>? tools;

  final ToolChoiceParam? toolChoice;

  final Prompt? prompt;

  final ResponsePropertiesTruncation? truncation;

  Map<String, dynamic> toJson() {
    return {
      'previous_response_id': ?previousResponseId,
      if (model != null) 'model': model?.toJson(),
      if (reasoning != null) 'reasoning': reasoning?.toJson(),
      'background': ?background,
      'max_output_tokens': ?maxOutputTokens,
      'max_tool_calls': ?maxToolCalls,
      if (text != null) 'text': text?.toJson(),
      if (tools != null) 'tools': tools?.map((e) => e.toJson()).toList(),
      if (toolChoice != null) 'tool_choice': toolChoice?.toJson(),
      if (prompt != null) 'prompt': prompt?.toJson(),
      if (truncation != null) 'truncation': truncation?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  ResponseProperties copyWith({
    String? Function()? previousResponseId,
    ModelIdsResponses Function()? model,
    Reasoning? Function()? reasoning,
    bool? Function()? background,
    int? Function()? maxOutputTokens,
    int? Function()? maxToolCalls,
    ResponseTextParam Function()? text,
    List<Tool> Function()? tools,
    ToolChoiceParam Function()? toolChoice,
    Prompt Function()? prompt,
    ResponsePropertiesTruncation? Function()? truncation,
  }) {
    return ResponseProperties(
      previousResponseId: previousResponseId != null
          ? previousResponseId()
          : this.previousResponseId,
      model: model != null ? model() : this.model,
      reasoning: reasoning != null ? reasoning() : this.reasoning,
      background: background != null ? background() : this.background,
      maxOutputTokens: maxOutputTokens != null
          ? maxOutputTokens()
          : this.maxOutputTokens,
      maxToolCalls: maxToolCalls != null ? maxToolCalls() : this.maxToolCalls,
      text: text != null ? text() : this.text,
      tools: tools != null ? tools() : this.tools,
      toolChoice: toolChoice != null ? toolChoice() : this.toolChoice,
      prompt: prompt != null ? prompt() : this.prompt,
      truncation: truncation != null ? truncation() : this.truncation,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is ResponseProperties &&
            previousResponseId == other.previousResponseId &&
            model == other.model &&
            reasoning == other.reasoning &&
            background == other.background &&
            maxOutputTokens == other.maxOutputTokens &&
            maxToolCalls == other.maxToolCalls &&
            text == other.text &&
            const ListEquality<dynamic>().equals(tools, other.tools) &&
            toolChoice == other.toolChoice &&
            prompt == other.prompt &&
            truncation == other.truncation;
  }

  @override
  int get hashCode {
    return Object.hash(
      previousResponseId,
      model,
      reasoning,
      background,
      maxOutputTokens,
      maxToolCalls,
      text,
      Object.hashAll(tools ?? const []),
      toolChoice,
      prompt,
      truncation,
    );
  }

  @override
  String toString() {
    return 'ResponseProperties(previousResponseId: $previousResponseId, model: $model, reasoning: $reasoning, background: $background, maxOutputTokens: $maxOutputTokens, maxToolCalls: $maxToolCalls, text: $text, tools: $tools, toolChoice: $toolChoice, prompt: $prompt, truncation: $truncation)';
  }
}
