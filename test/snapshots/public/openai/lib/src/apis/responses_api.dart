//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/include_enum.dart';
import '../models/list_input_items_order.dart';
import '../models/model_response_properties.dart';
import '../models/response_item_list.dart';

/// ResponsesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ResponsesApi {
  const ResponsesApi(this._config);

  final ApiConfig _config;

  /// Creates a model response. Provide [text](/docs/guides/text) or
  /// [image](/docs/guides/images) inputs to generate [text](/docs/guides/text)
  /// or [JSON](/docs/guides/structured-outputs) outputs. Have the model call
  /// your own [custom code](/docs/guides/function-calling) or use built-in
  /// [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search)
  /// or [file search](/docs/guides/tools-file-search) to use your own data
  /// as input for the model's response.
  ///
  ///
  /// `POST /responses`
  Future<ApiResult<ModelResponseProperties>> createResponse({
    required ModelResponseProperties body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/responses',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ModelResponseProperties.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Retrieves a model response with the given ID.
  ///
  ///
  /// `GET /responses/{response_id}`
  Future<ApiResult<ModelResponseProperties>> getResponse({
    required String responseId,
    List<IncludeEnum>? include,
    bool? stream,
    int? startingAfter,
    bool? includeObfuscation,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/responses/${Uri.encodeComponent(responseId)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (include != null) 'include': include.toString(),
        if (stream != null) 'stream': stream.toString(),
        if (startingAfter != null) 'starting_after': startingAfter.toString(),
        if (includeObfuscation != null)
          'include_obfuscation': includeObfuscation.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ModelResponseProperties.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes a model response with the given ID.
  ///
  ///
  /// `DELETE /responses/{response_id}`
  Future<ApiResult<void>> deleteResponse({required String responseId}) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/responses/${Uri.encodeComponent(responseId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Cancels a model response with the given ID. Only responses created with
  /// the `background` parameter set to `true` can be cancelled.
  /// [Learn more](/docs/guides/background).
  ///
  ///
  /// `POST /responses/{response_id}/cancel`
  Future<ApiResult<ModelResponseProperties>> cancelResponse({
    required String responseId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/responses/${Uri.encodeComponent(responseId)}/cancel',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ModelResponseProperties.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Returns a list of input items for a given response.
  ///
  /// `GET /responses/{response_id}/input_items`
  Future<ApiResult<ResponseItemList>> listInputItems({
    required String responseId,
    int? limit,
    ListInputItemsOrder? order,
    String? after,
    List<IncludeEnum>? include,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/responses/${Uri.encodeComponent(responseId)}/input_items',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (order != null) 'order': order.toJson(),
        'after': ?after,
        if (include != null) 'include': include.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseItemList.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
