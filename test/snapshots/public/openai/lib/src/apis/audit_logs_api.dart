//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/audit_log_event_type.dart';
import '../models/list_audit_logs_effective_at.dart';
import '../models/list_audit_logs_response.dart';

/// AuditLogsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AuditLogsApi {
  const AuditLogsApi(this._config);

  final ApiConfig _config;

  /// List user actions and configuration changes within this organization.
  ///
  /// `GET /organization/audit_logs`
  Future<ApiResult<ListAuditLogsResponse>> listAuditLogs({
    ListAuditLogsEffectiveAt? effectiveAt,
    List<String>? projectIds,
    List<AuditLogEventType>? eventTypes,
    List<String>? actorIds,
    List<String>? actorEmails,
    List<String>? resourceIds,
    int? limit,
    String? after,
    String? before,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/organization/audit_logs',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (effectiveAt != null) 'effective_at': effectiveAt.toString(),
        if (projectIds != null) 'project_ids[]': projectIds.toString(),
        if (eventTypes != null) 'event_types[]': eventTypes.toString(),
        if (actorIds != null) 'actor_ids[]': actorIds.toString(),
        if (actorEmails != null) 'actor_emails[]': actorEmails.toString(),
        if (resourceIds != null) 'resource_ids[]': resourceIds.toString(),
        if (limit != null) 'limit': limit.toString(),
        'after': ?after,
        'before': ?before,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListAuditLogsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
