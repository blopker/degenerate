//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/batch.dart';
import '../models/create_batch_request.dart';
import '../models/list_batches_response.dart';

/// BatchApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class BatchApi {
  const BatchApi(this._config);

  final ApiConfig _config;

  /// List your organization's batches.
  ///
  /// `GET /batches`
  Future<ApiResult<ListBatchesResponse>> listBatches({
    String? after,
    int? limit,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/batches',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'after': ?after,
        if (limit != null) 'limit': limit.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListBatchesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates and executes a batch from an uploaded file of requests
  ///
  /// `POST /batches`
  Future<ApiResult<Batch>> createBatch({
    required CreateBatchRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/batches',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return Batch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Retrieves a batch.
  ///
  /// `GET /batches/{batch_id}`
  Future<ApiResult<Batch>> retrieveBatch({required String batchId}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/batches/${Uri.encodeComponent(batchId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return Batch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file.
  ///
  /// `POST /batches/{batch_id}/cancel`
  Future<ApiResult<Batch>> cancelBatch({required String batchId}) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/batches/${Uri.encodeComponent(batchId)}/cancel',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return Batch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
