//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/create_skill_body.dart';
import '../models/create_skill_version_body.dart';
import '../models/deleted_skill_resource.dart';
import '../models/deleted_skill_version_resource.dart';
import '../models/order_enum.dart';
import '../models/set_default_skill_version_body.dart';
import '../models/skill_list_resource.dart';
import '../models/skill_resource.dart';
import '../models/skill_version_list_resource.dart';
import '../models/skill_version_resource.dart';

/// SkillsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SkillsApi {
  const SkillsApi(this._config);

  final ApiConfig _config;

  /// List all skills for the current project.
  ///
  /// `GET /skills`
  Future<ApiResult<SkillListResource>> listSkills({
    int? limit,
    OrderEnum? order,
    String? after,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/skills',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (order != null) 'order': order.toJson(),
        'after': ?after,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillListResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new skill.
  ///
  /// `POST /skills`
  Future<ApiResult<SkillResource>> createSkill({CreateSkillBody? body}) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/skills',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a skill by its ID.
  ///
  /// `GET /skills/{skill_id}`
  Future<ApiResult<SkillResource>> getSkill({required String skillId}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/skills/${Uri.encodeComponent(skillId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update the default version pointer for a skill.
  ///
  /// `POST /skills/{skill_id}`
  Future<ApiResult<SkillResource>> updateSkillDefaultVersion({
    required String skillId,
    SetDefaultSkillVersionBody? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/skills/${Uri.encodeComponent(skillId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a skill by its ID.
  ///
  /// `DELETE /skills/{skill_id}`
  Future<ApiResult<DeletedSkillResource>> deleteSkill({
    required String skillId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/skills/${Uri.encodeComponent(skillId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeletedSkillResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Download a skill zip bundle by its ID.
  ///
  /// `GET /skills/{skill_id}/content`
  Future<ApiResult<String>> getSkillContent({required String skillId}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/skills/${Uri.encodeComponent(skillId)}/content',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return response.body;
      },
    );
  }

  /// List skill versions for a skill.
  ///
  /// `GET /skills/{skill_id}/versions`
  Future<ApiResult<SkillVersionListResource>> listSkillVersions({
    required String skillId,
    int? limit,
    OrderEnum? order,
    String? after,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/skills/${Uri.encodeComponent(skillId)}/versions',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (order != null) 'order': order.toJson(),
        'after': ?after,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillVersionListResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new immutable skill version.
  ///
  /// `POST /skills/{skill_id}/versions`
  Future<ApiResult<SkillVersionResource>> createSkillVersion({
    required String skillId,
    CreateSkillVersionBody? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/skills/${Uri.encodeComponent(skillId)}/versions',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillVersionResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a specific skill version.
  ///
  /// `GET /skills/{skill_id}/versions/{version}`
  Future<ApiResult<SkillVersionResource>> getSkillVersion({
    required String skillId,
    required String version,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/skills/${Uri.encodeComponent(skillId)}/versions/${Uri.encodeComponent(version)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SkillVersionResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a skill version.
  ///
  /// `DELETE /skills/{skill_id}/versions/{version}`
  Future<ApiResult<DeletedSkillVersionResource>> deleteSkillVersion({
    required String skillId,
    required String version,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/skills/${Uri.encodeComponent(skillId)}/versions/${Uri.encodeComponent(version)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeletedSkillVersionResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Download a skill version zip bundle.
  ///
  /// `GET /skills/{skill_id}/versions/{version}/content`
  Future<ApiResult<String>> getSkillVersionContent({
    required String skillId,
    required String version,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/skills/${Uri.encodeComponent(skillId)}/versions/${Uri.encodeComponent(version)}/content',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return response.body;
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
