//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/create_group_user_body.dart';
import '../models/group_user_assignment.dart';
import '../models/group_user_deleted_resource.dart';
import '../models/list_group_users_order.dart';
import '../models/user_list_resource.dart';

/// GroupUsersApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class GroupUsersApi {
  const GroupUsersApi(this._config);

  final ApiConfig _config;

  /// Lists the users assigned to a group.
  ///
  /// `GET /organization/groups/{group_id}/users`
  Future<ApiResult<UserListResource>> listGroupUsers({
    required String groupId,
    int? limit,
    String? after,
    ListGroupUsersOrder? order,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/organization/groups/${Uri.encodeComponent(groupId)}/users',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        'after': ?after,
        if (order != null) 'order': order.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return UserListResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Adds a user to a group.
  ///
  /// `POST /organization/groups/{group_id}/users`
  Future<ApiResult<GroupUserAssignment>> addGroupUser({
    required String groupId,
    required CreateGroupUserBody body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/organization/groups/${Uri.encodeComponent(groupId)}/users',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GroupUserAssignment.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Removes a user from a group.
  ///
  /// `DELETE /organization/groups/{group_id}/users/{user_id}`
  Future<ApiResult<GroupUserDeletedResource>> removeGroupUser({
    required String groupId,
    required String userId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/organization/groups/${Uri.encodeComponent(groupId)}/users/${Uri.encodeComponent(userId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GroupUserDeletedResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
