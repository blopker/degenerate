//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/invite.dart';import '../models/invite_delete_response.dart';import '../models/invite_list_response.dart';import '../models/invite_request.dart';/// InvitesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class InvitesApi {const InvitesApi(this._config);

final ApiConfig _config;

/// Returns a list of invites in the organization.
///
/// `GET /organization/invites`
Future<ApiResult<InviteListResponse>> listInvites({int? limit, String? after, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/organization/invites',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (limit != null) 'limit': limit.toString(),
    'after': ?after,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return InviteListResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
///
/// `POST /organization/invites`
Future<ApiResult<Invite>> inviteUser({required InviteRequest body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/organization/invites',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return Invite.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Retrieves an invite.
///
/// `GET /organization/invites/{invite_id}`
Future<ApiResult<Invite>> retrieveInvite({required String inviteId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/organization/invites/${Uri.encodeComponent(inviteId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return Invite.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete an invite. If the invite has already been accepted, it cannot be deleted.
///
/// `DELETE /organization/invites/{invite_id}`
Future<ApiResult<InviteDeleteResponse>> deleteInvite({required String inviteId}) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/organization/invites/${Uri.encodeComponent(inviteId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return InviteDeleteResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
