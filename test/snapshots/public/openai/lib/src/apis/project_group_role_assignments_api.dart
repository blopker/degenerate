//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/deleted_role_assignment_resource.dart';
import '../models/group_role_assignment.dart';
import '../models/list_project_group_role_assignments_order.dart';
import '../models/public_assign_organization_group_role_body.dart';
import '../models/role_list_resource.dart';

/// ProjectGroupRoleAssignmentsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ProjectGroupRoleAssignmentsApi {
  const ProjectGroupRoleAssignmentsApi(this._config);

  final ApiConfig _config;

  /// Lists the project roles assigned to a group within a project.
  ///
  /// `GET /projects/{project_id}/groups/{group_id}/roles`
  Future<ApiResult<RoleListResource>> listProjectGroupRoleAssignments({
    required String projectId,
    required String groupId,
    int? limit,
    String? after,
    ListProjectGroupRoleAssignmentsOrder? order,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/projects/${Uri.encodeComponent(projectId)}/groups/${Uri.encodeComponent(groupId)}/roles',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        'after': ?after,
        if (order != null) 'order': order.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RoleListResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Assigns a project role to a group within a project.
  ///
  /// `POST /projects/{project_id}/groups/{group_id}/roles`
  Future<ApiResult<GroupRoleAssignment>> assignProjectGroupRole({
    required String projectId,
    required String groupId,
    required PublicAssignOrganizationGroupRoleBody body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/projects/${Uri.encodeComponent(projectId)}/groups/${Uri.encodeComponent(groupId)}/roles',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GroupRoleAssignment.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Unassigns a project role from a group within a project.
  ///
  /// `DELETE /projects/{project_id}/groups/{group_id}/roles/{role_id}`
  Future<ApiResult<DeletedRoleAssignmentResource>> unassignProjectGroupRole({
    required String projectId,
    required String groupId,
    required String roleId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/projects/${Uri.encodeComponent(projectId)}/groups/${Uri.encodeComponent(groupId)}/roles/${Uri.encodeComponent(roleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeletedRoleAssignmentResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
