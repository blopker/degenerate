//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from openai.yml
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/create_speech_request.dart';
import '../models/create_transcription_request.dart';
import '../models/create_transcription_response.dart';
import '../models/create_translation_request.dart';
import '../models/create_translation_response.dart';
import '../models/create_voice_consent_request.dart';
import '../models/create_voice_request.dart';
import '../models/update_voice_consent_request.dart';
import '../models/voice_consent_deleted_resource.dart';
import '../models/voice_consent_list_resource.dart';
import '../models/voice_consent_resource.dart';
import '../models/voice_resource.dart';

/// AudioApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AudioApi {
  const AudioApi(this._config);

  final ApiConfig _config;

  /// Generates audio from the input text.
  ///
  /// Returns the audio file content, or a stream of audio events.
  ///
  ///
  /// `POST /audio/speech`
  Future<ApiResult<void>> createSpeech({
    required CreateSpeechRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/speech',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Transcribes audio into the input language.
  ///
  /// Returns a transcription object in `json`, `diarized_json`, or `verbose_json`
  /// format, or a stream of transcript events.
  ///
  ///
  /// `POST /audio/transcriptions`
  Future<ApiResult<CreateTranscriptionResponse>> createTranscription({
    required CreateTranscriptionRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/transcriptions',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return CreateTranscriptionResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Translates audio into English.
  ///
  /// `POST /audio/translations`
  Future<ApiResult<CreateTranslationResponse>> createTranslation({
    required CreateTranslationRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/translations',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return CreateTranslationResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Returns a list of voice consent recordings.
  ///
  /// List consent recordings available to your organization for creating custom voices.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
  ///
  ///
  /// `GET /audio/voice_consents`
  Future<ApiResult<VoiceConsentListResource>> listVoiceConsents({
    String? after,
    int? limit,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/audio/voice_consents',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'after': ?after,
        if (limit != null) 'limit': limit.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceConsentListResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Upload a voice consent recording.
  ///
  /// Upload a consent recording that authorizes creation of a custom voice.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
  ///
  ///
  /// `POST /audio/voice_consents`
  Future<ApiResult<VoiceConsentResource>> createVoiceConsent({
    required CreateVoiceConsentRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/voice_consents',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceConsentResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Retrieves a voice consent recording.
  ///
  /// Retrieve consent recording metadata used for creating custom voices.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
  ///
  ///
  /// `GET /audio/voice_consents/{consent_id}`
  Future<ApiResult<VoiceConsentResource>> getVoiceConsent({
    required String consentId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/audio/voice_consents/${Uri.encodeComponent(consentId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceConsentResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Updates a voice consent recording (metadata only).
  ///
  /// Update consent recording metadata used for creating custom voices. This endpoint updates metadata only and does not replace the underlying audio.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
  ///
  ///
  /// `POST /audio/voice_consents/{consent_id}`
  Future<ApiResult<VoiceConsentResource>> updateVoiceConsent({
    required String consentId,
    required UpdateVoiceConsentRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/voice_consents/${Uri.encodeComponent(consentId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceConsentResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes a voice consent recording.
  ///
  /// Delete a consent recording that was uploaded for creating custom voices.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
  ///
  ///
  /// `DELETE /audio/voice_consents/{consent_id}`
  Future<ApiResult<VoiceConsentDeletedResource>> deleteVoiceConsent({
    required String consentId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/audio/voice_consents/${Uri.encodeComponent(consentId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceConsentDeletedResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates a custom voice.
  ///
  /// Create a custom voice you can use for audio output (for example, in Text-to-Speech and the Realtime API). This requires an audio sample and a previously uploaded consent recording.
  ///
  /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
  ///
  ///
  /// `POST /audio/voices`
  Future<ApiResult<VoiceResource>> createVoice({
    required CreateVoiceRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/audio/voices',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return VoiceResource.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
