//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';

final class UrlSubmit {
  const UrlSubmit({this.skippedUrls, this.submittedUrls});

  factory UrlSubmit.fromJson(Map<String, dynamic> json) {
    return UrlSubmit(
      skippedUrls: (json['skipped_urls'] as List<dynamic>?)
          ?.map(
            (e) => (e as Map<String, dynamic>).map(
              (k, v) => MapEntry(k, v as String),
            ),
          )
          .toList(),
      submittedUrls: (json['submitted_urls'] as List<dynamic>?)
          ?.map(
            (e) => (e as Map<String, dynamic>).map(
              (k, v) => MapEntry(k, v as String),
            ),
          )
          .toList(),
    );
  }

  final List<Map<String, String>>? skippedUrls;

  final List<Map<String, String>>? submittedUrls;

  Map<String, dynamic> toJson() {
    return {
      if (skippedUrls != null)
        'skipped_urls': skippedUrls?.map((e) => e).toList(),
      if (submittedUrls != null)
        'submitted_urls': submittedUrls?.map((e) => e).toList(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  UrlSubmit copyWith({
    List<Map<String, String>> Function()? skippedUrls,
    List<Map<String, String>> Function()? submittedUrls,
  }) {
    return UrlSubmit(
      skippedUrls: skippedUrls != null ? skippedUrls() : this.skippedUrls,
      submittedUrls: submittedUrls != null
          ? submittedUrls()
          : this.submittedUrls,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is UrlSubmit &&
            const ListEquality<dynamic>().equals(
              skippedUrls,
              other.skippedUrls,
            ) &&
            const ListEquality<dynamic>().equals(
              submittedUrls,
              other.submittedUrls,
            );
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(skippedUrls ?? const []),
      Object.hashAll(submittedUrls ?? const []),
    );
  }

  @override
  String toString() {
    return 'UrlSubmit(skippedUrls: $skippedUrls, submittedUrls: $submittedUrls)';
  }
}
