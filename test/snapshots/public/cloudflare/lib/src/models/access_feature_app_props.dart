//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'access_type.dart';

final class AccessFeatureAppProps {
  const AccessFeatureAppProps({
    this.allowedIdps,
    this.autoRedirectToIdentity,
    this.customDenyUrl,
    this.customNonIdentityDenyUrl,
    this.customPages,
    this.domain,
    this.name,
    this.sessionDuration,
    required this.type,
  });

  factory AccessFeatureAppProps.fromJson(Map<String, dynamic> json) {
    return AccessFeatureAppProps(
      allowedIdps: (json['allowed_idps'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      autoRedirectToIdentity: json['auto_redirect_to_identity'] as bool?,
      customDenyUrl: json['custom_deny_url'] as String?,
      customNonIdentityDenyUrl: json['custom_non_identity_deny_url'] as String?,
      customPages: (json['custom_pages'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      domain: json['domain'] as String?,
      name: json['name'] as String?,
      sessionDuration: json['session_duration'] as String?,
      type: AccessType.fromJson(json['type'] as String),
    );
  }

  final List<String>? allowedIdps;

  final bool? autoRedirectToIdentity;

  final String? customDenyUrl;

  final String? customNonIdentityDenyUrl;

  final List<String>? customPages;

  final String? domain;

  final String? name;

  final String? sessionDuration;

  final AccessType type;

  Map<String, dynamic> toJson() {
    return {
      'allowed_idps': ?allowedIdps,
      'auto_redirect_to_identity': ?autoRedirectToIdentity,
      'custom_deny_url': ?customDenyUrl,
      'custom_non_identity_deny_url': ?customNonIdentityDenyUrl,
      'custom_pages': ?customPages,
      'domain': ?domain,
      'name': ?name,
      'session_duration': ?sessionDuration,
      'type': type.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('type');
  }

  AccessFeatureAppProps copyWith({
    List<String> Function()? allowedIdps,
    bool Function()? autoRedirectToIdentity,
    String Function()? customDenyUrl,
    String Function()? customNonIdentityDenyUrl,
    List<String> Function()? customPages,
    String Function()? domain,
    String Function()? name,
    String Function()? sessionDuration,
    AccessType? type,
  }) {
    return AccessFeatureAppProps(
      allowedIdps: allowedIdps != null ? allowedIdps() : this.allowedIdps,
      autoRedirectToIdentity: autoRedirectToIdentity != null
          ? autoRedirectToIdentity()
          : this.autoRedirectToIdentity,
      customDenyUrl: customDenyUrl != null
          ? customDenyUrl()
          : this.customDenyUrl,
      customNonIdentityDenyUrl: customNonIdentityDenyUrl != null
          ? customNonIdentityDenyUrl()
          : this.customNonIdentityDenyUrl,
      customPages: customPages != null ? customPages() : this.customPages,
      domain: domain != null ? domain() : this.domain,
      name: name != null ? name() : this.name,
      sessionDuration: sessionDuration != null
          ? sessionDuration()
          : this.sessionDuration,
      type: type ?? this.type,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is AccessFeatureAppProps &&
            const ListEquality<dynamic>().equals(
              allowedIdps,
              other.allowedIdps,
            ) &&
            autoRedirectToIdentity == other.autoRedirectToIdentity &&
            customDenyUrl == other.customDenyUrl &&
            customNonIdentityDenyUrl == other.customNonIdentityDenyUrl &&
            const ListEquality<dynamic>().equals(
              customPages,
              other.customPages,
            ) &&
            domain == other.domain &&
            name == other.name &&
            sessionDuration == other.sessionDuration &&
            type == other.type;
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(allowedIdps ?? const []),
      autoRedirectToIdentity,
      customDenyUrl,
      customNonIdentityDenyUrl,
      Object.hashAll(customPages ?? const []),
      domain,
      name,
      sessionDuration,
      type,
    );
  }

  @override
  String toString() {
    return 'AccessFeatureAppProps(allowedIdps: $allowedIdps, autoRedirectToIdentity: $autoRedirectToIdentity, customDenyUrl: $customDenyUrl, customNonIdentityDenyUrl: $customNonIdentityDenyUrl, customPages: $customPages, domain: $domain, name: $name, sessionDuration: $sessionDuration, type: $type)';
  }
}
