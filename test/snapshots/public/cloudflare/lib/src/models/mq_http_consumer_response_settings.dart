//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class MqHttpConsumerResponseSettings {
  const MqHttpConsumerResponseSettings({
    this.batchSize,
    this.maxRetries,
    this.retryDelay,
    this.visibilityTimeoutMs,
  });

  factory MqHttpConsumerResponseSettings.fromJson(Map<String, dynamic> json) {
    return MqHttpConsumerResponseSettings(
      batchSize: json['batch_size'] != null
          ? (json['batch_size'] as num).toDouble()
          : null,
      maxRetries: json['max_retries'] != null
          ? (json['max_retries'] as num).toDouble()
          : null,
      retryDelay: json['retry_delay'] != null
          ? (json['retry_delay'] as num).toDouble()
          : null,
      visibilityTimeoutMs: json['visibility_timeout_ms'] != null
          ? (json['visibility_timeout_ms'] as num).toDouble()
          : null,
    );
  }

  final double? batchSize;

  final double? maxRetries;

  final double? retryDelay;

  final double? visibilityTimeoutMs;

  Map<String, dynamic> toJson() {
    return {
      'batch_size': ?batchSize,
      'max_retries': ?maxRetries,
      'retry_delay': ?retryDelay,
      'visibility_timeout_ms': ?visibilityTimeoutMs,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  MqHttpConsumerResponseSettings copyWith({
    double Function()? batchSize,
    double Function()? maxRetries,
    double Function()? retryDelay,
    double Function()? visibilityTimeoutMs,
  }) {
    return MqHttpConsumerResponseSettings(
      batchSize: batchSize != null ? batchSize() : this.batchSize,
      maxRetries: maxRetries != null ? maxRetries() : this.maxRetries,
      retryDelay: retryDelay != null ? retryDelay() : this.retryDelay,
      visibilityTimeoutMs: visibilityTimeoutMs != null
          ? visibilityTimeoutMs()
          : this.visibilityTimeoutMs,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is MqHttpConsumerResponseSettings &&
            batchSize == other.batchSize &&
            maxRetries == other.maxRetries &&
            retryDelay == other.retryDelay &&
            visibilityTimeoutMs == other.visibilityTimeoutMs;
  }

  @override
  int get hashCode {
    return Object.hash(batchSize, maxRetries, retryDelay, visibilityTimeoutMs);
  }

  @override
  String toString() {
    return 'MqHttpConsumerResponseSettings(batchSize: $batchSize, maxRetries: $maxRetries, retryDelay: $retryDelay, visibilityTimeoutMs: $visibilityTimeoutMs)';
  }
}
