//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';

final class DnsAnalyticsQuery {
  const DnsAnalyticsQuery({
    required this.dimensions,
    this.filters,
    required this.limit,
    required this.metrics,
    required this.since,
    this.sort,
    required this.until,
  });

  factory DnsAnalyticsQuery.fromJson(Map<String, dynamic> json) {
    return DnsAnalyticsQuery(
      dimensions: (json['dimensions'] as List<dynamic>)
          .map((e) => e as String)
          .toList(),
      filters: json['filters'] as String?,
      limit: (json['limit'] as num).toInt(),
      metrics: (json['metrics'] as List<dynamic>)
          .map((e) => e as String)
          .toList(),
      since: DateTime.parse(json['since'] as String),
      sort: (json['sort'] as List<dynamic>?)?.map((e) => e as String).toList(),
      until: DateTime.parse(json['until'] as String),
    );
  }

  /// Array of dimension names.
  final List<String> dimensions;

  final String? filters;

  final int limit;

  /// Array of metric names.
  final List<String> metrics;

  final DateTime since;

  /// Array of dimensions to sort by, where each dimension may be prefixed by - (descending) or + (ascending).
  final List<String>? sort;

  final DateTime until;

  Map<String, dynamic> toJson() {
    return {
      'dimensions': dimensions,
      'filters': ?filters,
      'limit': limit,
      'metrics': metrics,
      'since': since.toIso8601String(),
      'sort': ?sort,
      'until': until.toIso8601String(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('dimensions') &&
        json.containsKey('limit') &&
        json['limit'] is num &&
        json.containsKey('metrics') &&
        json.containsKey('since') &&
        json['since'] is String &&
        json.containsKey('until') &&
        json['until'] is String;
  }

  DnsAnalyticsQuery copyWith({
    List<String>? dimensions,
    String Function()? filters,
    int? limit,
    List<String>? metrics,
    DateTime? since,
    List<String> Function()? sort,
    DateTime? until,
  }) {
    return DnsAnalyticsQuery(
      dimensions: dimensions ?? this.dimensions,
      filters: filters != null ? filters() : this.filters,
      limit: limit ?? this.limit,
      metrics: metrics ?? this.metrics,
      since: since ?? this.since,
      sort: sort != null ? sort() : this.sort,
      until: until ?? this.until,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is DnsAnalyticsQuery &&
            const ListEquality<dynamic>().equals(
              dimensions,
              other.dimensions,
            ) &&
            filters == other.filters &&
            limit == other.limit &&
            const ListEquality<dynamic>().equals(metrics, other.metrics) &&
            since == other.since &&
            const ListEquality<dynamic>().equals(sort, other.sort) &&
            until == other.until;
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(dimensions),
      filters,
      limit,
      Object.hashAll(metrics),
      since,
      Object.hashAll(sort ?? const []),
      until,
    );
  }

  @override
  String toString() {
    return 'DnsAnalyticsQuery(dimensions: $dimensions, filters: $filters, limit: $limit, metrics: $metrics, since: $since, sort: $sort, until: $until)';
  }
}
