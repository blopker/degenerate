//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'tunnel_tunnel_type.dart';

final class TunnelTeamnet {
  const TunnelTeamnet({
    this.comment,
    this.createdAt,
    this.deletedAt,
    this.id,
    this.network,
    this.tunType,
    this.tunnelId,
    this.tunnelName,
    this.virtualNetworkId,
    this.virtualNetworkName,
  });

  factory TunnelTeamnet.fromJson(Map<String, dynamic> json) {
    return TunnelTeamnet(
      comment: json['comment'] as String?,
      createdAt: json['created_at'] != null
          ? DateTime.parse(json['created_at'] as String)
          : null,
      deletedAt: json['deleted_at'] != null
          ? DateTime.parse(json['deleted_at'] as String)
          : null,
      id: json['id'] as String?,
      network: json['network'] as String?,
      tunType: json['tun_type'] != null
          ? TunnelTunnelType.fromJson(json['tun_type'] as String)
          : null,
      tunnelId: json['tunnel_id'] as String?,
      tunnelName: json['tunnel_name'] as String?,
      virtualNetworkId: json['virtual_network_id'] as String?,
      virtualNetworkName: json['virtual_network_name'] as String?,
    );
  }

  final String? comment;

  final DateTime? createdAt;

  final DateTime? deletedAt;

  final String? id;

  final String? network;

  final TunnelTunnelType? tunType;

  final String? tunnelId;

  final String? tunnelName;

  final String? virtualNetworkId;

  final String? virtualNetworkName;

  Map<String, dynamic> toJson() {
    return {
      'comment': ?comment,
      if (createdAt != null) 'created_at': createdAt?.toIso8601String(),
      if (deletedAt != null) 'deleted_at': deletedAt?.toIso8601String(),
      'id': ?id,
      'network': ?network,
      if (tunType != null) 'tun_type': tunType?.toJson(),
      'tunnel_id': ?tunnelId,
      'tunnel_name': ?tunnelName,
      'virtual_network_id': ?virtualNetworkId,
      'virtual_network_name': ?virtualNetworkName,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  TunnelTeamnet copyWith({
    String Function()? comment,
    DateTime Function()? createdAt,
    DateTime Function()? deletedAt,
    String Function()? id,
    String Function()? network,
    TunnelTunnelType Function()? tunType,
    String Function()? tunnelId,
    String Function()? tunnelName,
    String Function()? virtualNetworkId,
    String Function()? virtualNetworkName,
  }) {
    return TunnelTeamnet(
      comment: comment != null ? comment() : this.comment,
      createdAt: createdAt != null ? createdAt() : this.createdAt,
      deletedAt: deletedAt != null ? deletedAt() : this.deletedAt,
      id: id != null ? id() : this.id,
      network: network != null ? network() : this.network,
      tunType: tunType != null ? tunType() : this.tunType,
      tunnelId: tunnelId != null ? tunnelId() : this.tunnelId,
      tunnelName: tunnelName != null ? tunnelName() : this.tunnelName,
      virtualNetworkId: virtualNetworkId != null
          ? virtualNetworkId()
          : this.virtualNetworkId,
      virtualNetworkName: virtualNetworkName != null
          ? virtualNetworkName()
          : this.virtualNetworkName,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is TunnelTeamnet &&
            comment == other.comment &&
            createdAt == other.createdAt &&
            deletedAt == other.deletedAt &&
            id == other.id &&
            network == other.network &&
            tunType == other.tunType &&
            tunnelId == other.tunnelId &&
            tunnelName == other.tunnelName &&
            virtualNetworkId == other.virtualNetworkId &&
            virtualNetworkName == other.virtualNetworkName;
  }

  @override
  int get hashCode {
    return Object.hash(
      comment,
      createdAt,
      deletedAt,
      id,
      network,
      tunType,
      tunnelId,
      tunnelName,
      virtualNetworkId,
      virtualNetworkName,
    );
  }

  @override
  String toString() {
    return 'TunnelTeamnet(comment: $comment, createdAt: $createdAt, deletedAt: $deletedAt, id: $id, network: $network, tunType: $tunType, tunnelId: $tunnelId, tunnelName: $tunnelName, virtualNetworkId: $virtualNetworkId, virtualNetworkName: $virtualNetworkName)';
  }
}
