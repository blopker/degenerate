//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class TeamsDevicesIpProfileCreateRequest {
  const TeamsDevicesIpProfileCreateRequest({
    this.description,
    this.enabled = true,
    required this.match,
    required this.name,
    required this.precedence,
    required this.subnetId,
  });

  factory TeamsDevicesIpProfileCreateRequest.fromJson(
    Map<String, dynamic> json,
  ) {
    return TeamsDevicesIpProfileCreateRequest(
      description: json['description'] as String?,
      enabled: json.containsKey('enabled') ? json['enabled'] as bool : true,
      match: json['match'] as String,
      name: json['name'] as String,
      precedence: (json['precedence'] as num).toInt(),
      subnetId: json['subnet_id'] as String,
    );
  }

  /// An optional description of the Device IP profile.
  final String? description;

  /// Whether the Device IP profile will be applied to matching devices.
  final bool enabled;

  final String match;

  final String name;

  final int precedence;

  final String subnetId;

  Map<String, dynamic> toJson() {
    return {
      'description': ?description,
      'enabled': enabled,
      'match': match,
      'name': name,
      'precedence': precedence,
      'subnet_id': subnetId,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('match') &&
        json['match'] is String &&
        json.containsKey('name') &&
        json['name'] is String &&
        json.containsKey('precedence') &&
        json['precedence'] is num &&
        json.containsKey('subnet_id') &&
        json['subnet_id'] is String;
  }

  TeamsDevicesIpProfileCreateRequest copyWith({
    String? Function()? description,
    bool Function()? enabled,
    String? match,
    String? name,
    int? precedence,
    String? subnetId,
  }) {
    return TeamsDevicesIpProfileCreateRequest(
      description: description != null ? description() : this.description,
      enabled: enabled != null ? enabled() : this.enabled,
      match: match ?? this.match,
      name: name ?? this.name,
      precedence: precedence ?? this.precedence,
      subnetId: subnetId ?? this.subnetId,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is TeamsDevicesIpProfileCreateRequest &&
            description == other.description &&
            enabled == other.enabled &&
            match == other.match &&
            name == other.name &&
            precedence == other.precedence &&
            subnetId == other.subnetId;
  }

  @override
  int get hashCode {
    return Object.hash(description, enabled, match, name, precedence, subnetId);
  }

  @override
  String toString() {
    return 'TeamsDevicesIpProfileCreateRequest(description: $description, enabled: $enabled, match: $match, name: $name, precedence: $precedence, subnetId: $subnetId)';
  }
}
