//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'access_allowed_methods2.dart';

final class AccessSchemasCorsHeaders {
  const AccessSchemasCorsHeaders({
    this.allowAllHeaders,
    this.allowAllMethods,
    this.allowAllOrigins,
    this.allowCredentials,
    this.allowedHeaders,
    this.allowedMethods,
    this.allowedOrigins,
    this.maxAge,
  });

  factory AccessSchemasCorsHeaders.fromJson(Map<String, dynamic> json) {
    return AccessSchemasCorsHeaders(
      allowAllHeaders: json['allow_all_headers'] as bool?,
      allowAllMethods: json['allow_all_methods'] as bool?,
      allowAllOrigins: json['allow_all_origins'] as bool?,
      allowCredentials: json['allow_credentials'] as bool?,
      allowedHeaders: (json['allowed_headers'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      allowedMethods: (json['allowed_methods'] as List<dynamic>?)
          ?.map((e) => AccessAllowedMethods2.fromJson(e as String))
          .toList(),
      allowedOrigins: (json['allowed_origins'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      maxAge: json['max_age'] != null
          ? (json['max_age'] as num).toDouble()
          : null,
    );
  }

  final bool? allowAllHeaders;

  final bool? allowAllMethods;

  final bool? allowAllOrigins;

  final bool? allowCredentials;

  final List<String>? allowedHeaders;

  final List<AccessAllowedMethods2>? allowedMethods;

  final List<String>? allowedOrigins;

  final double? maxAge;

  Map<String, dynamic> toJson() {
    return {
      'allow_all_headers': ?allowAllHeaders,
      'allow_all_methods': ?allowAllMethods,
      'allow_all_origins': ?allowAllOrigins,
      'allow_credentials': ?allowCredentials,
      'allowed_headers': ?allowedHeaders,
      if (allowedMethods != null)
        'allowed_methods': allowedMethods?.map((e) => e.toJson()).toList(),
      'allowed_origins': ?allowedOrigins,
      'max_age': ?maxAge,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  AccessSchemasCorsHeaders copyWith({
    bool Function()? allowAllHeaders,
    bool Function()? allowAllMethods,
    bool Function()? allowAllOrigins,
    bool Function()? allowCredentials,
    List<String> Function()? allowedHeaders,
    List<AccessAllowedMethods2> Function()? allowedMethods,
    List<String> Function()? allowedOrigins,
    double Function()? maxAge,
  }) {
    return AccessSchemasCorsHeaders(
      allowAllHeaders: allowAllHeaders != null
          ? allowAllHeaders()
          : this.allowAllHeaders,
      allowAllMethods: allowAllMethods != null
          ? allowAllMethods()
          : this.allowAllMethods,
      allowAllOrigins: allowAllOrigins != null
          ? allowAllOrigins()
          : this.allowAllOrigins,
      allowCredentials: allowCredentials != null
          ? allowCredentials()
          : this.allowCredentials,
      allowedHeaders: allowedHeaders != null
          ? allowedHeaders()
          : this.allowedHeaders,
      allowedMethods: allowedMethods != null
          ? allowedMethods()
          : this.allowedMethods,
      allowedOrigins: allowedOrigins != null
          ? allowedOrigins()
          : this.allowedOrigins,
      maxAge: maxAge != null ? maxAge() : this.maxAge,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is AccessSchemasCorsHeaders &&
            allowAllHeaders == other.allowAllHeaders &&
            allowAllMethods == other.allowAllMethods &&
            allowAllOrigins == other.allowAllOrigins &&
            allowCredentials == other.allowCredentials &&
            const ListEquality<dynamic>().equals(
              allowedHeaders,
              other.allowedHeaders,
            ) &&
            const ListEquality<dynamic>().equals(
              allowedMethods,
              other.allowedMethods,
            ) &&
            const ListEquality<dynamic>().equals(
              allowedOrigins,
              other.allowedOrigins,
            ) &&
            maxAge == other.maxAge;
  }

  @override
  int get hashCode {
    return Object.hash(
      allowAllHeaders,
      allowAllMethods,
      allowAllOrigins,
      allowCredentials,
      Object.hashAll(allowedHeaders ?? const []),
      Object.hashAll(allowedMethods ?? const []),
      Object.hashAll(allowedOrigins ?? const []),
      maxAge,
    );
  }

  @override
  String toString() {
    return 'AccessSchemasCorsHeaders(allowAllHeaders: $allowAllHeaders, allowAllMethods: $allowAllMethods, allowAllOrigins: $allowAllOrigins, allowCredentials: $allowCredentials, allowedHeaders: $allowedHeaders, allowedMethods: $allowedMethods, allowedOrigins: $allowedOrigins, maxAge: $maxAge)';
  }
}
