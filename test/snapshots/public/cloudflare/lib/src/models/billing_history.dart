//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'schemas_zone.dart';

final class BillingHistory {
  const BillingHistory({
    required this.action,
    required this.amount,
    required this.currency,
    required this.description,
    required this.id,
    required this.occurredAt,
    required this.type,
    required this.zone,
  });

  factory BillingHistory.fromJson(Map<String, dynamic> json) {
    return BillingHistory(
      action: json['action'] as String,
      amount: (json['amount'] as num).toDouble(),
      currency: json['currency'] as String,
      description: json['description'] as String,
      id: json['id'] as String,
      occurredAt: DateTime.parse(json['occurred_at'] as String),
      type: json['type'] as String,
      zone: SchemasZone.fromJson(json['zone'] as Map<String, dynamic>),
    );
  }

  final String action;

  final double amount;

  final String currency;

  final String description;

  final String id;

  final DateTime occurredAt;

  final String type;

  final SchemasZone zone;

  Map<String, dynamic> toJson() {
    return {
      'action': action,
      'amount': amount,
      'currency': currency,
      'description': description,
      'id': id,
      'occurred_at': occurredAt.toIso8601String(),
      'type': type,
      'zone': zone.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('action') &&
        json['action'] is String &&
        json.containsKey('amount') &&
        json['amount'] is num &&
        json.containsKey('currency') &&
        json['currency'] is String &&
        json.containsKey('description') &&
        json['description'] is String &&
        json.containsKey('id') &&
        json['id'] is String &&
        json.containsKey('occurred_at') &&
        json['occurred_at'] is String &&
        json.containsKey('type') &&
        json['type'] is String &&
        json.containsKey('zone');
  }

  BillingHistory copyWith({
    String? action,
    double? amount,
    String? currency,
    String? description,
    String? id,
    DateTime? occurredAt,
    String? type,
    SchemasZone? zone,
  }) {
    return BillingHistory(
      action: action ?? this.action,
      amount: amount ?? this.amount,
      currency: currency ?? this.currency,
      description: description ?? this.description,
      id: id ?? this.id,
      occurredAt: occurredAt ?? this.occurredAt,
      type: type ?? this.type,
      zone: zone ?? this.zone,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is BillingHistory &&
            action == other.action &&
            amount == other.amount &&
            currency == other.currency &&
            description == other.description &&
            id == other.id &&
            occurredAt == other.occurredAt &&
            type == other.type &&
            zone == other.zone;
  }

  @override
  int get hashCode {
    return Object.hash(
      action,
      amount,
      currency,
      description,
      id,
      occurredAt,
      type,
      zone,
    );
  }

  @override
  String toString() {
    return 'BillingHistory(action: $action, amount: $amount, currency: $currency, description: $description, id: $id, occurredAt: $occurredAt, type: $type, zone: $zone)';
  }
}
