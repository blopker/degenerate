//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';

final class StreamVideoUpdate {
  const StreamVideoUpdate({
    this.allowedOrigins,
    this.creator,
    this.maxDurationSeconds,
    this.meta,
    this.requireSignedUrLs,
    this.scheduledDeletion,
    this.thumbnailTimestampPct,
    this.uploadExpiry,
  });

  factory StreamVideoUpdate.fromJson(Map<String, dynamic> json) {
    return StreamVideoUpdate(
      allowedOrigins: (json['allowedOrigins'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      creator: json['creator'] as String?,
      maxDurationSeconds: json['maxDurationSeconds'] != null
          ? (json['maxDurationSeconds'] as num).toInt()
          : null,
      meta: (json['meta'] as Map<String, dynamic>?)?.map(
        (k, v) => MapEntry(k, v as String),
      ),
      requireSignedUrLs: json['requireSignedURLs'] as bool?,
      scheduledDeletion: json['scheduledDeletion'] != null
          ? DateTime.parse(json['scheduledDeletion'] as String)
          : null,
      thumbnailTimestampPct: json['thumbnailTimestampPct'] != null
          ? (json['thumbnailTimestampPct'] as num).toDouble()
          : null,
      uploadExpiry: json['uploadExpiry'] != null
          ? DateTime.parse(json['uploadExpiry'] as String)
          : null,
    );
  }

  final List<String>? allowedOrigins;

  final String? creator;

  final int? maxDurationSeconds;

  final Map<String, String>? meta;

  final bool? requireSignedUrLs;

  final DateTime? scheduledDeletion;

  final double? thumbnailTimestampPct;

  final DateTime? uploadExpiry;

  Map<String, dynamic> toJson() {
    return {
      'allowedOrigins': ?allowedOrigins,
      'creator': ?creator,
      'maxDurationSeconds': ?maxDurationSeconds,
      'meta': ?meta,
      'requireSignedURLs': ?requireSignedUrLs,
      if (scheduledDeletion != null)
        'scheduledDeletion': scheduledDeletion?.toIso8601String(),
      'thumbnailTimestampPct': ?thumbnailTimestampPct,
      if (uploadExpiry != null) 'uploadExpiry': uploadExpiry?.toIso8601String(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  StreamVideoUpdate copyWith({
    List<String> Function()? allowedOrigins,
    String Function()? creator,
    int Function()? maxDurationSeconds,
    Map<String, String> Function()? meta,
    bool Function()? requireSignedUrLs,
    DateTime Function()? scheduledDeletion,
    double Function()? thumbnailTimestampPct,
    DateTime Function()? uploadExpiry,
  }) {
    return StreamVideoUpdate(
      allowedOrigins: allowedOrigins != null
          ? allowedOrigins()
          : this.allowedOrigins,
      creator: creator != null ? creator() : this.creator,
      maxDurationSeconds: maxDurationSeconds != null
          ? maxDurationSeconds()
          : this.maxDurationSeconds,
      meta: meta != null ? meta() : this.meta,
      requireSignedUrLs: requireSignedUrLs != null
          ? requireSignedUrLs()
          : this.requireSignedUrLs,
      scheduledDeletion: scheduledDeletion != null
          ? scheduledDeletion()
          : this.scheduledDeletion,
      thumbnailTimestampPct: thumbnailTimestampPct != null
          ? thumbnailTimestampPct()
          : this.thumbnailTimestampPct,
      uploadExpiry: uploadExpiry != null ? uploadExpiry() : this.uploadExpiry,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is StreamVideoUpdate &&
            const ListEquality<dynamic>().equals(
              allowedOrigins,
              other.allowedOrigins,
            ) &&
            creator == other.creator &&
            maxDurationSeconds == other.maxDurationSeconds &&
            meta == other.meta &&
            requireSignedUrLs == other.requireSignedUrLs &&
            scheduledDeletion == other.scheduledDeletion &&
            thumbnailTimestampPct == other.thumbnailTimestampPct &&
            uploadExpiry == other.uploadExpiry;
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(allowedOrigins ?? const []),
      creator,
      maxDurationSeconds,
      meta,
      requireSignedUrLs,
      scheduledDeletion,
      thumbnailTimestampPct,
      uploadExpiry,
    );
  }

  @override
  String toString() {
    return 'StreamVideoUpdate(allowedOrigins: $allowedOrigins, creator: $creator, maxDurationSeconds: $maxDurationSeconds, meta: $meta, requireSignedUrLs: $requireSignedUrLs, scheduledDeletion: $scheduledDeletion, thumbnailTimestampPct: $thumbnailTimestampPct, uploadExpiry: $uploadExpiry)';
  }
}
