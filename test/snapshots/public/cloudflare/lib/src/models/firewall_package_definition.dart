//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'firewall_detection_mode.dart';
import 'firewall_status.dart';

final class FirewallPackageDefinition {
  const FirewallPackageDefinition({
    required this.description,
    required this.detectionMode,
    required this.id,
    required this.name,
    this.status,
    required this.zoneId,
  });

  factory FirewallPackageDefinition.fromJson(Map<String, dynamic> json) {
    return FirewallPackageDefinition(
      description: json['description'] as String,
      detectionMode: FirewallDetectionMode.fromJson(
        json['detection_mode'] as String,
      ),
      id: json['id'] as String,
      name: json['name'] as String,
      status: json['status'] != null
          ? FirewallStatus.fromJson(json['status'] as String)
          : null,
      zoneId: json['zone_id'] as String,
    );
  }

  final String description;

  final FirewallDetectionMode detectionMode;

  final String id;

  final String name;

  final FirewallStatus? status;

  final String zoneId;

  Map<String, dynamic> toJson() {
    return {
      'description': description,
      'detection_mode': detectionMode.toJson(),
      'id': id,
      'name': name,
      if (status != null) 'status': status?.toJson(),
      'zone_id': zoneId,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('description') &&
        json['description'] is String &&
        json.containsKey('detection_mode') &&
        json.containsKey('id') &&
        json['id'] is String &&
        json.containsKey('name') &&
        json['name'] is String &&
        json.containsKey('zone_id') &&
        json['zone_id'] is String;
  }

  FirewallPackageDefinition copyWith({
    String? description,
    FirewallDetectionMode? detectionMode,
    String? id,
    String? name,
    FirewallStatus Function()? status,
    String? zoneId,
  }) {
    return FirewallPackageDefinition(
      description: description ?? this.description,
      detectionMode: detectionMode ?? this.detectionMode,
      id: id ?? this.id,
      name: name ?? this.name,
      status: status != null ? status() : this.status,
      zoneId: zoneId ?? this.zoneId,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is FirewallPackageDefinition &&
            description == other.description &&
            detectionMode == other.detectionMode &&
            id == other.id &&
            name == other.name &&
            status == other.status &&
            zoneId == other.zoneId;
  }

  @override
  int get hashCode {
    return Object.hash(description, detectionMode, id, name, status, zoneId);
  }

  @override
  String toString() {
    return 'FirewallPackageDefinition(description: $description, detectionMode: $detectionMode, id: $id, name: $name, status: $status, zoneId: $zoneId)';
  }
}
