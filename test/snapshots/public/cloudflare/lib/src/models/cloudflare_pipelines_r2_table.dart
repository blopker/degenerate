//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'cloudflare_pipelines_r2_table_credentials.dart';
import 'cloudflare_pipelines_r2_table_file_naming.dart';
import 'cloudflare_pipelines_r2_table_partitioning.dart';
import 'cloudflare_pipelines_r2_table_rolling_policy.dart';

final class CloudflarePipelinesR2Table {
  const CloudflarePipelinesR2Table({
    required this.accountId,
    required this.bucket,
    required this.credentials,
    this.fileNaming,
    this.jurisdiction,
    this.partitioning,
    this.path,
    this.rollingPolicy,
  });

  factory CloudflarePipelinesR2Table.fromJson(Map<String, dynamic> json) {
    return CloudflarePipelinesR2Table(
      accountId: json['account_id'] as String,
      bucket: json['bucket'] as String,
      credentials: CloudflarePipelinesR2TableCredentials.fromJson(
        json['credentials'] as Map<String, dynamic>,
      ),
      fileNaming: json['file_naming'] != null
          ? CloudflarePipelinesR2TableFileNaming.fromJson(
              json['file_naming'] as Map<String, dynamic>,
            )
          : null,
      jurisdiction: json['jurisdiction'] as String?,
      partitioning: json['partitioning'] != null
          ? CloudflarePipelinesR2TablePartitioning.fromJson(
              json['partitioning'] as Map<String, dynamic>,
            )
          : null,
      path: json['path'] as String?,
      rollingPolicy: json['rolling_policy'] != null
          ? CloudflarePipelinesR2TableRollingPolicy.fromJson(
              json['rolling_policy'] as Map<String, dynamic>,
            )
          : null,
    );
  }

  /// Cloudflare Account ID for the bucket
  final String accountId;

  /// R2 Bucket to write to
  final String bucket;

  final CloudflarePipelinesR2TableCredentials credentials;

  /// Controls filename prefix/suffix and strategy.
  final CloudflarePipelinesR2TableFileNaming? fileNaming;

  /// Jurisdiction this bucket is hosted in
  final String? jurisdiction;

  /// Data-layout partitioning for sinks.
  final CloudflarePipelinesR2TablePartitioning? partitioning;

  /// Subpath within the bucket to write to
  final String? path;

  /// Rolling policy for file sinks (when & why to close a file and open a new one).
  final CloudflarePipelinesR2TableRollingPolicy? rollingPolicy;

  Map<String, dynamic> toJson() {
    return {
      'account_id': accountId,
      'bucket': bucket,
      'credentials': credentials.toJson(),
      if (fileNaming != null) 'file_naming': fileNaming?.toJson(),
      'jurisdiction': ?jurisdiction,
      if (partitioning != null) 'partitioning': partitioning?.toJson(),
      'path': ?path,
      if (rollingPolicy != null) 'rolling_policy': rollingPolicy?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('account_id') &&
        json['account_id'] is String &&
        json.containsKey('bucket') &&
        json['bucket'] is String &&
        json.containsKey('credentials');
  }

  CloudflarePipelinesR2Table copyWith({
    String? accountId,
    String? bucket,
    CloudflarePipelinesR2TableCredentials? credentials,
    CloudflarePipelinesR2TableFileNaming Function()? fileNaming,
    String Function()? jurisdiction,
    CloudflarePipelinesR2TablePartitioning Function()? partitioning,
    String Function()? path,
    CloudflarePipelinesR2TableRollingPolicy Function()? rollingPolicy,
  }) {
    return CloudflarePipelinesR2Table(
      accountId: accountId ?? this.accountId,
      bucket: bucket ?? this.bucket,
      credentials: credentials ?? this.credentials,
      fileNaming: fileNaming != null ? fileNaming() : this.fileNaming,
      jurisdiction: jurisdiction != null ? jurisdiction() : this.jurisdiction,
      partitioning: partitioning != null ? partitioning() : this.partitioning,
      path: path != null ? path() : this.path,
      rollingPolicy: rollingPolicy != null
          ? rollingPolicy()
          : this.rollingPolicy,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is CloudflarePipelinesR2Table &&
            accountId == other.accountId &&
            bucket == other.bucket &&
            credentials == other.credentials &&
            fileNaming == other.fileNaming &&
            jurisdiction == other.jurisdiction &&
            partitioning == other.partitioning &&
            path == other.path &&
            rollingPolicy == other.rollingPolicy;
  }

  @override
  int get hashCode {
    return Object.hash(
      accountId,
      bucket,
      credentials,
      fileNaming,
      jurisdiction,
      partitioning,
      path,
      rollingPolicy,
    );
  }

  @override
  String toString() {
    return 'CloudflarePipelinesR2Table(accountId: $accountId, bucket: $bucket, credentials: $credentials, fileNaming: $fileNaming, jurisdiction: $jurisdiction, partitioning: $partitioning, path: $path, rollingPolicy: $rollingPolicy)';
  }
}
