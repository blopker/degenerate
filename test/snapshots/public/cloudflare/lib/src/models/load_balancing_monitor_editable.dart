//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'load_balancing_type.dart';

final class LoadBalancingMonitorEditable {
  const LoadBalancingMonitorEditable({
    this.allowInsecure,
    this.consecutiveDown,
    this.consecutiveUp,
    this.description,
    this.expectedBody,
    this.expectedCodes,
    this.followRedirects,
    this.header,
    this.interval,
    this.method,
    this.path,
    this.port,
    this.probeZone,
    this.retries,
    this.timeout,
    this.type,
  });

  factory LoadBalancingMonitorEditable.fromJson(Map<String, dynamic> json) {
    return LoadBalancingMonitorEditable(
      allowInsecure: json['allow_insecure'] as bool?,
      consecutiveDown: json['consecutive_down'] != null
          ? (json['consecutive_down'] as num).toInt()
          : null,
      consecutiveUp: json['consecutive_up'] != null
          ? (json['consecutive_up'] as num).toInt()
          : null,
      description: json['description'] as String?,
      expectedBody: json['expected_body'] as String?,
      expectedCodes: json['expected_codes'] as String?,
      followRedirects: json['follow_redirects'] as bool?,
      header: (json['header'] as Map<String, dynamic>?)?.map(
        (k, v) =>
            MapEntry(k, (v as List<dynamic>).map((e) => e as String).toList()),
      ),
      interval: json['interval'] != null
          ? (json['interval'] as num).toInt()
          : null,
      method: json['method'] as String?,
      path: json['path'] as String?,
      port: json['port'] != null ? (json['port'] as num).toInt() : null,
      probeZone: json['probe_zone'] as String?,
      retries: json['retries'] != null
          ? (json['retries'] as num).toInt()
          : null,
      timeout: json['timeout'] != null
          ? (json['timeout'] as num).toInt()
          : null,
      type: json['type'] != null
          ? LoadBalancingType.fromJson(json['type'] as String)
          : null,
    );
  }

  final bool? allowInsecure;

  final int? consecutiveDown;

  final int? consecutiveUp;

  final String? description;

  final String? expectedBody;

  final String? expectedCodes;

  final bool? followRedirects;

  final Map<String, List<String>>? header;

  final int? interval;

  final String? method;

  final String? path;

  final int? port;

  final String? probeZone;

  final int? retries;

  final int? timeout;

  final LoadBalancingType? type;

  Map<String, dynamic> toJson() {
    return {
      'allow_insecure': ?allowInsecure,
      'consecutive_down': ?consecutiveDown,
      'consecutive_up': ?consecutiveUp,
      'description': ?description,
      'expected_body': ?expectedBody,
      'expected_codes': ?expectedCodes,
      'follow_redirects': ?followRedirects,
      if (header != null) 'header': header?.map((k, v) => MapEntry(k, v)),
      'interval': ?interval,
      'method': ?method,
      'path': ?path,
      'port': ?port,
      'probe_zone': ?probeZone,
      'retries': ?retries,
      'timeout': ?timeout,
      if (type != null) 'type': type?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  LoadBalancingMonitorEditable copyWith({
    bool Function()? allowInsecure,
    int Function()? consecutiveDown,
    int Function()? consecutiveUp,
    String Function()? description,
    String Function()? expectedBody,
    String Function()? expectedCodes,
    bool Function()? followRedirects,
    Map<String, List<String>> Function()? header,
    int Function()? interval,
    String Function()? method,
    String Function()? path,
    int? Function()? port,
    String Function()? probeZone,
    int Function()? retries,
    int Function()? timeout,
    LoadBalancingType Function()? type,
  }) {
    return LoadBalancingMonitorEditable(
      allowInsecure: allowInsecure != null
          ? allowInsecure()
          : this.allowInsecure,
      consecutiveDown: consecutiveDown != null
          ? consecutiveDown()
          : this.consecutiveDown,
      consecutiveUp: consecutiveUp != null
          ? consecutiveUp()
          : this.consecutiveUp,
      description: description != null ? description() : this.description,
      expectedBody: expectedBody != null ? expectedBody() : this.expectedBody,
      expectedCodes: expectedCodes != null
          ? expectedCodes()
          : this.expectedCodes,
      followRedirects: followRedirects != null
          ? followRedirects()
          : this.followRedirects,
      header: header != null ? header() : this.header,
      interval: interval != null ? interval() : this.interval,
      method: method != null ? method() : this.method,
      path: path != null ? path() : this.path,
      port: port != null ? port() : this.port,
      probeZone: probeZone != null ? probeZone() : this.probeZone,
      retries: retries != null ? retries() : this.retries,
      timeout: timeout != null ? timeout() : this.timeout,
      type: type != null ? type() : this.type,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is LoadBalancingMonitorEditable &&
            allowInsecure == other.allowInsecure &&
            consecutiveDown == other.consecutiveDown &&
            consecutiveUp == other.consecutiveUp &&
            description == other.description &&
            expectedBody == other.expectedBody &&
            expectedCodes == other.expectedCodes &&
            followRedirects == other.followRedirects &&
            header == other.header &&
            interval == other.interval &&
            method == other.method &&
            path == other.path &&
            port == other.port &&
            probeZone == other.probeZone &&
            retries == other.retries &&
            timeout == other.timeout &&
            type == other.type;
  }

  @override
  int get hashCode {
    return Object.hash(
      allowInsecure,
      consecutiveDown,
      consecutiveUp,
      description,
      expectedBody,
      expectedCodes,
      followRedirects,
      header,
      interval,
      method,
      path,
      port,
      probeZone,
      retries,
      timeout,
      type,
    );
  }

  @override
  String toString() {
    return 'LoadBalancingMonitorEditable(allowInsecure: $allowInsecure, consecutiveDown: $consecutiveDown, consecutiveUp: $consecutiveUp, description: $description, expectedBody: $expectedBody, expectedCodes: $expectedCodes, followRedirects: $followRedirects, header: $header, interval: $interval, method: $method, path: $path, port: $port, probeZone: $probeZone, retries: $retries, timeout: $timeout, type: $type)';
  }
}
