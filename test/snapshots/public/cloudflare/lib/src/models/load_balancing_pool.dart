//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'load_balancing_check_regions2.dart';
import 'load_balancing_load_shedding.dart';
import 'load_balancing_notification_filter.dart';
import 'load_balancing_origin.dart';
import 'load_balancing_origin_steering.dart';

final class LoadBalancingPool {
  const LoadBalancingPool({
    this.checkRegions,
    this.createdOn,
    this.description,
    this.disabledAt,
    this.enabled,
    this.id,
    this.latitude,
    this.loadShedding,
    this.longitude,
    this.minimumOrigins,
    this.modifiedOn,
    this.monitor,
    this.monitorGroup,
    this.name,
    this.networks,
    this.notificationEmail,
    this.notificationFilter,
    this.originSteering,
    this.origins,
  });

  factory LoadBalancingPool.fromJson(Map<String, dynamic> json) {
    return LoadBalancingPool(
      checkRegions: (json['check_regions'] as List<dynamic>?)
          ?.map((e) => LoadBalancingCheckRegions2.fromJson(e as String))
          .toList(),
      createdOn: json['created_on'] != null
          ? DateTime.parse(json['created_on'] as String)
          : null,
      description: json['description'] as String?,
      disabledAt: json['disabled_at'] != null
          ? DateTime.parse(json['disabled_at'] as String)
          : null,
      enabled: json['enabled'] as bool?,
      id: json['id'] as String?,
      latitude: json['latitude'] != null
          ? (json['latitude'] as num).toDouble()
          : null,
      loadShedding: json['load_shedding'] != null
          ? LoadBalancingLoadShedding.fromJson(
              json['load_shedding'] as Map<String, dynamic>,
            )
          : null,
      longitude: json['longitude'] != null
          ? (json['longitude'] as num).toDouble()
          : null,
      minimumOrigins: json['minimum_origins'] != null
          ? (json['minimum_origins'] as num).toInt()
          : null,
      modifiedOn: json['modified_on'] != null
          ? DateTime.parse(json['modified_on'] as String)
          : null,
      monitor: json['monitor'] as String?,
      monitorGroup: json['monitor_group'] as String?,
      name: json['name'] as String?,
      networks: (json['networks'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      notificationEmail: json['notification_email'] as String?,
      notificationFilter: json['notification_filter'] != null
          ? LoadBalancingNotificationFilter.fromJson(
              json['notification_filter'] as Map<String, dynamic>,
            )
          : null,
      originSteering: json['origin_steering'] != null
          ? LoadBalancingOriginSteering.fromJson(
              json['origin_steering'] as Map<String, dynamic>,
            )
          : null,
      origins: (json['origins'] as List<dynamic>?)
          ?.map((e) => LoadBalancingOrigin.fromJson(e as Map<String, dynamic>))
          .toList(),
    );
  }

  final List<LoadBalancingCheckRegions2>? checkRegions;

  final DateTime? createdOn;

  final String? description;

  final DateTime? disabledAt;

  final bool? enabled;

  final String? id;

  final double? latitude;

  final LoadBalancingLoadShedding? loadShedding;

  final double? longitude;

  final int? minimumOrigins;

  final DateTime? modifiedOn;

  final String? monitor;

  final String? monitorGroup;

  final String? name;

  final List<String>? networks;

  final String? notificationEmail;

  final LoadBalancingNotificationFilter? notificationFilter;

  final LoadBalancingOriginSteering? originSteering;

  final List<LoadBalancingOrigin>? origins;

  Map<String, dynamic> toJson() {
    return {
      if (checkRegions != null)
        'check_regions': checkRegions?.map((e) => e.toJson()).toList(),
      if (createdOn != null) 'created_on': createdOn?.toIso8601String(),
      'description': ?description,
      if (disabledAt != null) 'disabled_at': disabledAt?.toIso8601String(),
      'enabled': ?enabled,
      'id': ?id,
      'latitude': ?latitude,
      if (loadShedding != null) 'load_shedding': loadShedding?.toJson(),
      'longitude': ?longitude,
      'minimum_origins': ?minimumOrigins,
      if (modifiedOn != null) 'modified_on': modifiedOn?.toIso8601String(),
      'monitor': ?monitor,
      'monitor_group': ?monitorGroup,
      'name': ?name,
      'networks': ?networks,
      'notification_email': ?notificationEmail,
      if (notificationFilter != null)
        'notification_filter': notificationFilter?.toJson(),
      if (originSteering != null) 'origin_steering': originSteering?.toJson(),
      if (origins != null) 'origins': origins?.map((e) => e.toJson()).toList(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  LoadBalancingPool copyWith({
    List<LoadBalancingCheckRegions2>? Function()? checkRegions,
    DateTime Function()? createdOn,
    String Function()? description,
    DateTime Function()? disabledAt,
    bool Function()? enabled,
    String Function()? id,
    double Function()? latitude,
    LoadBalancingLoadShedding Function()? loadShedding,
    double Function()? longitude,
    int Function()? minimumOrigins,
    DateTime Function()? modifiedOn,
    String Function()? monitor,
    String Function()? monitorGroup,
    String Function()? name,
    List<String> Function()? networks,
    String Function()? notificationEmail,
    LoadBalancingNotificationFilter Function()? notificationFilter,
    LoadBalancingOriginSteering Function()? originSteering,
    List<LoadBalancingOrigin> Function()? origins,
  }) {
    return LoadBalancingPool(
      checkRegions: checkRegions != null ? checkRegions() : this.checkRegions,
      createdOn: createdOn != null ? createdOn() : this.createdOn,
      description: description != null ? description() : this.description,
      disabledAt: disabledAt != null ? disabledAt() : this.disabledAt,
      enabled: enabled != null ? enabled() : this.enabled,
      id: id != null ? id() : this.id,
      latitude: latitude != null ? latitude() : this.latitude,
      loadShedding: loadShedding != null ? loadShedding() : this.loadShedding,
      longitude: longitude != null ? longitude() : this.longitude,
      minimumOrigins: minimumOrigins != null
          ? minimumOrigins()
          : this.minimumOrigins,
      modifiedOn: modifiedOn != null ? modifiedOn() : this.modifiedOn,
      monitor: monitor != null ? monitor() : this.monitor,
      monitorGroup: monitorGroup != null ? monitorGroup() : this.monitorGroup,
      name: name != null ? name() : this.name,
      networks: networks != null ? networks() : this.networks,
      notificationEmail: notificationEmail != null
          ? notificationEmail()
          : this.notificationEmail,
      notificationFilter: notificationFilter != null
          ? notificationFilter()
          : this.notificationFilter,
      originSteering: originSteering != null
          ? originSteering()
          : this.originSteering,
      origins: origins != null ? origins() : this.origins,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is LoadBalancingPool &&
            const ListEquality<dynamic>().equals(
              checkRegions,
              other.checkRegions,
            ) &&
            createdOn == other.createdOn &&
            description == other.description &&
            disabledAt == other.disabledAt &&
            enabled == other.enabled &&
            id == other.id &&
            latitude == other.latitude &&
            loadShedding == other.loadShedding &&
            longitude == other.longitude &&
            minimumOrigins == other.minimumOrigins &&
            modifiedOn == other.modifiedOn &&
            monitor == other.monitor &&
            monitorGroup == other.monitorGroup &&
            name == other.name &&
            const ListEquality<dynamic>().equals(networks, other.networks) &&
            notificationEmail == other.notificationEmail &&
            notificationFilter == other.notificationFilter &&
            originSteering == other.originSteering &&
            const ListEquality<dynamic>().equals(origins, other.origins);
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(checkRegions ?? const []),
      createdOn,
      description,
      disabledAt,
      enabled,
      id,
      latitude,
      loadShedding,
      longitude,
      minimumOrigins,
      modifiedOn,
      monitor,
      monitorGroup,
      name,
      Object.hashAll(networks ?? const []),
      notificationEmail,
      notificationFilter,
      originSteering,
      Object.hashAll(origins ?? const []),
    );
  }

  @override
  String toString() {
    return 'LoadBalancingPool(checkRegions: $checkRegions, createdOn: $createdOn, description: $description, disabledAt: $disabledAt, enabled: $enabled, id: $id, latitude: $latitude, loadShedding: $loadShedding, longitude: $longitude, minimumOrigins: $minimumOrigins, modifiedOn: $modifiedOn, monitor: $monitor, monitorGroup: $monitorGroup, name: $name, networks: $networks, notificationEmail: $notificationEmail, notificationFilter: $notificationFilter, originSteering: $originSteering, origins: $origins)';
  }
}
