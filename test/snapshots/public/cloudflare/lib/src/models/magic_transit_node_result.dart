//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';

final class MagicTransitNodeResult {
  const MagicTransitNodeResult({
    this.asn,
    this.ip,
    this.labels,
    this.maxRttMs,
    this.meanRttMs,
    this.minRttMs,
    this.name,
    this.packetCount,
    this.stdDevRttMs,
  });

  factory MagicTransitNodeResult.fromJson(Map<String, dynamic> json) {
    return MagicTransitNodeResult(
      asn: json['asn'] as String?,
      ip: json['ip'] as String?,
      labels: (json['labels'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      maxRttMs: json['max_rtt_ms'] != null
          ? (json['max_rtt_ms'] as num).toDouble()
          : null,
      meanRttMs: json['mean_rtt_ms'] != null
          ? (json['mean_rtt_ms'] as num).toDouble()
          : null,
      minRttMs: json['min_rtt_ms'] != null
          ? (json['min_rtt_ms'] as num).toDouble()
          : null,
      name: json['name'] as String?,
      packetCount: json['packet_count'] != null
          ? (json['packet_count'] as num).toInt()
          : null,
      stdDevRttMs: json['std_dev_rtt_ms'] != null
          ? (json['std_dev_rtt_ms'] as num).toDouble()
          : null,
    );
  }

  final String? asn;

  final String? ip;

  final List<String>? labels;

  final double? maxRttMs;

  final double? meanRttMs;

  final double? minRttMs;

  final String? name;

  final int? packetCount;

  final double? stdDevRttMs;

  Map<String, dynamic> toJson() {
    return {
      'asn': ?asn,
      'ip': ?ip,
      'labels': ?labels,
      'max_rtt_ms': ?maxRttMs,
      'mean_rtt_ms': ?meanRttMs,
      'min_rtt_ms': ?minRttMs,
      'name': ?name,
      'packet_count': ?packetCount,
      'std_dev_rtt_ms': ?stdDevRttMs,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  MagicTransitNodeResult copyWith({
    String Function()? asn,
    String Function()? ip,
    List<String> Function()? labels,
    double Function()? maxRttMs,
    double Function()? meanRttMs,
    double Function()? minRttMs,
    String Function()? name,
    int Function()? packetCount,
    double Function()? stdDevRttMs,
  }) {
    return MagicTransitNodeResult(
      asn: asn != null ? asn() : this.asn,
      ip: ip != null ? ip() : this.ip,
      labels: labels != null ? labels() : this.labels,
      maxRttMs: maxRttMs != null ? maxRttMs() : this.maxRttMs,
      meanRttMs: meanRttMs != null ? meanRttMs() : this.meanRttMs,
      minRttMs: minRttMs != null ? minRttMs() : this.minRttMs,
      name: name != null ? name() : this.name,
      packetCount: packetCount != null ? packetCount() : this.packetCount,
      stdDevRttMs: stdDevRttMs != null ? stdDevRttMs() : this.stdDevRttMs,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is MagicTransitNodeResult &&
            asn == other.asn &&
            ip == other.ip &&
            const ListEquality<dynamic>().equals(labels, other.labels) &&
            maxRttMs == other.maxRttMs &&
            meanRttMs == other.meanRttMs &&
            minRttMs == other.minRttMs &&
            name == other.name &&
            packetCount == other.packetCount &&
            stdDevRttMs == other.stdDevRttMs;
  }

  @override
  int get hashCode {
    return Object.hash(
      asn,
      ip,
      Object.hashAll(labels ?? const []),
      maxRttMs,
      meanRttMs,
      minRttMs,
      name,
      packetCount,
      stdDevRttMs,
    );
  }

  @override
  String toString() {
    return 'MagicTransitNodeResult(asn: $asn, ip: $ip, labels: $labels, maxRttMs: $maxRttMs, meanRttMs: $meanRttMs, minRttMs: $minRttMs, name: $name, packetCount: $packetCount, stdDevRttMs: $stdDevRttMs)';
  }
}
