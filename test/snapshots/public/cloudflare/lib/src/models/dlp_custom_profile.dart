//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'dlp_confidence.dart';
import 'dlp_context_awareness.dart';
import 'dlp_entry.dart';

final class DlpCustomProfile {
  const DlpCustomProfile({
    this.aiContextEnabled = false,
    this.allowedMatchCount = 0,
    this.confidenceThreshold = DlpConfidence.low,
    this.contextAwareness,
    required this.createdAt,
    this.dataClasses,
    this.dataTags,
    this.description,
    this.entries,
    required this.id,
    required this.name,
    this.ocrEnabled = false,
    this.sensitivityLevels,
    this.sharedEntries,
    required this.updatedAt,
  });

  factory DlpCustomProfile.fromJson(Map<String, dynamic> json) {
    return DlpCustomProfile(
      aiContextEnabled: json.containsKey('ai_context_enabled')
          ? json['ai_context_enabled'] as bool
          : false,
      allowedMatchCount: (json['allowed_match_count'] as num).toInt(),
      confidenceThreshold: json.containsKey('confidence_threshold')
          ? DlpConfidence.fromJson(json['confidence_threshold'] as String)
          : DlpConfidence.low,
      contextAwareness: json['context_awareness'] != null
          ? DlpContextAwareness.fromJson(
              json['context_awareness'] as Map<String, dynamic>,
            )
          : null,
      createdAt: DateTime.parse(json['created_at'] as String),
      dataClasses: (json['data_classes'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      dataTags: (json['data_tags'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      description: json['description'] as String?,
      entries: (json['entries'] as List<dynamic>?)
          ?.map((e) => DlpEntry.fromJson(e as Map<String, dynamic>))
          .toList(),
      id: json['id'] as String,
      name: json['name'] as String,
      ocrEnabled: json['ocr_enabled'] as bool,
      sensitivityLevels: (json['sensitivity_levels'] as List<dynamic>?)
          ?.map((e) => (e as List<dynamic>).map((e) => e as String).toList())
          .toList(),
      sharedEntries: (json['shared_entries'] as List<dynamic>?)
          ?.map((e) => DlpEntry.fromJson(e as Map<String, dynamic>))
          .toList(),
      updatedAt: DateTime.parse(json['updated_at'] as String),
    );
  }

  final bool aiContextEnabled;

  /// Related DLP policies will trigger when the match count exceeds the number set.
  final int allowedMatchCount;

  final DlpConfidence confidenceThreshold;

  final DlpContextAwareness? contextAwareness;

  /// When the profile was created.
  final DateTime createdAt;

  /// Data classes associated with this profile.
  final List<String>? dataClasses;

  /// Data tags associated with this profile.
  final List<String>? dataTags;

  /// The description of the profile.
  final String? description;

  final List<DlpEntry>? entries;

  /// The id of the profile (uuid).
  final String id;

  /// The name of the profile.
  final String name;

  final bool ocrEnabled;

  /// Sensitivity levels associated with this profile as (group_id, level_id) tuples.
  final List<List<String>>? sensitivityLevels;

  final List<DlpEntry>? sharedEntries;

  /// When the profile was lasted updated.
  final DateTime updatedAt;

  Map<String, dynamic> toJson() {
    return {
      'ai_context_enabled': aiContextEnabled,
      'allowed_match_count': allowedMatchCount,
      'confidence_threshold': confidenceThreshold.toJson(),
      if (contextAwareness != null)
        'context_awareness': contextAwareness?.toJson(),
      'created_at': createdAt.toIso8601String(),
      'data_classes': ?dataClasses,
      'data_tags': ?dataTags,
      'description': ?description,
      if (entries != null) 'entries': entries?.map((e) => e.toJson()).toList(),
      'id': id,
      'name': name,
      'ocr_enabled': ocrEnabled,
      if (sensitivityLevels != null)
        'sensitivity_levels': sensitivityLevels?.map((e) => e).toList(),
      if (sharedEntries != null)
        'shared_entries': sharedEntries?.map((e) => e.toJson()).toList(),
      'updated_at': updatedAt.toIso8601String(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('allowed_match_count') &&
        json['allowed_match_count'] is num &&
        json.containsKey('created_at') &&
        json['created_at'] is String &&
        json.containsKey('id') &&
        json['id'] is String &&
        json.containsKey('name') &&
        json['name'] is String &&
        json.containsKey('ocr_enabled') &&
        json['ocr_enabled'] is bool &&
        json.containsKey('updated_at') &&
        json['updated_at'] is String;
  }

  DlpCustomProfile copyWith({
    bool Function()? aiContextEnabled,
    int? allowedMatchCount,
    DlpConfidence Function()? confidenceThreshold,
    DlpContextAwareness Function()? contextAwareness,
    DateTime? createdAt,
    List<String> Function()? dataClasses,
    List<String> Function()? dataTags,
    String? Function()? description,
    List<DlpEntry> Function()? entries,
    String? id,
    String? name,
    bool? ocrEnabled,
    List<List<String>> Function()? sensitivityLevels,
    List<DlpEntry> Function()? sharedEntries,
    DateTime? updatedAt,
  }) {
    return DlpCustomProfile(
      aiContextEnabled: aiContextEnabled != null
          ? aiContextEnabled()
          : this.aiContextEnabled,
      allowedMatchCount: allowedMatchCount ?? this.allowedMatchCount,
      confidenceThreshold: confidenceThreshold != null
          ? confidenceThreshold()
          : this.confidenceThreshold,
      contextAwareness: contextAwareness != null
          ? contextAwareness()
          : this.contextAwareness,
      createdAt: createdAt ?? this.createdAt,
      dataClasses: dataClasses != null ? dataClasses() : this.dataClasses,
      dataTags: dataTags != null ? dataTags() : this.dataTags,
      description: description != null ? description() : this.description,
      entries: entries != null ? entries() : this.entries,
      id: id ?? this.id,
      name: name ?? this.name,
      ocrEnabled: ocrEnabled ?? this.ocrEnabled,
      sensitivityLevels: sensitivityLevels != null
          ? sensitivityLevels()
          : this.sensitivityLevels,
      sharedEntries: sharedEntries != null
          ? sharedEntries()
          : this.sharedEntries,
      updatedAt: updatedAt ?? this.updatedAt,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is DlpCustomProfile &&
            aiContextEnabled == other.aiContextEnabled &&
            allowedMatchCount == other.allowedMatchCount &&
            confidenceThreshold == other.confidenceThreshold &&
            contextAwareness == other.contextAwareness &&
            createdAt == other.createdAt &&
            const ListEquality<dynamic>().equals(
              dataClasses,
              other.dataClasses,
            ) &&
            const ListEquality<dynamic>().equals(dataTags, other.dataTags) &&
            description == other.description &&
            const ListEquality<dynamic>().equals(entries, other.entries) &&
            id == other.id &&
            name == other.name &&
            ocrEnabled == other.ocrEnabled &&
            const ListEquality<dynamic>().equals(
              sensitivityLevels,
              other.sensitivityLevels,
            ) &&
            const ListEquality<dynamic>().equals(
              sharedEntries,
              other.sharedEntries,
            ) &&
            updatedAt == other.updatedAt;
  }

  @override
  int get hashCode {
    return Object.hash(
      aiContextEnabled,
      allowedMatchCount,
      confidenceThreshold,
      contextAwareness,
      createdAt,
      Object.hashAll(dataClasses ?? const []),
      Object.hashAll(dataTags ?? const []),
      description,
      Object.hashAll(entries ?? const []),
      id,
      name,
      ocrEnabled,
      Object.hashAll(sensitivityLevels ?? const []),
      Object.hashAll(sharedEntries ?? const []),
      updatedAt,
    );
  }

  @override
  String toString() {
    return 'DlpCustomProfile(aiContextEnabled: $aiContextEnabled, allowedMatchCount: $allowedMatchCount, confidenceThreshold: $confidenceThreshold, contextAwareness: $contextAwareness, createdAt: $createdAt, dataClasses: $dataClasses, dataTags: $dataTags, description: $description, entries: $entries, id: $id, name: $name, ocrEnabled: $ocrEnabled, sensitivityLevels: $sensitivityLevels, sharedEntries: $sharedEntries, updatedAt: $updatedAt)';
  }
}
