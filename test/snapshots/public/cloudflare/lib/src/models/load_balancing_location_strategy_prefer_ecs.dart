//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

/// Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as the authoritative location.
/// - `"always"`: Always prefer ECS.
/// - `"never"`: Never prefer ECS.
/// - `"proximity"`: Prefer ECS only when `steering_policy="proximity"`.
/// - `"geo"`: Prefer ECS only when `steering_policy="geo"`.
final class LoadBalancingLocationStrategyPreferEcs {
  const LoadBalancingLocationStrategyPreferEcs._(this.value);

  factory LoadBalancingLocationStrategyPreferEcs.fromJson(String json) {
    return switch (json) {
      'always' => always,
      'never' => never,
      'proximity' => proximity,
      'geo' => geo,
      _ => LoadBalancingLocationStrategyPreferEcs._(json),
    };
  }

  static const LoadBalancingLocationStrategyPreferEcs always =
      LoadBalancingLocationStrategyPreferEcs._('always');

  static const LoadBalancingLocationStrategyPreferEcs never =
      LoadBalancingLocationStrategyPreferEcs._('never');

  static const LoadBalancingLocationStrategyPreferEcs proximity =
      LoadBalancingLocationStrategyPreferEcs._('proximity');

  static const LoadBalancingLocationStrategyPreferEcs geo =
      LoadBalancingLocationStrategyPreferEcs._('geo');

  static const List<LoadBalancingLocationStrategyPreferEcs> values = [
    always,
    never,
    proximity,
    geo,
  ];

  final String value;

  String toJson() {
    return value;
  }

  /// Whether this value is unknown (not defined in the OpenAPI spec).
  bool get isUnknown {
    return !values.contains(this);
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is LoadBalancingLocationStrategyPreferEcs && other.value == value;
  }

  @override
  int get hashCode {
    return value.hashCode;
  }

  @override
  String toString() {
    return 'LoadBalancingLocationStrategyPreferEcs($value)';
  }
}
