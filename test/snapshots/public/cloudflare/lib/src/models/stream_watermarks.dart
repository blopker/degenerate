//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class StreamWatermarks {
  const StreamWatermarks({
    this.created,
    this.downloadedFrom,
    this.height,
    this.name,
    this.opacity,
    this.padding,
    this.position,
    this.scale,
    this.size,
    this.uid,
    this.width,
  });

  factory StreamWatermarks.fromJson(Map<String, dynamic> json) {
    return StreamWatermarks(
      created: json['created'] != null
          ? DateTime.parse(json['created'] as String)
          : null,
      downloadedFrom: json['downloadedFrom'] as String?,
      height: json['height'] != null ? (json['height'] as num).toInt() : null,
      name: json['name'] as String?,
      opacity: json['opacity'] != null
          ? (json['opacity'] as num).toDouble()
          : null,
      padding: json['padding'] != null
          ? (json['padding'] as num).toDouble()
          : null,
      position: json['position'] as String?,
      scale: json['scale'] != null ? (json['scale'] as num).toDouble() : null,
      size: json['size'] != null ? (json['size'] as num).toDouble() : null,
      uid: json['uid'] as String?,
      width: json['width'] != null ? (json['width'] as num).toInt() : null,
    );
  }

  final DateTime? created;

  final String? downloadedFrom;

  final int? height;

  final String? name;

  final double? opacity;

  final double? padding;

  final String? position;

  final double? scale;

  final double? size;

  final String? uid;

  final int? width;

  Map<String, dynamic> toJson() {
    return {
      if (created != null) 'created': created?.toIso8601String(),
      'downloadedFrom': ?downloadedFrom,
      'height': ?height,
      'name': ?name,
      'opacity': ?opacity,
      'padding': ?padding,
      'position': ?position,
      'scale': ?scale,
      'size': ?size,
      'uid': ?uid,
      'width': ?width,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  StreamWatermarks copyWith({
    DateTime Function()? created,
    String Function()? downloadedFrom,
    int Function()? height,
    String Function()? name,
    double Function()? opacity,
    double Function()? padding,
    String Function()? position,
    double Function()? scale,
    double Function()? size,
    String Function()? uid,
    int Function()? width,
  }) {
    return StreamWatermarks(
      created: created != null ? created() : this.created,
      downloadedFrom: downloadedFrom != null
          ? downloadedFrom()
          : this.downloadedFrom,
      height: height != null ? height() : this.height,
      name: name != null ? name() : this.name,
      opacity: opacity != null ? opacity() : this.opacity,
      padding: padding != null ? padding() : this.padding,
      position: position != null ? position() : this.position,
      scale: scale != null ? scale() : this.scale,
      size: size != null ? size() : this.size,
      uid: uid != null ? uid() : this.uid,
      width: width != null ? width() : this.width,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is StreamWatermarks &&
            created == other.created &&
            downloadedFrom == other.downloadedFrom &&
            height == other.height &&
            name == other.name &&
            opacity == other.opacity &&
            padding == other.padding &&
            position == other.position &&
            scale == other.scale &&
            size == other.size &&
            uid == other.uid &&
            width == other.width;
  }

  @override
  int get hashCode {
    return Object.hash(
      created,
      downloadedFrom,
      height,
      name,
      opacity,
      padding,
      position,
      scale,
      size,
      uid,
      width,
    );
  }

  @override
  String toString() {
    return 'StreamWatermarks(created: $created, downloadedFrom: $downloadedFrom, height: $height, name: $name, opacity: $opacity, padding: $padding, position: $position, scale: $scale, size: $size, uid: $uid, width: $width)';
  }
}
