//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class WorkersDomain {
  const WorkersDomain({
    this.environment,
    this.hostname,
    this.id,
    this.service,
    this.zoneId,
    this.zoneName,
  });

  factory WorkersDomain.fromJson(Map<String, dynamic> json) {
    return WorkersDomain(
      environment: json['environment'] as String?,
      hostname: json['hostname'] as String?,
      id: json['id'] as String?,
      service: json['service'] as String?,
      zoneId: json['zone_id'] as String?,
      zoneName: json['zone_name'] as String?,
    );
  }

  final String? environment;

  final String? hostname;

  final String? id;

  final String? service;

  final String? zoneId;

  final String? zoneName;

  Map<String, dynamic> toJson() {
    return {
      'environment': ?environment,
      'hostname': ?hostname,
      'id': ?id,
      'service': ?service,
      'zone_id': ?zoneId,
      'zone_name': ?zoneName,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  WorkersDomain copyWith({
    String Function()? environment,
    String Function()? hostname,
    String Function()? id,
    String Function()? service,
    String Function()? zoneId,
    String Function()? zoneName,
  }) {
    return WorkersDomain(
      environment: environment != null ? environment() : this.environment,
      hostname: hostname != null ? hostname() : this.hostname,
      id: id != null ? id() : this.id,
      service: service != null ? service() : this.service,
      zoneId: zoneId != null ? zoneId() : this.zoneId,
      zoneName: zoneName != null ? zoneName() : this.zoneName,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is WorkersDomain &&
            environment == other.environment &&
            hostname == other.hostname &&
            id == other.id &&
            service == other.service &&
            zoneId == other.zoneId &&
            zoneName == other.zoneName;
  }

  @override
  int get hashCode {
    return Object.hash(environment, hostname, id, service, zoneId, zoneName);
  }

  @override
  String toString() {
    return 'WorkersDomain(environment: $environment, hostname: $hostname, id: $id, service: $service, zoneId: $zoneId, zoneName: $zoneName)';
  }
}
