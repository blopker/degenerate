//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class AccessKeyConfig {
  const AccessKeyConfig({
    this.daysUntilNextRotation,
    this.keyRotationIntervalDays,
    this.lastKeyRotationAt,
  });

  factory AccessKeyConfig.fromJson(Map<String, dynamic> json) {
    return AccessKeyConfig(
      daysUntilNextRotation: json['days_until_next_rotation'] != null
          ? (json['days_until_next_rotation'] as num).toDouble()
          : null,
      keyRotationIntervalDays: json['key_rotation_interval_days'] != null
          ? (json['key_rotation_interval_days'] as num).toDouble()
          : null,
      lastKeyRotationAt: json['last_key_rotation_at'] != null
          ? DateTime.parse(json['last_key_rotation_at'] as String)
          : null,
    );
  }

  final double? daysUntilNextRotation;

  final double? keyRotationIntervalDays;

  final DateTime? lastKeyRotationAt;

  Map<String, dynamic> toJson() {
    return {
      'days_until_next_rotation': ?daysUntilNextRotation,
      'key_rotation_interval_days': ?keyRotationIntervalDays,
      if (lastKeyRotationAt != null)
        'last_key_rotation_at': lastKeyRotationAt?.toIso8601String(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  AccessKeyConfig copyWith({
    double Function()? daysUntilNextRotation,
    double Function()? keyRotationIntervalDays,
    DateTime Function()? lastKeyRotationAt,
  }) {
    return AccessKeyConfig(
      daysUntilNextRotation: daysUntilNextRotation != null
          ? daysUntilNextRotation()
          : this.daysUntilNextRotation,
      keyRotationIntervalDays: keyRotationIntervalDays != null
          ? keyRotationIntervalDays()
          : this.keyRotationIntervalDays,
      lastKeyRotationAt: lastKeyRotationAt != null
          ? lastKeyRotationAt()
          : this.lastKeyRotationAt,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is AccessKeyConfig &&
            daysUntilNextRotation == other.daysUntilNextRotation &&
            keyRotationIntervalDays == other.keyRotationIntervalDays &&
            lastKeyRotationAt == other.lastKeyRotationAt;
  }

  @override
  int get hashCode {
    return Object.hash(
      daysUntilNextRotation,
      keyRotationIntervalDays,
      lastKeyRotationAt,
    );
  }

  @override
  String toString() {
    return 'AccessKeyConfig(daysUntilNextRotation: $daysUntilNextRotation, keyRotationIntervalDays: $keyRotationIntervalDays, lastKeyRotationAt: $lastKeyRotationAt)';
  }
}
