//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'contact_properties.dart';
import 'transfer_in.dart';

final class DomainProperties {
  const DomainProperties({
    this.available,
    this.canRegister,
    this.createdAt,
    this.currentRegistrar,
    this.expiresAt,
    this.id,
    this.locked,
    this.registrantContact,
    this.registryStatuses,
    this.supportedTld,
    this.transferIn,
    this.updatedAt,
  });

  factory DomainProperties.fromJson(Map<String, dynamic> json) {
    return DomainProperties(
      available: json['available'] as bool?,
      canRegister: json['can_register'] as bool?,
      createdAt: json['created_at'] != null
          ? DateTime.parse(json['created_at'] as String)
          : null,
      currentRegistrar: json['current_registrar'] as String?,
      expiresAt: json['expires_at'] != null
          ? DateTime.parse(json['expires_at'] as String)
          : null,
      id: json['id'] as String?,
      locked: json['locked'] as bool?,
      registrantContact: json['registrant_contact'] != null
          ? ContactProperties.fromJson(
              json['registrant_contact'] as Map<String, dynamic>,
            )
          : null,
      registryStatuses: json['registry_statuses'] as String?,
      supportedTld: json['supported_tld'] as bool?,
      transferIn: json['transfer_in'] != null
          ? TransferIn.fromJson(json['transfer_in'] as Map<String, dynamic>)
          : null,
      updatedAt: json['updated_at'] != null
          ? DateTime.parse(json['updated_at'] as String)
          : null,
    );
  }

  final bool? available;

  final bool? canRegister;

  final DateTime? createdAt;

  final String? currentRegistrar;

  final DateTime? expiresAt;

  final String? id;

  final bool? locked;

  final ContactProperties? registrantContact;

  final String? registryStatuses;

  final bool? supportedTld;

  final TransferIn? transferIn;

  final DateTime? updatedAt;

  Map<String, dynamic> toJson() {
    return {
      'available': ?available,
      'can_register': ?canRegister,
      if (createdAt != null) 'created_at': createdAt?.toIso8601String(),
      'current_registrar': ?currentRegistrar,
      if (expiresAt != null) 'expires_at': expiresAt?.toIso8601String(),
      'id': ?id,
      'locked': ?locked,
      if (registrantContact != null)
        'registrant_contact': registrantContact?.toJson(),
      'registry_statuses': ?registryStatuses,
      'supported_tld': ?supportedTld,
      if (transferIn != null) 'transfer_in': transferIn?.toJson(),
      if (updatedAt != null) 'updated_at': updatedAt?.toIso8601String(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  DomainProperties copyWith({
    bool Function()? available,
    bool Function()? canRegister,
    DateTime Function()? createdAt,
    String Function()? currentRegistrar,
    DateTime Function()? expiresAt,
    String Function()? id,
    bool Function()? locked,
    ContactProperties Function()? registrantContact,
    String Function()? registryStatuses,
    bool Function()? supportedTld,
    TransferIn Function()? transferIn,
    DateTime Function()? updatedAt,
  }) {
    return DomainProperties(
      available: available != null ? available() : this.available,
      canRegister: canRegister != null ? canRegister() : this.canRegister,
      createdAt: createdAt != null ? createdAt() : this.createdAt,
      currentRegistrar: currentRegistrar != null
          ? currentRegistrar()
          : this.currentRegistrar,
      expiresAt: expiresAt != null ? expiresAt() : this.expiresAt,
      id: id != null ? id() : this.id,
      locked: locked != null ? locked() : this.locked,
      registrantContact: registrantContact != null
          ? registrantContact()
          : this.registrantContact,
      registryStatuses: registryStatuses != null
          ? registryStatuses()
          : this.registryStatuses,
      supportedTld: supportedTld != null ? supportedTld() : this.supportedTld,
      transferIn: transferIn != null ? transferIn() : this.transferIn,
      updatedAt: updatedAt != null ? updatedAt() : this.updatedAt,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is DomainProperties &&
            available == other.available &&
            canRegister == other.canRegister &&
            createdAt == other.createdAt &&
            currentRegistrar == other.currentRegistrar &&
            expiresAt == other.expiresAt &&
            id == other.id &&
            locked == other.locked &&
            registrantContact == other.registrantContact &&
            registryStatuses == other.registryStatuses &&
            supportedTld == other.supportedTld &&
            transferIn == other.transferIn &&
            updatedAt == other.updatedAt;
  }

  @override
  int get hashCode {
    return Object.hash(
      available,
      canRegister,
      createdAt,
      currentRegistrar,
      expiresAt,
      id,
      locked,
      registrantContact,
      registryStatuses,
      supportedTld,
      transferIn,
      updatedAt,
    );
  }

  @override
  String toString() {
    return 'DomainProperties(available: $available, canRegister: $canRegister, createdAt: $createdAt, currentRegistrar: $currentRegistrar, expiresAt: $expiresAt, id: $id, locked: $locked, registrantContact: $registrantContact, registryStatuses: $registryStatuses, supportedTld: $supportedTld, transferIn: $transferIn, updatedAt: $updatedAt)';
  }
}
