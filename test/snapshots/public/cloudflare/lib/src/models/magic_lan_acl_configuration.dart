//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'magic_acl_subnet.dart';

final class MagicLanAclConfiguration {
  const MagicLanAclConfiguration({
    required this.lanId,
    this.lanName,
    this.portRanges,
    this.ports,
    this.subnets,
  });

  factory MagicLanAclConfiguration.fromJson(Map<String, dynamic> json) {
    return MagicLanAclConfiguration(
      lanId: json['lan_id'] as String,
      lanName: json['lan_name'] as String?,
      portRanges: (json['port_ranges'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      ports: (json['ports'] as List<dynamic>?)
          ?.map((e) => (e as num).toInt())
          .toList(),
      subnets: (json['subnets'] as List<dynamic>?)
          ?.map((e) => MagicAclSubnet.fromJson(e as Map<String, dynamic>))
          .toList(),
    );
  }

  /// The identifier for the LAN you want to create an ACL policy with.
  final String lanId;

  /// The name of the LAN based on the provided lan_id.
  final String? lanName;

  /// Array of port ranges on the provided LAN that will be included in the ACL. If no ports or port rangess are provided, communication on any port on this LAN is allowed.
  final List<String>? portRanges;

  /// Array of ports on the provided LAN that will be included in the ACL. If no ports or port ranges are provided, communication on any port on this LAN is allowed.
  final List<int>? ports;

  /// Array of subnet IPs within the LAN that will be included in the ACL. If no subnets are provided, communication on any subnets on this LAN are allowed.
  final List<MagicAclSubnet>? subnets;

  Map<String, dynamic> toJson() {
    return {
      'lan_id': lanId,
      'lan_name': ?lanName,
      'port_ranges': ?portRanges,
      'ports': ?ports,
      if (subnets != null) 'subnets': subnets?.map((e) => e.toJson()).toList(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('lan_id') && json['lan_id'] is String;
  }

  MagicLanAclConfiguration copyWith({
    String? lanId,
    String Function()? lanName,
    List<String> Function()? portRanges,
    List<int> Function()? ports,
    List<MagicAclSubnet> Function()? subnets,
  }) {
    return MagicLanAclConfiguration(
      lanId: lanId ?? this.lanId,
      lanName: lanName != null ? lanName() : this.lanName,
      portRanges: portRanges != null ? portRanges() : this.portRanges,
      ports: ports != null ? ports() : this.ports,
      subnets: subnets != null ? subnets() : this.subnets,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is MagicLanAclConfiguration &&
            lanId == other.lanId &&
            lanName == other.lanName &&
            const ListEquality<dynamic>().equals(
              portRanges,
              other.portRanges,
            ) &&
            const ListEquality<dynamic>().equals(ports, other.ports) &&
            const ListEquality<dynamic>().equals(subnets, other.subnets);
  }

  @override
  int get hashCode {
    return Object.hash(
      lanId,
      lanName,
      Object.hashAll(portRanges ?? const []),
      Object.hashAll(ports ?? const []),
      Object.hashAll(subnets ?? const []),
    );
  }

  @override
  String toString() {
    return 'MagicLanAclConfiguration(lanId: $lanId, lanName: $lanName, portRanges: $portRanges, ports: $ports, subnets: $subnets)';
  }
}
