//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'schemas_frequency.dart';

final class AvailableRatePlan {
  const AvailableRatePlan({
    this.canSubscribe,
    this.currency,
    this.externallyManaged,
    this.frequency,
    this.id,
    this.isSubscribed,
    this.legacyDiscount,
    this.legacyId,
    this.name,
    this.price,
  });

  factory AvailableRatePlan.fromJson(Map<String, dynamic> json) {
    return AvailableRatePlan(
      canSubscribe: json['can_subscribe'] as bool?,
      currency: json['currency'] as String?,
      externallyManaged: json['externally_managed'] as bool?,
      frequency: json['frequency'] != null
          ? SchemasFrequency.fromJson(json['frequency'] as String)
          : null,
      id: json['id'] as String?,
      isSubscribed: json['is_subscribed'] as bool?,
      legacyDiscount: json['legacy_discount'] as bool?,
      legacyId: json['legacy_id'] as String?,
      name: json['name'] as String?,
      price: json['price'] != null ? (json['price'] as num).toDouble() : null,
    );
  }

  final bool? canSubscribe;

  final String? currency;

  final bool? externallyManaged;

  final SchemasFrequency? frequency;

  final String? id;

  final bool? isSubscribed;

  final bool? legacyDiscount;

  final String? legacyId;

  final String? name;

  final double? price;

  Map<String, dynamic> toJson() {
    return {
      'can_subscribe': ?canSubscribe,
      'currency': ?currency,
      'externally_managed': ?externallyManaged,
      if (frequency != null) 'frequency': frequency?.toJson(),
      'id': ?id,
      'is_subscribed': ?isSubscribed,
      'legacy_discount': ?legacyDiscount,
      'legacy_id': ?legacyId,
      'name': ?name,
      'price': ?price,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  AvailableRatePlan copyWith({
    bool Function()? canSubscribe,
    String Function()? currency,
    bool Function()? externallyManaged,
    SchemasFrequency Function()? frequency,
    String Function()? id,
    bool Function()? isSubscribed,
    bool Function()? legacyDiscount,
    String Function()? legacyId,
    String Function()? name,
    double Function()? price,
  }) {
    return AvailableRatePlan(
      canSubscribe: canSubscribe != null ? canSubscribe() : this.canSubscribe,
      currency: currency != null ? currency() : this.currency,
      externallyManaged: externallyManaged != null
          ? externallyManaged()
          : this.externallyManaged,
      frequency: frequency != null ? frequency() : this.frequency,
      id: id != null ? id() : this.id,
      isSubscribed: isSubscribed != null ? isSubscribed() : this.isSubscribed,
      legacyDiscount: legacyDiscount != null
          ? legacyDiscount()
          : this.legacyDiscount,
      legacyId: legacyId != null ? legacyId() : this.legacyId,
      name: name != null ? name() : this.name,
      price: price != null ? price() : this.price,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is AvailableRatePlan &&
            canSubscribe == other.canSubscribe &&
            currency == other.currency &&
            externallyManaged == other.externallyManaged &&
            frequency == other.frequency &&
            id == other.id &&
            isSubscribed == other.isSubscribed &&
            legacyDiscount == other.legacyDiscount &&
            legacyId == other.legacyId &&
            name == other.name &&
            price == other.price;
  }

  @override
  int get hashCode {
    return Object.hash(
      canSubscribe,
      currency,
      externallyManaged,
      frequency,
      id,
      isSubscribed,
      legacyDiscount,
      legacyId,
      name,
      price,
    );
  }

  @override
  String toString() {
    return 'AvailableRatePlan(canSubscribe: $canSubscribe, currency: $currency, externallyManaged: $externallyManaged, frequency: $frequency, id: $id, isSubscribed: $isSubscribed, legacyDiscount: $legacyDiscount, legacyId: $legacyId, name: $name, price: $price)';
  }
}
