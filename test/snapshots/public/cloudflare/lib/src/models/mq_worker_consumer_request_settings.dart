//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

final class MqWorkerConsumerRequestSettings {
  const MqWorkerConsumerRequestSettings({
    this.batchSize,
    this.maxConcurrency,
    this.maxRetries,
    this.maxWaitTimeMs,
    this.retryDelay,
  });

  factory MqWorkerConsumerRequestSettings.fromJson(Map<String, dynamic> json) {
    return MqWorkerConsumerRequestSettings(
      batchSize: json['batch_size'] != null
          ? (json['batch_size'] as num).toDouble()
          : null,
      maxConcurrency: json['max_concurrency'] != null
          ? (json['max_concurrency'] as num).toDouble()
          : null,
      maxRetries: json['max_retries'] != null
          ? (json['max_retries'] as num).toDouble()
          : null,
      maxWaitTimeMs: json['max_wait_time_ms'] != null
          ? (json['max_wait_time_ms'] as num).toDouble()
          : null,
      retryDelay: json['retry_delay'] != null
          ? (json['retry_delay'] as num).toDouble()
          : null,
    );
  }

  final double? batchSize;

  final double? maxConcurrency;

  final double? maxRetries;

  final double? maxWaitTimeMs;

  final double? retryDelay;

  Map<String, dynamic> toJson() {
    return {
      'batch_size': ?batchSize,
      'max_concurrency': ?maxConcurrency,
      'max_retries': ?maxRetries,
      'max_wait_time_ms': ?maxWaitTimeMs,
      'retry_delay': ?retryDelay,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  MqWorkerConsumerRequestSettings copyWith({
    double Function()? batchSize,
    double Function()? maxConcurrency,
    double Function()? maxRetries,
    double Function()? maxWaitTimeMs,
    double Function()? retryDelay,
  }) {
    return MqWorkerConsumerRequestSettings(
      batchSize: batchSize != null ? batchSize() : this.batchSize,
      maxConcurrency: maxConcurrency != null
          ? maxConcurrency()
          : this.maxConcurrency,
      maxRetries: maxRetries != null ? maxRetries() : this.maxRetries,
      maxWaitTimeMs: maxWaitTimeMs != null
          ? maxWaitTimeMs()
          : this.maxWaitTimeMs,
      retryDelay: retryDelay != null ? retryDelay() : this.retryDelay,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is MqWorkerConsumerRequestSettings &&
            batchSize == other.batchSize &&
            maxConcurrency == other.maxConcurrency &&
            maxRetries == other.maxRetries &&
            maxWaitTimeMs == other.maxWaitTimeMs &&
            retryDelay == other.retryDelay;
  }

  @override
  int get hashCode {
    return Object.hash(
      batchSize,
      maxConcurrency,
      maxRetries,
      maxWaitTimeMs,
      retryDelay,
    );
  }

  @override
  String toString() {
    return 'MqWorkerConsumerRequestSettings(batchSize: $batchSize, maxConcurrency: $maxConcurrency, maxRetries: $maxRetries, maxWaitTimeMs: $maxWaitTimeMs, retryDelay: $retryDelay)';
  }
}
