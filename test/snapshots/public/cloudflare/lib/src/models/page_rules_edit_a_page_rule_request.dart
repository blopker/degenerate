//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'zones_actions2.dart';
import 'zones_status.dart';
import 'zones_target.dart';

final class PageRulesEditAPageRuleRequest {
  const PageRulesEditAPageRuleRequest({
    this.actions,
    this.priority,
    this.status,
    this.targets,
  });

  factory PageRulesEditAPageRuleRequest.fromJson(Map<String, dynamic> json) {
    return PageRulesEditAPageRuleRequest(
      actions: (json['actions'] as List<dynamic>?)
          ?.map((e) => ZonesActions2.fromJson(e as Map<String, dynamic>))
          .toList(),
      priority: json['priority'] != null
          ? (json['priority'] as num).toInt()
          : null,
      status: json['status'] != null
          ? ZonesStatus.fromJson(json['status'] as String)
          : null,
      targets: (json['targets'] as List<dynamic>?)
          ?.map((e) => ZonesTarget.fromJson(e as Map<String, dynamic>))
          .toList(),
    );
  }

  final List<ZonesActions2>? actions;

  final int? priority;

  final ZonesStatus? status;

  final List<ZonesTarget>? targets;

  Map<String, dynamic> toJson() {
    return {
      if (actions != null) 'actions': actions?.map((e) => e.toJson()).toList(),
      'priority': ?priority,
      if (status != null) 'status': status?.toJson(),
      if (targets != null) 'targets': targets?.map((e) => e.toJson()).toList(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  PageRulesEditAPageRuleRequest copyWith({
    List<ZonesActions2> Function()? actions,
    int Function()? priority,
    ZonesStatus Function()? status,
    List<ZonesTarget> Function()? targets,
  }) {
    return PageRulesEditAPageRuleRequest(
      actions: actions != null ? actions() : this.actions,
      priority: priority != null ? priority() : this.priority,
      status: status != null ? status() : this.status,
      targets: targets != null ? targets() : this.targets,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is PageRulesEditAPageRuleRequest &&
            const ListEquality<dynamic>().equals(actions, other.actions) &&
            priority == other.priority &&
            status == other.status &&
            const ListEquality<dynamic>().equals(targets, other.targets);
  }

  @override
  int get hashCode {
    return Object.hash(
      Object.hashAll(actions ?? const []),
      priority,
      status,
      Object.hashAll(targets ?? const []),
    );
  }

  @override
  String toString() {
    return 'PageRulesEditAPageRuleRequest(actions: $actions, priority: $priority, status: $status, targets: $targets)';
  }
}
