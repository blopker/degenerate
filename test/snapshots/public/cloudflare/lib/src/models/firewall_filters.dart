//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'firewall_filters_configuration_target.dart';
import 'firewall_filters_match.dart';
import 'firewall_schemas_mode.dart';

final class FirewallFilters {
  const FirewallFilters({
    this.configurationTarget,
    this.configurationValue,
    this.match = FirewallFiltersMatch.all,
    this.mode,
    this.notes,
  });

  factory FirewallFilters.fromJson(Map<String, dynamic> json) {
    return FirewallFilters(
      configurationTarget: json['configuration.target'] != null
          ? FirewallFiltersConfigurationTarget.fromJson(
              json['configuration.target'] as String,
            )
          : null,
      configurationValue: json['configuration.value'] as String?,
      match: json.containsKey('match')
          ? FirewallFiltersMatch.fromJson(json['match'] as String)
          : FirewallFiltersMatch.all,
      mode: json['mode'] != null
          ? FirewallSchemasMode.fromJson(json['mode'] as String)
          : null,
      notes: json['notes'] as String?,
    );
  }

  /// The target to search in existing rules.
  final FirewallFiltersConfigurationTarget? configurationTarget;

  /// The target value to search for in existing rules: an IP address, an IP address range, or a country code, depending on the provided `configuration.target`.
  /// Notes: You can search for a single IPv4 address, an IP address range with a subnet of '/16' or '/24', or a two-letter ISO-3166-1 alpha-2 country code.
  final String? configurationValue;

  /// When set to `all`, all the search requirements must match. When set to `any`, only one of the search requirements has to match.
  final FirewallFiltersMatch match;

  final FirewallSchemasMode? mode;

  /// The string to search for in the notes of existing IP Access rules.
  /// Notes: For example, the string 'attack' would match IP Access rules with notes 'Attack 26/02' and 'Attack 27/02'. The search is case insensitive.
  final String? notes;

  Map<String, dynamic> toJson() {
    return {
      if (configurationTarget != null)
        'configuration.target': configurationTarget?.toJson(),
      'configuration.value': ?configurationValue,
      'match': match.toJson(),
      if (mode != null) 'mode': mode?.toJson(),
      'notes': ?notes,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  FirewallFilters copyWith({
    FirewallFiltersConfigurationTarget Function()? configurationTarget,
    String Function()? configurationValue,
    FirewallFiltersMatch Function()? match,
    FirewallSchemasMode Function()? mode,
    String Function()? notes,
  }) {
    return FirewallFilters(
      configurationTarget: configurationTarget != null
          ? configurationTarget()
          : this.configurationTarget,
      configurationValue: configurationValue != null
          ? configurationValue()
          : this.configurationValue,
      match: match != null ? match() : this.match,
      mode: mode != null ? mode() : this.mode,
      notes: notes != null ? notes() : this.notes,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is FirewallFilters &&
            configurationTarget == other.configurationTarget &&
            configurationValue == other.configurationValue &&
            match == other.match &&
            mode == other.mode &&
            notes == other.notes;
  }

  @override
  int get hashCode {
    return Object.hash(
      configurationTarget,
      configurationValue,
      match,
      mode,
      notes,
    );
  }

  @override
  String toString() {
    return 'FirewallFilters(configurationTarget: $configurationTarget, configurationValue: $configurationValue, match: $match, mode: $mode, notes: $notes)';
  }
}
