//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'package:collection/collection.dart';
import 'tunnel_schemas_connection.dart';

/// A client (typically cloudflared) that maintains connections to a Cloudflare data center.
final class TunnelTunnelClient {
  const TunnelTunnelClient({
    this.arch,
    this.configVersion,
    this.conns,
    this.features,
    this.id,
    this.runAt,
    this.version,
  });

  factory TunnelTunnelClient.fromJson(Map<String, dynamic> json) {
    return TunnelTunnelClient(
      arch: json['arch'] as String?,
      configVersion: json['config_version'] != null
          ? (json['config_version'] as num).toInt()
          : null,
      conns: (json['conns'] as List<dynamic>?)
          ?.map(
            (e) => TunnelSchemasConnection.fromJson(e as Map<String, dynamic>),
          )
          .toList(),
      features: (json['features'] as List<dynamic>?)
          ?.map((e) => e as String)
          .toList(),
      id: json['id'] as String?,
      runAt: json['run_at'] != null
          ? DateTime.parse(json['run_at'] as String)
          : null,
      version: json['version'] as String?,
    );
  }

  final String? arch;

  final int? configVersion;

  final List<TunnelSchemasConnection>? conns;

  final List<String>? features;

  final String? id;

  final DateTime? runAt;

  final String? version;

  Map<String, dynamic> toJson() {
    return {
      'arch': ?arch,
      'config_version': ?configVersion,
      if (conns != null) 'conns': conns?.map((e) => e.toJson()).toList(),
      'features': ?features,
      'id': ?id,
      if (runAt != null) 'run_at': runAt?.toIso8601String(),
      'version': ?version,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  TunnelTunnelClient copyWith({
    String Function()? arch,
    int Function()? configVersion,
    List<TunnelSchemasConnection> Function()? conns,
    List<String> Function()? features,
    String Function()? id,
    DateTime Function()? runAt,
    String Function()? version,
  }) {
    return TunnelTunnelClient(
      arch: arch != null ? arch() : this.arch,
      configVersion: configVersion != null
          ? configVersion()
          : this.configVersion,
      conns: conns != null ? conns() : this.conns,
      features: features != null ? features() : this.features,
      id: id != null ? id() : this.id,
      runAt: runAt != null ? runAt() : this.runAt,
      version: version != null ? version() : this.version,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is TunnelTunnelClient &&
            arch == other.arch &&
            configVersion == other.configVersion &&
            const ListEquality<dynamic>().equals(conns, other.conns) &&
            const ListEquality<dynamic>().equals(features, other.features) &&
            id == other.id &&
            runAt == other.runAt &&
            version == other.version;
  }

  @override
  int get hashCode {
    return Object.hash(
      arch,
      configVersion,
      Object.hashAll(conns ?? const []),
      Object.hashAll(features ?? const []),
      id,
      runAt,
      version,
    );
  }

  @override
  String toString() {
    return 'TunnelTunnelClient(arch: $arch, configVersion: $configVersion, conns: $conns, features: $features, id: $id, runAt: $runAt, version: $version)';
  }
}
