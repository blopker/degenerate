//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common3.dart';
import '../models/zone_level_access_groups_create_an_access_group_request.dart';
import '../models/zone_level_access_groups_update_an_access_group_request.dart';

/// ZoneLevelAccessGroupsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZoneLevelAccessGroupsApi {
  const ZoneLevelAccessGroupsApi(this._config);

  final ApiConfig _config;

  /// List Access groups
  ///
  /// Lists all Access groups.
  ///
  /// `GET /zones/{zone_id}/access/groups`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessGroupsListAccessGroups({
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/access/groups',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create an Access group
  ///
  /// Creates a new Access group.
  ///
  /// `POST /zones/{zone_id}/access/groups`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessGroupsCreateAnAccessGroup({
    required String zoneId,
    required ZoneLevelAccessGroupsCreateAnAccessGroupRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/zones/${Uri.encodeComponent(zoneId)}/access/groups',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get an Access group
  ///
  /// Fetches a single Access group.
  ///
  /// `GET /zones/{zone_id}/access/groups/{group_id}`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessGroupsGetAnAccessGroup({
    required String groupId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/groups/${Uri.encodeComponent(groupId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update an Access group
  ///
  /// Updates a configured Access group.
  ///
  /// `PUT /zones/{zone_id}/access/groups/{group_id}`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessGroupsUpdateAnAccessGroup({
    required String groupId,
    required String zoneId,
    required ZoneLevelAccessGroupsUpdateAnAccessGroupRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/groups/${Uri.encodeComponent(groupId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete an Access group
  ///
  /// Deletes an Access group.
  ///
  /// `DELETE /zones/{zone_id}/access/groups/{group_id}`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessGroupsDeleteAnAccessGroup({
    required String groupId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/groups/${Uri.encodeComponent(groupId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
