//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common3.dart';
import '../models/zone_level_access_policies_create_an_access_policy_request.dart';
import '../models/zone_level_access_policies_update_an_access_policy_request.dart';

/// ZoneLevelAccessPoliciesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZoneLevelAccessPoliciesApi {
  const ZoneLevelAccessPoliciesApi(this._config);

  final ApiConfig _config;

  /// List Access policies
  ///
  /// Lists Access policies configured for an application.
  ///
  /// `GET /zones/{zone_id}/access/apps/{app_id}/policies`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessPoliciesListAccessPolicies({
    required String appId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/apps/${Uri.encodeComponent(appId)}/policies',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create an Access policy
  ///
  /// Create a new Access policy for an application.
  ///
  /// `POST /zones/{zone_id}/access/apps/{app_id}/policies`
  Future<ApiResult<ResponseCommon3>>
  zoneLevelAccessPoliciesCreateAnAccessPolicy({
    required String appId,
    required String zoneId,
    required ZoneLevelAccessPoliciesCreateAnAccessPolicyRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/apps/${Uri.encodeComponent(appId)}/policies',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get an Access policy
  ///
  /// Fetches a single Access policy.
  ///
  /// `GET /zones/{zone_id}/access/apps/{app_id}/policies/{policy_id}`
  Future<ApiResult<ResponseCommon3>> zoneLevelAccessPoliciesGetAnAccessPolicy({
    required String policyId,
    required String appId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/apps/${Uri.encodeComponent(appId)}/policies/${Uri.encodeComponent(policyId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update an Access policy
  ///
  /// Update a configured Access policy.
  ///
  /// `PUT /zones/{zone_id}/access/apps/{app_id}/policies/{policy_id}`
  Future<ApiResult<ResponseCommon3>>
  zoneLevelAccessPoliciesUpdateAnAccessPolicy({
    required String policyId,
    required String appId,
    required String zoneId,
    required ZoneLevelAccessPoliciesUpdateAnAccessPolicyRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/apps/${Uri.encodeComponent(appId)}/policies/${Uri.encodeComponent(policyId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete an Access policy
  ///
  /// Delete an Access policy.
  ///
  /// `DELETE /zones/{zone_id}/access/apps/{app_id}/policies/{policy_id}`
  Future<ApiResult<ResponseCommon3>>
  zoneLevelAccessPoliciesDeleteAnAccessPolicy({
    required String policyId,
    required String appId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/access/apps/${Uri.encodeComponent(appId)}/policies/${Uri.encodeComponent(policyId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
