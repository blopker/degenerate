//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/account_user_group_member_create_request.dart';
import '../models/account_user_group_members_update_request.dart';
import '../models/iam_create_user_group_body.dart';
import '../models/iam_update_user_group_body.dart';
import '../models/response_common35.dart';

/// AccountUserGroupsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AccountUserGroupsApi {
  const AccountUserGroupsApi(this._config);

  final ApiConfig _config;

  /// List User Groups
  ///
  /// List all the user groups for an account.
  ///
  /// `GET /accounts/{account_id}/iam/user_groups`
  Future<ApiResult<ResponseCommon35>> accountUserGroupList({
    required String accountId,
    String? id,
    String? name,
    String? fuzzyName,
    double? page,
    double? perPage,
    String? direction,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'id': ?id,
        'name': ?name,
        'fuzzyName': ?fuzzyName,
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        'direction': ?direction,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create User Group
  ///
  /// Create a new user group under the specified account.
  ///
  /// `POST /accounts/{account_id}/iam/user_groups`
  Future<ApiResult<ResponseCommon35>> accountUserGroupCreate({
    required String accountId,
    required IamCreateUserGroupBody body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// User Group Details
  ///
  /// Get information about a specific user group in an account.
  ///
  /// `GET /accounts/{account_id}/iam/user_groups/{user_group_id}`
  Future<ApiResult<ResponseCommon35>> accountUserGroupDetails({
    required String accountId,
    required String userGroupId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update User Group
  ///
  /// Modify an existing user group.
  ///
  /// `PUT /accounts/{account_id}/iam/user_groups/{user_group_id}`
  Future<ApiResult<ResponseCommon35>> accountUserGroupUpdate({
    required String accountId,
    required String userGroupId,
    required IamUpdateUserGroupBody body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Remove User Group
  ///
  /// Remove a user group from an account.
  ///
  /// `DELETE /accounts/{account_id}/iam/user_groups/{user_group_id}`
  Future<ApiResult<ResponseCommon35>> accountUserGroupDelete({
    required String accountId,
    required String userGroupId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List User Group Members
  ///
  /// List all the members attached to a user group.
  ///
  /// `GET /accounts/{account_id}/iam/user_groups/{user_group_id}/members`
  Future<ApiResult<ResponseCommon35>> accountUserGroupMemberList({
    required String accountId,
    required String userGroupId,
    double? page,
    double? perPage,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}/members',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Add User Group Members
  ///
  /// Add members to a User Group.
  ///
  /// `POST /accounts/{account_id}/iam/user_groups/{user_group_id}/members`
  Future<ApiResult<ResponseCommon35>> accountUserGroupMemberCreate({
    required String accountId,
    required String userGroupId,
    required List<AccountUserGroupMemberCreateRequest> body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}/members',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update User Group Members
  ///
  /// Replace the set of members attached to a User Group.
  ///
  /// `PUT /accounts/{account_id}/iam/user_groups/{user_group_id}/members`
  Future<ApiResult<ResponseCommon35>> accountUserGroupMembersUpdate({
    required String accountId,
    required String userGroupId,
    required List<AccountUserGroupMembersUpdateRequest> body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}/members',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Remove User Group Member
  ///
  /// Remove a member from User Group
  ///
  /// `DELETE /accounts/{account_id}/iam/user_groups/{user_group_id}/members/{member_id}`
  Future<ApiResult<ResponseCommon35>> accountUserGroupMemberDelete({
    required String accountId,
    required String userGroupId,
    required String memberId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/iam/user_groups/${Uri.encodeComponent(userGroupId)}/members/${Uri.encodeComponent(memberId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon35.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
