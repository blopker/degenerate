//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/get_all_recordings_sort_by.dart';
import '../models/get_all_recordings_sort_order.dart';
import '../models/get_all_recordings_status.dart';
import '../models/pause_resume_stop_recording_request.dart';
import '../models/realtimekit_generic_success_response.dart';
import '../models/realtimekit_paging_response.dart';
import '../models/start_recording_request.dart';
import '../models/start_track_recording_for_a_meeting_request.dart';

/// RecordingsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RecordingsApi {
  const RecordingsApi(this._config);

  final ApiConfig _config;

  /// Fetch all recordings for an App
  ///
  /// Returns all recordings for an App. If the `meeting_id` parameter is passed, returns all recordings for the given meeting ID.
  ///
  /// `GET /accounts/{account_id}/realtime/kit/{app_id}/recordings`
  Future<ApiResult<RealtimekitPagingResponse>> getAllRecordings({
    required String accountId,
    required String appId,
    String? meetingId,
    double? pageNo,
    double? perPage,
    bool? expired,
    String? search,
    GetAllRecordingsSortBy? sortBy,
    GetAllRecordingsSortOrder? sortOrder,
    DateTime? startTime,
    DateTime? endTime,
    List<GetAllRecordingsStatus>? status,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'meeting_id': ?meetingId,
        if (pageNo != null) 'page_no': pageNo.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (expired != null) 'expired': expired.toString(),
        'search': ?search,
        if (sortBy != null) 'sort_by': sortBy.toJson(),
        if (sortOrder != null) 'sort_order': sortOrder.toJson(),
        if (startTime != null) 'start_time': startTime.toString(),
        if (endTime != null) 'end_time': endTime.toString(),
        if (status != null) 'status': status.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RealtimekitPagingResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Start recording a meeting
  ///
  /// Starts recording a meeting. The meeting can be started by an App admin directly, or a participant with permissions to start a recording, based on the type of authorization used.
  ///
  /// `POST /accounts/{account_id}/realtime/kit/{app_id}/recordings`
  Future<ApiResult<RealtimekitGenericSuccessResponse>> startRecording({
    required String accountId,
    required String appId,
    required StartRecordingRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RealtimekitGenericSuccessResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Fetch details of a recording
  ///
  /// Returns details of a recording for the given recording ID.
  ///
  /// `GET /accounts/{account_id}/realtime/kit/{app_id}/recordings/{recording_id}`
  Future<ApiResult<RealtimekitGenericSuccessResponse>> getOneRecording({
    required String accountId,
    required String appId,
    required String recordingId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings/${Uri.encodeComponent(recordingId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RealtimekitGenericSuccessResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Pause/Resume/Stop recording
  ///
  /// Pause/Resume/Stop a given recording ID.
  ///
  /// `PUT /accounts/{account_id}/realtime/kit/{app_id}/recordings/{recording_id}`
  Future<ApiResult<RealtimekitGenericSuccessResponse>>
  pauseResumeStopRecording({
    required String accountId,
    required String appId,
    required String recordingId,
    required PauseResumeStopRecordingRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings/${Uri.encodeComponent(recordingId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RealtimekitGenericSuccessResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Fetch active recording
  ///
  /// Returns the active recording details for the given meeting ID.
  ///
  /// `GET /accounts/{account_id}/realtime/kit/{app_id}/recordings/active-recording/{meeting_id}`
  Future<ApiResult<RealtimekitGenericSuccessResponse>> getActiveRecording({
    required String accountId,
    required String appId,
    required String meetingId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings/active-recording/${Uri.encodeComponent(meetingId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RealtimekitGenericSuccessResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Start recording audio and video tracks
  ///
  /// Starts a track recording in a meeting. Track recordings consist of "layers". Layers are used to map audio/video tracks in a meeting to output destinations. More information about track recordings is available in the [Track Recordings Guide Page](https://docs.realtime.cloudflare.com/guides/capabilities/recording/recording-overview).
  ///
  /// `POST /accounts/{account_id}/realtime/kit/{app_id}/recordings/track`
  Future<ApiResult<void>> startTrackRecordingForAMeeting({
    required String accountId,
    required String appId,
    required StartTrackRecordingForAMeetingRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/recordings/track',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
