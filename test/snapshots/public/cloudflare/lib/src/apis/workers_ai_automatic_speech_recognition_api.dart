//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import 'dart:typed_data';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/turbo_request.dart';
import '../models/workers_ai_post_run_cf_deepgram_flux_request.dart';
import '../models/workers_ai_post_run_cf_deepgram_nova3_request.dart';

/// WorkersAiAutomaticSpeechRecognitionApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiAutomaticSpeechRecognitionApi {
  const WorkersAiAutomaticSpeechRecognitionApi(this._config);

  final ApiConfig _config;

  /// Open Websocket connection with @cf/deepgram/flux model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/flux model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/flux`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramFlux({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/flux',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/deepgram/flux model.
  ///
  /// Runs inference on the @cf/deepgram/flux model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/deepgram/flux`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfDeepgramFlux({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfDeepgramFluxRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/flux',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Open Websocket connection with @cf/deepgram/nova-3 model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/nova-3 model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/nova-3`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramNova3({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/nova-3',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/deepgram/nova-3 model.
  ///
  /// Runs inference on the @cf/deepgram/nova-3 model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/deepgram/nova-3`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfDeepgramNova3({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfDeepgramNova3Request? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/nova-3',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Open Websocket connection with @cf/deepgram/nova-3-internal model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/nova-3-internal model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/nova-3-internal`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramNova3Internal({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/nova-3-internal',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/openai/whisper model.
  ///
  /// Runs inference on the @cf/openai/whisper model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/openai/whisper`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfOpenaiWhisper({
    required String accountId,
    String? queueRequest,
    String? tags,
    Uint8List? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/openai/whisper',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Execute @cf/openai/whisper-large-v3-turbo model.
  ///
  /// Runs inference on the @cf/openai/whisper-large-v3-turbo model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/openai/whisper-large-v3-turbo`
  Future<ApiResult<Map<String, String>>>
  workersAiPostRunCfOpenaiWhisperLargeV3Turbo({
    required String accountId,
    String? queueRequest,
    String? tags,
    TurboRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/openai/whisper-large-v3-turbo',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Execute @cf/openai/whisper-tiny-en model.
  ///
  /// Runs inference on the @cf/openai/whisper-tiny-en model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/openai/whisper-tiny-en`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfOpenaiWhisperTinyEn({
    required String accountId,
    String? queueRequest,
    String? tags,
    Uint8List? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/openai/whisper-tiny-en',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
