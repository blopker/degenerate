//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/access_service_tokens_create_a_service_token_request.dart';
import '../models/access_service_tokens_rotate_a_service_token_request.dart';
import '../models/access_service_tokens_update_a_service_token_request.dart';
import '../models/response_common3.dart';

/// AccessServiceTokensApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AccessServiceTokensApi {
  const AccessServiceTokensApi(this._config);

  final ApiConfig _config;

  /// List service tokens
  ///
  /// Lists all service tokens.
  ///
  /// `GET /accounts/{account_id}/access/service_tokens`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensListServiceTokens({
    required String accountId,
    String? name,
    String? search,
    int? page,
    int? perPage,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'name': ?name,
        'search': ?search,
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a service token
  ///
  /// Generates a new service token. **Note:** This is the only time you can get the Client Secret. If you lose the Client Secret, you will have to rotate the Client Secret or create a new service token.
  ///
  /// `POST /accounts/{account_id}/access/service_tokens`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensCreateAServiceToken({
    required String accountId,
    required AccessServiceTokensCreateAServiceTokenRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a service token
  ///
  /// Fetches a single service token.
  ///
  /// `GET /accounts/{account_id}/access/service_tokens/{service_token_id}`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensGetAServiceToken({
    required String serviceTokenId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens/${Uri.encodeComponent(serviceTokenId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a service token
  ///
  /// Updates a configured service token.
  ///
  /// `PUT /accounts/{account_id}/access/service_tokens/{service_token_id}`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensUpdateAServiceToken({
    required String serviceTokenId,
    required String accountId,
    required AccessServiceTokensUpdateAServiceTokenRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens/${Uri.encodeComponent(serviceTokenId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a service token
  ///
  /// Deletes a service token.
  ///
  /// `DELETE /accounts/{account_id}/access/service_tokens/{service_token_id}`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensDeleteAServiceToken({
    required String serviceTokenId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens/${Uri.encodeComponent(serviceTokenId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Refresh a service token
  ///
  /// Refreshes the expiration of a service token.
  ///
  /// `POST /accounts/{account_id}/access/service_tokens/{service_token_id}/refresh`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensRefreshAServiceToken({
    required String serviceTokenId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens/${Uri.encodeComponent(serviceTokenId)}/refresh',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Rotate a service token
  ///
  /// Generates a new Client Secret for a service token and revokes the old one.
  ///
  /// `POST /accounts/{account_id}/access/service_tokens/{service_token_id}/rotate`
  Future<ApiResult<ResponseCommon3>> accessServiceTokensRotateAServiceToken({
    required String serviceTokenId,
    required String accountId,
    AccessServiceTokensRotateAServiceTokenRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/service_tokens/${Uri.encodeComponent(serviceTokenId)}/rotate',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
