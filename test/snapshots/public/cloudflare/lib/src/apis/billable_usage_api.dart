//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/usage_response.dart';/// BillableUsageApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class BillableUsageApi {const BillableUsageApi(this._config);

final ApiConfig _config;

/// Get PayGo Account Billable Usage (Beta)
///
/// Returns billable usage data for PayGo (self-serve) accounts.
/// When no query parameters are provided, returns usage for the current
/// billing period.
/// 
/// Supports two mutually exclusive query modes:
/// 
/// **Billing period mode:** Use `last_year_period_start` and
/// `last_month_period_start` to query a specific billing period.
/// 
/// **Date range mode:** Use `from` and `to` to query a custom date range
/// (maximum 62 days).
/// 
/// This endpoint is currently in beta and access is restricted to select
/// accounts.
/// 
///
/// `GET /accounts/{account_id}/billing/usage/paygo`
Future<ApiResult<UsageResponse>> billableUsageGetPaygoAccountUsage({required String accountId, String? from, String? to, int? lastYearPeriodStart, int? lastMonthPeriodStart, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/billing/usage/paygo',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'from': ?from,
    'to': ?to,
    if (lastYearPeriodStart != null) 'last_year_period_start': lastYearPeriodStart.toString(),
    if (lastMonthPeriodStart != null) 'last_month_period_start': lastMonthPeriodStart.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return UsageResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
