//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/inline_object1827.dart';
import '../models/response_common6.dart';
import '../models/shield_global_setting_change_base.dart';
import '../models/shield_per_operation_setting_change_base.dart';

/// SchemaValidationSettingsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SchemaValidationSettingsApi {
  const SchemaValidationSettingsApi(this._config);

  final ApiConfig _config;

  /// Get global schema validation settings
  ///
  /// Retrieves the current global schema validation settings for a zone.
  ///
  /// `GET /zones/{zone_id}/schema_validation/settings`
  Future<ApiResult<ResponseCommon6>> schemaValidationGetSettings({
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update global schema validation settings
  ///
  /// Fully updates global schema validation settings for a zone, replacing existing configuration.
  ///
  /// `PUT /zones/{zone_id}/schema_validation/settings`
  Future<ApiResult<ResponseCommon6>> schemaValidationUpdateSettings({
    required String zoneId,
    required ShieldGlobalSettingChangeBase body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path: '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Edit global schema validation settings
  ///
  /// Partially updates global schema validation settings for a zone using PATCH semantics.
  ///
  /// `PATCH /zones/{zone_id}/schema_validation/settings`
  Future<ApiResult<ResponseCommon6>> schemaValidationEditSettings({
    required String zoneId,
    required ShieldGlobalSettingChangeBase body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path: '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List per-operation schema validation settings
  ///
  /// Lists all per-operation schema validation settings configured for the zone.
  ///
  /// `GET /zones/{zone_id}/schema_validation/settings/operations`
  Future<ApiResult<ResponseCommon6>> schemaValidationListPerOperationSettings({
    required String zoneId,
    int? page,
    int? perPage,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings/operations',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Bulk edit per-operation schema validation settings
  ///
  /// Updates schema validation settings for multiple API operations in a single request. Efficient for applying consistent validation rules across endpoints.
  ///
  /// `PATCH /zones/{zone_id}/schema_validation/settings/operations`
  Future<ApiResult<ResponseCommon6>>
  schemaValidationBulkEditPerOperationSettings({
    required String zoneId,
    required Map<String, InlineObject1827> body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings/operations',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get per-operation schema validation setting
  ///
  /// Retrieves the schema validation settings configured for a specific API operation.
  ///
  /// `GET /zones/{zone_id}/schema_validation/settings/operations/{operation_id}`
  Future<ApiResult<ResponseCommon6>> schemaValidationGetPerOperationSetting({
    required String zoneId,
    required String operationId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings/operations/${Uri.encodeComponent(operationId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update per-operation schema validation setting
  ///
  /// Fully updates schema validation settings for a specific API operation.
  ///
  /// `PUT /zones/{zone_id}/schema_validation/settings/operations/{operation_id}`
  Future<ApiResult<ResponseCommon6>> schemaValidationUpdatePerOperationSetting({
    required String zoneId,
    required String operationId,
    required ShieldPerOperationSettingChangeBase body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings/operations/${Uri.encodeComponent(operationId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete per-operation schema validation setting
  ///
  /// Removes custom schema validation settings for a specific API operation, reverting to zone-level defaults.
  ///
  /// `DELETE /zones/{zone_id}/schema_validation/settings/operations/{operation_id}`
  Future<ApiResult<ResponseCommon6>> schemaValidationDeletePerOperationSetting({
    required String zoneId,
    required String operationId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/schema_validation/settings/operations/${Uri.encodeComponent(operationId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
