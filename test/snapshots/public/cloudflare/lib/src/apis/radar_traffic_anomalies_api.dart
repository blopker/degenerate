//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/radar_get_traffic_anomalies_format.dart';
import '../models/radar_get_traffic_anomalies_response.dart';
import '../models/radar_get_traffic_anomalies_status.dart';
import '../models/radar_get_traffic_anomalies_top_format.dart';
import '../models/radar_get_traffic_anomalies_top_response.dart';
import '../models/radar_get_traffic_anomalies_top_status.dart';
import '../models/radar_get_traffic_anomalies_type.dart';

/// RadarTrafficAnomaliesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RadarTrafficAnomaliesApi {
  const RadarTrafficAnomaliesApi(this._config);

  final ApiConfig _config;

  /// Get latest Internet traffic anomalies
  ///
  /// Retrieves the latest Internet traffic anomalies, which are signals that might indicate an outage. These alerts are automatically detected by Radar and manually verified by our team.
  ///
  /// `GET /radar/traffic_anomalies`
  Future<ApiResult<RadarGetTrafficAnomaliesResponse>> radarGetTrafficAnomalies({
    int? limit,
    int? offset,
    String? dateRange,
    DateTime? dateStart,
    DateTime? dateEnd,
    RadarGetTrafficAnomaliesStatus? status,
    List<RadarGetTrafficAnomaliesType>? type,
    int? asn,
    String? location,
    String? origin,
    RadarGetTrafficAnomaliesFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/traffic_anomalies',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (offset != null) 'offset': offset.toString(),
        'dateRange': ?dateRange,
        if (dateStart != null) 'dateStart': dateStart.toString(),
        if (dateEnd != null) 'dateEnd': dateEnd.toString(),
        if (status != null) 'status': status.toJson(),
        if (type != null) 'type': type.toString(),
        if (asn != null) 'asn': asn.toString(),
        'location': ?location,
        'origin': ?origin,
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetTrafficAnomaliesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get top locations by total traffic anomalies
  ///
  /// Retrieves the sum of Internet traffic anomalies, grouped by location. These anomalies are signals that might indicate an outage, automatically detected by Radar and manually verified by our team.
  ///
  /// `GET /radar/traffic_anomalies/locations`
  Future<ApiResult<RadarGetTrafficAnomaliesTopResponse>>
  radarGetTrafficAnomaliesTop({
    int? limit,
    String? dateRange,
    DateTime? dateStart,
    DateTime? dateEnd,
    RadarGetTrafficAnomaliesTopStatus? status,
    RadarGetTrafficAnomaliesTopFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/traffic_anomalies/locations',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        'dateRange': ?dateRange,
        if (dateStart != null) 'dateStart': dateStart.toString(),
        if (dateEnd != null) 'dateEnd': dateEnd.toString(),
        if (status != null) 'status': status.toJson(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetTrafficAnomaliesTopResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
