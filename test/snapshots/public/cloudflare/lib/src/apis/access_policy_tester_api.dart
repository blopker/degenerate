//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/access_policy_init_req.dart';
import '../models/access_policy_tests_get_a_user_page_status.dart';
import '../models/response_common3.dart';

/// AccessPolicyTesterApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AccessPolicyTesterApi {
  const AccessPolicyTesterApi(this._config);

  final ApiConfig _config;

  /// Start Access policy test
  ///
  /// Starts an Access policy test.
  ///
  /// `POST /accounts/{account_id}/access/policy-tests`
  Future<ApiResult<ResponseCommon3>> accessPolicyTests({
    required String accountId,
    required AccessPolicyInitReq body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/access/policy-tests',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get the current status of a given Access policy test
  ///
  /// Fetches the current status of a given Access policy test.
  ///
  /// `GET /accounts/{account_id}/access/policy-tests/{policy_test_id}`
  Future<ApiResult<ResponseCommon3>> accessPolicyTestsGetAnUpdate({
    required String accountId,
    required String policyTestId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/policy-tests/${Uri.encodeComponent(policyTestId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get an Access policy test users page
  ///
  /// Fetches a single page of user results from an Access policy test.
  ///
  /// `GET /accounts/{account_id}/access/policy-tests/{policy_test_id}/users`
  Future<ApiResult<ResponseCommon3>> accessPolicyTestsGetAUserPage({
    required String accountId,
    required String policyTestId,
    int? page,
    int? perPage,
    AccessPolicyTestsGetAUserPageStatus? status,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/policy-tests/${Uri.encodeComponent(policyTestId)}/users',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (status != null) 'status': status.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
