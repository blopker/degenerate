//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/firewall_rule.dart';
import '../models/firewall_schemas_mode.dart';
import '../models/ip_access_rules_for_an_account_create_an_ip_access_rule_request.dart';
import '../models/ip_access_rules_for_an_account_list_ip_access_rules_configuration_target.dart';
import '../models/ip_access_rules_for_an_account_list_ip_access_rules_direction.dart';
import '../models/ip_access_rules_for_an_account_list_ip_access_rules_match.dart';
import '../models/ip_access_rules_for_an_account_list_ip_access_rules_order.dart';
import '../models/response_common31.dart';

/// IpAccessRulesForAnAccountApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class IpAccessRulesForAnAccountApi {
  const IpAccessRulesForAnAccountApi(this._config);

  final ApiConfig _config;

  /// List IP Access rules
  ///
  /// Fetches IP Access rules of an account. These rules apply to all the zones in the account. You can filter the results using several optional parameters.
  ///
  /// `GET /accounts/{account_id}/firewall/access_rules/rules`
  Future<ApiResult<ResponseCommon31>>
  ipAccessRulesForAnAccountListIpAccessRules({
    required String accountId,
    FirewallSchemasMode? mode,
    IpAccessRulesForAnAccountListIpAccessRulesConfigurationTarget?
    configurationTarget,
    String? configurationValue,
    String? notes,
    IpAccessRulesForAnAccountListIpAccessRulesMatch? match,
    double? page,
    double? perPage,
    IpAccessRulesForAnAccountListIpAccessRulesOrder? order,
    IpAccessRulesForAnAccountListIpAccessRulesDirection? direction,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/firewall/access_rules/rules',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (mode != null) 'mode': mode.toJson(),
        if (configurationTarget != null)
          'configuration.target': configurationTarget.toJson(),
        'configuration.value': ?configurationValue,
        'notes': ?notes,
        if (match != null) 'match': match.toJson(),
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (order != null) 'order': order.toJson(),
        if (direction != null) 'direction': direction.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create an IP Access rule
  ///
  /// Creates a new IP Access rule for an account. The rule will apply to all zones in the account.
  ///
  /// Note: To create an IP Access rule that applies to a single zone, refer to the [IP Access rules for a zone](#ip-access-rules-for-a-zone) endpoints.
  ///
  /// `POST /accounts/{account_id}/firewall/access_rules/rules`
  Future<ApiResult<ResponseCommon31>>
  ipAccessRulesForAnAccountCreateAnIpAccessRule({
    required String accountId,
    required IpAccessRulesForAnAccountCreateAnIpAccessRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/firewall/access_rules/rules',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get an IP Access rule
  ///
  /// Fetches the details of an IP Access rule defined at the account level.
  ///
  /// `GET /accounts/{account_id}/firewall/access_rules/rules/{rule_id}`
  Future<ApiResult<ResponseCommon31>>
  ipAccessRulesForAnAccountGetAnIpAccessRule({
    required String ruleId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/firewall/access_rules/rules/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update an IP Access rule
  ///
  /// Updates an IP Access rule defined at the account level.
  ///
  /// Note: This operation will affect all zones in the account.
  ///
  /// `PATCH /accounts/{account_id}/firewall/access_rules/rules/{rule_id}`
  Future<ApiResult<ResponseCommon31>>
  ipAccessRulesForAnAccountUpdateAnIpAccessRule({
    required String ruleId,
    required String accountId,
    required FirewallRule body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/firewall/access_rules/rules/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete an IP Access rule
  ///
  /// Deletes an existing IP Access rule defined at the account level.
  ///
  /// Note: This operation will affect all zones in the account.
  ///
  /// `DELETE /accounts/{account_id}/firewall/access_rules/rules/{rule_id}`
  Future<ApiResult<ResponseCommon31>>
  ipAccessRulesForAnAccountDeleteAnIpAccessRule({
    required String ruleId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/firewall/access_rules/rules/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
