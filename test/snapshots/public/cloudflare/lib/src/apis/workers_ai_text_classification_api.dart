//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/workers_ai_post_run_cf_baai_bge_reranker_base_request.dart';import '../models/workers_ai_post_run_cf_huggingface_distilbert_sst2_int8_request.dart';/// WorkersAiTextClassificationApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiTextClassificationApi {const WorkersAiTextClassificationApi(this._config);

final ApiConfig _config;

/// Execute @cf/baai/bge-reranker-base model.
///
/// Runs inference on the @cf/baai/bge-reranker-base model.
///
/// `POST /accounts/{account_id}/ai/run/@cf/baai/bge-reranker-base`
Future<ApiResult<Map<String, String>>> workersAiPostRunCfBaaiBgeRerankerBase({required String accountId, String? queueRequest, String? tags, WorkersAiPostRunCfBaaiBgeRerankerBaseRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/baai/bge-reranker-base',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    'queueRequest': ?queueRequest,
    'tags': ?tags,
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Execute @cf/huggingface/distilbert-sst-2-int8 model.
///
/// Runs inference on the @cf/huggingface/distilbert-sst-2-int8 model.
///
/// `POST /accounts/{account_id}/ai/run/@cf/huggingface/distilbert-sst-2-int8`
Future<ApiResult<Map<String, String>>> workersAiPostRunCfHuggingfaceDistilbertSst2Int8({required String accountId, String? queueRequest, String? tags, WorkersAiPostRunCfHuggingfaceDistilbertSst2Int8Request? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/huggingface/distilbert-sst-2-int8',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  queryParameters: {
    'queueRequest': ?queueRequest,
    'tags': ?tags,
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return (jsonDecode(response.body) as Map<String, dynamic>).map((k, v) => MapEntry(k, v as String));
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
