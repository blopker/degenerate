//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common28.dart';

/// BotnetThreatFeedApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class BotnetThreatFeedApi {
  const BotnetThreatFeedApi(this._config);

  final ApiConfig _config;

  /// Get daily report
  ///
  /// Gets all the data the botnet tracking database has for a given ASN registered to user account for given date. If no date is given, it will return results for the previous day.
  ///
  /// `GET /accounts/{account_id}/botnet_feed/asn/{asn_id}/day_report`
  Future<ApiResult<ResponseCommon28>> botnetThreatFeedGetDayReport({
    required String accountId,
    required int asnId,
    DateTime? date,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/botnet_feed/asn/${Uri.encodeComponent(asnId.toString())}/day_report',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (date != null) 'date': date.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon28.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get full report
  ///
  /// Gets all the data the botnet threat feed tracking database has for a given ASN registered to user account.
  ///
  /// `GET /accounts/{account_id}/botnet_feed/asn/{asn_id}/full_report`
  Future<ApiResult<ResponseCommon28>> botnetThreatFeedGetFullReport({
    required String accountId,
    required int asnId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/botnet_feed/asn/${Uri.encodeComponent(asnId.toString())}/full_report',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon28.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get list of ASNs
  ///
  /// Gets a list of all ASNs registered for a user for the DDoS Botnet Feed API.
  ///
  /// `GET /accounts/{account_id}/botnet_feed/configs/asn`
  Future<ApiResult<ResponseCommon28>> botnetThreatFeedListAsn({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/botnet_feed/configs/asn',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon28.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete an ASN
  ///
  /// Delete an ASN from botnet threat feed for a given user.
  ///
  /// `DELETE /accounts/{account_id}/botnet_feed/configs/asn/{asn_id}`
  Future<ApiResult<ResponseCommon28>> botnetThreatFeedDeleteAsn({
    required String accountId,
    required int asnId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/botnet_feed/configs/asn/${Uri.encodeComponent(asnId.toString())}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon28.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
