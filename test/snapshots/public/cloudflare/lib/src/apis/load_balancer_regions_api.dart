//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/load_balancing_region_code.dart';
import '../models/response_common42.dart';

/// LoadBalancerRegionsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class LoadBalancerRegionsApi {
  const LoadBalancerRegionsApi(this._config);

  final ApiConfig _config;

  /// List Regions
  ///
  /// List all region mappings.
  ///
  /// `GET /accounts/{account_id}/load_balancers/regions`
  Future<ApiResult<ResponseCommon42>> loadBalancerRegionsListRegions({
    required String accountId,
    String? subdivisionCode,
    String? subdivisionCodeA2,
    String? countryCodeA2,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/load_balancers/regions',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'subdivision_code': ?subdivisionCode,
        'subdivision_code_a2': ?subdivisionCodeA2,
        'country_code_a2': ?countryCodeA2,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Region
  ///
  /// Get a single region mapping.
  ///
  /// `GET /accounts/{account_id}/load_balancers/regions/{region_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerRegionsGetRegion({
    required LoadBalancingRegionCode regionId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/load_balancers/regions/${Uri.encodeComponent(regionId.toString())}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
