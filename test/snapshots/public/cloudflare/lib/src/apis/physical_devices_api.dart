//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/delete_device_response.dart';
import '../models/delete_registrations_response.dart';
import '../models/get_device_response.dart';
import '../models/list_devices_active_registrations.dart';
import '../models/list_devices_response.dart';
import '../models/list_devices_sort_by.dart';
import '../models/list_devices_sort_order.dart';
import '../models/revoke_device_response.dart';

/// PhysicalDevicesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class PhysicalDevicesApi {
  const PhysicalDevicesApi(this._config);

  final ApiConfig _config;

  /// List devices
  ///
  /// Lists WARP devices.
  ///
  /// `GET /accounts/{account_id}/devices/physical-devices`
  Future<ApiResult<ListDevicesResponse>> listDevices({
    required String accountId,
    String? cursor,
    ListDevicesSortBy? sortBy,
    ListDevicesSortOrder? sortOrder,
    String? lastSeenUserEmail,
    String? seenAfter,
    String? seenBefore,
    int? perPage,
    String? search,
    ListDevicesActiveRegistrations? activeRegistrations,
    List<String>? id,
    String? include,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/physical-devices',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'cursor': ?cursor,
        if (sortBy != null) 'sort_by': sortBy.toJson(),
        if (sortOrder != null) 'sort_order': sortOrder.toJson(),
        'last_seen_user.email': ?lastSeenUserEmail,
        'seen_after': ?seenAfter,
        'seen_before': ?seenBefore,
        if (perPage != null) 'per_page': perPage.toString(),
        'search': ?search,
        if (activeRegistrations != null)
          'active_registrations': activeRegistrations.toJson(),
        if (id != null) 'id': id.toString(),
        'include': ?include,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListDevicesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get device
  ///
  /// Fetches a single WARP device.
  ///
  /// `GET /accounts/{account_id}/devices/physical-devices/{device_id}`
  Future<ApiResult<GetDeviceResponse>> getDevice({
    required String deviceId,
    required String accountId,
    String? include,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/physical-devices/${Uri.encodeComponent(deviceId)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {'include': ?include},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetDeviceResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete device
  ///
  /// Deletes a WARP device.
  ///
  /// `DELETE /accounts/{account_id}/devices/physical-devices/{device_id}`
  Future<ApiResult<DeleteDeviceResponse>> deleteDevice({
    required String deviceId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/physical-devices/${Uri.encodeComponent(deviceId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeleteDeviceResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Revoke device registrations
  ///
  /// Revokes all WARP registrations associated with the specified device.
  ///
  /// `POST /accounts/{account_id}/devices/physical-devices/{device_id}/revoke`
  Future<ApiResult<RevokeDeviceResponse>> revokeDevice({
    required String accountId,
    required String deviceId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/physical-devices/${Uri.encodeComponent(deviceId)}/revoke',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RevokeDeviceResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete registrations
  ///
  /// Deletes a list of WARP registrations.
  ///
  /// `DELETE /accounts/{account_id}/devices/registrations`
  Future<ApiResult<DeleteRegistrationsResponse>> deleteRegistrations({
    required String accountId,
    required List<String> id,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations',
      headers: {..._config.defaultHeaders},
      queryParameters: {'id': id.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeleteRegistrationsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
