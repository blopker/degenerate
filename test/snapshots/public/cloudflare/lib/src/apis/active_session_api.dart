//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/create_poll_request.dart';import '../models/create_poll_response.dart';import '../models/get_active_session_response.dart';import '../models/kick_all_participants_response.dart';import '../models/kick_partcipants_request.dart';import '../models/kick_partcipants_response.dart';import '../models/mute_all_participants_request.dart';import '../models/mute_all_participants_response.dart';import '../models/mute_participants_request.dart';import '../models/mute_participants_response.dart';/// ActiveSessionApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ActiveSessionApi {const ActiveSessionApi(this._config);

final ApiConfig _config;

/// Fetch details of an active session
///
/// Returns details of an ongoing active session for the given meeting ID.
///
/// `GET /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session`
Future<ApiResult<GetActiveSessionResponse>> getActiveSession({required String accountId, required String appId, required String meetingId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return GetActiveSessionResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Kick participants from an active session
///
/// Kicks one or more participants from an active session using user ID or custom participant ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session/kick`
Future<ApiResult<KickPartcipantsResponse>> kickPartcipants({required String accountId, required String appId, required String meetingId, required KickPartcipantsRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session/kick',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return KickPartcipantsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Kick all participants
///
/// Kicks all participants from an active session for the given meeting ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session/kick-all`
Future<ApiResult<KickAllParticipantsResponse>> kickAllParticipants({required String accountId, required String appId, required String meetingId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session/kick-all',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return KickAllParticipantsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Mute participants of an active session
///
/// Mutes one or more participants from an active session using user ID or custom participant ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session/mute`
Future<ApiResult<MuteParticipantsResponse>> muteParticipants({required String accountId, required String appId, required String meetingId, required MuteParticipantsRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session/mute',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return MuteParticipantsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Mute all participants
///
/// Mutes all participants of an active session for the given meeting ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session/mute-all`
Future<ApiResult<MuteAllParticipantsResponse>> muteAllParticipants({required String accountId, required String appId, required String meetingId, required MuteAllParticipantsRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session/mute-all',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return MuteAllParticipantsResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a poll
///
/// Creates a new poll in an active session for the given meeting ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/active-session/poll`
Future<ApiResult<CreatePollResponse>> createPoll({required String accountId, required String appId, required String meetingId, required CreatePollRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/active-session/poll',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return CreatePollResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
