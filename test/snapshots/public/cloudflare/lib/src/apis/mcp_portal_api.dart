//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/create_portals_request.dart';
import '../models/create_portals_response.dart';
import '../models/delete_portals_response.dart';
import '../models/fetch_gateways_response.dart';
import '../models/list_portals_response.dart';
import '../models/update_portals_request.dart';
import '../models/update_portals_response.dart';

/// McpPortalApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class McpPortalApi {
  const McpPortalApi(this._config);

  final ApiConfig _config;

  /// List MCP Portals
  ///
  /// `GET /accounts/{account_id}/access/ai-controls/mcp/portals`
  Future<ApiResult<ListPortalsResponse>> mcpPortalsApiListPortals({
    required String accountId,
    int? page,
    int? perPage,
    String? search,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/ai-controls/mcp/portals',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        'search': ?search,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListPortalsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new MCP Portal
  ///
  /// `POST /accounts/{account_id}/access/ai-controls/mcp/portals`
  Future<ApiResult<CreatePortalsResponse>> mcpPortalsApiCreatePortals({
    required String accountId,
    CreatePortalsRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/ai-controls/mcp/portals',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return CreatePortalsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Read details of an MCP Portal
  ///
  /// `GET /accounts/{account_id}/access/ai-controls/mcp/portals/{id}`
  Future<ApiResult<FetchGatewaysResponse>> mcpPortalsApiFetchGateways({
    required String id,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/ai-controls/mcp/portals/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return FetchGatewaysResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a MCP Portal
  ///
  /// `PUT /accounts/{account_id}/access/ai-controls/mcp/portals/{id}`
  Future<ApiResult<UpdatePortalsResponse>> mcpPortalsApiUpdatePortals({
    required String id,
    required String accountId,
    UpdatePortalsRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/ai-controls/mcp/portals/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return UpdatePortalsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a MCP Portal
  ///
  /// `DELETE /accounts/{account_id}/access/ai-controls/mcp/portals/{id}`
  Future<ApiResult<DeletePortalsResponse>> mcpPortalsApiDeletePortals({
    required String accountId,
    required String id,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/ai-controls/mcp/portals/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeletePortalsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
