//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/workers_ai_post_run_cf_deepgram_aura1_request.dart';
import '../models/workers_ai_post_run_cf_deepgram_aura2_en_request.dart';
import '../models/workers_ai_post_run_cf_deepgram_aura2_es_request.dart';
import '../models/workers_ai_post_run_cf_myshell_ai_melotts_request.dart';

/// WorkersAiTextToSpeechApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiTextToSpeechApi {
  const WorkersAiTextToSpeechApi(this._config);

  final ApiConfig _config;

  /// Open Websocket connection with @cf/deepgram/aura model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Open Websocket connection with @cf/deepgram/aura-1 model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura-1 model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura-1`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura1({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-1',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/deepgram/aura-1 model.
  ///
  /// Runs inference on the @cf/deepgram/aura-1 model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/deepgram/aura-1`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfDeepgramAura1({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfDeepgramAura1Request? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-1',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Open Websocket connection with @cf/deepgram/aura-1-internal model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura-1-internal model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura-1-internal`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura1Internal({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-1-internal',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Open Websocket connection with @cf/deepgram/aura-2 model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura-2 model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura-2`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura2({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-2',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Open Websocket connection with @cf/deepgram/aura-2-en model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura-2-en model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura-2-en`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura2En({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-2-en',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/deepgram/aura-2-en model.
  ///
  /// Runs inference on the @cf/deepgram/aura-2-en model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/deepgram/aura-2-en`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfDeepgramAura2En({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfDeepgramAura2EnRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-2-en',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Open Websocket connection with @cf/deepgram/aura-2-es model.
  ///
  /// Opens a WebSocket connection to stream inference results from the @cf/deepgram/aura-2-es model.
  ///
  /// `GET /accounts/{account_id}/ai/run/@cf/deepgram/aura-2-es`
  Future<ApiResult<void>> workersAiPostWebsocketRunCfDeepgramAura2Es({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-2-es',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Execute @cf/deepgram/aura-2-es model.
  ///
  /// Runs inference on the @cf/deepgram/aura-2-es model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/deepgram/aura-2-es`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfDeepgramAura2Es({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfDeepgramAura2EsRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/deepgram/aura-2-es',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Execute @cf/myshell-ai/melotts model.
  ///
  /// Runs inference on the @cf/myshell-ai/melotts model.
  ///
  /// `POST /accounts/{account_id}/ai/run/@cf/myshell-ai/melotts`
  Future<ApiResult<Map<String, String>>> workersAiPostRunCfMyshellAiMelotts({
    required String accountId,
    String? queueRequest,
    String? tags,
    WorkersAiPostRunCfMyshellAiMelottsRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/myshell-ai/melotts',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {'queueRequest': ?queueRequest, 'tags': ?tags},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return (jsonDecode(response.body) as Map<String, dynamic>).map(
          (k, v) => MapEntry(k, v as String),
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
