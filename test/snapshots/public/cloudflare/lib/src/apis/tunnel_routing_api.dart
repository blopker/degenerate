//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common69.dart';
import '../models/tunnel_route_create_a_tunnel_route_request.dart';
import '../models/tunnel_route_update_a_tunnel_route_request.dart';
import '../models/tunnel_tunnel_type.dart';

/// TunnelRoutingApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TunnelRoutingApi {
  const TunnelRoutingApi(this._config);

  final ApiConfig _config;

  /// List tunnel routes
  ///
  /// Lists and filters private network routes in an account.
  ///
  /// `GET /accounts/{account_id}/teamnet/routes`
  Future<ApiResult<ResponseCommon69>> tunnelRouteListTunnelRoutes({
    required String accountId,
    String? comment,
    bool? isDeleted,
    String? networkSubset,
    String? networkSuperset,
    String? existedAt,
    String? tunnelId,
    String? routeId,
    List<TunnelTunnelType>? tunTypes,
    String? virtualNetworkId,
    double? perPage,
    double? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'comment': ?comment,
        if (isDeleted != null) 'is_deleted': isDeleted.toString(),
        'network_subset': ?networkSubset,
        'network_superset': ?networkSuperset,
        'existed_at': ?existedAt,
        'tunnel_id': ?tunnelId,
        'route_id': ?routeId,
        if (tunTypes != null) 'tun_types': tunTypes.toString(),
        'virtual_network_id': ?virtualNetworkId,
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a tunnel route
  ///
  /// Routes a private network through a Cloudflare Tunnel.
  ///
  /// `POST /accounts/{account_id}/teamnet/routes`
  Future<ApiResult<ResponseCommon69>> tunnelRouteCreateATunnelRoute({
    required String accountId,
    required TunnelRouteCreateATunnelRouteRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get tunnel route
  ///
  /// Get a private network route in an account.
  ///
  /// `GET /accounts/{account_id}/teamnet/routes/{route_id}`
  Future<ApiResult<ResponseCommon69>> tunnelRouteGetTunnelRoute({
    required String accountId,
    required String routeId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes/${Uri.encodeComponent(routeId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a tunnel route
  ///
  /// Updates an existing private network route in an account. The fields that are meant to be updated should be provided in the body of the request.
  ///
  /// `PATCH /accounts/{account_id}/teamnet/routes/{route_id}`
  Future<ApiResult<ResponseCommon69>> tunnelRouteUpdateATunnelRoute({
    required String routeId,
    required String accountId,
    required TunnelRouteUpdateATunnelRouteRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes/${Uri.encodeComponent(routeId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a tunnel route
  ///
  /// Deletes a private network route from an account.
  ///
  ///
  /// `DELETE /accounts/{account_id}/teamnet/routes/{route_id}`
  Future<ApiResult<ResponseCommon69>> tunnelRouteDeleteATunnelRoute({
    required String routeId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes/${Uri.encodeComponent(routeId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get tunnel route by IP
  ///
  /// Fetches routes that contain the given IP address.
  ///
  /// `GET /accounts/{account_id}/teamnet/routes/ip/{ip}`
  Future<ApiResult<ResponseCommon69>> tunnelRouteGetTunnelRouteByIp({
    required String ip,
    required String accountId,
    String? virtualNetworkId,
    bool? defaultVirtualNetworkFallback,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/teamnet/routes/ip/${Uri.encodeComponent(ip)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'virtual_network_id': ?virtualNetworkId,
        if (defaultVirtualNetworkFallback != null)
          'default_virtual_network_fallback': defaultVirtualNetworkFallback
              .toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
