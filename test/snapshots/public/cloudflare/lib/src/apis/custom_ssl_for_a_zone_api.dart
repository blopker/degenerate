//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/custom_ssl_for_a_zone_create_ssl_configuration_request.dart';
import '../models/custom_ssl_for_a_zone_edit_ssl_configuration_request.dart';
import '../models/custom_ssl_for_a_zone_list_ssl_configurations_match.dart';
import '../models/custom_ssl_for_a_zone_re_prioritize_ssl_certificates_request.dart';
import '../models/response_common68.dart';

/// CustomSslForAZoneApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class CustomSslForAZoneApi {
  const CustomSslForAZoneApi(this._config);

  final ApiConfig _config;

  /// List SSL Configurations
  ///
  /// List, search, and filter all of your custom SSL certificates. The higher priority will break ties across overlapping 'legacy_custom' certificates, but 'legacy_custom' certificates will always supercede 'sni_custom' certificates.
  ///
  /// `GET /zones/{zone_id}/custom_certificates`
  Future<ApiResult<ResponseCommon68>> customSslForAZoneListSslConfigurations({
    required String zoneId,
    double? page,
    double? perPage,
    CustomSslForAZoneListSslConfigurationsMatch? match,
    String? status,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (match != null) 'match': match.toJson(),
        'status': ?status,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create SSL Configuration
  ///
  /// Upload a new SSL certificate for a zone.
  ///
  /// `POST /zones/{zone_id}/custom_certificates`
  Future<ApiResult<ResponseCommon68>> customSslForAZoneCreateSslConfiguration({
    required String zoneId,
    required CustomSslForAZoneCreateSslConfigurationRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// SSL Configuration Details
  ///
  /// Retrieves details for a specific custom SSL certificate, including certificate metadata, bundle method, geographic restrictions, and associated keyless server configuration.
  ///
  /// `GET /zones/{zone_id}/custom_certificates/{custom_certificate_id}`
  Future<ApiResult<ResponseCommon68>> customSslForAZoneSslConfigurationDetails({
    required String customCertificateId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates/${Uri.encodeComponent(customCertificateId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Edit SSL Configuration
  ///
  /// Upload a new private key and/or PEM/CRT for the SSL certificate. Note: PATCHing a configuration for sni_custom certificates will result in a new resource id being returned, and the previous one being deleted.
  ///
  /// `PATCH /zones/{zone_id}/custom_certificates/{custom_certificate_id}`
  Future<ApiResult<ResponseCommon68>> customSslForAZoneEditSslConfiguration({
    required String customCertificateId,
    required String zoneId,
    required CustomSslForAZoneEditSslConfigurationRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates/${Uri.encodeComponent(customCertificateId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete SSL Configuration
  ///
  /// Remove a SSL certificate from a zone.
  ///
  /// `DELETE /zones/{zone_id}/custom_certificates/{custom_certificate_id}`
  Future<ApiResult<ResponseCommon68>> customSslForAZoneDeleteSslConfiguration({
    required String customCertificateId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates/${Uri.encodeComponent(customCertificateId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Re-prioritize SSL Certificates
  ///
  /// If a zone has multiple SSL certificates, you can set the order in which they should be used during a request. The higher priority will break ties across overlapping 'legacy_custom' certificates.
  ///
  /// `PUT /zones/{zone_id}/custom_certificates/prioritize`
  Future<ApiResult<ResponseCommon68>>
  customSslForAZoneRePrioritizeSslCertificates({
    required String zoneId,
    required CustomSslForAZoneRePrioritizeSslCertificatesRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/custom_certificates/prioritize',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon68.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
