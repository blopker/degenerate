//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/cache_schemas_patch.dart';
import '../models/response_single_id2.dart';

/// AutomaticSslTlsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AutomaticSslTlsApi {
  const AutomaticSslTlsApi(this._config);

  final ApiConfig _config;

  /// Get Automatic SSL/TLS enrollment status for the given zone
  ///
  /// If the system is enabled, the response will include next_scheduled_scan, representing the next time this zone will be scanned and the zone's ssl/tls encryption mode is potentially upgraded by the system. If the system is disabled, next_scheduled_scan will not be present in the response body.
  ///
  /// `GET /zones/{zone_id}/settings/ssl_automatic_mode`
  Future<ApiResult<ResponseSingleId2>> sslDetectorAutomaticModeGetEnrollment({
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/settings/ssl_automatic_mode',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseSingleId2.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Patch Automatic SSL/TLS Enrollment status for given zone
  ///
  /// The automatic system is enabled when this endpoint is hit with value in the request body is set to "auto", and disabled when the request body value is set to "custom".
  ///
  /// `PATCH /zones/{zone_id}/settings/ssl_automatic_mode`
  Future<ApiResult<ResponseSingleId2>> sslDetectorAutomaticModePatchEnrollment({
    required String zoneId,
    required CacheSchemasPatch body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path: '/zones/${Uri.encodeComponent(zoneId)}/settings/ssl_automatic_mode',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseSingleId2.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
