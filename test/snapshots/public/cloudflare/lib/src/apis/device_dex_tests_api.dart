//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/device_dex_test_details_kind.dart';
import '../models/digital_experience_monitoring_device_dex_test_schemas_http.dart';
import '../models/response_common19.dart';

/// DeviceDexTestsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DeviceDexTestsApi {
  const DeviceDexTestsApi(this._config);

  final ApiConfig _config;

  /// List Device DEX tests
  ///
  /// Fetch all DEX tests
  ///
  /// `GET /accounts/{account_id}/dex/devices/dex_tests`
  Future<ApiResult<ResponseCommon19>> deviceDexTestDetails({
    required String accountId,
    double? page,
    double? perPage,
    String? testName,
    DeviceDexTestDetailsKind? kind,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dex/devices/dex_tests',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        'testName': ?testName,
        if (kind != null) 'kind': kind.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon19.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create Device DEX test
  ///
  /// Create a DEX test.
  ///
  /// `POST /accounts/{account_id}/dex/devices/dex_tests`
  Future<ApiResult<ResponseCommon19>> deviceDexTestCreateDeviceDexTest({
    required String accountId,
    required DigitalExperienceMonitoringDeviceDexTestSchemasHttp body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dex/devices/dex_tests',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon19.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Device DEX test
  ///
  /// Fetch a single DEX test.
  ///
  /// `GET /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}`
  Future<ApiResult<ResponseCommon19>> deviceDexTestGetDeviceDexTest({
    required String accountId,
    required String dexTestId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dex/devices/dex_tests/${Uri.encodeComponent(dexTestId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon19.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Device DEX test
  ///
  /// Update a DEX test.
  ///
  /// `PUT /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}`
  Future<ApiResult<ResponseCommon19>> deviceDexTestUpdateDeviceDexTest({
    required String accountId,
    required String dexTestId,
    required DigitalExperienceMonitoringDeviceDexTestSchemasHttp body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dex/devices/dex_tests/${Uri.encodeComponent(dexTestId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon19.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Device DEX test
  ///
  /// Delete a Device DEX test. Returns the remaining device dex tests for the account.
  ///
  /// `DELETE /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}`
  Future<ApiResult<ResponseCommon19>> deviceDexTestDeleteDeviceDexTest({
    required String accountId,
    required String dexTestId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dex/devices/dex_tests/${Uri.encodeComponent(dexTestId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon19.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
