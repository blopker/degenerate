//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/query.dart';
import '../models/query_bulk.dart';
import '../models/query_match.dart';

/// DomainSearchApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DomainSearchApi {
  const DomainSearchApi(this._config);

  final ApiConfig _config;

  /// Read matches for string queries by ID
  ///
  /// Return matches for string queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/matches`
  Future<ApiResult<QueryMatch>> getAccountsBrandProtectionMatches({
    required String accountId,
    String? id,
    int? offset,
    int? limit,
    bool? includeDomainId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/matches',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'id': ?id,
        if (offset != null) 'offset': offset.toString(),
        if (limit != null) 'limit': limit.toString(),
        if (includeDomainId != null)
          'include_domain_id': includeDomainId.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return QueryMatch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Download matches for string queries by ID
  ///
  /// Return matches as CSV for string queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/matches/download`
  Future<ApiResult<QueryMatch>> getAccountsBrandProtectionMatchesDownload({
    required String accountId,
    String? id,
    int? offset,
    int? limit,
    bool? includeDomainId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/matches/download',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'id': ?id,
        if (offset != null) 'offset': offset.toString(),
        if (limit != null) 'limit': limit.toString(),
        if (includeDomainId != null)
          'include_domain_id': includeDomainId.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return QueryMatch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Read string queries by ID
  ///
  /// Return string queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/queries`
  Future<ApiResult<void>> getAccountsBrandProtectionQueries({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/queries',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new saved string queries
  ///
  /// Return a success message after creating new saved string queries
  ///
  /// `POST /accounts/{account_id}/brand-protection/queries`
  Future<ApiResult<void>> postAccountsBrandProtectionQueries({
    required String accountId,
    String? id,
    String? tag,
    bool? scan,
    required Query body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/queries',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {
        'id': ?id,
        'tag': ?tag,
        if (scan != null) 'scan': scan.toString(),
      },
      body: jsonEncode(body.toJson()),
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Update saved string queries by ID
  ///
  /// Return a success message after updating saved string queries by ID
  ///
  /// `PATCH /accounts/{account_id}/brand-protection/queries`
  Future<ApiResult<void>> patchAccountsBrandProtectionQueries({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/queries',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Delete saved string queries by ID
  ///
  /// Return a success message after deleting saved string queries by ID
  ///
  /// `DELETE /accounts/{account_id}/brand-protection/queries`
  Future<ApiResult<void>> deleteAccountsBrandProtectionQueries({
    required String accountId,
    String? id,
    String? tag,
    bool? scan,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/queries',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'id': ?id,
        'tag': ?tag,
        if (scan != null) 'scan': scan.toString(),
      },
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new saved string queries in bulk
  ///
  /// Return a success message after creating new saved string queries in bulk
  ///
  /// `POST /accounts/{account_id}/brand-protection/queries/bulk`
  Future<ApiResult<void>> postAccountsBrandProtectionQueriesBulk({
    required String accountId,
    required QueryBulk body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/queries/bulk',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new string queries
  ///
  /// Return new string queries
  ///
  /// `POST /accounts/{account_id}/brand-protection/search`
  Future<ApiResult<void>> postAccountsBrandProtectionSearch({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/search',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Read the total number of saved string queries
  ///
  /// Return the total number of saved string queries
  ///
  /// `GET /accounts/{account_id}/brand-protection/total-queries`
  Future<ApiResult<void>> getAccountsBrandProtectionTotalQueries({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/total-queries',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
