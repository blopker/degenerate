//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common31.dart';
import '../models/user_agent_blocking_rules_create_a_user_agent_blocking_rule_request.dart';
import '../models/user_agent_blocking_rules_update_a_user_agent_blocking_rule_request.dart';

/// UserAgentBlockingRulesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class UserAgentBlockingRulesApi {
  const UserAgentBlockingRulesApi(this._config);

  final ApiConfig _config;

  /// List User Agent Blocking rules
  ///
  /// Fetches User Agent Blocking rules in a zone. You can filter the results using several optional parameters.
  ///
  /// `GET /zones/{zone_id}/firewall/ua_rules`
  Future<ApiResult<ResponseCommon31>>
  userAgentBlockingRulesListUserAgentBlockingRules({
    required String zoneId,
    double? page,
    String? description,
    double? perPage,
    String? userAgent,
    bool? paused,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/firewall/ua_rules',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        'description': ?description,
        if (perPage != null) 'per_page': perPage.toString(),
        'user_agent': ?userAgent,
        if (paused != null) 'paused': paused.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a User Agent Blocking rule
  ///
  /// Creates a new User Agent Blocking rule in a zone.
  ///
  /// `POST /zones/{zone_id}/firewall/ua_rules`
  Future<ApiResult<ResponseCommon31>>
  userAgentBlockingRulesCreateAUserAgentBlockingRule({
    required String zoneId,
    required UserAgentBlockingRulesCreateAUserAgentBlockingRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/zones/${Uri.encodeComponent(zoneId)}/firewall/ua_rules',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a User Agent Blocking rule
  ///
  /// Fetches the details of a User Agent Blocking rule.
  ///
  /// `GET /zones/{zone_id}/firewall/ua_rules/{ua_rule_id}`
  Future<ApiResult<ResponseCommon31>>
  userAgentBlockingRulesGetAUserAgentBlockingRule({
    required String uaRuleId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/ua_rules/${Uri.encodeComponent(uaRuleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a User Agent Blocking rule
  ///
  /// Updates an existing User Agent Blocking rule.
  ///
  /// `PUT /zones/{zone_id}/firewall/ua_rules/{ua_rule_id}`
  Future<ApiResult<ResponseCommon31>>
  userAgentBlockingRulesUpdateAUserAgentBlockingRule({
    required String uaRuleId,
    required String zoneId,
    required UserAgentBlockingRulesUpdateAUserAgentBlockingRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/ua_rules/${Uri.encodeComponent(uaRuleId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a User Agent Blocking rule
  ///
  /// Deletes an existing User Agent Blocking rule.
  ///
  /// `DELETE /zones/{zone_id}/firewall/ua_rules/{ua_rule_id}`
  Future<ApiResult<ResponseCommon31>>
  userAgentBlockingRulesDeleteAUserAgentBlockingRule({
    required String uaRuleId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/ua_rules/${Uri.encodeComponent(uaRuleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
