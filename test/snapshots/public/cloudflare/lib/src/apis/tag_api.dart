//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/delete_tag_delete_response.dart';
import '../models/get_tag_indicators_list_response.dart';
import '../models/get_tag_list_response.dart';
import '../models/patch_tag_update_request.dart';
import '../models/patch_tag_update_response.dart';
import '../models/post_tag_create_request.dart';
import '../models/post_tag_create_response.dart';

/// TagApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TagApi {
  const TagApi(this._config);

  final ApiConfig _config;

  /// List indicators related to a tag
  ///
  /// Returns indicators associated with the provided tag UUID across all indicator datasets, with pagination.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/tags/{tag_uuid}/indicators`
  Future<ApiResult<GetTagIndicatorsListResponse>> getTagIndicatorsList({
    required String accountId,
    required String tagUuid,
    required String datasetId,
    double? page,
    double? pageSize,
    String? indicatorType,
    List<String>? relatedEvent,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/tags/${Uri.encodeComponent(tagUuid)}/indicators',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (pageSize != null) 'pageSize': pageSize.toString(),
        'indicatorType': ?indicatorType,
        if (relatedEvent != null) 'relatedEvent': relatedEvent.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetTagIndicatorsListResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Lists all tags (SoT)
  ///
  /// Returns all Source-of-Truth tags for an account.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/tags`
  Future<ApiResult<GetTagListResponse>> getTagList({
    required String accountId,
    double? page,
    double? pageSize,
    String? search,
    String? categoryUuid,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/tags',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (pageSize != null) 'pageSize': pageSize.toString(),
        'search': ?search,
        'categoryUuid': ?categoryUuid,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetTagListResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Updates a tag (SoT)
  ///
  /// Updates a Source-of-Truth tag by UUID.
  ///
  /// `PATCH /accounts/{account_id}/cloudforce-one/events/tags/{tag_uuid}`
  Future<ApiResult<PatchTagUpdateResponse>> patchTagUpdate({
    required String accountId,
    required String tagUuid,
    PatchTagUpdateRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/tags/${Uri.encodeComponent(tagUuid)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PatchTagUpdateResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes a tag (SoT)
  ///
  /// Deletes a Source-of-Truth tag by UUID.
  ///
  /// `DELETE /accounts/{account_id}/cloudforce-one/events/tags/{tag_uuid}`
  Future<ApiResult<DeleteTagDeleteResponse>> deleteTagDelete({
    required String accountId,
    required String tagUuid,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/tags/${Uri.encodeComponent(tagUuid)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeleteTagDeleteResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates a new tag
  ///
  /// Creates a new tag to be used accross threat events.
  ///
  /// `POST /accounts/{account_id}/cloudforce-one/events/tags/create`
  Future<ApiResult<PostTagCreateResponse>> postTagCreate({
    required String accountId,
    PostTagCreateRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/tags/create',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PostTagCreateResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
