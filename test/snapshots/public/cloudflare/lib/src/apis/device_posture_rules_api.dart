//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/device_posture_rules_create_device_posture_rule_request.dart';
import '../models/device_posture_rules_update_device_posture_rule_request.dart';
import '../models/response_common67.dart';

/// DevicePostureRulesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DevicePostureRulesApi {
  const DevicePostureRulesApi(this._config);

  final ApiConfig _config;

  /// List device posture rules
  ///
  /// Fetches device posture rules for a Zero Trust account.
  ///
  /// `GET /accounts/{account_id}/devices/posture`
  Future<ApiResult<ResponseCommon67>> devicePostureRulesListDevicePostureRules({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/devices/posture',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon67.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a device posture rule
  ///
  /// Creates a new device posture rule.
  ///
  /// `POST /accounts/{account_id}/devices/posture`
  Future<ApiResult<ResponseCommon67>>
  devicePostureRulesCreateDevicePostureRule({
    required String accountId,
    required DevicePostureRulesCreateDevicePostureRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/devices/posture',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon67.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get device posture rule details
  ///
  /// Fetches a single device posture rule.
  ///
  /// `GET /accounts/{account_id}/devices/posture/{rule_id}`
  Future<ApiResult<ResponseCommon67>> devicePostureRulesDetails({
    required String ruleId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/posture/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon67.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a device posture rule
  ///
  /// Updates a device posture rule.
  ///
  /// `PUT /accounts/{account_id}/devices/posture/{rule_id}`
  Future<ApiResult<ResponseCommon67>>
  devicePostureRulesUpdateDevicePostureRule({
    required String ruleId,
    required String accountId,
    required DevicePostureRulesUpdateDevicePostureRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/posture/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon67.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a device posture rule
  ///
  /// Deletes a device posture rule.
  ///
  /// `DELETE /accounts/{account_id}/devices/posture/{rule_id}`
  Future<ApiResult<ResponseCommon67>>
  devicePostureRulesDeleteDevicePostureRule({
    required String ruleId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/posture/${Uri.encodeComponent(ruleId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon67.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
