//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common6.dart';
import '../models/shield_credentials.dart';
import '../models/token_validation_config_create_request.dart';
import '../models/token_validation_config_edit_request.dart';

/// TokenValidationTokenConfigurationApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class TokenValidationTokenConfigurationApi {
  const TokenValidationTokenConfigurationApi(this._config);

  final ApiConfig _config;

  /// List token validation configurations
  ///
  /// Lists all token validation configurations for this zone
  ///
  /// `GET /zones/{zone_id}/token_validation/config`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigList({
    required String zoneId,
    int? page,
    int? perPage,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new Token Validation configuration
  ///
  /// `POST /zones/{zone_id}/token_validation/config`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigCreate({
    required String zoneId,
    required TokenValidationConfigCreateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a single Token Configuration
  ///
  /// `GET /zones/{zone_id}/token_validation/config/{config_id}`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigGet({
    required String zoneId,
    required String configId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config/${Uri.encodeComponent(configId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Edit an existing Token Configuration
  ///
  /// Edit fields of an existing Token Configuration
  ///
  /// `PATCH /zones/{zone_id}/token_validation/config/{config_id}`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigEdit({
    required String zoneId,
    required String configId,
    required TokenValidationConfigEditRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config/${Uri.encodeComponent(configId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Token Configuration
  ///
  /// `DELETE /zones/{zone_id}/token_validation/config/{config_id}`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigDelete({
    required String zoneId,
    required String configId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config/${Uri.encodeComponent(configId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Token Configuration credentials
  ///
  /// `PUT /zones/{zone_id}/token_validation/config/{config_id}/credentials`
  Future<ApiResult<ResponseCommon6>> tokenValidationConfigCredentialsUpdate({
    required String zoneId,
    required String configId,
    required ShieldCredentials body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/token_validation/config/${Uri.encodeComponent(configId)}/credentials',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon6.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
