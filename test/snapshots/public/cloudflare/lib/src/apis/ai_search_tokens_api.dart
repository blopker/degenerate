//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/ai_search_create_tokens_request.dart';
import '../models/ai_search_create_tokens_response.dart';
import '../models/ai_search_delete_tokens_response.dart';
import '../models/ai_search_fetch_tokens_response.dart';
import '../models/ai_search_list_tokens_order_by.dart';
import '../models/ai_search_list_tokens_order_by_direction.dart';
import '../models/ai_search_list_tokens_response.dart';
import '../models/ai_search_update_tokens_request.dart';
import '../models/ai_search_update_tokens_response.dart';

/// AiSearchTokensApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AiSearchTokensApi {
  const AiSearchTokensApi(this._config);

  final ApiConfig _config;

  /// List tokens.
  ///
  /// `GET /accounts/{account_id}/ai-search/tokens`
  Future<ApiResult<AiSearchListTokensResponse>> aiSearchListTokens({
    required String accountId,
    int? page,
    int? perPage,
    AiSearchListTokensOrderBy? orderBy,
    AiSearchListTokensOrderByDirection? orderByDirection,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai-search/tokens',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (orderBy != null) 'order_by': orderBy.toJson(),
        if (orderByDirection != null)
          'order_by_direction': orderByDirection.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AiSearchListTokensResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create new tokens.
  ///
  /// Create a new tokens.
  ///
  /// `POST /accounts/{account_id}/ai-search/tokens`
  Future<ApiResult<AiSearchCreateTokensResponse>> aiSearchCreateTokens({
    required String accountId,
    AiSearchCreateTokensRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai-search/tokens',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AiSearchCreateTokensResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Read tokens.
  ///
  /// `GET /accounts/{account_id}/ai-search/tokens/{id}`
  Future<ApiResult<AiSearchFetchTokensResponse>> aiSearchFetchTokens({
    required String accountId,
    required String id,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-search/tokens/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AiSearchFetchTokensResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update tokens.
  ///
  /// `PUT /accounts/{account_id}/ai-search/tokens/{id}`
  Future<ApiResult<AiSearchUpdateTokensResponse>> aiSearchUpdateTokens({
    required String accountId,
    required String id,
    AiSearchUpdateTokensRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-search/tokens/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AiSearchUpdateTokensResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete tokens.
  ///
  /// `DELETE /accounts/{account_id}/ai-search/tokens/{id}`
  Future<ApiResult<AiSearchDeleteTokensResponse>> aiSearchDeleteTokens({
    required String accountId,
    required String id,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-search/tokens/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AiSearchDeleteTokensResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
