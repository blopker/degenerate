//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/delete_indicator_delete_response.dart';
import '../models/get_indicator_list_format.dart';
import '../models/get_indicator_list_legacy_response.dart';
import '../models/get_indicator_list_response.dart';
import '../models/get_indicator_read_response.dart';
import '../models/patch_indicator_update_request.dart';
import '../models/patch_indicator_update_response.dart';
import '../models/post_indicator_create_bulk_request.dart';
import '../models/post_indicator_create_request.dart';
import '../models/post_indicator_create_response.dart';

/// IndicatorApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class IndicatorApi {
  const IndicatorApi(this._config);

  final ApiConfig _config;

  /// Lists indicators
  ///
  /// Retrieves a paginated list of indicators for the account.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators`
  Future<ApiResult<GetIndicatorListLegacyResponse>> getIndicatorListLegacy({
    required String accountId,
    required String datasetId,
    double? page,
    double? pageSize,
    String? search,
    String? indicatorType,
    List<String>? relatedEvent,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (pageSize != null) 'pageSize': pageSize.toString(),
        'search': ?search,
        'indicatorType': ?indicatorType,
        if (relatedEvent != null) 'relatedEvent': relatedEvent.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetIndicatorListLegacyResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Reads an indicator
  ///
  /// Retrieves a specific indicator by its UUID.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/{indicator_id}`
  Future<ApiResult<GetIndicatorReadResponse>> getIndicatorRead({
    required String accountId,
    required String datasetId,
    required String indicatorId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/${Uri.encodeComponent(indicatorId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetIndicatorReadResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Updates an indicator
  ///
  /// Updates an existing indicator's properties.
  ///
  /// `PATCH /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/{indicator_id}`
  Future<ApiResult<PatchIndicatorUpdateResponse>> patchIndicatorUpdate({
    required String accountId,
    required String datasetId,
    required String indicatorId,
    PatchIndicatorUpdateRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/${Uri.encodeComponent(indicatorId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PatchIndicatorUpdateResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes an indicator
  ///
  /// Deletes a specific indicator by its UUID.
  ///
  /// `DELETE /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/{indicator_id}`
  Future<ApiResult<DeleteIndicatorDeleteResponse>> deleteIndicatorDelete({
    required String accountId,
    required String datasetId,
    required String indicatorId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/${Uri.encodeComponent(indicatorId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeleteIndicatorDeleteResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates multiple indicators in bulk
  ///
  /// Creates multiple indicators at once with their respective types and related datasets.
  ///
  /// `POST /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/bulk`
  Future<ApiResult<double>> postIndicatorCreateBulk({
    required String accountId,
    required String datasetId,
    PostIndicatorCreateBulkRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/bulk',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return double.parse(response.body);
      },
    );
  }

  /// Creates a new indicator
  ///
  /// Creates a new indicator with the specified type and related datasets.
  ///
  /// `POST /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/create`
  Future<ApiResult<PostIndicatorCreateResponse>> postIndicatorCreate({
    required String accountId,
    required String datasetId,
    PostIndicatorCreateRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/create',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PostIndicatorCreateResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List mirrored tags for an indicator dataset
  ///
  /// Returns all mirrored tags from the indicator dataset (DO mirror table). No pagination.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/dataset/{dataset_id}/indicators/tags`
  Future<ApiResult<List<Map<String, String>>>> getIndicatorTagsList({
    required String accountId,
    required String datasetId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/dataset/${Uri.encodeComponent(datasetId)}/indicators/tags',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map(
              (e) => (e as Map<String, dynamic>).map(
                (k, v) => MapEntry(k, v as String),
              ),
            )
            .toList();
      },
    );
  }

  /// Lists indicators across multiple datasets
  ///
  /// Retrieves a paginated list of indicators across specified datasets. Use datasetIds=all or datasetIds=* to query all datasets for the account. If no datasetIds provided, uses the default dataset.
  ///
  /// `GET /accounts/{account_id}/cloudforce-one/events/indicators`
  Future<ApiResult<GetIndicatorListResponse>> getIndicatorList({
    required String accountId,
    List<String>? datasetIds,
    double? page,
    double? pageSize,
    String? search,
    String? indicatorType,
    List<String>? relatedEvents,
    List<String>? tags,
    DateTime? createdAfter,
    DateTime? createdBefore,
    double? relatedEventsLimit,
    bool? includeTags,
    bool? includeTotalCount,
    GetIndicatorListFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/cloudforce-one/events/indicators',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (datasetIds != null) 'datasetIds': datasetIds.toString(),
        if (page != null) 'page': page.toString(),
        if (pageSize != null) 'pageSize': pageSize.toString(),
        'search': ?search,
        'indicatorType': ?indicatorType,
        if (relatedEvents != null) 'relatedEvents': relatedEvents.toString(),
        if (tags != null) 'tags': tags.toString(),
        if (createdAfter != null) 'createdAfter': createdAfter.toString(),
        if (createdBefore != null) 'createdBefore': createdBefore.toString(),
        if (relatedEventsLimit != null)
          'relatedEventsLimit': relatedEventsLimit.toString(),
        if (includeTags != null) 'includeTags': includeTags.toString(),
        if (includeTotalCount != null)
          'includeTotalCount': includeTotalCount.toString(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetIndicatorListResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
