//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/load_balancing_monitor_editable.dart';
import '../models/response_common42.dart';

/// LoadBalancerMonitorsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class LoadBalancerMonitorsApi {
  const LoadBalancerMonitorsApi(this._config);

  final ApiConfig _config;

  /// List Monitors
  ///
  /// List configured monitors for a user.
  ///
  /// `GET /user/load_balancers/monitors`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsListMonitors() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/user/load_balancers/monitors',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create Monitor
  ///
  /// Create a configured monitor.
  ///
  /// `POST /user/load_balancers/monitors`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsCreateMonitor({
    required LoadBalancingMonitorEditable body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/user/load_balancers/monitors',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Monitor Details
  ///
  /// List a single configured monitor for a user.
  ///
  /// `GET /user/load_balancers/monitors/{monitor_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsMonitorDetails({
    required String monitorId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Monitor
  ///
  /// Modify a configured monitor.
  ///
  /// `PUT /user/load_balancers/monitors/{monitor_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsUpdateMonitor({
    required String monitorId,
    required LoadBalancingMonitorEditable body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path: '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Patch Monitor
  ///
  /// Apply changes to an existing monitor, overwriting the supplied properties.
  ///
  /// `PATCH /user/load_balancers/monitors/{monitor_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsPatchMonitor({
    required String monitorId,
    required LoadBalancingMonitorEditable body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path: '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Monitor
  ///
  /// Delete a configured monitor.
  ///
  /// `DELETE /user/load_balancers/monitors/{monitor_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsDeleteMonitor({
    required String monitorId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Preview Monitor
  ///
  /// Preview pools using the specified monitor with provided monitor details. The returned preview_id can be used in the preview endpoint to retrieve the results.
  ///
  /// `POST /user/load_balancers/monitors/{monitor_id}/preview`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsPreviewMonitor({
    required String monitorId,
    required LoadBalancingMonitorEditable body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}/preview',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List Monitor References
  ///
  /// Get the list of resources that reference the provided monitor.
  ///
  /// `GET /user/load_balancers/monitors/{monitor_id}/references`
  Future<ApiResult<ResponseCommon42>>
  loadBalancerMonitorsListMonitorReferences({required String monitorId}) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/user/load_balancers/monitors/${Uri.encodeComponent(monitorId)}/references',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Preview Result
  ///
  /// Get the result of a previous preview operation using the provided preview_id.
  ///
  /// `GET /user/load_balancers/preview/{preview_id}`
  Future<ApiResult<ResponseCommon42>> loadBalancerMonitorsPreviewResult({
    required String previewId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/user/load_balancers/preview/${Uri.encodeComponent(previewId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon42.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
