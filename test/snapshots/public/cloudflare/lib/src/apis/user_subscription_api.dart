//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common8.dart';
import '../models/subscription2.dart';
import '../models/user_subscription_delete_user_subscription_response.dart';

/// UserSubscriptionApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class UserSubscriptionApi {
  const UserSubscriptionApi(this._config);

  final ApiConfig _config;

  /// Get User Subscriptions
  ///
  /// Lists all of a user's subscriptions.
  ///
  /// `GET /user/subscriptions`
  Future<ApiResult<ResponseCommon8>>
  userSubscriptionGetUserSubscriptions() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/user/subscriptions',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon8.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update User Subscription
  ///
  /// Updates a user's subscriptions.
  ///
  /// `PUT /user/subscriptions/{identifier}`
  Future<ApiResult<ResponseCommon8>> userSubscriptionUpdateUserSubscription({
    required String identifier,
    required Subscription2 body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path: '/user/subscriptions/${Uri.encodeComponent(identifier)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon8.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete User Subscription
  ///
  /// Deletes a user's subscription.
  ///
  /// `DELETE /user/subscriptions/{identifier}`
  Future<ApiResult<UserSubscriptionDeleteUserSubscriptionResponse>>
  userSubscriptionDeleteUserSubscription({required String identifier}) async {
    final request = ApiRequest(
      method: 'DELETE',
      path: '/user/subscriptions/${Uri.encodeComponent(identifier)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return UserSubscriptionDeleteUserSubscriptionResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
