//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/no_result_response.dart';
import '../models/pay_per_crawl_create_stripe_config_response.dart';
import '../models/pay_per_crawl_get_stripe_config_response.dart';

/// PpcStripeApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class PpcStripeApi {
  const PpcStripeApi(this._config);

  final ApiConfig _config;

  /// Gets the stripe config for a crawler
  ///
  /// Gets the stripe config for a crawler.
  ///
  /// `GET /accounts/{account_id}/pay-per-crawl/crawler/stripe`
  Future<ApiResult<PayPerCrawlGetStripeConfigResponse>>
  payPerCrawlCrawlerGetStripeConfig({required String accountId}) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/crawler/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PayPerCrawlGetStripeConfigResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates the stripe config for a crawler
  ///
  /// Creates the stripe config for a crawler.
  ///
  /// `POST /accounts/{account_id}/pay-per-crawl/crawler/stripe`
  Future<ApiResult<PayPerCrawlCreateStripeConfigResponse>>
  payPerCrawlCrawlerCreateStripeConfig({required String accountId}) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/crawler/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PayPerCrawlCreateStripeConfigResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes the stripe config for a crawler
  ///
  /// Deletes the stripe config for a crawler.
  ///
  /// `DELETE /accounts/{account_id}/pay-per-crawl/crawler/stripe`
  Future<ApiResult<NoResultResponse>> payPerCrawlCrawlerDeleteStripeConfig({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/crawler/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NoResultResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Gets the stripe config for a publisher
  ///
  /// Gets the stripe config for a publisher.
  ///
  /// `GET /accounts/{account_id}/pay-per-crawl/publisher/stripe`
  Future<ApiResult<PayPerCrawlGetStripeConfigResponse>>
  payPerCrawlPublisherGetStripeConfig({required String accountId}) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/publisher/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PayPerCrawlGetStripeConfigResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Creates the stripe config for a publisher
  ///
  /// Creates the stripe config for a publisher.
  ///
  /// `POST /accounts/{account_id}/pay-per-crawl/publisher/stripe`
  Future<ApiResult<PayPerCrawlCreateStripeConfigResponse>>
  payPerCrawlPublisherCreateStripeConfig({required String accountId}) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/publisher/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PayPerCrawlCreateStripeConfigResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deletes the stripe config for a publisher
  ///
  /// Deletes the stripe config for a publisher.
  ///
  /// `DELETE /accounts/{account_id}/pay-per-crawl/publisher/stripe`
  Future<ApiResult<NoResultResponse>> payPerCrawlPublisherDeleteStripeConfig({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pay-per-crawl/publisher/stripe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return NoResultResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
