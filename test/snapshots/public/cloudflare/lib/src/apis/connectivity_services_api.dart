//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/connectivity_services_list_type.dart';
import '../models/infra_service_config.dart';
import '../models/response_common37.dart';

/// ConnectivityServicesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ConnectivityServicesApi {
  const ConnectivityServicesApi(this._config);

  final ApiConfig _config;

  /// List connectivity services
  ///
  /// `GET /accounts/{account_id}/connectivity/directory/services`
  Future<ApiResult<ResponseCommon37>> connectivityServicesList({
    required String accountId,
    ConnectivityServicesListType? type,
    int? page,
    int? perPage,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/connectivity/directory/services',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (type != null) 'type': type.toString(),
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon37.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create connectivity service
  ///
  /// `POST /accounts/{account_id}/connectivity/directory/services`
  Future<ApiResult<ResponseCommon37>> connectivityServicesPost({
    required String accountId,
    required InfraServiceConfig body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/connectivity/directory/services',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon37.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get connectivity service
  ///
  /// `GET /accounts/{account_id}/connectivity/directory/services/{service_id}`
  Future<ApiResult<ResponseCommon37>> connectivityServicesGet({
    required String accountId,
    required String serviceId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/connectivity/directory/services/${Uri.encodeComponent(serviceId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon37.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update connectivity service
  ///
  /// `PUT /accounts/{account_id}/connectivity/directory/services/{service_id}`
  Future<ApiResult<ResponseCommon37>> connectivityServicesPut({
    required String accountId,
    required String serviceId,
    required InfraServiceConfig body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/connectivity/directory/services/${Uri.encodeComponent(serviceId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon37.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete connectivity service
  ///
  /// `DELETE /accounts/{account_id}/connectivity/directory/services/{service_id}`
  Future<ApiResult<void>> connectivityServicesDelete({
    required String accountId,
    required String serviceId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/connectivity/directory/services/${Uri.encodeComponent(serviceId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
