//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/image_file.dart';
import '../models/logo.dart';
import '../models/logo_match.dart';

/// LogoMatchApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class LogoMatchApi {
  const LogoMatchApi(this._config);

  final ApiConfig _config;

  /// Read matches for logo queries by ID
  ///
  /// Return matches for logo queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/logo-matches`
  Future<ApiResult<LogoMatch>> getAccountsBrandProtectionLogoMatches({
    required String accountId,
    List<String>? logoId,
    String? offset,
    String? limit,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logo-matches',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (logoId != null) 'logo_id': logoId.toString(),
        'offset': ?offset,
        'limit': ?limit,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return LogoMatch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Download matches for logo queries by ID
  ///
  /// Return matches as CSV for logo queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/logo-matches/download`
  Future<ApiResult<LogoMatch>> getAccountsBrandProtectionLogoMatchesDownload({
    required String accountId,
    List<String>? logoId,
    String? offset,
    String? limit,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logo-matches/download',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (logoId != null) 'logo_id': logoId.toString(),
        'offset': ?offset,
        'limit': ?limit,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return LogoMatch.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Read all saved logo queries
  ///
  /// Return all saved logo queries
  ///
  /// `GET /accounts/{account_id}/brand-protection/logos`
  Future<ApiResult<void>> getAccountsBrandProtectionLogos({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logos',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new saved logo queries from image files
  ///
  /// Return new saved logo queries created from image files
  ///
  /// `POST /accounts/{account_id}/brand-protection/logos`
  Future<ApiResult<Logo>> postAccountsBrandProtectionLogos({
    required String accountId,
    String? tag,
    String? matchType,
    double? threshold,
    required ImageFile body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logos',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      queryParameters: {
        'tag': ?tag,
        'match_type': ?matchType,
        if (threshold != null) 'threshold': threshold.toString(),
      },
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return Logo.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
      },
    );
  }

  /// Read saved logo queries by ID
  ///
  /// Return saved logo queries based on ID
  ///
  /// `GET /accounts/{account_id}/brand-protection/logos/{logo_id}`
  Future<ApiResult<void>> getAccountsBrandProtectionLogos2({
    required String accountId,
    required String logoId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logos/${Uri.encodeComponent(logoId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Delete saved logo queries by ID
  ///
  /// Return a success message after deleting saved logo queries by ID
  ///
  /// `DELETE /accounts/{account_id}/brand-protection/logos/{logo_id}`
  Future<ApiResult<void>> deleteAccountsBrandProtectionLogos({
    required String accountId,
    required String logoId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/logos/${Uri.encodeComponent(logoId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new logo queries from image files
  ///
  /// Return new logo queries created from image files
  ///
  /// `POST /accounts/{account_id}/brand-protection/scan-logo`
  Future<ApiResult<void>> postAccountsBrandProtectionScanLogo({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/scan-logo',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Create new logo queries from URLs
  ///
  /// Return new logo queries created from URLs
  ///
  /// `POST /accounts/{account_id}/brand-protection/scan-page`
  Future<ApiResult<void>> postAccountsBrandProtectionScanPage({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/brand-protection/scan-page',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Internal route for testing signed URLs
  ///
  /// `GET /signed-url`
  Future<ApiResult<void>> getSignedUrl() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/signed-url',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
