//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common3.dart';

/// AccessAuthenticationLogsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AccessAuthenticationLogsApi {
  const AccessAuthenticationLogsApi(this._config);

  final ApiConfig _config;

  /// Get Access authentication logs
  ///
  /// Gets a list of Access authentication audit logs for an account.
  ///
  /// `GET /accounts/{account_id}/access/logs/access_requests`
  Future<ApiResult<ResponseCommon3>>
  accessAuthenticationLogsGetAccessAuthenticationLogs({
    required String accountId,
    int? limit,
    String? direction,
    DateTime? since,
    DateTime? until,
    int? page,
    int? perPage,
    String? email,
    bool? emailExact,
    String? userId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/access/logs/access_requests',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        'direction': ?direction,
        if (since != null) 'since': since.toString(),
        if (until != null) 'until': until.toString(),
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        'email': ?email,
        if (emailExact != null) 'email_exact': emailExact.toString(),
        'user_id': ?userId,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon3.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
