//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/aig_config_create_gateway_request.dart';
import '../models/aig_config_create_gateway_response.dart';
import '../models/aig_config_delete_gateway_response.dart';
import '../models/aig_config_fetch_gateway_response.dart';
import '../models/aig_config_get_gateway_url_response.dart';
import '../models/aig_config_list_gateway_response.dart';
import '../models/aig_config_update_gateway_request.dart';
import '../models/aig_config_update_gateway_response.dart';

/// AiGatewayGatewaysApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class AiGatewayGatewaysApi {
  const AiGatewayGatewaysApi(this._config);

  final ApiConfig _config;

  /// List Gateways
  ///
  /// Lists all AI Gateway evaluator types configured for the account.
  ///
  /// `GET /accounts/{account_id}/ai-gateway/gateways`
  Future<ApiResult<AigConfigListGatewayResponse>> aigConfigListGateway({
    required String accountId,
    int? page,
    int? perPage,
    String? search,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        'search': ?search,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigListGatewayResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new Gateway
  ///
  /// Creates a new AI Gateway.
  ///
  /// `POST /accounts/{account_id}/ai-gateway/gateways`
  Future<ApiResult<AigConfigCreateGatewayResponse>> aigConfigCreateGateway({
    required String accountId,
    AigConfigCreateGatewayRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigCreateGatewayResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Gateway URL
  ///
  /// Retrieves the endpoint URL for an AI Gateway.
  ///
  /// `GET /accounts/{account_id}/ai-gateway/gateways/{gateway_id}/url/{provider}`
  Future<ApiResult<AigConfigGetGatewayUrlResponse>> aigConfigGetGatewayUrl({
    required String gatewayId,
    required String accountId,
    required String provider,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways/${Uri.encodeComponent(gatewayId)}/url/${Uri.encodeComponent(provider)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigGetGatewayUrlResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Fetch a Gateway
  ///
  /// Retrieves details for a specific AI Gateway dataset.
  ///
  /// `GET /accounts/{account_id}/ai-gateway/gateways/{id}`
  Future<ApiResult<AigConfigFetchGatewayResponse>> aigConfigFetchGateway({
    required String accountId,
    required String id,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigFetchGatewayResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a Gateway
  ///
  /// Updates an existing AI Gateway dataset.
  ///
  /// `PUT /accounts/{account_id}/ai-gateway/gateways/{id}`
  Future<ApiResult<AigConfigUpdateGatewayResponse>> aigConfigUpdateGateway({
    required String accountId,
    required String id,
    AigConfigUpdateGatewayRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigUpdateGatewayResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a Gateway
  ///
  /// Deletes an AI Gateway dataset.
  ///
  /// `DELETE /accounts/{account_id}/ai-gateway/gateways/{id}`
  Future<ApiResult<AigConfigDeleteGatewayResponse>> aigConfigDeleteGateway({
    required String accountId,
    required String id,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai-gateway/gateways/${Uri.encodeComponent(id)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return AigConfigDeleteGatewayResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
