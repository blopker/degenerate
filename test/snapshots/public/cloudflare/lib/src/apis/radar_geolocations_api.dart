//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/radar_get_geolocation_details_format.dart';
import '../models/radar_get_geolocation_details_response.dart';
import '../models/radar_get_geolocations_format.dart';
import '../models/radar_get_geolocations_response.dart';

/// RadarGeolocationsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RadarGeolocationsApi {
  const RadarGeolocationsApi(this._config);

  final ApiConfig _config;

  /// List Geolocations
  ///
  /// Retrieves a list of geolocations.
  ///
  /// `GET /radar/geolocations`
  Future<ApiResult<RadarGetGeolocationsResponse>> radarGetGeolocations({
    int? limit,
    int? offset,
    String? geoId,
    String? location,
    RadarGetGeolocationsFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/geolocations',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (offset != null) 'offset': offset.toString(),
        'geoId': ?geoId,
        'location': ?location,
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetGeolocationsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Geolocation details
  ///
  /// Retrieves the requested Geolocation information.
  ///
  /// `GET /radar/geolocations/{geo_id}`
  Future<ApiResult<RadarGetGeolocationDetailsResponse>>
  radarGetGeolocationDetails({
    required String geoId,
    RadarGetGeolocationDetailsFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/geolocations/${Uri.encodeComponent(geoId)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (format != null) 'format': format.toJson()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetGeolocationDetailsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
