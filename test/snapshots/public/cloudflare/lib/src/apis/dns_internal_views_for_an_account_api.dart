//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/dns_settings_direction.dart';
import '../models/dns_settings_dns_view.dart';
import '../models/dns_settings_match.dart';
import '../models/dns_settings_order.dart';
import '../models/dns_views_for_an_account_delete_internal_dns_view_response.dart';
import '../models/response_common26.dart';

/// DnsInternalViewsForAnAccountApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DnsInternalViewsForAnAccountApi {
  const DnsInternalViewsForAnAccountApi(this._config);

  final ApiConfig _config;

  /// List Internal DNS Views
  ///
  /// List DNS Internal Views for an Account
  ///
  /// `GET /accounts/{account_id}/dns_settings/views`
  Future<ApiResult<ResponseCommon26>> dnsViewsForAnAccountListInternalDnsViews({
    required String accountId,
    String? name,
    String? nameExact,
    String? nameContains,
    String? nameStartswith,
    String? nameEndswith,
    String? zoneId,
    String? zoneName,
    DnsSettingsMatch? match,
    double? page,
    double? perPage,
    DnsSettingsOrder? order,
    DnsSettingsDirection? direction,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dns_settings/views',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'name': ?name,
        'name.exact': ?nameExact,
        'name.contains': ?nameContains,
        'name.startswith': ?nameStartswith,
        'name.endswith': ?nameEndswith,
        'zone_id': ?zoneId,
        'zone_name': ?zoneName,
        if (match != null) 'match': match.toJson(),
        if (page != null) 'page': page.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (order != null) 'order': order.toJson(),
        if (direction != null) 'direction': direction.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon26.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create Internal DNS View
  ///
  /// Create Internal DNS View for an account
  ///
  /// `POST /accounts/{account_id}/dns_settings/views`
  Future<ApiResult<ResponseCommon26>>
  dnsViewsForAnAccountCreateInternalDnsViews({
    required String accountId,
    required DnsSettingsDnsView body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dns_settings/views',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon26.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// DNS Internal View Details
  ///
  /// Get DNS Internal View
  ///
  /// `GET /accounts/{account_id}/dns_settings/views/{view_id}`
  Future<ApiResult<ResponseCommon26>> dnsViewsForAnAccountGetInternalDnsView({
    required String accountId,
    required String viewId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dns_settings/views/${Uri.encodeComponent(viewId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon26.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Internal DNS View
  ///
  /// Update an existing Internal DNS View
  ///
  /// `PATCH /accounts/{account_id}/dns_settings/views/{view_id}`
  Future<ApiResult<ResponseCommon26>>
  dnsViewsForAnAccountUpdateInternalDnsView({
    required String accountId,
    required String viewId,
    required DnsSettingsDnsView body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dns_settings/views/${Uri.encodeComponent(viewId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon26.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Internal DNS View
  ///
  /// Delete an existing Internal DNS View
  ///
  /// `DELETE /accounts/{account_id}/dns_settings/views/{view_id}`
  Future<ApiResult<DnsViewsForAnAccountDeleteInternalDnsViewResponse>>
  dnsViewsForAnAccountDeleteInternalDnsView({
    required String accountId,
    required String viewId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dns_settings/views/${Uri.encodeComponent(viewId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DnsViewsForAnAccountDeleteInternalDnsViewResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
