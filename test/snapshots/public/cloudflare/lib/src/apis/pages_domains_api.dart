//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/pages_domains_add_domain_request.dart';
import '../models/response_common51.dart';

/// PagesDomainsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class PagesDomainsApi {
  const PagesDomainsApi(this._config);

  final ApiConfig _config;

  /// Get domains
  ///
  /// Fetch a list of all domains associated with a Pages project.
  ///
  /// `GET /accounts/{account_id}/pages/projects/{project_name}/domains`
  Future<ApiResult<ResponseCommon51>> pagesDomainsGetDomains({
    required String projectName,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pages/projects/${Uri.encodeComponent(projectName)}/domains',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon51.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Add domain
  ///
  /// Add a new domain for the Pages project.
  ///
  /// `POST /accounts/{account_id}/pages/projects/{project_name}/domains`
  Future<ApiResult<ResponseCommon51>> pagesDomainsAddDomain({
    required String projectName,
    required String accountId,
    required PagesDomainsAddDomainRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pages/projects/${Uri.encodeComponent(projectName)}/domains',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon51.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get domain
  ///
  /// Fetch a single domain.
  ///
  /// `GET /accounts/{account_id}/pages/projects/{project_name}/domains/{domain_name}`
  Future<ApiResult<ResponseCommon51>> pagesDomainsGetDomain({
    required String domainName,
    required String projectName,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pages/projects/${Uri.encodeComponent(projectName)}/domains/${Uri.encodeComponent(domainName)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon51.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Patch domain
  ///
  /// Retry the validation status of a single domain.
  ///
  /// `PATCH /accounts/{account_id}/pages/projects/{project_name}/domains/{domain_name}`
  Future<ApiResult<ResponseCommon51>> pagesDomainsPatchDomain({
    required String domainName,
    required String projectName,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pages/projects/${Uri.encodeComponent(projectName)}/domains/${Uri.encodeComponent(domainName)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon51.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete domain
  ///
  /// Delete a Pages project's domain.
  ///
  /// `DELETE /accounts/{account_id}/pages/projects/{project_name}/domains/{domain_name}`
  Future<ApiResult<ResponseCommon51>> pagesDomainsDeleteDomain({
    required String domainName,
    required String projectName,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/pages/projects/${Uri.encodeComponent(projectName)}/domains/${Uri.encodeComponent(domainName)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon51.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
