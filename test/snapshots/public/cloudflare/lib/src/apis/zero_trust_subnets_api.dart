//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common69.dart';
import '../models/tunnel_address_family.dart';
import '../models/zero_trust_networks_subnet_create_warp_request.dart';
import '../models/zero_trust_networks_subnet_update_cloudflare_source_request.dart';
import '../models/zero_trust_networks_subnet_update_warp_request.dart';
import '../models/zero_trust_networks_subnets_list_sort_order.dart';
import '../models/zero_trust_networks_subnets_list_subnet_types.dart';

/// ZeroTrustSubnetsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZeroTrustSubnetsApi {
  const ZeroTrustSubnetsApi(this._config);

  final ApiConfig _config;

  /// List Subnets
  ///
  /// Lists and filters subnets in an account.
  ///
  /// `GET /accounts/{account_id}/zerotrust/subnets`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksSubnetsList({
    required String accountId,
    String? name,
    String? comment,
    String? network,
    String? existedAt,
    TunnelAddressFamily? addressFamily,
    bool? isDefaultNetwork,
    bool? isDeleted,
    ZeroTrustNetworksSubnetsListSortOrder? sortOrder,
    ZeroTrustNetworksSubnetsListSubnetTypes? subnetTypes,
    double? perPage,
    double? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'name': ?name,
        'comment': ?comment,
        'network': ?network,
        'existed_at': ?existedAt,
        if (addressFamily != null) 'address_family': addressFamily.toJson(),
        if (isDefaultNetwork != null)
          'is_default_network': isDefaultNetwork.toString(),
        if (isDeleted != null) 'is_deleted': isDeleted.toString(),
        if (sortOrder != null) 'sort_order': sortOrder.toJson(),
        if (subnetTypes != null) 'subnet_types': subnetTypes.toJson(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Cloudflare Source Subnet
  ///
  /// Updates the Cloudflare Source subnet of the given address family
  ///
  /// `PATCH /accounts/{account_id}/zerotrust/subnets/cloudflare_source/{address_family}`
  Future<ApiResult<ResponseCommon69>>
  zeroTrustNetworksSubnetUpdateCloudflareSource({
    required String accountId,
    required TunnelAddressFamily addressFamily,
    required ZeroTrustNetworksSubnetUpdateCloudflareSourceRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets/cloudflare_source/${Uri.encodeComponent(addressFamily.toString())}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create WARP IP subnet
  ///
  /// Create a WARP IP assignment subnet. Currently, only IPv4 subnets can be created.
  ///
  /// **Network constraints:**
  /// - The network must be within one of the following private IP ranges:
  ///   - `10.0.0.0/8` (RFC 1918)
  ///   - `172.16.0.0/12` (RFC 1918)
  ///   - `192.168.0.0/16` (RFC 1918)
  ///   - `100.64.0.0/10` (RFC 6598 - CGNAT)
  /// - The subnet must have a prefix length of `/24` or larger (e.g., `/16`, `/20`, `/24` are valid; `/25`, `/28` are not)
  ///
  ///
  /// `POST /accounts/{account_id}/zerotrust/subnets/warp`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksSubnetCreateWarp({
    required String accountId,
    required ZeroTrustNetworksSubnetCreateWarpRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets/warp',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get WARP IP subnet
  ///
  /// Get a WARP IP assignment subnet.
  ///
  /// `GET /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksSubnetGetWarp({
    required String accountId,
    required String subnetId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets/warp/${Uri.encodeComponent(subnetId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update WARP IP subnet
  ///
  /// Updates a WARP IP assignment subnet.
  ///
  /// **Update constraints:**
  /// - The `network` field cannot be modified for WARP subnets. Only `name`, `comment`, and `is_default_network` can be updated.
  /// - IPv6 subnets cannot be updated
  ///
  ///
  /// `PATCH /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksSubnetUpdateWarp({
    required String accountId,
    required String subnetId,
    required ZeroTrustNetworksSubnetUpdateWarpRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets/warp/${Uri.encodeComponent(subnetId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete WARP IP subnet
  ///
  /// Delete a WARP IP assignment subnet. This operation is idempotent - deleting an already-deleted or non-existent subnet will return success with a null result.
  ///
  /// `DELETE /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksSubnetDeleteWarp({
    required String accountId,
    required String subnetId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/subnets/warp/${Uri.encodeComponent(subnetId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
