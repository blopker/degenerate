//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/delete_registration_response.dart';
import '../models/get_registration_response.dart';
import '../models/list_registrations_response.dart';
import '../models/list_registrations_sort_by.dart';
import '../models/list_registrations_sort_order.dart';
import '../models/list_registrations_status.dart';
import '../models/revoke_registrations_response.dart';
import '../models/unrevoke_registrations_response.dart';

/// RegistrationsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RegistrationsApi {
  const RegistrationsApi(this._config);

  final ApiConfig _config;

  /// List registrations
  ///
  /// Lists WARP registrations.
  ///
  /// `GET /accounts/{account_id}/devices/registrations`
  Future<ApiResult<ListRegistrationsResponse>> listRegistrations({
    required String accountId,
    List<String>? userId,
    String? seenAfter,
    String? seenBefore,
    ListRegistrationsStatus? status,
    int? perPage,
    String? search,
    ListRegistrationsSortBy? sortBy,
    ListRegistrationsSortOrder? sortOrder,
    String? cursor,
    List<String>? id,
    String? deviceId,
    String? include,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (userId != null) 'user.id': userId.toString(),
        'seen_after': ?seenAfter,
        'seen_before': ?seenBefore,
        if (status != null) 'status': status.toJson(),
        if (perPage != null) 'per_page': perPage.toString(),
        'search': ?search,
        if (sortBy != null) 'sort_by': sortBy.toJson(),
        if (sortOrder != null) 'sort_order': sortOrder.toJson(),
        'cursor': ?cursor,
        if (id != null) 'id': id.toString(),
        'device.id': ?deviceId,
        'include': ?include,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ListRegistrationsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get registration
  ///
  /// Fetches a single WARP registration.
  ///
  /// `GET /accounts/{account_id}/devices/registrations/{registration_id}`
  Future<ApiResult<GetRegistrationResponse>> getRegistration({
    required String registrationId,
    required String accountId,
    String? include,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations/${Uri.encodeComponent(registrationId)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {'include': ?include},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return GetRegistrationResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete registration
  ///
  /// Deletes a WARP registration.
  ///
  /// `DELETE /accounts/{account_id}/devices/registrations/{registration_id}`
  Future<ApiResult<DeleteRegistrationResponse>> deleteRegistration({
    required String registrationId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations/${Uri.encodeComponent(registrationId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return DeleteRegistrationResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Revoke registrations
  ///
  /// Revokes a list of WARP registrations.
  ///
  /// `POST /accounts/{account_id}/devices/registrations/revoke`
  Future<ApiResult<RevokeRegistrationsResponse>> revokeRegistrations({
    required String accountId,
    required List<String> id,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations/revoke',
      headers: {..._config.defaultHeaders},
      queryParameters: {'id': id.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RevokeRegistrationsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Unrevoke registrations
  ///
  /// Unrevokes a list of WARP registrations.
  ///
  /// `POST /accounts/{account_id}/devices/registrations/unrevoke`
  Future<ApiResult<UnrevokeRegistrationsResponse>> unrevokeRegistrations({
    required String accountId,
    required List<String> id,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/devices/registrations/unrevoke',
      headers: {..._config.defaultHeaders},
      queryParameters: {'id': id.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return UnrevokeRegistrationsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
