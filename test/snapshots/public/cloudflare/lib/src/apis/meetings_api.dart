//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/add_participant_request.dart';import '../models/create_meeting_request.dart';import '../models/edit_participant_request.dart';import '../models/realtimekit_generic_success_response.dart';import '../models/realtimekit_paging_response.dart';import '../models/replace_meeting_request.dart';import '../models/update_meeting_request.dart';/// MeetingsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class MeetingsApi {const MeetingsApi(this._config);

final ApiConfig _config;

/// Fetch all meetings for an App
///
/// Returns all meetings for the given App ID.
///
/// `GET /accounts/{account_id}/realtime/kit/{app_id}/meetings`
Future<ApiResult<RealtimekitPagingResponse>> getAllMeetings({required String accountId, required String appId, double? pageNo, double? perPage, DateTime? startTime, DateTime? endTime, String? search, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (pageNo != null) 'page_no': pageNo.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
    if (startTime != null) 'start_time': startTime.toString(),
    if (endTime != null) 'end_time': endTime.toString(),
    'search': ?search,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitPagingResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a meeting
///
/// Create a meeting for the given App ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings`
Future<ApiResult<RealtimekitGenericSuccessResponse>> createMeeting({required String accountId, required String appId, required CreateMeetingRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Fetch a meeting for an App
///
/// Returns a meeting details in an App for the given meeting ID.
///
/// `GET /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> getMeeting({required String accountId, required String appId, required String meetingId, String? name, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'name': ?name,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Replace a meeting
///
/// Replaces all the details for the given meeting ID.
///
/// `PUT /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> replaceMeeting({required String accountId, required String appId, required String meetingId, required ReplaceMeetingRequest body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update a meeting
///
/// Updates a meeting in an App for the given meeting ID.
///
/// `PATCH /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> updateMeeting({required String accountId, required String appId, required String meetingId, required UpdateMeetingRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Fetch all participants of a meeting
///
/// Returns all participants detail for the given meeting ID.
///
/// `GET /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants`
Future<ApiResult<RealtimekitPagingResponse>> getMeetingParticipants({required String accountId, required String appId, required String meetingId, double? pageNo, double? perPage, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (pageNo != null) 'page_no': pageNo.toString(),
    if (perPage != null) 'per_page': perPage.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitPagingResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Add a participant
///
/// Adds a participant to the given meeting ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants`
Future<ApiResult<RealtimekitGenericSuccessResponse>> addParticipant({required String accountId, required String appId, required String meetingId, required AddParticipantRequest body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Fetch a participant's detail
///
/// Returns a participant details for the given meeting and participant ID.
///
/// `GET /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants/{participant_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> getMeetingParticipant({required String accountId, required String appId, required String meetingId, required String participantId, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants/${Uri.encodeComponent(participantId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Edit a participant's detail
///
/// Updates a participant's details for the given meeting and participant ID.
///
/// `PATCH /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants/{participant_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> editParticipant({required String accountId, required String appId, required String meetingId, required String participantId, required EditParticipantRequest body, }) async  { final request = ApiRequest(
  method: 'PATCH',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants/${Uri.encodeComponent(participantId)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Delete a participant
///
/// Deletes a participant for the given meeting and participant ID.
///
/// `DELETE /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants/{participant_id}`
Future<ApiResult<RealtimekitGenericSuccessResponse>> deleteMeetingParticipant({required String accountId, required String appId, required String meetingId, required String participantId, }) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants/${Uri.encodeComponent(participantId)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Refresh participant's authentication token
///
/// Regenerates participant's authentication token for the given meeting and participant ID.
///
/// `POST /accounts/{account_id}/realtime/kit/{app_id}/meetings/{meeting_id}/participants/{participant_id}/token`
Future<ApiResult<RealtimekitGenericSuccessResponse>> regenerateToken({required String accountId, required String appId, required String meetingId, required String participantId, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/realtime/kit/${Uri.encodeComponent(appId)}/meetings/${Uri.encodeComponent(meetingId)}/participants/${Uri.encodeComponent(participantId)}/token',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return RealtimekitGenericSuccessResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
