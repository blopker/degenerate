//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/radar_get_ranking_internet_services_categories_format.dart';
import '../models/radar_get_ranking_internet_services_categories_response.dart';
import '../models/radar_get_ranking_internet_services_timeseries_format.dart';
import '../models/radar_get_ranking_internet_services_timeseries_response.dart';
import '../models/radar_get_ranking_top_internet_services_format.dart';
import '../models/radar_get_ranking_top_internet_services_response.dart';

/// RadarInternetServicesRankingApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RadarInternetServicesRankingApi {
  const RadarInternetServicesRankingApi(this._config);

  final ApiConfig _config;

  /// List Internet services categories
  ///
  /// Retrieves the list of Internet services categories.
  ///
  /// `GET /radar/ranking/internet_services/categories`
  Future<ApiResult<RadarGetRankingInternetServicesCategoriesResponse>>
  radarGetRankingInternetServicesCategories({
    int? limit,
    List<String>? name,
    List<String>? date,
    RadarGetRankingInternetServicesCategoriesFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/ranking/internet_services/categories',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (name != null) 'name': name.toString(),
        if (date != null) 'date': date.toString(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetRankingInternetServicesCategoriesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Internet services rank time series
  ///
  /// Retrieves Internet Services rank update changes over time.
  ///
  /// `GET /radar/ranking/internet_services/timeseries_groups`
  Future<ApiResult<RadarGetRankingInternetServicesTimeseriesResponse>>
  radarGetRankingInternetServicesTimeseries({
    List<String>? serviceCategory,
    int? limit,
    List<String>? name,
    List<String>? dateRange,
    List<DateTime>? dateStart,
    List<DateTime>? dateEnd,
    RadarGetRankingInternetServicesTimeseriesFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/ranking/internet_services/timeseries_groups',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (serviceCategory != null)
          'serviceCategory': serviceCategory.toString(),
        if (limit != null) 'limit': limit.toString(),
        if (name != null) 'name': name.toString(),
        if (dateRange != null) 'dateRange': dateRange.toString(),
        if (dateStart != null) 'dateStart': dateStart.toString(),
        if (dateEnd != null) 'dateEnd': dateEnd.toString(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetRankingInternetServicesTimeseriesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get top Internet services
  ///
  /// Retrieves top Internet services based on their rank.
  ///
  /// `GET /radar/ranking/internet_services/top`
  Future<ApiResult<RadarGetRankingTopInternetServicesResponse>>
  radarGetRankingTopInternetServices({
    List<String>? serviceCategory,
    int? limit,
    List<String>? name,
    List<String>? date,
    RadarGetRankingTopInternetServicesFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/ranking/internet_services/top',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (serviceCategory != null)
          'serviceCategory': serviceCategory.toString(),
        if (limit != null) 'limit': limit.toString(),
        if (name != null) 'name': name.toString(),
        if (date != null) 'date': date.toString(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetRankingTopInternetServicesResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
