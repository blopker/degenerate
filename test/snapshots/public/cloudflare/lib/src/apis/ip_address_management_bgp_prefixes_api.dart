//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/addressing_bgp_prefix_create.dart';
import '../models/addressing_bgp_prefix_update_advertisement.dart';
import '../models/response_common4.dart';

/// IpAddressManagementBgpPrefixesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class IpAddressManagementBgpPrefixesApi {
  const IpAddressManagementBgpPrefixesApi(this._config);

  final ApiConfig _config;

  /// List BGP Prefixes
  ///
  /// List all BGP Prefixes within the specified IP Prefix. BGP Prefixes are used to control which specific subnets are advertised to the Internet. It is possible to advertise subnets more specific than an IP Prefix by creating more specific BGP Prefixes.
  ///
  /// `GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes`
  Future<ApiResult<ResponseCommon4>>
  ipAddressManagementPrefixesListBgpPrefixes({
    required String accountId,
    required String prefixId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/addressing/prefixes/${Uri.encodeComponent(prefixId)}/bgp/prefixes',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon4.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create BGP Prefix
  ///
  /// Create a BGP prefix, controlling the BGP advertisement status of a specific subnet. When created, BGP prefixes are initially withdrawn, and can be advertised with the Update BGP Prefix API.
  ///
  /// `POST /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes`
  Future<ApiResult<ResponseCommon4>>
  ipAddressManagementPrefixesCreateBgpPrefix({
    required String accountId,
    required String prefixId,
    required AddressingBgpPrefixCreate body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/addressing/prefixes/${Uri.encodeComponent(prefixId)}/bgp/prefixes',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon4.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Fetch BGP Prefix
  ///
  /// Retrieve a single BGP Prefix according to its identifier
  ///
  /// `GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}`
  Future<ApiResult<ResponseCommon4>> ipAddressManagementPrefixesFetchBgpPrefix({
    required String accountId,
    required String prefixId,
    required String bgpPrefixId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/addressing/prefixes/${Uri.encodeComponent(prefixId)}/bgp/prefixes/${Uri.encodeComponent(bgpPrefixId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon4.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update BGP Prefix
  ///
  /// Update the properties of a BGP Prefix, such as the on demand advertisement status (advertised or withdrawn).
  ///
  /// `PATCH /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}`
  Future<ApiResult<ResponseCommon4>>
  ipAddressManagementPrefixesUpdateBgpPrefix({
    required String accountId,
    required String prefixId,
    required String bgpPrefixId,
    required AddressingBgpPrefixUpdateAdvertisement body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/addressing/prefixes/${Uri.encodeComponent(prefixId)}/bgp/prefixes/${Uri.encodeComponent(bgpPrefixId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon4.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete BGP Prefix
  ///
  /// Delete a BGP Prefix associated with the specified IP Prefix. A BGP Prefix must be withdrawn before it can be deleted.
  ///
  /// `DELETE /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}`
  Future<ApiResult<ResponseCommon4>>
  ipAddressManagementPrefixesDeleteBgpPrefix({
    required String accountId,
    required String prefixId,
    required String bgpPrefixId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/addressing/prefixes/${Uri.encodeComponent(prefixId)}/bgp/prefixes/${Uri.encodeComponent(bgpPrefixId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon4.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
