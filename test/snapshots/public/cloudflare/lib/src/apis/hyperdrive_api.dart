//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/hyperdrive_hyperdrive_config.dart';
import '../models/hyperdrive_hyperdrive_config_patch.dart';
import '../models/response_common34.dart';

/// HyperdriveApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class HyperdriveApi {
  const HyperdriveApi(this._config);

  final ApiConfig _config;

  /// List Hyperdrives
  ///
  /// Returns a list of Hyperdrives.
  ///
  /// `GET /accounts/{account_id}/hyperdrive/configs`
  Future<ApiResult<ResponseCommon34>> listHyperdrive({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create Hyperdrive
  ///
  /// Creates and returns a new Hyperdrive configuration.
  ///
  /// `POST /accounts/{account_id}/hyperdrive/configs`
  Future<ApiResult<ResponseCommon34>> createHyperdrive({
    required String accountId,
    required HyperdriveHyperdriveConfig body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Hyperdrive
  ///
  /// Returns the specified Hyperdrive configuration.
  ///
  /// `GET /accounts/{account_id}/hyperdrive/configs/{hyperdrive_id}`
  Future<ApiResult<ResponseCommon34>> getHyperdrive({
    required String accountId,
    required String hyperdriveId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs/${Uri.encodeComponent(hyperdriveId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update Hyperdrive
  ///
  /// Updates and returns the specified Hyperdrive configuration.
  ///
  /// `PUT /accounts/{account_id}/hyperdrive/configs/{hyperdrive_id}`
  Future<ApiResult<ResponseCommon34>> updateHyperdrive({
    required String accountId,
    required String hyperdriveId,
    required HyperdriveHyperdriveConfig body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs/${Uri.encodeComponent(hyperdriveId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Patch Hyperdrive
  ///
  /// Patches and returns the specified Hyperdrive configuration. Custom caching settings are not kept if caching is disabled.
  ///
  /// `PATCH /accounts/{account_id}/hyperdrive/configs/{hyperdrive_id}`
  Future<ApiResult<ResponseCommon34>> patchHyperdrive({
    required String accountId,
    required String hyperdriveId,
    required HyperdriveHyperdriveConfigPatch body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs/${Uri.encodeComponent(hyperdriveId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Hyperdrive
  ///
  /// Deletes the specified Hyperdrive.
  ///
  /// `DELETE /accounts/{account_id}/hyperdrive/configs/{hyperdrive_id}`
  Future<ApiResult<ResponseCommon34>> deleteHyperdrive({
    required String accountId,
    required String hyperdriveId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/hyperdrive/configs/${Uri.encodeComponent(hyperdriveId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon34.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
