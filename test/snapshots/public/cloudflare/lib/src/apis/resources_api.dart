//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/mcn_resource_type.dart';
import '../models/mcn_resources_catalog_policy_preview_request.dart';
import '../models/mcn_response.dart';
import '../models/mcn_response_collection.dart';

/// ResourcesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ResourcesApi {
  const ResourcesApi(this._config);

  final ApiConfig _config;

  /// List Resources
  ///
  /// List resources in the Resource Catalog (Closed Beta).
  ///
  /// `GET /accounts/{account_id}/magic/cloud/resources`
  Future<ApiResult<McnResponseCollection>> resourcesCatalogList({
    required String accountId,
    String? providerId,
    List<McnResourceType>? resourceType,
    List<String>? resourceId,
    String? region,
    String? resourceGroup,
    bool? managed,
    List<String>? search,
    String? orderBy,
    bool? desc,
    int? perPage,
    int? page,
    bool? cloudflare,
    bool? v2,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/magic/cloud/resources',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'provider_id': ?providerId,
        if (resourceType != null) 'resource_type': resourceType.toString(),
        if (resourceId != null) 'resource_id': resourceId.toString(),
        'region': ?region,
        'resource_group': ?resourceGroup,
        if (managed != null) 'managed': managed.toString(),
        if (search != null) 'search': search.toString(),
        'order_by': ?orderBy,
        if (desc != null) 'desc': desc.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
        if (cloudflare != null) 'cloudflare': cloudflare.toString(),
        if (v2 != null) 'v2': v2.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return McnResponseCollection.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Read Resource
  ///
  /// Read an resource from the Resource Catalog (Closed Beta).
  ///
  /// `GET /accounts/{account_id}/magic/cloud/resources/{resource_id}`
  Future<ApiResult<McnResponse>> resourcesCatalogRead({
    required String accountId,
    required String resourceId,
    bool? v2,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/magic/cloud/resources/${Uri.encodeComponent(resourceId)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (v2 != null) 'v2': v2.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return McnResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Export Resources
  ///
  /// Export resources in the Resource Catalog as a JSON file (Closed Beta).
  ///
  /// `GET /accounts/{account_id}/magic/cloud/resources/export`
  Future<ApiResult<void>> resourcesCatalogExport({
    required String accountId,
    String? providerId,
    List<McnResourceType>? resourceType,
    List<String>? resourceId,
    String? region,
    String? resourceGroup,
    List<String>? search,
    String? orderBy,
    bool? desc,
    bool? v2,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/magic/cloud/resources/export',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'provider_id': ?providerId,
        if (resourceType != null) 'resource_type': resourceType.toString(),
        if (resourceId != null) 'resource_id': resourceId.toString(),
        'region': ?region,
        'resource_group': ?resourceGroup,
        if (search != null) 'search': search.toString(),
        'order_by': ?orderBy,
        if (desc != null) 'desc': desc.toString(),
        if (v2 != null) 'v2': v2.toString(),
      },
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Preview Rego Query
  ///
  /// Preview Rego query result against the latest resource catalog (Closed Beta).
  ///
  /// `POST /accounts/{account_id}/magic/cloud/resources/policy-preview`
  Future<ApiResult<McnResponse>> resourcesCatalogPolicyPreview({
    required String accountId,
    required McnResourcesCatalogPolicyPreviewRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/magic/cloud/resources/policy-preview',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return McnResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
