//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';/// WorkersAiDumbPipeApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiDumbPipeApi {const WorkersAiDumbPipeApi(this._config);

final ApiConfig _config;

/// Open Websocket connection with @cf/pipecat-ai/smart-turn-v2 model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/pipecat-ai/smart-turn-v2 model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/pipecat-ai/smart-turn-v2`
Future<ApiResult<void>> workersAiPostWebsocketRunCfPipecatAiSmartTurnV2({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/pipecat-ai/smart-turn-v2',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Open Websocket connection with @cf/pipecat-ai/smart-turn-v3 model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/pipecat-ai/smart-turn-v3 model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/pipecat-ai/smart-turn-v3`
Future<ApiResult<void>> workersAiPostWebsocketRunCfPipecatAiSmartTurnV3({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/pipecat-ai/smart-turn-v3',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Open Websocket connection with @cf/test/hello-world-cog model.
///
/// Opens a WebSocket connection to stream inference results from the @cf/test/hello-world-cog model.
///
/// `GET /accounts/{account_id}/ai/run/@cf/test/hello-world-cog`
Future<ApiResult<void>> workersAiPostWebsocketRunCfTestHelloWorldCog({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/run/@cf/test/hello-world-cog',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
