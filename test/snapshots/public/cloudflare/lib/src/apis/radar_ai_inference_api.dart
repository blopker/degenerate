//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/radar_get_ai_inference_summary_dimension.dart';
import '../models/radar_get_ai_inference_summary_format.dart';
import '../models/radar_get_ai_inference_summary_response.dart';
import '../models/radar_get_ai_inference_timeseries_group_agg_interval.dart';
import '../models/radar_get_ai_inference_timeseries_group_dimension.dart';
import '../models/radar_get_ai_inference_timeseries_group_format.dart';
import '../models/radar_get_ai_inference_timeseries_group_normalization.dart';
import '../models/radar_get_ai_inference_timeseries_group_response.dart';

/// RadarAiInferenceApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RadarAiInferenceApi {
  const RadarAiInferenceApi(this._config);

  final ApiConfig _config;

  /// Get Workers AI inference distribution by dimension
  ///
  /// Retrieves an aggregated summary of unique accounts using Workers AI inference grouped by the specified dimension.
  ///
  /// `GET /radar/ai/inference/summary/{dimension}`
  Future<ApiResult<RadarGetAiInferenceSummaryResponse>>
  radarGetAiInferenceSummary({
    required RadarGetAiInferenceSummaryDimension dimension,
    List<String>? name,
    List<String>? dateRange,
    List<DateTime>? dateStart,
    List<DateTime>? dateEnd,
    List<String>? asn,
    List<String>? location,
    List<String>? continent,
    int? limitPerGroup,
    RadarGetAiInferenceSummaryFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/radar/ai/inference/summary/${Uri.encodeComponent(dimension.toString())}',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (name != null) 'name': name.toString(),
        if (dateRange != null) 'dateRange': dateRange.toString(),
        if (dateStart != null) 'dateStart': dateStart.toString(),
        if (dateEnd != null) 'dateEnd': dateEnd.toString(),
        if (asn != null) 'asn': asn.toString(),
        if (location != null) 'location': location.toString(),
        if (continent != null) 'continent': continent.toString(),
        if (limitPerGroup != null) 'limitPerGroup': limitPerGroup.toString(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetAiInferenceSummaryResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get time series distribution of Workers AI inference by dimension.
  ///
  /// Retrieves the distribution of unique accounts using Workers AI inference, grouped by the specified dimension over time.
  ///
  /// `GET /radar/ai/inference/timeseries_groups/{dimension}`
  Future<ApiResult<RadarGetAiInferenceTimeseriesGroupResponse>>
  radarGetAiInferenceTimeseriesGroup({
    required RadarGetAiInferenceTimeseriesGroupDimension dimension,
    RadarGetAiInferenceTimeseriesGroupAggInterval? aggInterval,
    List<String>? name,
    List<String>? dateRange,
    List<DateTime>? dateStart,
    List<DateTime>? dateEnd,
    List<String>? asn,
    List<String>? location,
    List<String>? continent,
    int? limitPerGroup,
    RadarGetAiInferenceTimeseriesGroupNormalization? normalization,
    RadarGetAiInferenceTimeseriesGroupFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/radar/ai/inference/timeseries_groups/${Uri.encodeComponent(dimension.toString())}',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (aggInterval != null) 'aggInterval': aggInterval.toJson(),
        if (name != null) 'name': name.toString(),
        if (dateRange != null) 'dateRange': dateRange.toString(),
        if (dateStart != null) 'dateStart': dateStart.toString(),
        if (dateEnd != null) 'dateEnd': dateEnd.toString(),
        if (asn != null) 'asn': asn.toString(),
        if (location != null) 'location': location.toString(),
        if (continent != null) 'continent': continent.toString(),
        if (limitPerGroup != null) 'limitPerGroup': limitPerGroup.toString(),
        if (normalization != null) 'normalization': normalization.toJson(),
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetAiInferenceTimeseriesGroupResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
