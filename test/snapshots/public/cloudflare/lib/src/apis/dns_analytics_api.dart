//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/dns_analytics_time_delta.dart';
import '../models/response_common22.dart';

/// DnsAnalyticsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DnsAnalyticsApi {
  const DnsAnalyticsApi(this._config);

  final ApiConfig _config;

  /// Table
  ///
  /// Retrieves a list of summarised aggregate metrics over a given time period.
  ///
  /// See [Analytics API properties](https://developers.cloudflare.com/dns/reference/analytics-api-properties/) for detailed information about the available query parameters.
  ///
  /// `GET /zones/{zone_id}/dns_analytics/report`
  Future<ApiResult<ResponseCommon22>> dnsAnalyticsTable({
    required String zoneId,
    String? metrics,
    String? dimensions,
    DateTime? since,
    DateTime? until,
    int? limit,
    String? sort,
    String? filters,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/dns_analytics/report',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'metrics': ?metrics,
        'dimensions': ?dimensions,
        if (since != null) 'since': since.toString(),
        if (until != null) 'until': until.toString(),
        if (limit != null) 'limit': limit.toString(),
        'sort': ?sort,
        'filters': ?filters,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon22.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// By Time
  ///
  /// Retrieves a list of aggregate metrics grouped by time interval.
  ///
  /// See [Analytics API properties](https://developers.cloudflare.com/dns/reference/analytics-api-properties/) for detailed information about the available query parameters.
  ///
  /// `GET /zones/{zone_id}/dns_analytics/report/bytime`
  Future<ApiResult<ResponseCommon22>> dnsAnalyticsByTime({
    required String zoneId,
    String? metrics,
    String? dimensions,
    DateTime? since,
    DateTime? until,
    int? limit,
    String? sort,
    String? filters,
    DnsAnalyticsTimeDelta? timeDelta,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/dns_analytics/report/bytime',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'metrics': ?metrics,
        'dimensions': ?dimensions,
        if (since != null) 'since': since.toString(),
        if (until != null) 'until': until.toString(),
        if (limit != null) 'limit': limit.toString(),
        'sort': ?sort,
        'filters': ?filters,
        if (timeDelta != null) 'time_delta': timeDelta.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon22.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
