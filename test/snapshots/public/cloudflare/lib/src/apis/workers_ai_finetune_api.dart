//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/workers_ai_create_finetune_request.dart';import '../models/workers_ai_create_finetune_response.dart';import '../models/workers_ai_list_finetunes_response.dart';import '../models/workers_ai_list_public_finetunes_response.dart';import '../models/workers_ai_upload_finetune_asset_request.dart';import '../models/workers_ai_upload_finetune_asset_response.dart';/// WorkersAiFinetuneApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiFinetuneApi {const WorkersAiFinetuneApi(this._config);

final ApiConfig _config;

/// List Finetunes
///
/// `GET /accounts/{account_id}/ai/finetunes`
Future<ApiResult<WorkersAiListFinetunesResponse>> workersAiListFinetunes({required String accountId}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/finetunes',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return WorkersAiListFinetunesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Create a new Finetune
///
/// `POST /accounts/{account_id}/ai/finetunes`
Future<ApiResult<WorkersAiCreateFinetuneResponse>> workersAiCreateFinetune({required String accountId, WorkersAiCreateFinetuneRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/finetunes',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return WorkersAiCreateFinetuneResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Upload a Finetune Asset
///
/// `POST /accounts/{account_id}/ai/finetunes/{finetune_id}/finetune-assets`
Future<ApiResult<WorkersAiUploadFinetuneAssetResponse>> workersAiUploadFinetuneAsset({required String accountId, required String finetuneId, WorkersAiUploadFinetuneAssetRequest? body, }) async  { final request = ApiRequest(
  method: 'POST',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/finetunes/${Uri.encodeComponent(finetuneId)}/finetune-assets',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return WorkersAiUploadFinetuneAssetResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// List Public Finetunes
///
/// `GET /accounts/{account_id}/ai/finetunes/public`
Future<ApiResult<WorkersAiListPublicFinetunesResponse>> workersAiListPublicFinetunes({required String accountId, double? limit, double? offset, String? orderBy, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/accounts/${Uri.encodeComponent(accountId)}/ai/finetunes/public',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (limit != null) 'limit': limit.toString(),
    if (offset != null) 'offset': offset.toString(),
    'orderBy': ?orderBy,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return WorkersAiListPublicFinetunesResponse.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
