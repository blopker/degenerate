//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/radar_get_tld_details_format.dart';
import '../models/radar_get_tld_details_response.dart';
import '../models/radar_get_tlds_format.dart';
import '../models/radar_get_tlds_response.dart';
import '../models/radar_get_tlds_tld_type.dart';

/// RadarTopLevelDomainsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class RadarTopLevelDomainsApi {
  const RadarTopLevelDomainsApi(this._config);

  final ApiConfig _config;

  /// List TLDs
  ///
  /// Retrieves a list of TLDs.
  ///
  /// `GET /radar/tlds`
  Future<ApiResult<RadarGetTldsResponse>> radarGetTlds({
    int? limit,
    int? offset,
    String? tldManager,
    RadarGetTldsTldType? tldType,
    String? tld,
    RadarGetTldsFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/tlds',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (offset != null) 'offset': offset.toString(),
        'tldManager': ?tldManager,
        if (tldType != null) 'tldType': tldType.toJson(),
        'tld': ?tld,
        if (format != null) 'format': format.toJson(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetTldsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get TLD details
  ///
  /// Retrieves the requested TLD information.
  ///
  /// `GET /radar/tlds/{tld}`
  Future<ApiResult<RadarGetTldDetailsResponse>> radarGetTldDetails({
    required String tld,
    RadarGetTldDetailsFormat? format,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/radar/tlds/${Uri.encodeComponent(tld)}',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (format != null) 'format': format.toJson()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return RadarGetTldDetailsResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
