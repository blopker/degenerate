//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/dlp_custom_profile_update.dart';
import '../models/dlp_new_custom_profile.dart';
import '../models/dlp_predefined_profile_config_update.dart';
import '../models/dlp_predefined_profile_update.dart';
import '../models/response_common20.dart';

/// DlpProfilesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DlpProfilesApi {
  const DlpProfilesApi(this._config);

  final ApiConfig _config;

  /// List all profiles
  ///
  /// Lists all DLP profiles in an account.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles`
  Future<ApiResult<ResponseCommon20>> dlpProfilesListAllProfiles({
    required String accountId,
    bool? all,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (all != null) 'all': all.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get DLP Profile
  ///
  /// Fetches a DLP profile by ID.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesGetDlpProfile({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// List all custom profiles
  ///
  /// Lists all DLP custom profiles in an account.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles/custom`
  Future<ApiResult<ResponseCommon20>> dlpProfilesListAllCustomProfiles({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/custom',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create custom profile
  ///
  /// Creates a DLP custom profile.
  ///
  /// `POST /accounts/{account_id}/dlp/profiles/custom`
  Future<ApiResult<ResponseCommon20>> dlpProfilesCreateCustomProfiles({
    required String accountId,
    required DlpNewCustomProfile body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/custom',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get custom profile
  ///
  /// Fetches a custom DLP profile by id.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles/custom/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesGetCustomProfile({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/custom/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update custom profile
  ///
  /// Updates a DLP custom profile.
  ///
  /// `PUT /accounts/{account_id}/dlp/profiles/custom/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesUpdateCustomProfile({
    required String accountId,
    required String profileId,
    required DlpCustomProfileUpdate body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/custom/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete custom profile
  ///
  /// Deletes a DLP custom profile.
  ///
  /// `DELETE /accounts/{account_id}/dlp/profiles/custom/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesDeleteCustomProfile({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/custom/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create predefined profile
  ///
  /// Creates a DLP predefined profile. Only supports enabling/disabling entries.
  ///
  /// `POST /accounts/{account_id}/dlp/profiles/predefined`
  Future<ApiResult<ResponseCommon20>> dlpProfilesCreatePredefinedProfile({
    required String accountId,
    required DlpPredefinedProfileUpdate body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get predefined profile
  ///
  /// Fetches a predefined DLP profile by id.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles/predefined/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesGetPredefinedProfile({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update predefined profile
  ///
  /// Updates a DLP predefined profile. Only supports enabling/disabling entries.
  ///
  /// `PUT /accounts/{account_id}/dlp/profiles/predefined/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesUpdatePredefinedProfile({
    required String accountId,
    required String profileId,
    required DlpPredefinedProfileUpdate body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete predefined profile
  ///
  /// This is a no-op as predefined profiles can't be deleted but is needed for our generated terraform API.
  ///
  /// `DELETE /accounts/{account_id}/dlp/profiles/predefined/{profile_id}`
  Future<ApiResult<ResponseCommon20>> dlpProfilesDeletePredefinedProfile({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get predefined profile config
  ///
  /// This is similar to `get_predefined` but only returns entries that are enabled.
  /// This is needed for our terraform API
  /// Fetches a predefined DLP profile by id.
  ///
  /// `GET /accounts/{account_id}/dlp/profiles/predefined/{profile_id}/config`
  Future<ApiResult<ResponseCommon20>> dlpProfilesGetPredefinedProfileConfig({
    required String accountId,
    required String profileId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}/config',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create predefined profile
  ///
  /// This is similar to `update_predefined` but only returns entries that are enabled.
  /// This is needed for our terraform API
  /// Creates a DLP predefined profile. Only supports enabling/disabling entries.
  ///
  /// `POST /accounts/{account_id}/dlp/profiles/predefined/{profile_id}/config`
  Future<ApiResult<ResponseCommon20>> dlpProfilesCreatePredefinedProfileConfig({
    required String accountId,
    required String profileId,
    required DlpPredefinedProfileConfigUpdate body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}/config',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update predefined profile config
  ///
  /// This is similar to `update_predefined` but only returns entries that are enabled.
  /// This is needed for our terraform API
  /// Updates a DLP predefined profile. Only supports enabling/disabling entries.
  ///
  /// `PUT /accounts/{account_id}/dlp/profiles/predefined/{profile_id}/config`
  Future<ApiResult<ResponseCommon20>> dlpProfilesUpdatePredefinedProfileConfig({
    required String accountId,
    required String profileId,
    required DlpPredefinedProfileConfigUpdate body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/profiles/predefined/${Uri.encodeComponent(profileId)}/config',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
