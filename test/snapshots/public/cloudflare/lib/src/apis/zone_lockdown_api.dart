//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common31.dart';
import '../models/zone_lockdown_create_a_zone_lockdown_rule_request.dart';
import '../models/zone_lockdown_delete_a_zone_lockdown_rule_response.dart';
import '../models/zone_lockdown_update_a_zone_lockdown_rule_request.dart';

/// ZoneLockdownApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZoneLockdownApi {
  const ZoneLockdownApi(this._config);

  final ApiConfig _config;

  /// List Zone Lockdown rules
  ///
  /// Fetches Zone Lockdown rules. You can filter the results using several optional parameters.
  ///
  /// `GET /zones/{zone_id}/firewall/lockdowns`
  Future<ApiResult<ResponseCommon31>> zoneLockdownListZoneLockdownRules({
    required String zoneId,
    double? page,
    String? description,
    DateTime? modifiedOn,
    String? ip,
    double? priority,
    String? uriSearch,
    String? ipRangeSearch,
    double? perPage,
    DateTime? createdOn,
    String? descriptionSearch,
    String? ipSearch,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/zones/${Uri.encodeComponent(zoneId)}/firewall/lockdowns',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (page != null) 'page': page.toString(),
        'description': ?description,
        if (modifiedOn != null) 'modified_on': modifiedOn.toString(),
        'ip': ?ip,
        if (priority != null) 'priority': priority.toString(),
        'uri_search': ?uriSearch,
        'ip_range_search': ?ipRangeSearch,
        if (perPage != null) 'per_page': perPage.toString(),
        if (createdOn != null) 'created_on': createdOn.toString(),
        'description_search': ?descriptionSearch,
        'ip_search': ?ipSearch,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a Zone Lockdown rule
  ///
  /// Creates a new Zone Lockdown rule.
  ///
  /// `POST /zones/{zone_id}/firewall/lockdowns`
  Future<ApiResult<ResponseCommon31>> zoneLockdownCreateAZoneLockdownRule({
    required String zoneId,
    required ZoneLockdownCreateAZoneLockdownRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/zones/${Uri.encodeComponent(zoneId)}/firewall/lockdowns',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get a Zone Lockdown rule
  ///
  /// Fetches the details of a Zone Lockdown rule.
  ///
  /// `GET /zones/{zone_id}/firewall/lockdowns/{lock_downs_id}`
  Future<ApiResult<ResponseCommon31>> zoneLockdownGetAZoneLockdownRule({
    required String lockDownsId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/lockdowns/${Uri.encodeComponent(lockDownsId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update a Zone Lockdown rule
  ///
  /// Updates an existing Zone Lockdown rule.
  ///
  /// `PUT /zones/{zone_id}/firewall/lockdowns/{lock_downs_id}`
  Future<ApiResult<ResponseCommon31>> zoneLockdownUpdateAZoneLockdownRule({
    required String lockDownsId,
    required String zoneId,
    required ZoneLockdownUpdateAZoneLockdownRuleRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/lockdowns/${Uri.encodeComponent(lockDownsId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon31.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a Zone Lockdown rule
  ///
  /// Deletes an existing Zone Lockdown rule.
  ///
  /// `DELETE /zones/{zone_id}/firewall/lockdowns/{lock_downs_id}`
  Future<ApiResult<ZoneLockdownDeleteAZoneLockdownRuleResponse>>
  zoneLockdownDeleteAZoneLockdownRule({
    required String lockDownsId,
    required String zoneId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/zones/${Uri.encodeComponent(zoneId)}/firewall/lockdowns/${Uri.encodeComponent(lockDownsId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ZoneLockdownDeleteAZoneLockdownRuleResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
