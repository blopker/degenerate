//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common69.dart';
import '../models/zero_trust_networks_route_hostname_create_request.dart';
import '../models/zero_trust_networks_route_hostname_update_request.dart';

/// ZeroTrustHostnameRouteApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZeroTrustHostnameRouteApi {
  const ZeroTrustHostnameRouteApi(this._config);

  final ApiConfig _config;

  /// List hostname routes
  ///
  /// Lists and filters hostname routes in an account.
  ///
  /// `GET /accounts/{account_id}/zerotrust/routes/hostname`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksRouteHostnameList({
    required String accountId,
    String? id,
    String? hostname,
    String? tunnelId,
    String? comment,
    String? existedAt,
    bool? isDeleted,
    double? perPage,
    double? page,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/routes/hostname',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        'id': ?id,
        'hostname': ?hostname,
        'tunnel_id': ?tunnelId,
        'comment': ?comment,
        'existed_at': ?existedAt,
        if (isDeleted != null) 'is_deleted': isDeleted.toString(),
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create hostname route
  ///
  /// Create a hostname route.
  ///
  /// `POST /accounts/{account_id}/zerotrust/routes/hostname`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksRouteHostnameCreate({
    required String accountId,
    required ZeroTrustNetworksRouteHostnameCreateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/routes/hostname',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get hostname route
  ///
  /// Get a hostname route.
  ///
  /// `GET /accounts/{account_id}/zerotrust/routes/hostname/{hostname_route_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksRouteHostnameGet({
    required String accountId,
    required String hostnameRouteId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/routes/hostname/${Uri.encodeComponent(hostnameRouteId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update hostname route
  ///
  /// Updates a hostname route.
  ///
  /// `PATCH /accounts/{account_id}/zerotrust/routes/hostname/{hostname_route_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksRouteHostnameUpdate({
    required String accountId,
    required String hostnameRouteId,
    required ZeroTrustNetworksRouteHostnameUpdateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PATCH',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/routes/hostname/${Uri.encodeComponent(hostnameRouteId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete hostname route
  ///
  /// Delete a hostname route.
  ///
  /// `DELETE /accounts/{account_id}/zerotrust/routes/hostname/{hostname_route_id}`
  Future<ApiResult<ResponseCommon69>> zeroTrustNetworksRouteHostnameDelete({
    required String accountId,
    required String hostnameRouteId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/zerotrust/routes/hostname/${Uri.encodeComponent(hostnameRouteId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon69.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
