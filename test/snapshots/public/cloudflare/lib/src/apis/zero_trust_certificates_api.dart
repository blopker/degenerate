//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/response_common82.dart';
import '../models/zero_trust_gateway_generate_cert_request.dart';

/// ZeroTrustCertificatesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class ZeroTrustCertificatesApi {
  const ZeroTrustCertificatesApi(this._config);

  final ApiConfig _config;

  /// List Zero Trust certificates
  ///
  /// List all Zero Trust certificates for an account.
  ///
  /// `GET /accounts/{account_id}/gateway/certificates`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesListZeroTrustCertificates({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create Zero Trust certificate
  ///
  /// Create a new Zero Trust certificate.
  ///
  /// `POST /accounts/{account_id}/gateway/certificates`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesCreateZeroTrustCertificate({
    required String accountId,
    ZeroTrustGatewayGenerateCertRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Zero Trust certificate details
  ///
  /// Get a single Zero Trust certificate.
  ///
  /// `GET /accounts/{account_id}/gateway/certificates/{certificate_id}`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesZeroTrustCertificateDetails({
    required String certificateId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates/${Uri.encodeComponent(certificateId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete Zero Trust certificate
  ///
  /// Delete a gateway-managed Zero Trust certificate. You must deactivate the certificate from the edge (inactive) before deleting it.
  ///
  /// `DELETE /accounts/{account_id}/gateway/certificates/{certificate_id}`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesDeleteZeroTrustCertificate({
    required String certificateId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates/${Uri.encodeComponent(certificateId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Activate a Zero Trust certificate
  ///
  /// Bind a single Zero Trust certificate to the edge.
  ///
  /// `POST /accounts/{account_id}/gateway/certificates/{certificate_id}/activate`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesActivateZeroTrustCertificate({
    required String certificateId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates/${Uri.encodeComponent(certificateId)}/activate',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Deactivate a Zero Trust certificate
  ///
  /// Unbind a single Zero Trust certificate from the edge.
  ///
  /// `POST /accounts/{account_id}/gateway/certificates/{certificate_id}/deactivate`
  Future<ApiResult<ResponseCommon82>>
  zeroTrustCertificatesDeactivateZeroTrustCertificate({
    required String certificateId,
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/gateway/certificates/${Uri.encodeComponent(certificateId)}/deactivate',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon82.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
