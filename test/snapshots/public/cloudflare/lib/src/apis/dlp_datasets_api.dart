//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import 'dart:typed_data';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/dlp_datasets_create_request.dart';
import '../models/dlp_datasets_define_columns_request.dart';
import '../models/dlp_datasets_update_request.dart';
import '../models/response_common20.dart';

/// DlpDatasetsApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class DlpDatasetsApi {
  const DlpDatasetsApi(this._config);

  final ApiConfig _config;

  /// Fetch all datasets
  ///
  /// `GET /accounts/{account_id}/dlp/datasets`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsReadAll({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Create a new dataset
  ///
  /// `POST /accounts/{account_id}/dlp/datasets`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsCreate({
    required String accountId,
    required DlpDatasetsCreateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Fetch a specific dataset
  ///
  /// `GET /accounts/{account_id}/dlp/datasets/{dataset_id}`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsRead({
    required String accountId,
    required String datasetId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Update details about a dataset
  ///
  /// `PUT /accounts/{account_id}/dlp/datasets/{dataset_id}`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsUpdate({
    required String accountId,
    required String datasetId,
    required DlpDatasetsUpdateRequest body,
  }) async {
    final request = ApiRequest(
      method: 'PUT',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Delete a dataset
  ///
  /// This deletes all versions of the dataset.
  ///
  /// `DELETE /accounts/{account_id}/dlp/datasets/{dataset_id}`
  Future<ApiResult<void>> dlpDatasetsDelete({
    required String accountId,
    required String datasetId,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Prepare to upload a new version of a dataset
  ///
  /// `POST /accounts/{account_id}/dlp/datasets/{dataset_id}/upload`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsCreateVersion({
    required String accountId,
    required String datasetId,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}/upload',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Upload a new version of a dataset
  ///
  /// This is used for single-column EDMv1 and Custom Word Lists. The EDM format
  /// can only be created in the Cloudflare dashboard. For other clients, this
  /// operation can only be used for non-secret Custom Word Lists. The body must
  /// be a UTF-8 encoded, newline (NL or CRNL) separated list of words to be matched.
  ///
  /// `POST /accounts/{account_id}/dlp/datasets/{dataset_id}/upload/{version}`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsUploadVersion({
    required String accountId,
    required String datasetId,
    required int version,
    required Uint8List body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}/upload/${Uri.encodeComponent(version.toString())}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Sets the column information for a multi-column upload
  ///
  /// This is used for multi-column EDMv2 datasets. The EDMv2 format can only be
  /// created in the Cloudflare dashboard. The columns in the response appear in
  /// the same order as in the request.
  ///
  /// `POST /accounts/{account_id}/dlp/datasets/{dataset_id}/versions/{version}`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsDefineColumns({
    required String accountId,
    required String datasetId,
    required int version,
    required List<DlpDatasetsDefineColumnsRequest> body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}/versions/${Uri.encodeComponent(version.toString())}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Upload a new version of a multi-column dataset
  ///
  /// This is used for multi-column EDMv2 datasets. The EDMv2 format can only be
  /// created in the Cloudflare dashboard.
  ///
  /// `POST /accounts/{account_id}/dlp/datasets/{dataset_id}/versions/{version}/entries/{entry_id}`
  Future<ApiResult<ResponseCommon20>> dlpDatasetsUploadDatasetColumn({
    required String accountId,
    required String datasetId,
    required int version,
    required String entryId,
    required Uint8List body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/dlp/datasets/${Uri.encodeComponent(datasetId)}/versions/${Uri.encodeComponent(version.toString())}/entries/${Uri.encodeComponent(entryId)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return ResponseCommon20.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
