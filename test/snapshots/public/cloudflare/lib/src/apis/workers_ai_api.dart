//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from cloudflare.yaml
//  OpenAPI spec version: 3.0.3

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/workers_ai_get_model_schema_response.dart';
import '../models/workers_ai_get_to_markdown_supported_response.dart';
import '../models/workers_ai_post_run_model_request.dart';
import '../models/workers_ai_post_run_model_response.dart';
import '../models/workers_ai_post_to_markdown_request.dart';
import '../models/workers_ai_post_to_markdown_response.dart';
import '../models/workers_ai_search_author_response.dart';
import '../models/workers_ai_search_model_response.dart';
import '../models/workers_ai_search_task_response.dart';

/// WorkersAiApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class WorkersAiApi {
  const WorkersAiApi(this._config);

  final ApiConfig _config;

  /// Author Search
  ///
  /// `GET /accounts/{account_id}/ai/authors/search`
  Future<ApiResult<WorkersAiSearchAuthorResponse>> workersAiSearchAuthor({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai/authors/search',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiSearchAuthorResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Model Schema
  ///
  /// Retrieves the input and output JSON schema definition for a Workers AI model.
  ///
  /// `GET /accounts/{account_id}/ai/models/schema`
  Future<ApiResult<WorkersAiGetModelSchemaResponse>> workersAiGetModelSchema({
    required String accountId,
    required String model,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai/models/schema',
      headers: {..._config.defaultHeaders},
      queryParameters: {'model': model},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiGetModelSchemaResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Model Search
  ///
  /// `GET /accounts/{account_id}/ai/models/search`
  Future<ApiResult<WorkersAiSearchModelResponse>> workersAiSearchModel({
    required String accountId,
    int? perPage,
    int? page,
    String? task,
    String? author,
    double? source,
    bool? hideExperimental,
    String? search,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai/models/search',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (perPage != null) 'per_page': perPage.toString(),
        if (page != null) 'page': page.toString(),
        'task': ?task,
        'author': ?author,
        if (source != null) 'source': source.toString(),
        if (hideExperimental != null)
          'hide_experimental': hideExperimental.toString(),
        'search': ?search,
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiSearchModelResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Execute AI model
  ///
  /// This endpoint provides users with the capability to run specific AI models on-demand.
  ///
  /// By submitting the required input data, users can receive real-time predictions or results generated by the chosen AI
  /// model. The endpoint supports various AI model types, ensuring flexibility and adaptability for diverse use cases.
  ///
  /// Model specific inputs available in [Cloudflare Docs](https://developers.cloudflare.com/workers-ai/models/).
  ///
  /// `POST /accounts/{account_id}/ai/run/{model_name}`
  Future<ApiResult<WorkersAiPostRunModelResponse>> workersAiPostRunModel({
    required String accountId,
    required String modelName,
    WorkersAiPostRunModelRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/run/${Uri.encodeComponent(modelName)}',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiPostRunModelResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Task Search
  ///
  /// `GET /accounts/{account_id}/ai/tasks/search`
  Future<ApiResult<WorkersAiSearchTaskResponse>> workersAiSearchTask({
    required String accountId,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai/tasks/search',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiSearchTaskResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Convert Files into Markdown
  ///
  /// Converts uploaded files into Markdown format using Workers AI.
  ///
  /// `POST /accounts/{account_id}/ai/tomarkdown`
  Future<ApiResult<WorkersAiPostToMarkdownResponse>> workersAiPostToMarkdown({
    required String accountId,
    WorkersAiPostToMarkdownRequest? body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path: '/accounts/${Uri.encodeComponent(accountId)}/ai/tomarkdown',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body?.toJson()),
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiPostToMarkdownResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get all converted formats supported
  ///
  /// Lists all file formats supported for conversion to Markdown.
  ///
  /// `GET /accounts/{account_id}/ai/tomarkdown/supported`
  Future<ApiResult<WorkersAiGetToMarkdownSupportedResponse>>
  workersAiGetToMarkdownSupported({required String accountId}) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/accounts/${Uri.encodeComponent(accountId)}/ai/tomarkdown/supported',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return WorkersAiGetToMarkdownSupportedResponse.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
