//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from totem-mobile.json
//  OpenAPI spec version: 3.1.0

import 'dart:convert';import '../client/api_client.dart';import '../client/api_config.dart';import '../client/api_result.dart';import '../models/blog_post_schema.dart';import '../models/paged_blog_post_list_schema.dart';/// BlogApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class BlogApi {const BlogApi(this._config);

final ApiConfig _config;

/// List Posts
///
/// List all blog posts
///
/// `GET /api/mobile/protected/blog/posts`
Future<ApiResult<PagedBlogPostListSchema>> totemBlogMobileApiListPosts({int? limit, int? offset, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/api/mobile/protected/blog/posts',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    if (limit != null) 'limit': limit.toString(),
    if (offset != null) 'offset': offset.toString(),
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return PagedBlogPostListSchema.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Post
///
/// `GET /api/mobile/protected/blog/post/{slug}`
Future<ApiResult<BlogPostSchema>> totemBlogMobileApiPost({required String slug}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/api/mobile/protected/blog/post/${Uri.encodeComponent(slug)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return BlogPostSchema.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T>> _execute<T>(ApiRequest request, {required T Function(ApiResponse) onSuccess, }) async  { var req = request;
try {
  for (final interceptor in _config.interceptors) {
    req = await interceptor.onRequest(req);
  }

  var response = _config.timeout != null
      ? await _config.client.send(req).timeout(_config.timeout!)
      : await _config.client.send(req);

  for (final interceptor in _config.interceptors) {
    response = await interceptor.onResponse(response);
  }

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  for (final interceptor in _config.interceptors) {
    try {
      final recovered = await interceptor.onError(e, st, req);
      if (recovered.isSuccessful) {
        return ApiSuccess(onSuccess(recovered), statusCode: recovered.statusCode, headers: recovered.headers);
      }
      return ApiError(statusCode: recovered.statusCode, rawBody: recovered.body, headers: recovered.headers);
    } catch (_) {
      // Interceptor couldn't handle it, continue to next or fall through
    }
  }
  return ApiException(e, st);
}
 } 
 }
