//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from totem-mobile.json
//  OpenAPI spec version: 3.1.0

import 'dart:convert';
import '../client/api_client.dart';
import '../client/api_config.dart';
import '../client/api_result.dart';
import '../models/mobile_space_detail_schema.dart';
import '../models/paged_mobile_space_detail_schema.dart';
import '../models/session_detail_schema.dart';
import '../models/session_feedback_schema.dart';
import '../models/space_schema.dart';
import '../models/summary_spaces_schema.dart';

/// SpacesApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class SpacesApi {
  const SpacesApi(this._config);

  final ApiConfig _config;

  /// Subscribe To Space
  ///
  /// `POST /api/mobile/protected/spaces/subscribe/{space_slug}`
  Future<ApiResult<bool>> totemSpacesMobileApiSubscribeToSpace({
    required String spaceSlug,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/api/mobile/protected/spaces/subscribe/${Uri.encodeComponent(spaceSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return jsonDecode(response.body) as bool;
      },
    );
  }

  /// Unsubscribe To Space
  ///
  /// `DELETE /api/mobile/protected/spaces/subscribe/{space_slug}`
  Future<ApiResult<bool>> totemSpacesMobileApiUnsubscribeToSpace({
    required String spaceSlug,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/api/mobile/protected/spaces/subscribe/${Uri.encodeComponent(spaceSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return jsonDecode(response.body) as bool;
      },
    );
  }

  /// List Subscriptions
  ///
  /// `GET /api/mobile/protected/spaces/subscribe`
  Future<ApiResult<List<SpaceSchema>>>
  totemSpacesMobileApiListSubscriptions() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/subscribe',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => SpaceSchema.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// List Spaces
  ///
  /// `GET /api/mobile/protected/spaces/`
  Future<ApiResult<PagedMobileSpaceDetailSchema>>
  totemSpacesMobileApiListSpaces({int? limit, int? offset}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/',
      headers: {..._config.defaultHeaders},
      queryParameters: {
        if (limit != null) 'limit': limit.toString(),
        if (offset != null) 'offset': offset.toString(),
      },
    );

    return _execute(
      request,
      onSuccess: (response) {
        return PagedMobileSpaceDetailSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Space Detail
  ///
  /// `GET /api/mobile/protected/spaces/space/{space_slug}`
  Future<ApiResult<MobileSpaceDetailSchema>>
  totemSpacesMobileApiGetSpaceDetail({required String spaceSlug}) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/api/mobile/protected/spaces/space/${Uri.encodeComponent(spaceSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return MobileSpaceDetailSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Get Keeper Spaces
  ///
  /// `GET /api/mobile/protected/spaces/keeper/{slug}/`
  Future<ApiResult<List<MobileSpaceDetailSchema>>>
  totemSpacesMobileApiGetKeeperSpaces({required String slug}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/keeper/${Uri.encodeComponent(slug)}/',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map(
              (e) =>
                  MobileSpaceDetailSchema.fromJson(e as Map<String, dynamic>),
            )
            .toList();
      },
    );
  }

  /// Get Session Detail
  ///
  /// `GET /api/mobile/protected/spaces/session/{event_slug}`
  Future<ApiResult<SessionDetailSchema>> totemSpacesMobileApiGetSessionDetail({
    required String eventSlug,
  }) async {
    final request = ApiRequest(
      method: 'GET',
      path:
          '/api/mobile/protected/spaces/session/${Uri.encodeComponent(eventSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SessionDetailSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Post Session Feedback
  ///
  /// `POST /api/mobile/protected/spaces/session/{event_slug}/feedback`
  Future<ApiResult<void>> totemSpacesMobileApiPostSessionFeedback({
    required String eventSlug,
    required SessionFeedbackSchema body,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/api/mobile/protected/spaces/session/${Uri.encodeComponent(eventSlug)}/feedback',
      headers: {..._config.defaultHeaders, 'Content-Type': 'application/json'},
      body: jsonEncode(body.toJson()),
    );

    return _execute(request, onSuccess: (_) {});
  }

  /// Get Sessions History
  ///
  /// `GET /api/mobile/protected/spaces/sessions/history`
  Future<ApiResult<List<SessionDetailSchema>>>
  totemSpacesMobileApiGetSessionsHistory() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/sessions/history',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => SessionDetailSchema.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Get Recommended Spaces
  ///
  /// `GET /api/mobile/protected/spaces/sessions/recommended`
  Future<ApiResult<List<SessionDetailSchema>>>
  totemSpacesMobileApiGetRecommendedSpaces({int? limit}) async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/sessions/recommended',
      headers: {..._config.defaultHeaders},
      queryParameters: {if (limit != null) 'limit': limit.toString()},
    );

    return _execute(
      request,
      onSuccess: (response) {
        final json = jsonDecode(response.body) as List<dynamic>;
        return json
            .map((e) => SessionDetailSchema.fromJson(e as Map<String, dynamic>))
            .toList();
      },
    );
  }

  /// Get Spaces Summary
  ///
  /// `GET /api/mobile/protected/spaces/summary`
  Future<ApiResult<SummarySpacesSchema>>
  totemSpacesMobileApiGetSpacesSummary() async {
    final request = ApiRequest(
      method: 'GET',
      path: '/api/mobile/protected/spaces/summary',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SummarySpacesSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Rsvp Confirm
  ///
  /// `POST /api/mobile/protected/spaces/rsvp/{event_slug}`
  Future<ApiResult<SessionDetailSchema>> totemSpacesMobileApiRsvpConfirm({
    required String eventSlug,
  }) async {
    final request = ApiRequest(
      method: 'POST',
      path:
          '/api/mobile/protected/spaces/rsvp/${Uri.encodeComponent(eventSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SessionDetailSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Rsvp Cancel
  ///
  /// `DELETE /api/mobile/protected/spaces/rsvp/{event_slug}`
  Future<ApiResult<SessionDetailSchema>> totemSpacesMobileApiRsvpCancel({
    required String eventSlug,
  }) async {
    final request = ApiRequest(
      method: 'DELETE',
      path:
          '/api/mobile/protected/spaces/rsvp/${Uri.encodeComponent(eventSlug)}',
      headers: {..._config.defaultHeaders},
    );

    return _execute(
      request,
      onSuccess: (response) {
        return SessionDetailSchema.fromJson(
          jsonDecode(response.body) as Map<String, dynamic>,
        );
      },
    );
  }

  /// Shared execution pipeline: interceptors -> send -> deserialize.
  Future<ApiResult<T>> _execute<T>(
    ApiRequest request, {
    required T Function(ApiResponse) onSuccess,
  }) async {
    var req = request;
    try {
      for (final interceptor in _config.interceptors) {
        req = await interceptor.onRequest(req);
      }

      var response = _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);

      for (final interceptor in _config.interceptors) {
        response = await interceptor.onResponse(response);
      }

      if (response.isSuccessful) {
        return ApiSuccess(
          onSuccess(response),
          statusCode: response.statusCode,
          headers: response.headers,
        );
      }
      return ApiError(
        statusCode: response.statusCode,
        rawBody: response.body,
        headers: response.headers,
      );
    } catch (e, st) {
      for (final interceptor in _config.interceptors) {
        try {
          final recovered = await interceptor.onError(e, st, req);
          if (recovered.isSuccessful) {
            return ApiSuccess(
              onSuccess(recovered),
              statusCode: recovered.statusCode,
              headers: recovered.headers,
            );
          }
          return ApiError(
            statusCode: recovered.statusCode,
            rawBody: recovered.body,
            headers: recovered.headers,
          );
        } catch (_) {
          // Interceptor couldn't handle it, continue to next or fall through
        }
      }
      return ApiException(e, st);
    }
  }
}
