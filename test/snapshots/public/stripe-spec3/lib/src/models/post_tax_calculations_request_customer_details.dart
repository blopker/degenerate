//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from stripe-spec3.yaml
//  OpenAPI spec version: 3.0.0

import 'package:collection/collection.dart';
import 'post_tax_calculations_request_customer_details_address.dart';
import 'post_tax_calculations_request_customer_details_address_source.dart';
import 'post_tax_calculations_request_customer_details_tax_ids.dart';
import 'post_tax_calculations_request_customer_details_taxability_override.dart';

/// Details about the customer, including address and tax IDs.
final class PostTaxCalculationsRequestCustomerDetails {
  const PostTaxCalculationsRequestCustomerDetails({
    this.address,
    this.addressSource,
    this.ipAddress,
    this.taxIds,
    this.taxabilityOverride,
  });

  factory PostTaxCalculationsRequestCustomerDetails.fromJson(
    Map<String, dynamic> json,
  ) {
    return PostTaxCalculationsRequestCustomerDetails(
      address: json['address'] != null
          ? PostTaxCalculationsRequestCustomerDetailsAddress.fromJson(
              json['address'] as Map<String, dynamic>,
            )
          : null,
      addressSource: json['address_source'] != null
          ? PostTaxCalculationsRequestCustomerDetailsAddressSource.fromJson(
              json['address_source'] as String,
            )
          : null,
      ipAddress: json['ip_address'] as String?,
      taxIds: (json['tax_ids'] as List<dynamic>?)
          ?.map(
            (e) => PostTaxCalculationsRequestCustomerDetailsTaxIds.fromJson(
              e as Map<String, dynamic>,
            ),
          )
          .toList(),
      taxabilityOverride: json['taxability_override'] != null
          ? PostTaxCalculationsRequestCustomerDetailsTaxabilityOverride.fromJson(
              json['taxability_override'] as String,
            )
          : null,
    );
  }

  final PostTaxCalculationsRequestCustomerDetailsAddress? address;

  final PostTaxCalculationsRequestCustomerDetailsAddressSource? addressSource;

  final String? ipAddress;

  final List<PostTaxCalculationsRequestCustomerDetailsTaxIds>? taxIds;

  final PostTaxCalculationsRequestCustomerDetailsTaxabilityOverride?
  taxabilityOverride;

  Map<String, dynamic> toJson() {
    return {
      if (address != null) 'address': address?.toJson(),
      if (addressSource != null) 'address_source': addressSource?.toJson(),
      'ip_address': ?ipAddress,
      if (taxIds != null) 'tax_ids': taxIds?.map((e) => e.toJson()).toList(),
      if (taxabilityOverride != null)
        'taxability_override': taxabilityOverride?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return true;
  }

  PostTaxCalculationsRequestCustomerDetails copyWith({
    PostTaxCalculationsRequestCustomerDetailsAddress Function()? address,
    PostTaxCalculationsRequestCustomerDetailsAddressSource Function()?
    addressSource,
    String Function()? ipAddress,
    List<PostTaxCalculationsRequestCustomerDetailsTaxIds> Function()? taxIds,
    PostTaxCalculationsRequestCustomerDetailsTaxabilityOverride Function()?
    taxabilityOverride,
  }) {
    return PostTaxCalculationsRequestCustomerDetails(
      address: address != null ? address() : this.address,
      addressSource: addressSource != null
          ? addressSource()
          : this.addressSource,
      ipAddress: ipAddress != null ? ipAddress() : this.ipAddress,
      taxIds: taxIds != null ? taxIds() : this.taxIds,
      taxabilityOverride: taxabilityOverride != null
          ? taxabilityOverride()
          : this.taxabilityOverride,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is PostTaxCalculationsRequestCustomerDetails &&
            address == other.address &&
            addressSource == other.addressSource &&
            ipAddress == other.ipAddress &&
            const ListEquality<dynamic>().equals(taxIds, other.taxIds) &&
            taxabilityOverride == other.taxabilityOverride;
  }

  @override
  int get hashCode {
    return Object.hash(
      address,
      addressSource,
      ipAddress,
      Object.hashAll(taxIds ?? const []),
      taxabilityOverride,
    );
  }

  @override
  String toString() {
    return 'PostTaxCalculationsRequestCustomerDetails(address: $address, addressSource: $addressSource, ipAddress: $ipAddress, taxIds: $taxIds, taxabilityOverride: $taxabilityOverride)';
  }
}
