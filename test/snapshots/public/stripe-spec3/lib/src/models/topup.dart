//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from stripe-spec3.yaml
//  OpenAPI spec version: 3.0.0

import 'topup_balance_transaction.dart';
import 'topup_object.dart';
import 'topup_source.dart';
import 'topup_status.dart';

/// To top up your Stripe balance, you create a top-up object. You can retrieve
/// individual top-ups, as well as list all top-ups. Top-ups are identified by a
/// unique, random ID.
///
/// Related guide: [Topping up your platform account](https://docs.stripe.com/connect/top-ups)
final class Topup {
  const Topup({
    required this.amount,
    this.balanceTransaction,
    required this.created,
    required this.currency,
    this.description,
    this.expectedAvailabilityDate,
    this.failureCode,
    this.failureMessage,
    required this.id,
    required this.livemode,
    required this.metadata,
    required this.object,
    this.source,
    this.statementDescriptor,
    required this.status,
    this.transferGroup,
  });

  factory Topup.fromJson(Map<String, dynamic> json) {
    return Topup(
      amount: (json['amount'] as num).toInt(),
      balanceTransaction: json['balance_transaction'] != null
          ? TopupBalanceTransaction.fromJson(
              json['balance_transaction'] as Map<String, dynamic>,
            )
          : null,
      created: (json['created'] as num).toInt(),
      currency: json['currency'] as String,
      description: json['description'] as String?,
      expectedAvailabilityDate: json['expected_availability_date'] != null
          ? (json['expected_availability_date'] as num).toInt()
          : null,
      failureCode: json['failure_code'] as String?,
      failureMessage: json['failure_message'] as String?,
      id: json['id'] as String,
      livemode: json['livemode'] as bool,
      metadata: (json['metadata'] as Map<String, dynamic>).map(
        (k, v) => MapEntry(k, v as String),
      ),
      object: TopupObject.fromJson(json['object'] as String),
      source: json['source'] != null
          ? TopupSource.fromJson(json['source'] as Map<String, dynamic>)
          : null,
      statementDescriptor: json['statement_descriptor'] as String?,
      status: TopupStatus.fromJson(json['status'] as String),
      transferGroup: json['transfer_group'] as String?,
    );
  }

  /// Amount transferred.
  final int amount;

  /// ID of the balance transaction that describes the impact of this top-up on your account balance. May not be specified depending on status of top-up.
  final TopupBalanceTransaction? balanceTransaction;

  /// Time at which the object was created. Measured in seconds since the Unix epoch.
  final int created;

  /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  final String currency;

  /// An arbitrary string attached to the object. Often useful for displaying to users.
  final String? description;

  /// Date the funds are expected to arrive in your Stripe account for payouts. This factors in delays like weekends or bank holidays. May not be specified depending on status of top-up.
  final int? expectedAvailabilityDate;

  /// Error code explaining reason for top-up failure if available (see [the errors section](https://docs.stripe.com/api#errors) for a list of codes).
  final String? failureCode;

  /// Message to user further explaining reason for top-up failure if available.
  final String? failureMessage;

  /// Unique identifier for the object.
  final String id;

  /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
  final bool livemode;

  /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
  final Map<String, String> metadata;

  /// String representing the object's type. Objects of the same type share the same value.
  final TopupObject object;

  /// The source field is deprecated. It might not always be present in the API response.
  final TopupSource? source;

  /// Extra information about a top-up. This will appear on your source's bank statement. It must contain at least one letter.
  final String? statementDescriptor;

  /// The status of the top-up is either `canceled`, `failed`, `pending`, `reversed`, or `succeeded`.
  final TopupStatus status;

  /// A string that identifies this top-up as part of a group.
  final String? transferGroup;

  Map<String, dynamic> toJson() {
    return {
      'amount': amount,
      if (balanceTransaction != null)
        'balance_transaction': balanceTransaction?.toJson(),
      'created': created,
      'currency': currency,
      'description': ?description,
      'expected_availability_date': ?expectedAvailabilityDate,
      'failure_code': ?failureCode,
      'failure_message': ?failureMessage,
      'id': id,
      'livemode': livemode,
      'metadata': metadata,
      'object': object.toJson(),
      if (source != null) 'source': source?.toJson(),
      'statement_descriptor': ?statementDescriptor,
      'status': status.toJson(),
      'transfer_group': ?transferGroup,
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('amount') &&
        json['amount'] is num &&
        json.containsKey('created') &&
        json['created'] is num &&
        json.containsKey('currency') &&
        json['currency'] is String &&
        json.containsKey('id') &&
        json['id'] is String &&
        json.containsKey('livemode') &&
        json['livemode'] is bool &&
        json.containsKey('metadata') &&
        json.containsKey('object') &&
        json.containsKey('status');
  }

  Topup copyWith({
    int? amount,
    TopupBalanceTransaction? Function()? balanceTransaction,
    int? created,
    String? currency,
    String? Function()? description,
    int? Function()? expectedAvailabilityDate,
    String? Function()? failureCode,
    String? Function()? failureMessage,
    String? id,
    bool? livemode,
    Map<String, String>? metadata,
    TopupObject? object,
    TopupSource? Function()? source,
    String? Function()? statementDescriptor,
    TopupStatus? status,
    String? Function()? transferGroup,
  }) {
    return Topup(
      amount: amount ?? this.amount,
      balanceTransaction: balanceTransaction != null
          ? balanceTransaction()
          : this.balanceTransaction,
      created: created ?? this.created,
      currency: currency ?? this.currency,
      description: description != null ? description() : this.description,
      expectedAvailabilityDate: expectedAvailabilityDate != null
          ? expectedAvailabilityDate()
          : this.expectedAvailabilityDate,
      failureCode: failureCode != null ? failureCode() : this.failureCode,
      failureMessage: failureMessage != null
          ? failureMessage()
          : this.failureMessage,
      id: id ?? this.id,
      livemode: livemode ?? this.livemode,
      metadata: metadata ?? this.metadata,
      object: object ?? this.object,
      source: source != null ? source() : this.source,
      statementDescriptor: statementDescriptor != null
          ? statementDescriptor()
          : this.statementDescriptor,
      status: status ?? this.status,
      transferGroup: transferGroup != null
          ? transferGroup()
          : this.transferGroup,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is Topup &&
            amount == other.amount &&
            balanceTransaction == other.balanceTransaction &&
            created == other.created &&
            currency == other.currency &&
            description == other.description &&
            expectedAvailabilityDate == other.expectedAvailabilityDate &&
            failureCode == other.failureCode &&
            failureMessage == other.failureMessage &&
            id == other.id &&
            livemode == other.livemode &&
            metadata == other.metadata &&
            object == other.object &&
            source == other.source &&
            statementDescriptor == other.statementDescriptor &&
            status == other.status &&
            transferGroup == other.transferGroup;
  }

  @override
  int get hashCode {
    return Object.hash(
      amount,
      balanceTransaction,
      created,
      currency,
      description,
      expectedAvailabilityDate,
      failureCode,
      failureMessage,
      id,
      livemode,
      metadata,
      object,
      source,
      statementDescriptor,
      status,
      transferGroup,
    );
  }

  @override
  String toString() {
    return 'Topup(amount: $amount, balanceTransaction: $balanceTransaction, created: $created, currency: $currency, description: $description, expectedAvailabilityDate: $expectedAvailabilityDate, failureCode: $failureCode, failureMessage: $failureMessage, id: $id, livemode: $livemode, metadata: $metadata, object: $object, source: $source, statementDescriptor: $statementDescriptor, status: $status, transferGroup: $transferGroup)';
  }
}
