//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from stripe-spec3.yaml
//  OpenAPI spec version: 3.0.0

import 'package:collection/collection.dart';
import 'funding_instructions_bank_transfer_financial_address.dart';
import 'payment_intent_next_action_display_bank_transfer_instructions_type.dart';

///
final class PaymentIntentNextActionDisplayBankTransferInstructions {
  const PaymentIntentNextActionDisplayBankTransferInstructions({
    this.amountRemaining,
    this.currency,
    this.financialAddresses,
    this.hostedInstructionsUrl,
    this.reference,
    required this.type,
  });

  factory PaymentIntentNextActionDisplayBankTransferInstructions.fromJson(
    Map<String, dynamic> json,
  ) {
    return PaymentIntentNextActionDisplayBankTransferInstructions(
      amountRemaining: json['amount_remaining'] != null
          ? (json['amount_remaining'] as num).toInt()
          : null,
      currency: json['currency'] as String?,
      financialAddresses: (json['financial_addresses'] as List<dynamic>?)
          ?.map(
            (e) => FundingInstructionsBankTransferFinancialAddress.fromJson(
              e as Map<String, dynamic>,
            ),
          )
          .toList(),
      hostedInstructionsUrl: json['hosted_instructions_url'] as String?,
      reference: json['reference'] as String?,
      type: PaymentIntentNextActionDisplayBankTransferInstructionsType.fromJson(
        json['type'] as String,
      ),
    );
  }

  /// The remaining amount that needs to be transferred to complete the payment.
  final int? amountRemaining;

  /// Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  final String? currency;

  /// A list of financial addresses that can be used to fund the customer balance
  final List<FundingInstructionsBankTransferFinancialAddress>?
  financialAddresses;

  /// A link to a hosted page that guides your customer through completing the transfer.
  final String? hostedInstructionsUrl;

  /// A string identifying this payment. Instruct your customer to include this code in the reference or memo field of their bank transfer.
  final String? reference;

  /// Type of bank transfer
  final PaymentIntentNextActionDisplayBankTransferInstructionsType type;

  Map<String, dynamic> toJson() {
    return {
      'amount_remaining': ?amountRemaining,
      'currency': ?currency,
      if (financialAddresses != null)
        'financial_addresses': financialAddresses
            ?.map((e) => e.toJson())
            .toList(),
      'hosted_instructions_url': ?hostedInstructionsUrl,
      'reference': ?reference,
      'type': type.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('type');
  }

  PaymentIntentNextActionDisplayBankTransferInstructions copyWith({
    int? Function()? amountRemaining,
    String? Function()? currency,
    List<FundingInstructionsBankTransferFinancialAddress> Function()?
    financialAddresses,
    String? Function()? hostedInstructionsUrl,
    String? Function()? reference,
    PaymentIntentNextActionDisplayBankTransferInstructionsType? type,
  }) {
    return PaymentIntentNextActionDisplayBankTransferInstructions(
      amountRemaining: amountRemaining != null
          ? amountRemaining()
          : this.amountRemaining,
      currency: currency != null ? currency() : this.currency,
      financialAddresses: financialAddresses != null
          ? financialAddresses()
          : this.financialAddresses,
      hostedInstructionsUrl: hostedInstructionsUrl != null
          ? hostedInstructionsUrl()
          : this.hostedInstructionsUrl,
      reference: reference != null ? reference() : this.reference,
      type: type ?? this.type,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is PaymentIntentNextActionDisplayBankTransferInstructions &&
            amountRemaining == other.amountRemaining &&
            currency == other.currency &&
            const ListEquality<dynamic>().equals(
              financialAddresses,
              other.financialAddresses,
            ) &&
            hostedInstructionsUrl == other.hostedInstructionsUrl &&
            reference == other.reference &&
            type == other.type;
  }

  @override
  int get hashCode {
    return Object.hash(
      amountRemaining,
      currency,
      Object.hashAll(financialAddresses ?? const []),
      hostedInstructionsUrl,
      reference,
      type,
    );
  }

  @override
  String toString() {
    return 'PaymentIntentNextActionDisplayBankTransferInstructions(amountRemaining: $amountRemaining, currency: $currency, financialAddresses: $financialAddresses, hostedInstructionsUrl: $hostedInstructionsUrl, reference: $reference, type: $type)';
  }
}
