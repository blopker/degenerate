//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from stripe-spec3.yaml
//  OpenAPI spec version: 3.0.0

import 'account.dart';
import 'bank_account.dart';
import 'card.dart';
import 'source.dart';

final class PaymentSource {
  const PaymentSource({this.account, this.bankAccount, this.card, this.source});

  factory PaymentSource.fromJson(Map<String, dynamic> json) {
    return PaymentSource(
      account: Account.canParse(json) ? Account.fromJson(json) : null,
      bankAccount: BankAccount.canParse(json)
          ? BankAccount.fromJson(json)
          : null,
      card: Card.canParse(json) ? Card.fromJson(json) : null,
      source: Source.canParse(json) ? Source.fromJson(json) : null,
    );
  }

  final Account? account;

  final BankAccount? bankAccount;

  final Card? card;

  final Source? source;

  /// At least one variant must be present.
  bool get isValid {
    return account != null ||
        bankAccount != null ||
        card != null ||
        source != null;
  }

  Map<String, dynamic> toJson() {
    return {
      ...?account?.toJson(),
      ...?bankAccount?.toJson(),
      ...?card?.toJson(),
      ...?source?.toJson(),
    };
  }
}
