//  GENERATED CODE — DO NOT MODIFY BY HAND
//
//  Generated by degenerate from stripe-spec3.yaml
//  OpenAPI spec version: 3.0.0

import 'package:collection/collection.dart';
import 'balance_transaction.dart';
import 'issuing_dispute_evidence.dart';
import 'issuing_dispute_loss_reason.dart';
import 'issuing_dispute_object.dart';
import 'issuing_dispute_status.dart';
import 'issuing_dispute_transaction.dart';
import 'issuing_dispute_treasury2.dart';

/// As a [card issuer](https://docs.stripe.com/issuing), you can dispute transactions that the cardholder does not recognize, suspects to be fraudulent, or has other issues with.
///
/// Related guide: [Issuing disputes](https://docs.stripe.com/issuing/purchases/disputes)
final class IssuingDispute {
  const IssuingDispute({
    required this.amount,
    this.balanceTransactions,
    required this.created,
    required this.currency,
    required this.evidence,
    required this.id,
    required this.livemode,
    this.lossReason,
    required this.metadata,
    required this.object,
    required this.status,
    required this.transaction,
    this.treasury,
  });

  factory IssuingDispute.fromJson(Map<String, dynamic> json) {
    return IssuingDispute(
      amount: (json['amount'] as num).toInt(),
      balanceTransactions: (json['balance_transactions'] as List<dynamic>?)
          ?.map((e) => BalanceTransaction.fromJson(e as Map<String, dynamic>))
          .toList(),
      created: (json['created'] as num).toInt(),
      currency: json['currency'] as String,
      evidence: IssuingDisputeEvidence.fromJson(
        json['evidence'] as Map<String, dynamic>,
      ),
      id: json['id'] as String,
      livemode: json['livemode'] as bool,
      lossReason: json['loss_reason'] != null
          ? IssuingDisputeLossReason.fromJson(json['loss_reason'] as String)
          : null,
      metadata: (json['metadata'] as Map<String, dynamic>).map(
        (k, v) => MapEntry(k, v as String),
      ),
      object: IssuingDisputeObject.fromJson(json['object'] as String),
      status: IssuingDisputeStatus.fromJson(json['status'] as String),
      transaction: IssuingDisputeTransaction.fromJson(
        json['transaction'] as Map<String, dynamic>,
      ),
      treasury: json['treasury'] != null
          ? IssuingDisputeTreasury2.fromJson(
              json['treasury'] as Map<String, dynamic>,
            )
          : null,
    );
  }

  /// Disputed amount in the card's currency and in the [smallest currency unit](https://docs.stripe.com/currencies#zero-decimal). Usually the amount of the `transaction`, but can differ (usually because of currency fluctuation).
  final int amount;

  /// List of balance transactions associated with the dispute.
  final List<BalanceTransaction>? balanceTransactions;

  /// Time at which the object was created. Measured in seconds since the Unix epoch.
  final int created;

  /// The currency the `transaction` was made in.
  final String currency;

  final IssuingDisputeEvidence evidence;

  /// Unique identifier for the object.
  final String id;

  /// Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode.
  final bool livemode;

  /// The enum that describes the dispute loss outcome. If the dispute is not lost, this field will be absent. New enum values may be added in the future, so be sure to handle unknown values.
  final IssuingDisputeLossReason? lossReason;

  /// Set of [key-value pairs](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
  final Map<String, String> metadata;

  /// String representing the object's type. Objects of the same type share the same value.
  final IssuingDisputeObject object;

  /// Current status of the dispute.
  final IssuingDisputeStatus status;

  /// The transaction being disputed.
  final IssuingDisputeTransaction transaction;

  /// [Treasury](https://docs.stripe.com/api/treasury) details related to this dispute if it was created on a [FinancialAccount](/docs/api/treasury/financial_accounts
  final IssuingDisputeTreasury2? treasury;

  Map<String, dynamic> toJson() {
    return {
      'amount': amount,
      if (balanceTransactions != null)
        'balance_transactions': balanceTransactions
            ?.map((e) => e.toJson())
            .toList(),
      'created': created,
      'currency': currency,
      'evidence': evidence.toJson(),
      'id': id,
      'livemode': livemode,
      if (lossReason != null) 'loss_reason': lossReason?.toJson(),
      'metadata': metadata,
      'object': object.toJson(),
      'status': status.toJson(),
      'transaction': transaction.toJson(),
      if (treasury != null) 'treasury': treasury?.toJson(),
    };
  }

  static bool canParse(Map<String, dynamic> json) {
    return json.containsKey('amount') &&
        json['amount'] is num &&
        json.containsKey('created') &&
        json['created'] is num &&
        json.containsKey('currency') &&
        json['currency'] is String &&
        json.containsKey('evidence') &&
        json.containsKey('id') &&
        json['id'] is String &&
        json.containsKey('livemode') &&
        json['livemode'] is bool &&
        json.containsKey('metadata') &&
        json.containsKey('object') &&
        json.containsKey('status') &&
        json.containsKey('transaction');
  }

  IssuingDispute copyWith({
    int? amount,
    List<BalanceTransaction>? Function()? balanceTransactions,
    int? created,
    String? currency,
    IssuingDisputeEvidence? evidence,
    String? id,
    bool? livemode,
    IssuingDisputeLossReason Function()? lossReason,
    Map<String, String>? metadata,
    IssuingDisputeObject? object,
    IssuingDisputeStatus? status,
    IssuingDisputeTransaction? transaction,
    IssuingDisputeTreasury2? Function()? treasury,
  }) {
    return IssuingDispute(
      amount: amount ?? this.amount,
      balanceTransactions: balanceTransactions != null
          ? balanceTransactions()
          : this.balanceTransactions,
      created: created ?? this.created,
      currency: currency ?? this.currency,
      evidence: evidence ?? this.evidence,
      id: id ?? this.id,
      livemode: livemode ?? this.livemode,
      lossReason: lossReason != null ? lossReason() : this.lossReason,
      metadata: metadata ?? this.metadata,
      object: object ?? this.object,
      status: status ?? this.status,
      transaction: transaction ?? this.transaction,
      treasury: treasury != null ? treasury() : this.treasury,
    );
  }

  @override
  bool operator ==(Object other) {
    return identical(this, other) ||
        other is IssuingDispute &&
            amount == other.amount &&
            const ListEquality<dynamic>().equals(
              balanceTransactions,
              other.balanceTransactions,
            ) &&
            created == other.created &&
            currency == other.currency &&
            evidence == other.evidence &&
            id == other.id &&
            livemode == other.livemode &&
            lossReason == other.lossReason &&
            metadata == other.metadata &&
            object == other.object &&
            status == other.status &&
            transaction == other.transaction &&
            treasury == other.treasury;
  }

  @override
  int get hashCode {
    return Object.hash(
      amount,
      Object.hashAll(balanceTransactions ?? const []),
      created,
      currency,
      evidence,
      id,
      livemode,
      lossReason,
      metadata,
      object,
      status,
      transaction,
      treasury,
    );
  }

  @override
  String toString() {
    return 'IssuingDispute(amount: $amount, balanceTransactions: $balanceTransactions, created: $created, currency: $currency, evidence: $evidence, id: $id, livemode: $livemode, lossReason: $lossReason, metadata: $metadata, object: $object, status: $status, transaction: $transaction, treasury: $treasury)';
  }
}
