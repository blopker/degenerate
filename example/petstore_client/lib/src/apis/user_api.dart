//  dart format off
//  GENERATED CODE — DO NOT MODIFY BY HAND
// 
//  Generated by degenerate from petstore3.json
//  OpenAPI spec version: 3.0.4

import 'dart:convert';import 'package:degenerate_runtime/degenerate_runtime.dart';import '../models/user.dart';/// UserApi operations.
///
/// All operations return [ApiResult] — use pattern matching to handle
/// success, error, and exception cases.
final class UserApi {const UserApi(this._config);

final ApiConfig _config;

/// Create user.
///
/// This can only be done by the logged in user.
///
/// `POST /user`
Future<ApiResult<User, Never>> createUser({User? body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/user',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (response) {
    return User.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Creates list of users with given input array.
///
/// `POST /user/createWithList`
Future<ApiResult<User, Never>> createUsersWithListInput({List<User>? body}) async  { final request = ApiRequest(
  method: 'POST',
  path: '/user/createWithList',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body),
);

return _execute(
  request,
  onSuccess: (response) {
    return User.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Logs user into the system.
///
/// Log into the system.
///
/// `GET /user/login`
Future<ApiResult<String, Never>> loginUser({String? username, String? password, }) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/login',
  headers: {..._config.defaultHeaders
  },
  queryParameters: {
    'username': ?username,
    'password': ?password,
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return response.body;
  },
);
 } 
/// Logs out current logged in user session.
///
/// Log user out of the system.
///
/// `GET /user/logout`
Future<ApiResult<void, Never>> logoutUser() async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/logout',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Get user by user name.
///
/// Get user detail based on username.
///
/// `GET /user/{username}`
Future<ApiResult<User, Never>> getUserByName({required String username}) async  { final request = ApiRequest(
  method: 'GET',
  path: '/user/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (response) {
    return User.fromJson(jsonDecode(response.body) as Map<String, dynamic>);
  },
);
 } 
/// Update user resource.
///
/// This can only be done by the logged in user.
///
/// `PUT /user/{username}`
Future<ApiResult<void, Never>> updateUser({required String username, User? body, }) async  { final request = ApiRequest(
  method: 'PUT',
  path: '/user/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
    , 'Content-Type': 'application/json'
  },
  body: jsonEncode(body?.toJson()),
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Delete user resource.
///
/// This can only be done by the logged in user.
///
/// `DELETE /user/{username}`
Future<ApiResult<void, Never>> deleteUser({required String username}) async  { final request = ApiRequest(
  method: 'DELETE',
  path: '/user/${Uri.encodeComponent(username)}',
  headers: {..._config.defaultHeaders
  },
);

return _execute(
  request,
  onSuccess: (_) {},
);
 } 
/// Shared execution pipeline: interceptors -> send -> deserialize.
Future<ApiResult<T, E>> _execute<T,E>(ApiRequest request, {required T Function(ApiResponse) onSuccess, E? Function(ApiResponse)? onError, }) async  { try {
  final chain = buildInterceptorChain(
    interceptors: _config.interceptors,
    terminal: (req) async {
      return _config.timeout != null
          ? await _config.client.send(req).timeout(_config.timeout!)
          : await _config.client.send(req);
    },
  );

  final response = await chain(request);

  if (response.isSuccessful) {
    return ApiSuccess(
      onSuccess(response),
      statusCode: response.statusCode,
      headers: response.headers,
    );
  }
  return ApiError(
    statusCode: response.statusCode,
    error: onError != null ? onError(response) : null,
    rawBody: response.body,
    headers: response.headers,
  );
} catch (e, st) {
  return ApiException(e, st);
}
 } 
 }
